# Vendored evidence citations (synthetic fixture)

`vendor/foldlab/` is read-only pinned evidence. Its own internal citations
belong to its own repository and are not this gate's business.

Guardedness witnesses live at
`vendor/foldlab/pinned/tree/library/cas/Cas/Schema/Guarded.lean:29-50`.
The sealed reference pin's predicate is at
`vendor/foldlab/pinned/tree/library/effects/test/SchemaReferencesPin.test.ts:67-75`.

Two near-miss basenames must also be accepted, because they are different
files from the protected five:

- `vendor/foldlab/pinned/tree/library/cas/CORE-ABSTRACTIONS-PLAN.md:127-131`
- `harness/AGENTS.md:12`
