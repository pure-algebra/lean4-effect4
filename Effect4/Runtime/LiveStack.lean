import Effect4.Runtime.Runtime

/-!
# Live continuation-stack traversal

The independent packet in `test/contracts/live-stack.contract.md` owns the
additive public surface. This breaker stub declares nothing. The existing
primitive, fiber, result and event carriers remain owned by `Runtime.lean`.
The builder implements only after the red packet is frozen and committed.
-/
