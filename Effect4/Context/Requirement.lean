/-!
# Context.Requirement.lean

Owner: Context interpretation and discharge of `Effect4.Data.Row` requirements.

The canonical row carrier and its union laws belong to `Effect4.Data.Row`;
Context does not introduce a second requirement-row representation.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
