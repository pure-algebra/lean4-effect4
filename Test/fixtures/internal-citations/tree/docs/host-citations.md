# Host-source citations (synthetic fixture)

Every citation on this page targets a pinned third-party source. The gate must
examine and accept all of them.

The `Literal` number leg is `Schema.Finite` (`SchemaRepresentation.ts:1005`),
while the `Enum` leg is `Schema.Number` (`:999`, used at `:1020`).
`SchemaAST.ts:3209` types the filter `run` closure, and `SchemaAST.ts:529`
records that a reported input is retained by reference.
`internal/schema/fromRepresentation.ts:67-68` throws `Invalid reference`, and
`ReferenceSlot` is at `:19-31`.
`Schema.ts:1250` and `:1254` are a documentation example only.
`SchemaGetter.ts:194-200` constructs the forbidden getter.
`SchemaTransformation.ts:41` and `SchemaIssue.ts:107-113` close the alphabet.
`SchemaParser.ts:446-448` throws at the synchronous boundary.
`Filter.ts:12` and `internal/schema/parser.ts:5` are cited here too.
