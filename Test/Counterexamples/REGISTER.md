# Effect4 counterexample register

Every row here has a witness on `main`: a battery file under `Test/` that a
change to the attacked statement makes red. Rows whose witness lives only in
history (an archived branch, a moved repository, a retired battery) are in
[`Archive/REGISTER.md`](Archive/REGISTER.md) with the same IDs; that file is the
history and this one is the live register. The split was made on 2026-09-13,
by whether the witness path exists in this tree: 138 rows here, 260 there.

Stable IDs in this file are never reused. A row closes only when its witness
is retained and the repaired declaration or theorem mechanically rejects the
attack. `PINNED` means the witness already exists at the named immutable source
revision; `SEEDED` means the packet has frozen the attack but no immutable
provenance pin for its Effect4 witness has yet been recorded. `RESERVED` means
the stable row and forced repair are frozen now, while the executable witness
belongs to a later packet whose declarations are not open yet. `MOVED` means
the row and its witness now live in another repository at a named tag; the
ID is retained here so citations resolve, and the row is not reused.

| ID | Status | Attacked statement | Witness / evidence | Forced repair |
| --- | --- | --- | --- | --- |
| `E4-TARGET-TYPE-CE-001` | SEEDED | Every readable program prints a declaration for an arbitrary raw `EffTy`, even after target annotations become structural | `Test/Codegen/PrintContract.lean`: an empty-requirement answer `handle "not a type !"` fails `declarationTypeRepresentable` and `printDecl` returns `typeSpelling` | parse legacy target spellings without raw fallback; expose representability in module-printing adequacy and the API composition law; retain successful-print reconstruction and exact expression reading |
| `E4-SCHEMA-CE-017` | SEEDED | Identical persisted field shapes permit semantic collapse; or the flat API follows as a theorem from field shape | `Test/Counterexamples/Schema/SemanticTagSeparation.lean`, plus the exact recursor snapshot in `Test/Schema/RepresentationContract.lean` | preserve all 22 nominal values; independently freeze the flat carrier as a native minimality/API ruling without minting a duplicate `KeywordKind` alphabet |
| `E4-SCHEMA-CE-018` | SEEDED | A Lean constructor name determines the persisted `_tag` spelling | `Test/Counterexamples/Schema/WireSpellingDrift.lean` | pin the case-sensitive wire string per tag and prove injectivity plus a partial inverse |
| `E4-SCHEMA-CE-019` | SEEDED | `census.length = 22` establishes the census | `Test/Counterexamples/Schema/CensusCoverage.lean`, `decoyCensus` with 22 entries, one repeat, one omission | require length, duplicate freedom, and coverage as three separate theorems, coverage by case analysis |
| `E4-SCHEMA-CE-020` | SEEDED | Enum member values and literal payloads may share one kind alphabet | `Test/Counterexamples/Schema/KindAlphabetSeparation.lean` | keep `EnumValueKind` and `LiteralKind` separate and relate them by an explicit non-surjective kind-level embedding |
| `E4-SCHEMA-CE-021` | SEEDED | `null` is a persisted literal payload kind | compile-negative in `Test/Counterexamples/Schema/NoNullLiteralKind.lean`, plus the 4-entry census | enforce the exclusion by absent constructor, not by a later admission rule; keep the `Null` representation tag distinct |
| `E4-SCHEMA-CE-022` | SEEDED | Object property keys may include live-only local symbols | compile-negative in `Test/Counterexamples/Schema/NoLocalSymbolPropertyKey.lean`, plus the 3-entry census | admit only string, number, and global-symbol keys; necessary condition for reserved `E4-SCHEMA-CE-010`, not a discharge |
| `E4-SCHEMA-CE-026` | SEEDED | A correct 22-member tag alphabet licenses a payload carrier that merges the twelve identical-shape keyword nodes | `Test/Schema/PayloadContract.lean`: 22 constructor ascriptions, `Representation.cases_census`, and the `Representation.keyword` compile-negative | keep twelve payload constructors; `E4-SCHEMA-CE-017` binds the tag layer only and says nothing about the payload carrier |
| `E4-SCHEMA-CE-027` | SEEDED | A payload carrier cannot drift from the frozen tag alphabet, public declaration surface, or downward module ownership | `Test/Schema/PayloadContract.lean`: without `Representation.tag`, a 21- or 23-constructor carrier typechecks and every census theorem in `RepresentationContract.lean` still passes; an existential census also cannot reject an uninhabited extra constructor or a constructor permutation; mere reachability does not prove which module owns D2-D3 | require `tag`, its 22 `rfl` equations, `tag_surjective`, and `cases_census`, plus the elaborated-surface reaction gate for four declaration mutations and the frozen `payloadBoundaryImportProbe`/expected-owner rows proving D0-D3 reachable through `Effect4.Schema.Payload`, D4-D7 unreachable there, D0-D1 owned by `Data.Json`, D2-D3 owned by `Schema.Payload`, and no upward import |
| `E4-SCHEMA-CE-038` | SEEDED | `Document` is a `MultiDocument` with one root | `Test/Schema/PayloadContract.lean`: exact `toMulti_mk`, injectivity, and an admissible two-root value outside the image | keep the two shapes nominally distinct; prove non-surjectivity with two roots rather than a D7-rejected empty-root artifact |
| `E4-SCHEMA-CE-039` | SEEDED | A Lean field or binder name determines a persisted key spelling, or the payload carrier owns persisted field keys | `Test/Schema/PayloadContract.lean` compile-negatives on `Representation.persistedFieldName` and `Representation.fieldKeyName`; `$ref` (`:1068`) is not a legal Lean identifier and the scalar envelope adds `type`/`value` keys and five discriminator values (`:992`, `:998-1008`, `:1043`) | assign every persisted key spelling to the wire-profile packet (`SC-WIRE-01`/`SC-WIRE-02`); until it exists `SC-REP-FIELD-PIN` stays a single-route lexical extraction with no Lean cross-check |
| `E4-SCHEMA-CE-041` | SEEDED | Structural equality on the payload's binary64 datum is the host's `===`, signed zero may be normalized, or five named cases characterize finiteness | `Test/Schema/PayloadContract.lean`: bit-pattern bijection, `Float64.negZero_ne_zero`, and all-bit exponent-field finiteness characterizations; executed `-0` behaviour in `test/fixtures/schema-representation/` §2g | name structural equality and its three non-claims, preserve signed zero, and characterize finiteness for every `UInt64` pattern rather than only the named NaN/infinity/zero constants |
| `E4-SCHEMA-CE-043` | SEEDED | Traversing direct `Representation` children and `FilterGroup.checks` is a complete general recursion over the payload family | `Test/Counterexamples/Schema/RecursiveElimination.lean`: a root with one filter and one nested annotation schema has shallow trace `[String, Filter]` and complete trace `[String, Filter, Number]`; the twelve-label witness gives every recursive route a distinct observation, detecting omission, duplication, and reordering | expose one two-sorted nondependent fold whose algebra receives recursively substituted `CheckRepresentationAnnotationOf`, `ElementOf`, `PropertySignatureOf`, and `IndexSignatureOf` values, plus mapped representation/check lists; retain all 24 public constructor equations and both rebuild identity theorems |
| `E4-SCHEMA-CE-044` | SEEDED | A `Reference` with no annotation field is indistinguishable from a node whose present annotation field stores `none` | `Test/Counterexamples/Schema/AnnotationDataPlane.lean`, `collapsedNodeAnnotations` and `no_focus_ne_present_none` | expose `Representation.nodeAnnotations` as an `Optional`: `Reference` has no focus, while every other node has `some annotations`, including `some none` |
| `E4-SCHEMA-CE-045` | SEEDED | Looking up the first annotation key, or first collapsing the bag to a map, is enough for annotation updates | `Test/Counterexamples/Schema/AnnotationDataPlane.lean`, three ordered duplicate `k` entries around an unrelated entry | keep `Annotations` as the existing optional ordered list and make `payloadsAt` visit and update every matching payload in place without reordering, deduplicating, or touching unrelated entries |
| `E4-SCHEMA-CE-046` | SEEDED | `decode (encode value) = some value` is sufficient for an exact typed annotation view | `Test/Counterexamples/Schema/AnnotationDataPlane.lean`, `canonicalUnit` decodes every string but re-encodes `"authored-spelling"` as `"canonical"` | require both `decode_encode` and `encode_decode`; prove `entry_of_decodeEntry` so identity modification reconstructs the exact existing `AnnotationEntry` without a second Schema carrier |
| `E4-SCHEMA-CE-047` | SEEDED | Traversing only node annotation fields covers the recursive annotation data plane | `Test/Counterexamples/Schema/AnnotationDataPlane.lean`, `nestedRoutes`: the shallow result is `[arrays]`, while the exact nine-label result includes group/filter schemas, nested checks, an element bag and type, and a property bag and type | derive `Representation.annotationBags` and `Check.annotationBags` from the closed two-sorted fold; preserve structural preorder across every recursive route and every annotation-bearing child record |
| `E4-SCHEMA-CE-048` | SEEDED | A document annotation traversal may follow resolved or reachable references and skip the rest of the reference table | `Test/Counterexamples/Schema/AnnotationDataPlane.lean`, a root with no references plus three dead entries, including a duplicate key | traverse the root(s), then every `ReferenceEntry.representation` in stored list order; do not resolve keys or filter dead, duplicate, dangling, or unreachable entries in the structural data plane |
| `E4-SCHEMA-CE-049` | SEEDED | Typed annotation lookup is sufficient for effectful-field admission, so malformed and duplicate raw occurrences may be filtered away | `Test/Counterexamples/Schema/EffectfulField.lean`, one duplicate-valid bag and one malformed-plus-valid bag | derive admission from the exact raw `Annotations.payloadsAt` occurrence list; require one canonical payload and reject every other multiplicity or malformed spelling |
| `E4-SCHEMA-CE-050` | SEEDED | Equal read and write operation numbers identify an effectful field across alphabets | `Test/Counterexamples/Schema/EffectfulField.lean`, `operations` versus `otherAlphabet` | require alphabet, read-operation, and write-operation identities to agree; attach no service or refusal meaning at this layer |
| `E4-SCHEMA-CE-051` | SEEDED | Effectful `modify` may transform `Lens.get source` and skip its declared read operation | `Test/Counterexamples/Schema/EffectfulField.lean`, `field.modify` starts at `Op.read` while `staleModify` starts at `Op.write 11` | generate `modify` as effectful read followed by write and local replacement; the source lens value is not the read result |
| `E4-SCHEMA-CE-052` | SEEDED | Effectful `set` may return the pure lens replacement without performing the declared write | `Test/Counterexamples/Schema/EffectfulField.lean`, root operation of `field.set` versus the pure replacement program | perform the existing signature operation before returning the lens-replaced source and prove the same order through interpretation |
| `E4-SCHEMA-CE-053` | SEEDED | An effectful-field marker on a representation node marks an object property | `Test/Counterexamples/Schema/EffectfulFieldProperties.lean`, `nodeOnly` versus `propertyOnly` | inspect only the existing `PropertySignatureOf.annotations` bag for direct discovery; node, check, element, and property-type annotations do not mark the property |
| `E4-SCHEMA-CE-054` | SEEDED | Scanning only the current object's property list inventories effectful fields recursively | `Test/Counterexamples/Schema/EffectfulFieldProperties.lean`, `recursiveWitness` with fourteen exact discoveries through object properties, property types, index positions, arrays, templates, unions, suspend, and both check-schema routes | derive one collector from the existing `Representation.fold`; preserve the fold's structural preorder and reuse its closed recursion proof graph |
| `E4-SCHEMA-CE-055` | SEEDED | A map or set is an adequate effectful-field inventory because property names identify occurrences | `Test/Counterexamples/Schema/EffectfulFieldProperties.lean`, the two stored `duplicate` properties retain read-operation identities `21` then `31` | return an ordered list of the existing full property paired with its existing spec; preserve duplicate names, multiplicity, pairing, and authored order |
| `E4-SCHEMA-CE-056` | PINNED | Every typed natural round-trips through binary64 JSON | `Test/Counterexamples/Schema/Codec.lean`, `natural_collision` and `natural_roundTrip_impossible`: 2^53 and 2^53+1 have the same image | owner amendment 2026-09-11: check exact value recovery before accepting an encoding |
| `E4-SCHEMA-CE-057` | PINNED | Ordinary subtyping alone guarantees identical JSON encodings | `Test/Counterexamples/Schema/Codec.lean`, `subtype_encoding_impossible`: one constructor is both Result Failure and Exit Success, each a subtype of their union | require a shared JSON layout in addition to subtyping and membership |
| `E4-SCHEMA-CE-058` | PINNED | Every value admitted as a list is an ordinary list | `Test/Counterexamples/Schema/Codec.lean`, `snapshot_has_list_bool`, `snapshot_not_list`, and `list_totality_impossible`: empty fiber snapshot is admitted but has a distinct carrier | JSON value admission excludes snapshots; do not flatten away their identity |
| `E4-SCHEMA-CE-059` | PINNED | Left-biased union encoding and decoding always recover the original constructor | `Test/Counterexamples/Schema/Codec.lean`, `left_biased_roundTrip_impossible`; `Test/Codegen/SchemaGenerationContract.lean` retains the current empty-list/empty-Cause ambiguity | use pinned Result.success and validate exact recovery at union boundaries |
| `E4-ENV-CE-001` | SEEDED | A context key carries its service type as a Lean type index, so `Environment` is a dependent map | `Test/Machine/Environment/ContextKeyContract.lean`: `@Effect4.ServiceKey : Type` plus the exact `ServiceKey.rec` snapshot, and the `ServiceKey.Service`/`ServiceKey.universe` compile-negatives | keep the key first-order: a nominal `ServiceName` paired with a first-order `ServiceTypeCode`, with the reading of a code as a Lean type supplied separately by `ServiceUniverse`; `DecidableEq` alone does not exclude the indexed family and is not cited as if it did |
| `E4-ENV-CE-002` | SEEDED | A key's nominal name determines its service, so name equality is key equality | `Test/Machine/Environment/ContextKeyContract.lean`: `ServiceKey.conflict_iff`, one conflicting and two non-conflicting ground pairs, and the same-name disequality stated without `Conflict` | make the whole pair the identity and freeze nominal collision as a named decidable relation at the key node; leave to `Context/Environment` whether a colliding pair may be held |
| `E4-ENV-CE-003` | SEEDED | A first-order service type code is a faithful name for a Lean type, so type equality recovers code equality | `Test/Machine/Environment/ContextKeyContract.lean`, `ServiceUniverse.exists_carrier_collision`, plus the `ServiceUniverse.code` compile-negative | prove the collision rather than warn about it; route every typing statement through the code, never through `Type` equality, and supply no inverse interpretation |
| `E4-ENV-CE-004` | SEEDED | `DecidableEq` on keys is enough for a requirement row, as the `Context/Key -> Context/Requirement` edge description states | `Test/Machine/Environment/ContextKeyContract.lean`: `ServiceKey.lt_iff`, `lt_irrefl`, `lt_trans`, `lt_trichotomy`, a synthesized `Decidable (a < b)`, and four kernel-decided ground comparisons | freeze a decidable strict linear order at the key node so `Context/Requirement` neither mints a second canonical-order notion nor falls back to authored insertion order; state no `DATA-ROW` law here |
| `E4-ENV-CE-005` | SEEDED | Any decidable strict linear order on keys serves equally, so the three order laws settle the canonical spelling | `Test/Machine/Environment/ContextKeyContract.lean`: `ServiceKey.lt_iff` and the ground comparison `⟨⟨0⟩, ⟨9⟩⟩ < ⟨⟨1⟩, ⟨0⟩⟩`, which is false under a service-major order | pin the exact name-major lexicographic relation, not only its three laws; record contiguity of nominally equal keys as the reason for the direction and prove no adjacency theorem at `L0` |
| `E4-ENV-CE-006` | SEEDED | A service value may cross service type codes without an equality proof | `Test/Machine/Environment/ContextKeyContract.lean`: the ascribed signature of `ServiceKey.transport`, `ServiceKey.transport_rfl`, and the `ServiceKey.cast` compile-negative | supply exactly one transport, with the code equality as an explicit argument and no instance argument that would let a default value replace the transported one |
| `E4-DATA-CE-001` | SEEDED | A row is a raw list, or equal raw membership implies raw-list equality, so canonicality may remain an external agreement check | `Test/Data/RowContract.lean`: exact proof-carrying `Row.mk`, `Row.eq_of_mem_iff`, absent `Row.ofList`, and the raw witnesses that `[1,2]` and `[2,1]` have equal membership but unequal spellings while canonicality and agreement are independent | retain one proof-carrying `Row`; make noncanonical construction unspellable and restrict membership extensionality to checked rows |
| `E4-DATA-CE-002` | SEEDED | Order-preserving duplicate removal is normalization | `Test/Data/RowContract.lean`: `ascending_iff`, `Row.mem_normalize`, and ground reductions requiring `normalize [3,1,2] = [1,2,3]` and `normalize [2,1,2] = [1,2]`; Foldlab late `s2/attack-EC1-T012.lean` supplies the adopted failure shape | normalize by structurally recursive sorted insertion against the ambient standard lawful order, never by first-occurrence dedup |
| `E4-DATA-CE-003` | SEEDED | Normalization retains multiplicity or is injective | `Test/Data/RowContract.lean`: `Row.normalize_duplicate` and the ground equality `normalize [1,1] = normalize [1]`, while raw list inequality is immediate | record presence rather than multiplicity and expose duplicate erasure as a theorem so no consumer relies on a converse |
| `E4-DATA-CE-004` | SEEDED | `Data.Row` may bundle or invent an order/comparator independent of the element owner's frozen relation | `Test/Data/RowContract.lean`: every operation quantifies over `LE`/`LT`/`DecidableEq`/`DecidableLT`/`Std.IsLinearOrder`/`Std.LawfulOrderLT`; a test-only reverse Nat instance changes canonical spelling; absence guards reject `Effect4.RowOrder` and `Row.Comparator` | consume Lean's standard lawful-order classes and the element owner's existing relation; mint no Effect4 comparator or order-law carrier |
| `E4-DATA-CE-005` | SEEDED | Concatenation or a left-favouring merge is row union | `Test/Data/RowContract.lean`: `Row.mem_union`, both operand-order reductions, the commutative/idempotent/identity laws, and absent `Row.append` | define union through canonical normalization and lift membership equality through `Row.eq_of_mem_iff` |
| `E4-DATA-CE-006` | SEEDED | Row membership may be a test defined independently of the stored canonical elements | `Test/Data/RowContract.lean`: `Row.mem_def`, synthesized decidable membership, and every insert/normalize/union theorem stated through the same `Membership` instance | freeze one membership observation and prove it is exactly underlying-list membership |
| `E4-DATA-CE-007` | SEEDED | Weakening is spelling containment, or subset direction may be silently reversed | `Test/Data/RowContract.lean`: `Row.subset_iff`, decidability, `subset_refl`, `subset_trans`, `subset_union_left`, `subset_union_right`, and the positive ground subset | use conventional member inclusion: `Subset r s := ∀ a, a ∈ r → a ∈ s`; state both union inclusions explicitly and leave program-level provision meaning to Context |
| `E4-DATA-CE-008` | SEEDED | An opaque or classical decision procedure is acceptable because it inhabits `Decidable` | `Test/Data/RowContract.lean`: explicit `DecidableEq`/`DecidableLT` binders plus kernel reductions for insert, normalization, duplicate erasure, union, and subset; the satisfiable reference reaches `[propext, Quot.sound]` and not `Classical.choice` | implement structural sorted insertion that reduces in the kernel and keep every exported theorem within the no-choice axiom ceiling |
| `E4-DATA-CE-009` | SEEDED | Normalization idempotence or membership preservation establishes denotational preservation | `Test/Data/RowContract.lean`: `normalize_idempotent`, `normalize_duplicate`, and the absent `normalization_preserves_denotation`; `[1,1]` and `[1]` collapse despite distinct authored multiplicity | report only canonical row equalities; require a later calculus to prove its observation multiplicity-insensitive before relating normalization to denotation |
| `E4-SEM-CE-001` | SEEDED | A tree-shaped cause with sequential and parallel nodes faithfully refines rc.112's cause | `Test/Counterexamples/Machine/Semantics/CauseExit.lean`, `tree_invents_structure` and `tree_empty_not_unique` | keep one flat ordered reason list; `Cause.ext` makes the list the whole content and `Cause.combine_no_new_reason` forbids a node |
| `E4-SEM-CE-002` | SEEDED | `causeCombine` is concatenation | `Test/Counterexamples/Machine/Semantics/CauseExit.lean`, `append_duplicates_reasons` and `append_breaks_self_combine` | deduplicate by first occurrence; freeze both `Cause.dedup` equations, `Cause.combine_reasons`, `Cause.combine_order`, and the `Cause.combine_self` short circuit |
| `E4-SEM-CE-003` | SEEDED | `causeSquash` may report whichever reason comes first, or may prefer an earlier defect over a typed error | `Test/Counterexamples/Machine/Semantics/CauseExit.lean`, `squash_must_partition_before_choosing`, `squash_interrupt_does_not_shadow_defect`, `squash_empty_is_a_fourth_arm` | partition before choosing: a `Fail` anywhere beats a `Die` anywhere, the first `Fail` wins among fails, and `emptyCause` is a distinct fourth arm |
| `E4-SEM-CE-004` | SEEDED | Annotation merge may let the incoming map win, or may move an overwritten key to the end | `Test/Counterexamples/Machine/Semantics/CauseExit.lean`, `annotate_must_not_overwrite_by_default`, `annotate_overwrite_keeps_position`, `annotate_empty_is_identity` | freeze `ReasonAnnotations.annotate_entries`; keep existing values unless `overwrite := true`, keep the key's slot when overwriting, and make `annotate` with the empty map the identity |
| `E4-SEM-CE-005` | SEEDED | A finalizer failure may be discarded in favour of a successful exit, or the merge itself re-raises the original exit cause | `Test/Counterexamples/Machine/Semantics/CauseExit.lean`, `finalizer_failure_stands_alone` and `successful_finalizer_under_failed_exit` | four exact `Exit.mergeFinalizer_*` arms from `internal/effect.ts:3800-3804`, plus three `Exit.restoreAfterFinalizer_*` arms for the caller-side composite at `:4023-4028` |
| `E4-SEM-CE-006` | SEEDED | `exitAsVoidAll` fails whenever any input exit is a `Failure` | `Test/Counterexamples/Machine/Semantics/CauseExit.lean`, `empty_cause_failure_joins_to_success` and `join_is_not_combine` | test the concatenated reasons, not the exits: `Exit.asVoidAll_reasons`, `asVoidAll_empty_cause`, `asVoidAll_keeps_duplicates` |
| `E4-SEM-CE-007` | SEEDED | `Effect4.Data.Row`, the canonical finite-set carrier, can carry a cause | `Test/Counterexamples/Machine/Semantics/CauseExit.lean`, `union_is_not_commutative` and `order_changes_squash` | retain an ordered `List`; state `Cause.combine_order` as an operand-order-preserving union and record the `Row` separation in `docs/research/CAUSE-DAG.md` |
| `E4-PROV-CE-001` | SEEDED | `Layer.merge`/`mergeAll` provides a sibling's requirement, so bindings and the services built from them may sit side by side | `Test/Program/ProvisionContract.lean`, `sibling_mistake_stays_open` (the signature still requires both bindings) and the machine `#guard` (`buildSucceeds … siblingMistake = some false`: `serviceNotFound`, a defect); positive control `deployment_closed` under `provideMerge` | `LayerTy.merge_requires`: a `merge` unions requirement rows and discharges nothing; only `provide`/`provideMerge` discharge (`LayerTy.provide_discharges`, `provide_closed`) |
| `E4-PROV-CE-002` | SEEDED | A layer's signature determines the context it builds | `Test/Program/ProvisionContract.lean`, `order_invisible_to_type` (`leftWins`, `rightWins` share a `layerTy`) beside the two `#guard`s that build `(10, 2)` and `(10, 1)` through the specification and the machine — `Machine/Context.lean` CE 5 lifted to layers | the type sees rows (`LayerTy.merge_rows_comm`); the run sees a right-biased merge (`Context.get?_merge`); the signature never claims provider order |
| `E4-PROV-CE-003` | SEEDED | `Layer.orDie` is one more layer description | `Test/Program/ProvisionContract.lean`, `orDie_typed`: typed with `E := never` (`Layer.ts:3327`); since the join (2026-09-07) the compile route builds it as `catch_(build, die)` and a leaf's typed failure comes out as the defect (`src/Effect4/Program/Provision.lean`'s `#guard`s) | before the join the Layer machine's lowering refused it by name (`PROV-FB-ORDIE-DESC`); the frame is now `Prim.onFailure` with `EffName.orDie`, no description alphabet in sight |
| `E4-PROV-CE-004` | SEEDED | A string literal is a layer value | `Test/Program/ProvisionContract.lean`, `string_value_refused` | `PROV-FB-STRING-VALUE`: `litVal` (`Program/Typing.lean`) admits no string as a layer value; the typing refuses. Since DB-15 (2026-09-08) strings are machine values on the native route (`Program/Native.lean` `Lit.toVal`); the layer-value refusal is the provision route's own |
| `E4-CONF-CE-001` | SEEDED | `mapInput` is pre-composition on `load`, so a provider is a function of the path it is asked for | `Test/Program/ConfigContract.lean`, `ce001_mapInput_is_post_composition` and `ce001_precomposition_reading_misses` beside the two `#guard`s they restate: `(P.mapInput f).load []` answers `ok (some (value "hit"))` where `P.load (f [])` answers `ok none`; positive control `ce001_fresh_is_precomposition` | `makeSource(get, flow(transform, f))` post-composes (`ConfigProvider.ts:373`), so `load` is `get (f (t p))`; `load_mapInput_fresh` is the fresh-source special case the naive reading over-generalises, and `mapInput_mapInput` says the later transform runs outermost |
| `E4-CONF-CE-002` | SEEDED | An env-backed provider answers wherever rc.112 answers, the flat `_`-joined spelling included | `Test/Program/ConfigContract.lean`, the pair of `#guard`s: `(fromEnvRecord envDb).load [key "DATABASE_HOST"]` is `ok none` where rc.112 answers `"localhost"`, while the segmented `[key "DATABASE", key "HOST"]` — the positive control — answers `"localhost"` on both readings | the model keeps the trie (`ConfigProvider.ts:1206-1207`) and drops the flat alias (`:1227`); every `ConfigTerm` reaches a leaf through `prefix ++ [key name]` (`eval`, `reads`), so no term of §4 can observe the quotient and a provider written against the flat spelling would |
| `E4-CONF-CE-003` | SEEDED | `${}` substitution is a total rewriting, so every dotenv file has a normal form | `Test/Program/ConfigContract.lean`, `ce003_self_cycle_refused` and `ce003_two_key_cycle_refused` beside the `#guard`s on `expand 8`; positive control `ce003_acyclic_chain_expands` (`A=${B}, B=x` expands to `"x"`) | rc.112 recurses on the substituted text with no depth bound and no cycle check (`ConfigProvider.ts:1396-1399`) and, transcribed and run in node on 2026-09-04, exceeds a call depth of 2000 and throws; the model refuses by fuel, `expand_ref_self_refused` refuses at *every* fuel, and `expand_fuel_mono` says more fuel never loses an answer, so the refusal is not impatience |
| `E4-CONF-CE-004` | SEEDED | `orElse` is a commutative monoid on `load`, so the order of a provider stack does not matter | `Test/Program/ConfigContract.lean`, `ce004_left_wins` and `ce004_swap_changes_the_answer` beside the two `#guard`s: two sources disagreeing at one path answer `"a"` and `"b"` under the two orders; positive controls `ce004_associative`, `ce004_idempotent`, `ce004_empty_is_a_unit` | `makeOrElse` returns the first *found* node and consults its fallback on absence only (`ConfigProvider.ts:379-383`); every other monoid law stands — `orElse_assoc`, `orElse_idem`, `orElse_empty_left`, `orElse_empty_right` — and commutativity is the one the algebra never claims |
| `E4-CONF-CE-005` | SEEDED | `withDefault` over a group fires whenever the group produced no value, so forgetting one member of a pair silently takes the default | `Test/Program/ConfigContract.lean`, the pair of `#guard`s on `withDefault (zip (string "host") (string "port")) (str "fallback")`: with `host` supplied it is `Failure ⟨missing [key "port"], true⟩`, with neither supplied it is the default with `hasInput = false` (the positive control), and with both supplied it resolves | `withDefault` maps the success channel's `Absent` case only (`Config.ts:847-852`), and a group one of whose members read input while another is missing is a failure, not an absence (`Config.ts:674-675`, `:589-593`); `eval_withDefault_absent` and `eval_withDefault_resolved` are the two arms, and a found container with no co-located scalar *is* absent (`Config.ts:1041`, `:1211-1213`), which is what the default legitimately fires on |
| `E4-CONF-CE-006` | SEEDED | `nested` names a path in the provider's own coordinates, so `nested` and `constantCase` commute | `Test/Program/ConfigContract.lean`, the pair of `#guard`s: `constantCase` then `nested "app"` reads `app_HOST` and answers `"localhost"`, `nested "APP"` then `constantCase` reads `APP_HOST` and answers `"prod"`; the law beside them as `ce006_later_nested_is_outer`, the refuted reading as `ce006_inner_prefix_reading_misses` | `nested` is `mapInput` with a prepended prefix (`ConfigProvider.ts:883-886`) and `mapInput` post-composes (`:373`), so a later `nested` prepends outside every transform already on the provider and reaches the source uncased (`:767-772`); `nested_nested` is the law that stands: the later prefix is the outer one |
| `E4-RUN-CE-001` | SEEDED | Scope close may run its finalizers and then write the `Closed` state | `Test/Counterexamples/Machine/Runtime/Scope.lean`, `close_must_write_state_first` and `close_state_is_independent_of_finalizers` | write the state first; `Scope.closeState` takes no `run` argument, `Scope.close_state_independent_of_run` holds by construction, and `Scope.close_reentrant_add` states what a re-entrant finalizer observes |
| `E4-RUN-CE-002` | SEEDED | Scope finalizers run in registration order | `Test/Counterexamples/Machine/Runtime/Scope.lean`, `close_order_is_lifo` and `close_order_changes_the_cause` | reverse the materialised list; `Scope.closeOrder_eq`, `Scope.closeOrder_last_first`, `Scope.closeExits_reverse` |
| `E4-RUN-CE-003` | SEEDED | Closing a scope twice re-runs its finalizers, or overwrites the stored closing exit | `Test/Counterexamples/Machine/Runtime/Scope.lean`, `close_is_idempotent` and `close_guard_prevents_double_run` | guard on `Closed` first; `Scope.close_idempotent`, `Scope.close_twice`, `Scope.closeState_idempotent`, `Scope.closeResult_closed` |
| `E4-RUN-CE-004` | SEEDED | Adding a finalizer to a `Closed` scope registers it, or silently drops it | `Test/Counterexamples/Machine/Runtime/Scope.lean`, `add_after_closed_runs_now` and `add_after_closed_registers_nothing` | run it now with the stored closing exit and register nothing; `Scope.addExit_closed`, `Scope.addExit_closed_registers_nothing`, `Scope.addUnsafe_closed` |
| `E4-RUN-CE-005` | SEEDED | Finalizer removal may normalise every scope into a keyed map, and a cleared inline slot is just the empty map | `Test/Counterexamples/Machine/Runtime/Scope.lean`, `remove_leaves_non_open_untouched`, `cleared_inline_slot_is_its_own_state`, `remove_inline_miss_is_a_no_op` | leave a non-`Open` scope untouched and keep `empty`, `openEmpty`, and `openMap []` three distinct states; `Scope.removeUnsafe_not_open`, `ScopeState.openEmpty_ne_openMap_nil`, `Scope.addUnsafe_openEmpty` |
| `E4-RUN-CE-006` | SEEDED | A child scope forked from a `Closed` parent is a fresh `Empty` scope | `Test/Counterexamples/Machine/Runtime/Scope.lean`, `fork_of_closed_parent_is_born_closed` | born `Closed` carrying the parent's exit; `Scope.fork_closed_parent`, `Scope.fork_closed_parent_child_exit` |
| `E4-RUN-CE-007` | SEEDED | The parent-side and child-side fork finalizers may each mint their own key | `Test/Counterexamples/Machine/Runtime/Scope.lean`, `fork_link_needs_one_shared_key` and `fork_registers_the_same_key_on_both_sides` | one shared key on both sides so the child's finalizer detaches the parent exactly; `Scope.fork_shared_key`, `Scope.fork_detach` |
| `E4-RUN-CE-008` | SEEDED | A failing finalizer aborts the sequential scope close | `Test/Counterexamples/Machine/Runtime/Scope.lean`, `sequential_close_captures_failures` and `short_circuit_loses_a_reason` | capture each finalizer's exit and keep going; `Scope.closeExits_length`, `Scope.closeResult_reasons` |
| `E4-RUN-CE-009` | SEEDED | Scope close always merges its finalizer exits through `exitAsVoidAll` | `Test/Counterexamples/Machine/Runtime/Scope.lean`, `single_finalizer_is_not_merged`, `many_finalizers_are_merged_flat`, `merge_is_concatenation_not_union` | keep three result arms: nothing, the single finalizer's own exit, and the merge; `Scope.closeResult_nil`, `Scope.closeResult_single`, `Scope.closeResult_many` |
| `E4-RUN-CE-010` | SEEDED | `getCont` runs a frame's `contAll` only when that frame answers the demanded arm | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `contAll_runs_on_skipped_frames`, `contAll_skipped_frame_is_still_popped` | run the ensure hook on every frame passed; `FrameFiber.popFrom_ranContAll`, `FrameFiber.getCont_ranContAll` |
| `E4-RUN-CE-011` | SEEDED | A user `catchCause` handler catches an interruption like any other cause | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `interrupt_skips_user_handlers`, `skip_requires_a_pending_cause` | keep popping `contE` frames while the fiber is interruptible with a pending cause; `FrameFiber.interrupt_skips_every_handler`, `FrameFiber.getCont_skip_of_no_pending_cause` |
| `E4-RUN-CE-012` | SEEDED | The handler skip is decided once, before the pop loop | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `mask_frame_stops_the_skip` | re-read the flag after every `contAll`, so a mask frame stops the skip; `FrameFiber.getCont_mask_stops_skip` |
| `E4-RUN-CE-013` | SEEDED | A `SetInterruptible` frame's `contAll` only assigns the flag | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `set_interruptible_substitutes_with_a_pending_cause`, `set_interruptible_without_a_cause_substitutes_nothing` | with a pending cause it returns `failCause(cause)` for either arm; `Prim.ensure_setInterruptible_substitutes`, `Prim.answerOf_replacement` |
| `E4-RUN-CE-014` | SEEDED | An `OnExit` frame's `contAll` leaves the interruptible flag alone | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `on_exit_finalizer_runs_masked`, `on_exit_told_not_to_mask` | clear the flag and push `SetInterruptible(true)` unless told not to; `Prim.ensure_onExit_masks`, `Prim.ensure_onExit_told_not_to` |
| `E4-RUN-CE-015` | SEEDED | Either `OnExit` arm returns whatever the finalizer produced | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `on_exit_restores_the_original_exit` | run the finalizer, then restore the original exit; `Prim.armA_onExit`, `Prim.onExit_finalizer_success_restores` |
| `E4-RUN-CE-016` | SEEDED | When both the computation and its finalizer fail, one cause is dropped | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `finalizer_failure_merges_by_combine`, `finalizer_failure_under_success_stands_alone` | merge by `causeCombine` on the failure arm only; `Prim.onExit_finalizer_failure_merges`, `Prim.onExit_success_finalizer_failure` |
| `E4-RUN-CE-017` | SEEDED | The continuation arms live on the op prototype, as `evaluate` does | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `arms_are_assigned_per_instance`, `the_three_frames_answer_different_arms` | store the continuation name per frame; `Prim.onSuccess_arm_is_per_instance`, `Prim.onFailure_arm_is_per_instance`, `Prim.onSuccessAndFailure_arms_are_per_instance` |
| `E4-RUN-CE-018` | SEEDED | An `Exit` yielded by a generator is returned as the next primitive like any other effect | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `iterator_folds_exits_inline`, `iterator_pushes_only_for_a_real_effect` | fold the inline run inside `contA`, so no `getCont` checkpoint sits between the folded values; `Prim.iterator_folds_inline`, `Prim.armA_iterator_resume` |
| `E4-RUN-CE-019` | SEEDED | `While`'s `contA` steps the cursor and runs the body again without re-testing | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `while_retests_through_contA`, `while_steps_before_it_retests` | step, re-test the stepped cursor, and only then push and run the body; `Prim.armA_whileLoop_continue`, `Prim.armA_whileLoop_stop` |
| `E4-RUN-CE-020` | SEEDED | Producing a value with an empty stack is a defect, or the value is dropped | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `empty_stack_yields_the_exit`, `empty_stack_yields_a_failure_unchanged` | yield the Exit the fiber produced; `FrameFiber.resumeValue_empty`, `FrameFiber.resumeCause_empty`, `FrameFiber.step_ofExit_finishes` |
| `E4-RUN-CE-021` | SEEDED | `withFiber` and `YieldableError` can be given behavioural models of the host object | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `with_fiber_cannot_see_the_raw_fiber`, `yieldable_error_cannot_see_the_host_class`, `yieldable_error_evaluates_to_its_own_failure` | refuse both identities by theorem while keeping the behaviours; `Prim.withFiber_refused`, `Prim.yieldableError_host_class_refused` |
| `E4-RUN-CE-025` | SEEDED | Restoring interruptibility replaces any completed scope exit with its pending interruption, including an existing cleanup failure | `Test/Counterexamples/Machine/Runtime/ScopeRestorationBoundary.lean`: `pending_does_not_replace_cleanup_failure`, `overwrite_failure_is_observably_wrong`; `harness/trace/scope-restoration.mjs`: real pending requests, body/cleanup failure and legal defect cases at two thresholds | existing FrameFiber failure-arm skipping retains the incoming failure through the restoration frame and delegates the arbitrary tail to its real step; `Test/contracts/scope-restoration.contract.md` freezes the full cause, tail and event equation without changing generic Runtime |
| `E4-RUN-CE-026` | SEEDED | Once inner restoration delivers interruption, later outer cleanup can discard its failures or see a predicted successful inner exit | `Test/Counterexamples/Machine/Runtime/ScopeRestorationBoundary.lean`: `outer_cleanup_retains_delivered_interrupt`, `dropping_outer_cleanup_is_observably_wrong`, `outer_cleanup_receives_actual_outgoing_exit`; `harness/trace/scope-restoration.mjs`: full Interrupt/Fail/Die reason order and retained state | start the actual outer ScopeMachine with the inner frame step's outgoing full Exit and service state; retain all actual responses, then use existing zero/one/many folding and Cause.combine; fallible-release JavaScript probes and legal release defects remain distinct host evidence classes |
| `E4-RUN-CE-022` | SEEDED | Answer shape or final exit alone identifies a full continuation-pop observation | `Test/Counterexamples/Machine/Runtime/LiveStack.lean`, `arm_shape_does_not_identify_payload`, `answer_only_misses_current`, `cause_tags_do_not_identify_annotations`, `answer_only_misses_event_order` | retain complete primitive payloads, current program, ordered/repeated cause reasons and annotations, popped frames, events and all returned fiber fields; the live-stack contract requires whole `FramePop` equality |
| `E4-RUN-CE-023` | SEEDED | A pop may decide interruption before its hook, or a failure entry may discard every deferred answer solely because skipping was requested | `Test/Counterexamples/Machine/Runtime/LiveStack.lean`, `post_hook_mask_stops_discard`, `stale_decision_positive_control`, `masked_deferred_distinguishes_inner_and_legacy`, `legacy_discards_deferred_event` | re-read interruption after hooks; keep a masked deferred answer and retain the event when an unmasked answer is discarded; old-entry compatibility is guarded by no deferred interruption, with separate unrestricted false-skip equality; representable-state evidence does not establish source reachability |
| `E4-RUN-CE-027` | SEEDED | A generator's progress is a function of its name and the last answer, so the `Iterator` frame can be pushed back unchanged | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `same_name_reenters_the_first_yield`, `advanced_name_reaches_done` | `IterStep.resume next continueAs` names the advanced generator and `Prim.armA` pushes `iterator continueAs cursor` back (`Prim.armA_iterator_resume`, `iterator_folds_inline` restated); found 2026-09-04 by writing a two-yield `Effect.gen` as `src/Effect4/Program/Eff.lean` data, `docs/research/2026-09-04-eff-compile.md` §0; no battery had run a two-yield generator — `docs/research/2026-09-04-stress-plan.md` |
| `E4-RUN-CE-028` | SEEDED | `While`'s step is a function of the body's answer alone, so the frame's stored cursor can be dropped at `contA` | `Test/Counterexamples/Machine/Runtime/Frames.lean`, `answer_only_step_cannot_count`, `cursor_step_counts` | `PrimInterp.loopStep : ν → β → β → β` reads the stored cursor and the answer, as rc.112's `step(value)` reads the closure it mutates (`internal/effect.ts:4624-4645`); `Prim.armA_whileLoop_continue`/`_stop` restated; found 2026-09-04 with a counting `Effect.whileLoop` as `Eff` data, `docs/research/2026-09-04-eff-compile.md` §4 G3 |
| `E4-RUN-CE-029` | SEEDED | The interrupt-children middleware is installed separately from `fork`, so a tracked (non-daemon) child can outlive its parent's exit when nothing installs it | `src/Effect4/Laws/Machine/Witnesses.lean`, `w5_fork_latches_the_middleware` (a tape with nothing but the root's start: the fork sets the latch and the parent's exit interrupts the child) and `w5_daemon_child_survives_parent_exit` (the child that survives is the daemon one, host golden `Test/fixtures/traces/fiber-m3/daemonSurvivesParentExit.tsv`); `Test/Program/CompileContract.lean`, `pMiddleware` on the bare tape and `pDaemon` | `forkChild` calls `interruptChildrenPatch()` (`internal/effect.ts:5253`, `:6656-6658`), so the `fork` arm with `daemon = false` sets `middlewareInstalled` (`withFiber_fork` restated); `RunDecision.installMiddleware` remains for installs that are not forks; the retired witness `w5_no_middleware_leaves_children` had pinned the opposite (`docs/research/2026-09-04-deep-review-2.md`, R2-6, found 2026-09-04 by the second review) |
| `E4-RUN-CE-040` | SEEDED | `Task.wake` and a `scheduled` owed resume are reachable from a printed program in this tree | `Test/Machine/Runtime/SchedulerCoreContract.lean` §Wake: the batch wake and the scheduled drain run only from a fixture store (`winterp`) and a posted task; the truth corpus (14 programs) never posts one — Deferred completes inline (`Deferred.ts:1655-1659`), and the rc.112 producers are `Latch.flushScheduled` (`internal/effect.ts:5583`), Queue's `releaseTakers` (`Queue.ts:1974`), the permit sweep (`Semaphore.ts:260`), the Pool wake (`Pool.ts:703`) and the STM pending wake (`Effect.ts:24350`), none an `Eff` form yet | `SCHED-FB-PRODUCER`: the shapes' meaning is fixed by `Machine/Wake.lean`, `taskCmds`, `drainOwed` and `RunInterp.wakeList` and pinned by fixtures, not by the truth harness, until Latch/Queue land (`docs/research/2026-09-08-scheduler-surface-dispatch.md` §5) |
| `E4-RUN-CE-043` | SEEDED | The logical clock can be set to a time, so a tape can move it backwards | `Test/Machine/Runtime/SchedulerCoreContract.lean` §Timer: the only clock decision is `advance (millis)`, a duration (`TestClock.adjust`, `testing/TestClock.ts:377-381`); `t8` moves the clock by `7` and by `0` and reads `7` | `TIMER-FB-SET-TIME`: rc.112's `setTime` (`:383-385`) goes backwards (finding 3 of `docs/research/2026-09-04-timer-semantics-and-proofs.md`) and is refused (R1); `TimerStore.WF` — every pending deadline at or after the clock — is the law a backwards move would break |
| `E4-RUN-CE-024` | SEEDED | The detached-stack extension to source AsyncFinalizer is safe because it selects the same next continuation or eventually returns the same exit | `Test/Counterexamples/Machine/Runtime/LiveStack.lean`, `current_profile_no_answer_keeps_stack` (every constructor but `asyncFinalizer`) and `asyncFinalizer_pushes_without_answering` (the shape the row named, reachable since the run-loop packet made `AsyncFinalizer` a `Prim` constructor); `src/Effect4/Machine/Frames.lean`, `FrameFiber.popFrom_asyncFinalizer_pops_its_push` (the divergence: the pushed restoring frame is popped inside the same pop, answering `replacement (failCause)`) and `popFrom_pass_no_push` (the fused reading survives for every frame that pushes nothing); independent source prefix regression still required by `git:c407ab7:test/contracts/live-stack.contract.md` | repaired 2026-09-03 (spike S1, `docs/research/2026-09-03-spike-s1-prim-parking.md` §2): the attack held — the fused loop did **not** agree with rc.112 on a successful async resume — so `popFrom` is unfused, structurally, with `passPushed` draining the frame a hook pushed before the frames below it; the executing-finalizer relation is the fiber machine's (`src/Effect4/Machine/Fibers.lean`) |
| `E4-CAS-CE-001` | SEEDED | One payload has one address, so the kind a node is filed under is a label rather than identity | `Test/Store/NodeContract.lean`, `entryNode` and `entryTwin`: the same 74-byte payload under the same spec is `1437a1…705b` at kind `export` and `ca0785…0990` at kind `annotation`, and everything after the kind byte agrees (`entryTwin.encode.drop 2 = entryNode.encode.drop 2`) | put the kind inside the addressed bytes: `nodeBytes = version ∷ kind.byte ∷ spec ∷ payload` (`docs/research/2026-09-04-cas-trait-facts.md` §5, Q3), never beside them, so refiling a payload at another kind is a different node and never a second name for the same one |
| `E4-CAS-CE-002` | SEEDED | A reference is thirty-two bytes, so a wrong-kind or short address is a lookup failure the store reports | `Test/Store/NodeContract.lean`, the `Ref` refusals: `Val.ref 6 addr` and `Val.ref 2 (addr.drop 1)` are both refused by `Canonical.decode (α := Ref Entry)` with no store in hand; `Test/Store/TraitContract.lean` repeats it for an annotation's `subject` and `prev` | frame a reference as `Tag.ref = 11` with the expected kind byte and thirty-two address bytes (Q4), and check both in `ofVal`, so a malformed pointer never reaches admission and a wrong-kind one never reaches a lookup |
| `E4-CAS-CE-003` | SEEDED | A trait, an annotation or any other side table may participate in what a node is | `Test/Store/TraitContract.lean`: seven trait-world nodes on top of the census entry, and `traitStore.find probeEntryAddress` is byte for byte `probeEntry` at the address it had; adding the superseding trait changes neither. `src/Effect4/Store/Traits.lean`, `nodeBytes_trait_free` and `trait_put_preserves` are the theorems | keep traits in plane B — `annotation` nodes with a typed payload and an `AnyRef` subject (Q8) — and prove that `nodeBytes`, `accepts`, `decode` and admission read the shape and the identity-bound plane only |
| `E4-CAS-CE-004` | SEEDED | A content-addressed store is a set, so putting the same content twice is idempotent by definition and needs no outcome | `Test/Store/WordContract.lean`: `probeStore.putNode probeEntry` answers `duplicate`, not `fresh`, and `(afterPut probeStore probeEntry).nodes.length = 2`; the third outcome is exhibited too — a hand-built occupant at the entry's address answers `conflict` with the occupant's bytes | make `put` answer `fresh \| duplicate \| conflict occupant` (Q6): the store is grow-only and keeps bytes, so a collision is surfaced with what is already there and never overwritten, and a caller that cannot tell the three apart cannot be written |
| `E4-CAS-CE-005` | SEEDED | Replaying a transfer word twice doubles it, or replaying it needs the store it was cut from | `Test/Store/WordContract.lean`: `Word.apply probeWord (replayed probeWord)` leaves the nodes and the roots exactly as they were, and `probeLocal.sync` applied to its own result is that result. `src/Effect4/Store/Word.lean`, `apply_idempotent` and `sync_idempotent` | make replay a fold of `put`, so idempotence follows from `duplicate` rather than from a set semantics; the reverse word is refused (`dangling`) instead, which is the children-first order the closure emits |
| `E4-CAS-CE-006` | SEEDED | Content addressing makes reflection unconditional: an address determines its bytes, so no store needs a conflict case | `Test/Store/WordContract.lean`, the degenerate store that files `⟨0, .«export», probeSchemaAddress, .nat 1⟩` at `probeEntryAddress` — a node at an address that is not the hash of its bytes; `putNode` of the real entry there answers `conflict` with the occupant, and `verify` on the one-byte-flipped entry (`1947 = 0x079b` to `1946 = 0x079a`) answers `digestMismatch probeEntryAddress` | state the hash lattice in levels (Q6): level 0 with no premise (`address a = address b → a = b ∨ an exhibited collision`), level 1 only under a named `Function.Injective sha256`, level 2 shown empty; `Store.verify` is what re-establishes level 0's left disjunct for a store in hand, and it is a check, never an assumption |
| `E4-CAS-CE-007` | SEEDED | Every schema node may name its own spec, so a store of documents needs no distinguished root | `Test/Store/NodeContract.lean`: `specFor` gives the zero spec to the meta-schema and `genesisAddress` to every other document, `nodeOf (Canonical.document Entry)` is not `IsGenesis`, and `src/Effect4/Store/Store.lean` refuses a non-genesis node with the zero spec as `dangling zeroDigest` (`Test/Store/WordContract.lean`) | make the meta-schema the unique zero-spec node (Q6) and every other schema node cite it; admission resolves edge 0 for everything else, so a schema node whose spec is not the meta-schema's address is a dangling node and not a store of its own |
| `E4-CAS-CE-008` | SEEDED | An optional field may render as a present-or-absent key in one place and a null in another, since both read back | `Test/Store/StoreContract.lean`, the `option` block: `print (some 3)` is the number and `print none` is `null` at top level, inside a pair and inside a list, while `encode (some 3) ≠ encode [3]` and `encode none ≠ encode ([] : List Nat)`, so one rendering does not collapse two shapes into one address; `Test/Store/Templates.lean` pins the same table for a `Nat`, an all-nullary sum and a struct | fix one rendering per shape constructor under version byte 0 (Q5): `option s ↦ anyOf [s, null]` everywhere, `struct` in declaration order, an all-nullary `sum` as string literals, `ref kind` as hexadecimal with a typed `ref = kind` annotation; a second rendering of the same shape is a version bump, not a choice |
| `E4-TYPED-CE-001` | RETIRED | A term that types always evaluates | retired 2026-09-08 (DB-15, the host rows slice): strings are machine values on the native route, `.lit (.str "x")` evaluates to `Val.str "x"` (`Test/Program/TypedContract.lean`, packet `Test/contracts/program-denotation.contract.md`), and the attacked statement is the theorem `evalTerm_isSome` with no `noStr` premise | none; the ID is kept and never reused |
| `E4-TYPED-CE-002` | SEEDED | `Val.nat` inhabits `.int` because both print as `number` | `Test/Program/TypedContract.lean`: `Val.hasTy (.nat 1) .int = false` while `Ty.render .nat = Ty.render .int` | `.int` is a refusal of the value typing (`TYPED-FB-INT`); the printer's identification is not the typing's |
| `E4-STORES-CE-001` | SEEDED | `syncOpStep` is total | `Test/Machine/Runtime/StoresLawsContract.lean`: `syncOpStep (.refGet ⟨0⟩) Stores.empty = none` | `syncOpStep_isSome_of_valid` carries `SyncOp.validIn` |
| `E4-STORES-CE-002` | SEEDED | A valid request's answer is valid without a heap invariant | `Test/Machine/Runtime/StoresLawsContract.lean`: the heap `[Val.cell ⟨9⟩]` answers the dangling `cell ⟨9⟩` to `refGet ⟨0⟩` | `syncOpStep_answer_valid` carries `Stores.WF` |
| `E4-DEN-CE-001` | SEEDED | An ill-formed `sync` term is a defect, as an ill-formed `succeed` is | `Test/Program/DenoteContract.lean`: `.sync (.var 3)` at the empty environment answers `success unit` on the machine (`syncValueAt`'s `getD Val.unit`) and `.succeed (.var 3)` answers `die badName` | `denote (.sync t)` uses `getD Val.unit`; `denote (.succeed v)` uses `badShapeExit` |
| `E4-DEN-CE-002` | SEEDED | The store handler may fail on a key the store never minted | `Test/Program/DenoteContract.lean`: `storeHandler.handle (.refGet ⟨5⟩) Stores.empty = (Val.unit, Stores.empty)`, as `stores.syncState` answers `none` and the machine falls back to `syncValue` | the handler's `none` arm is the machine's fallback, never a defect |
| `E4-DEN-CE-003` | SEEDED | The denotation and the machine agree on the trace | `Test/Program/DenoteContract.lean`: the trace of `Api.run pBindSync 400` holds `frame` events; `denote` performs store operations only | `run_eq_meaning` is stated on the exit and the stores; trace agreement is a later row under a mask |
| `E4-STORES-CE-003` | SEEDED | `Stores.WF` covers the program a completed Deferred stores | `Test/Machine/Runtime/StoresLawsContract.lean`: `deferredCompleteWith ⟨0⟩ (Completion.ofRefGet ⟨9⟩)` on a fresh cell is valid, steps, and leaves a `WF` store whose stored program reads the cell `⟨9⟩` the heap never minted | `WF` is not widened (`STORES-FB-COMPLETION` in the module header); the stored program is a `Prim` that `Val.validIn` never traverses |
| `E4-STORES-CE-004` | SEEDED | An entry of the memo world is present exactly while it is observed, by store steps alone | `Test/Machine/Runtime/StoresLawsContract.lean` §Rows: fork, build, hit, build again, release — the second build keys the map twice and the release leaves the second entry present with no observer (`observers = [1, 0]`) | The refcount law holds under the protocol's premise: `syncOpStep_memoObserved` needs `Stores.MemoKeysNodup`, which `syncOpStep_memoKeysNodup` keeps only when `memoBuild` follows a miss (`memoLookup`, `Compile.lean`); `Stores.MemoObserved` (`StoresLaws.lean`, the join review 2026-09-08). rc.112's `memoMapBuild` (`Layer.ts:396-411`) is reached only from a `get` miss (`:434-443`) |
| `E4-APPROX-CE-001` | SEEDED | The command loop is stable in fuel without a side condition | `Test/Machine/Runtime/ApproximationContract.lean` (packet `Test/contracts/machine-approximation.contract.md`): `pBindSync` under `evaluate` records one event at fuel `1` and two at fuel `2`, while fuel `1` leaves two commands unrun | `drive_stable_of_done` carries the residual `(driveState …).2 = []` |
| `E4-APPROX-CE-002` | REPAIRED | The outcome `finished` means the fuel sufficed | `Test/Machine/Runtime/ApproximationContract.lean`: `pSucceed` at fuel `3` has an exit but two drains left; it now reports a frontier, as do the yield and deferred-child witnesses at insufficient fuel | Stop replay at unfinished work; `Suffices_of_replay_terminal`; retired `APPROX-FB-FINISHED` |
| `E4-APPROX-CE-003` | REPAIRED | The fuel chain is monotone along every tape | `Test/Machine/Runtime/ApproximationContract.lean`: `pBindSync` at fuel `1` and `2` under `[evaluate, interruptFrom none ∅ root]`; the interrupt now waits and the traces are prefixes | `replay_obs_mono`; retired `APPROX-FB-REFRESH` |
| `E4-APPROX-CE-004` | REPAIRED | `fire` is monotone in fuel | `Test/Machine/Runtime/ApproximationContract.lean`: two queued starts at fuel `1` and `2`; the second task now waits behind the first unfinished task | `fire_trace_mono`; retired `APPROX-FB-REFRESH` |
| `E4-DEN-CE-004` | SEEDED | `run_eq_meaning` covers the whole straight-line fragment | `Test/Program/AgreementContract.lean`: `Straight pOnExit = true`; the witness was `Plain pOnExit = false` at the first landing and is `Plain pOnExit = true` since the repair the same evening (`Plain_eq_Straight`) | the `OnExit` frame's finalizer mask (`Frames.lean`, `ensure`) is modelled: `exitFrom` runs the finalizer program under the mask with the fiber's interruptible flag carried (`maskStack`), and the theorem is stated on `Straight` |
| `E4-PROGRESS-CE-001` | SEEDED | A well-formed store answers typed values: `answer_typed` needs no more than `Stores.WF` | `Test/Program/ProgressContract.lean`: the heap `[Val.bool true]` is `WF`, `refGet ⟨0⟩` is valid there with a typed request and answers `Val.bool true`, which has not the row's answer type `.nat`; that heap is not `HeapNat` | `answer_typed` carries `Stores.HeapNat`, the typing of the heap |
| `E4-PROGRESS-CE-002` | SEEDED | `HeapNat` survives every valid step, so its preservation can be stated on `SyncOp.validIn` | `Test/Program/ProgressContract.lean`: `refMake (Val.bool true)` is valid on the empty store and steps, and the store it leaves is not `HeapNat`; the request `Val.bool true` has not `refMake`'s request type | `step_heapNat` is stated on a typed request decoded through `syncOpOf` |
| `E4-DEN-CE-005` | SEEDED | The command loop never yields on a plain program | `Test/Program/AgreementContract.lean`: under the tape `[yieldVerdict root true, evaluate, flush]` the first iteration injects a yield (`yieldInjected` in the trace) and the run still finishes with the meaning; and `pLong`, a chain of `2100` binds (`4200` steps), yields twice under the ordinary run (`yieldInjected` at `defaultBudget` twice, `resumedWith` twice, `started` three times) and still finishes with the meaning | repaired the same night: `run_eq_meaning` carries no budget hypothesis. `drive_localRun` owes the exit path or a yield with at least `defaultBudget - 1` fewer steps left (`Owes`); `drive_loop_yield` parks the root (`Myield`), `fire_Myield` resumes it at count zero, `flushAll_Myield` takes the rounds; `pLong_agrees` is the instance |
| `E4-BEH-CE-001` | SEEDED | Trace-prefix order on arbitrary replay frontiers implies preservation of exits and allocated stores | `Test/Machine/Runtime/BehaviourContract.lean`: `frontier_projection_false`, two empty traces with one allocated cell versus none | `BEH-FB-TRACE-ORDER`; retain the terminal projection only, and require an execution relation or stronger order before a frontier projection |
| `E4-BEH-CE-002` | SEEDED | A sufficient tape is equivalent to a non-frontier result | `Test/Machine/Runtime/BehaviourContract.lean`: the empty tape suffices at fuel zero while the loaded root is still live | Keep command sufficiency separate from termination and unanswered tape decisions |
| `E4-HANDLE-CE-001` | SEEDED; repair proved and battery green, 2026-09-06 | A well-typed source program guarantees that all handles in the replay state exist | `Test/Machine/Runtime/CompletionContract.lean`: external answers return fiber seven or Ref seven, neither allocated, from the well-typed `waiting` program; `Test/Machine/Runtime/HandlesContract.lean`: both tapes fail `AnswersValid` and leave machines that are not `Minted` | `handles_minted` is now proved at `[propext, Quot.sound]` under the settled valid-input premise. Replay admission is unchanged; the unconditional claim remains false. The collected positions and exclusions are frozen in `Test/contracts/machine-handles.contract.md` |
| `E4-HANDLE-CE-002` | SEEDED | `Minted` can collect the cause's interruptor as a handle | `Test/Machine/Runtime/HandlesContract.lean`: the tape `interruptFrom (some ⟨7⟩)` interrupts `waiting`'s root from a fiber that never existed; the exit carries `interrupt (some ⟨7⟩)`, no fiber seven exists, the machine is `Minted` and the tape passes `AnswersValid` | The interruptor is provenance, not a handle: `exitKeys (Exit.failure _) = []` and `Cause` contributes nothing (refused by name in the header of `Machine/Handles.lean`) |
| `E4-HANDLE-CE-003` | SEEDED | `Val.validIn` certifies a handle, so the invariant can be stated over the stores alone | `Test/Machine/Runtime/HandlesContract.lean`: `(Val.fiber ⟨7⟩).validIn forgedFiber.stores` holds while `(Handle.fiber ⟨7⟩).existsIn forgedFiber.machine.world` fails | `Handle.existsIn` asks the machine's world (its fiber ids and its stores); `validIn`'s conditions are reused for the three store handles only |
| `E4-D6-CE-001` | SEEDED | Every interpreter can read a Ref from an external Completion | `Test/Machine/Runtime/CompletionContract.lean`: the Layer instance has no Ref heap and answers its existing unsupported-operation defect | `D6-FB-LAYER-REF`; exit answers work at that profile, Ref reads belong to the stores profile |
| `E4-SCHED-CE-001` | SEEDED | The summed handler of `Program/Sched.lean` is a semantics of the fiber operations | `Test/Program/SchedContract.lean`: `rHandler.handle (.inr FiberOp.getId)` on the empty store answers `Val.unit` and leaves the store, a `getId` naming no fiber | the right half remains a placeholder (`SCHED-FB-FIBER-HANDLER`); actual fiber execution is in `Program/RuntimeR.lean` (R3–R4) |
| `E4-SCHED-CE-002` | SEEDED | The R2 scout's assertion that reifying a successful error value collides with reifying a failed exit | `Test/Program/SchedContract.lean`: `exit_encoding_distinguishes` disproves that equality; `exit_encoding_roundtrip` proves the existing encoding is reversible for every exit | correct the design rationale; dependent answers expose exits directly but do not repair an encoding collision |
| `E4-SCHED-CE-003` | SEEDED | The R2 draft's zero-fuel failed exit satisfies straight-line agreement without a fuel premise | `Test/Program/SchedContract.lean`: `draft_zero_not_straight`; the actual compiler at zero fuel returns a live frontier, and a chosen branch does not consume compile fuel | keep fuel and unanswered choices unfinished; require sufficient fuel for the straight-line restriction and distinguish compile fuel from an unrolling budget |
| `E4-RTERM-CE-006` | SEEDED | An operation whose answer arrives as code through the shared loop costs the term the frame's count when it is saved as a lexical resume boundary | the R4 term counted more commands than the frame machine on the same shapes (`docs/research/2026-09-06-p0-fable-record.md` §2, `Term R4` column: `scopedContext` 28 against 20, `forkJoinImmediate` 7 against 5); `Test/Program/RuntimeRContract.lean` (P2 Lockstep) now pins equal exits, stores, command counts and least command budgets on 38 shapes and the audit's ten local command pairs | `ScopeFrame.answer` is the operation-answer adapter slot, distinct from the lexical `resume`: value answers install the continuation directly (`answerWith`), code answers are saved with `saveAnswerR`, and `popR` runs the saved continuation inside the pop, continuing the walk on `pure` and `unguard` (`E4-RTERM-CE-001` still holds: the slot is higher-order) |
| `E4-RTERM-CE-007` | SEEDED | The term needs an unfolding budget beside the compile fuel to denote a loop or a generator | at the same source and compile fuel the R4 term stopped at an unfolding frontier where the frame machine iterated; `Test/Program/DenoteRContract.lean` pins `denoteR` on `endlessLoop` and on the inline and resumed generators to the entry operations at every fuel, and `RuntimeRContract` runs `pGenLoop`, `pWhileLoop` and `pGenTwoYields` in lockstep with the frame | `denoteR root e p` is structural on `e`; `FiberOp.gen` and `FiberOp.loop` push the first-order `iter`/`loop` slots and the later iterations run inside the pop (`popR`, `walkR`), so the compile fuel is the only budget (`BehR_fuel_irrelevant` fixes the loaded code, not a second budget) |
| `E4-CHECK-CE-001` | SEEDED | `Effect.exit` always pushes an exit frame, so the frame machine's three counted steps on `exit (succeed v)` are the host's | the pinned host folds an `Exit` body into `exitSucceed(self)` (`internal/effect.ts:3621-3622`), one `Success` primitive: `docs/research/probes/p0-fable/host-results.json` rows `exitSucceed`, `exitFail`, `exitExit`; `Test/Program/CompileContract.lean` pins the corrected compile and `DenoteRContract` the classifier and the absent boundary | `compileEff (.exit b)` and `denoteR` fold through `Prim.asExit?` and `inlineYield` (`compileEff_exit_fold`, `denoteR_exit`); `meaning_of_asExit` and `denote_of_inlineYield` carry the straight agreements |
| `E4-CORE-CE-001` | SEEDED | Every choice of the generic evaluator preserves trace-prefix order with more fuel | `Test/Machine/Runtime/SchedulerCoreContract.lean`: `erasesTrace` makes trace lengths one and zero at fuel one and two | `CORE-FB-TRACE`; generic trace laws carry a command-step premise, discharged by the concrete frame instance |
| `E4-LIVE-CE-001` | SEEDED | The initial queue length bounds a flush's service time independently of command fuel | `Test/Machine/Runtime/SchedulingContract.lean`: `round_bound_alone_false`, two valid owners and two rounds at fuel one leave owner one unserviced | `LIVE-FB-FUEL`; `flush_fair` requires `FlushReady` for the initial queue |
| `E4-LIVE-CE-002` | SEEDED | A flush advances even when its armed queue names an unknown owner | `Test/Machine/Runtime/SchedulingContract.lean`: `unknownHead`, owner seven blocks owners zero and one | `LIVE-FB-UNKNOWN-OWNER`; the bound requires real callback owners |
| `E4-HOST-CE-001` | REPAIRED 2026-09-09 | A command fuel frontier means no malformed oracle entry was inspected | `Test/Api/ExternalContract.lean`: external natural-number row, Boolean oracle head, command fuel 2; the first draft returned a frontier with a recorded rejection | `replayCheckedFrom` checks the rejection produced by the current decision before testing its continuation flag; it does not inspect the remaining tape |
| `E4-HOST-CE-002` | REPAIRED 2026-09-09 | The oracle head remaining after a decision identifies the first failed registration | `Test/Api/ExternalContract.lean`: an eager natural-number child refuses the Boolean head, then a Boolean root consumes it in the same decision | The external store retains the first rejected row, completion and queue position; the checked walker reports that evidence even if the head later moves |
| `E4-HOST-CE-004` | SEEDED 2026-09-09 | Typing is admission: a program that types can be run, and `perform` at an operation is the same call as `callback` at it on every face | `src/Effect4/Program/Compile.lean:549-554` (read at `66ee4657`) with the foundation admission probe, 110 route comparisons over `NativeOp.all` (55 operations), exit 0 at `[propext]`: `perform sleep 1` **types**, prints the same call as `callback sleep 1`, and **dies `badName`**, because the `perform` async branch assumes every async operation is a Deferred await; `perform (external i)` types and parks at a frontier through the placeholder row's `.program` kind; and a row registered `.deferred` types, passes `LawfulTable`, and parks with its answer unused. The executable witness is `Test/Program/InvocationContract.lean` (the 55 × 2 route matrix and the six external negatives) when S1a lands | DI-61: one shared `asyncRoute` dispatcher extracted from the `callback` arm, with externals routed **first** and the placeholder's kind no longer consulted, so `compileEff (.perform op r) p = compileEff (.callback op r) p` for an external or async `op` (`compile_perform_eq_callback`), and the same equality at the API under `externalRow table i = some row`; plus `Api.checkTable` refusing an unregistrable supplied row **with its position**, and `Api.admitProgram`/`Api.runAdmitted` consuming a certificate whose fields are `typed`, `lawful`, `runnable` — `readable` deliberately **not** among them, since the historical wire program `pAwait` is typed, runnable and not image-certifiable (S1a). Landed at S1a: the extraction, `checkTable` and the certificate; the `perform` routing is pinned as `THE GAP` in the battery and is the scoped task S1a-b, because the reference denotation mirrors the old arm and `run_eq_ref` has no premise |

Area-specific attack shapes are in
[`Machine/Environment/ATTACKS.md`](Machine/Environment/ATTACKS.md),
[`Machine/Semantics/ATTACKS.md`](Machine/Semantics/ATTACKS.md),
[`Machine/Runtime/ATTACKS.md`](Machine/Runtime/ATTACKS.md),
[`Codegen/ATTACKS.md`](Codegen/ATTACKS.md) (the TypeScript target),
[`Schema/ATTACKS.md`](Schema/ATTACKS.md),
[`Data/ATTACKS.md`](Data/ATTACKS.md), and, for the retired areas,
[`Archive/algebra/ATTACKS.md`](Archive/algebra/ATTACKS.md),
[`Archive/flow/ATTACKS.md`](Archive/flow/ATTACKS.md) and
[`Archive/ocaml5-avatar/ATTACKS.md`](Archive/ocaml5-avatar/ATTACKS.md).
Fired implementation attacks
add a `BROKE / LAW / WITNESS / CLASS / FIXED-BY` record to the owning contract
packet without deleting the stable row.


## Conditional-handler target control (2026-09-10)

| ID | Status | Attacked statement | Witness / evidence | Forced repair |
| --- | --- | --- | --- | --- |
| `E4-HOST-CE-005` | SEEDED | Distinct fiber/token keys make answer application commute even when continuations share a Ref | `Test/Api/KeyedHostContract.lean`, `shared`: admitted checked AB exits 2, BA exits 1 | `reply_commute` covers receiving/storing replies; applications are separate explicitly ordered tape records |
| `E4-HOST-CE-006` | SEEDED | An unkeyed oracle answer queue identifies concurrent calls | `Test/Api/KeyedHostContract.lean`, `program`: reversed queue gives second child 2; keyed replies give it 3 | v2 records carry fiber/token keys and exact call claims; legacy queues retain their single-fiber limitation |
| `E4-RESID-CE-001` | PINNED | The tag residual of a `catchIf` whose test is `tagIs tag` is a preservation claim on every cause: a missed cause admitted at the body's column is admitted at `diffTag tag` of it | `Test/Program/CatchIfContract.lean`, `secondMiss` (the two-`Fail` miss retains the whole cause, `Api.run … = failure mixedValue`) and `twoFailTag` (the cause `[Fail ["B","x"], Fail ["A","m"]]` under a catch of `"A"` misses on its first `Fail`, is re-raised whole, and `causeAdmits` refuses it at `diffTag "A"`); the first-`Fail` rule is DI-09 | The adequacy statement carries the premise `SingleFail cause` (`Effect4.Program.SingleFail`, `Laws/Program/Residual.lean`, `catchIf_miss_admits`); the residual is a fidelity claim to rc.112's printed type (DI-17, amended 2026-09-11); `caughtErrorValue?` is unchanged |
| `E4-RESID-CE-002` | PINNED | `supportedErrTy e → supportedErrTy (diffTag tag e)` on every raw `e` | `Test/Program/TypeAlgebraContract.lean`: `e = union (prod never string) (prod (lit "X") string)` normalizes to `prod (lit "X") string` (supported); `diffTag "X" e = prod never string` is not | The law is stated on canonical columns (`supportedErrTy_diffTag` takes `Ty.Canonical e`), which is the only shape the checker cuts (`catchIfError` cuts `b.error.normalize`) |
