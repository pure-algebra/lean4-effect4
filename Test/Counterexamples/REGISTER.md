# Effect4 counterexample register

2026-09-04: the Flow route was archived off `main` to branch
`archive/flow-route` (decision D1 of
`docs/research/2026-09-04-prod-cleanup-inventory.md`). Every row below whose
attacked module now lives only on that branch — the flow, region, structure,
lowering, trace, family and foldlab lanes — is kept here as history: its ID
still resolves and is never reused, but no battery on `main` checks it any
more. Read those rows against the archive branch, not against this tree.

Stable IDs in this file are never reused. A row closes only when its witness
is retained and the repaired declaration or theorem mechanically rejects the
attack. `PINNED` means the witness already exists at the named immutable source
revision; `SEEDED` means the packet has frozen the attack but no immutable
provenance pin for its Effect4 witness has yet been recorded. `RESERVED` means
the stable row and forced repair are frozen now, while the executable witness
belongs to a later packet whose declarations are not open yet. `MOVED` means
the row and its witness now live in another repository at a named tag; the
ID is retained here so citations resolve, and the row is not reused.

| ID | Status | Attacked statement | Witness / evidence | Forced repair |
| --- | --- | --- | --- | --- |
| `E4-ALG-CE-001` | MOVED | Handler sum can be characterized by one arm or by examples | moved to lean4-effects `v0.1.0` (`5611c3a`), `test/counterexamples/REGISTER.md` there (origin status SEEDED; was: `Effect4Test/Algebra/ExtractionContract.lean`, `swappedSum`, differs on both arms) | require both projection equations and `Handler.sum_unique` |
| `E4-ALG-CE-002` | MOVED | Monad-morphism laws alone pin an interpreter | moved to lean4-effects `v0.1.0` (`5611c3a`), `test/counterexamples/REGISTER.md` there (origin status PINNED; was: Foldlab `Universal.lean:701–714`, `pinned_needs_op_agreement`, at commit `feb29321fd50204aa338209d313e84a3f8b71c66`) | require agreement with the supplied handler on every one-operation program |
| `E4-ALG-CE-003` | MOVED | Operation agreement alone pins an interpreter; or `Program S` is initial among bare monads | moved to lean4-effects `v0.1.0` (`5611c3a`), `test/counterexamples/REGISTER.md` there (origin status PINNED; was: Foldlab `Universal.lean:716–727`, `pinned_needs_the_morphism_law`, plus its two distinct state morphisms, same pin) | require both pure/bind preservation and operation agreement; state initiality only in the category of `S`-models |
| `E4-ALG-CE-004` | MOVED | All handler towers form one monoid | moved to lean4-effects `v0.1.0` (`5611c3a`), `test/counterexamples/REGISTER.md` there (origin status PINNED; was: Foldlab `Universal.lean:739–790`: composition changes source/target signatures; only `Handler S (Program S)` is closed) | state a category across signatures and a monoid only on endomorphisms |
| `E4-ALG-CE-005` | MOVED | Program result universes may vary independently of the signature answer and handler input universe | moved to lean4-effects `v0.1.0` (`5611c3a`), `test/counterexamples/REGISTER.md` there (origin status SEEDED; was: guarded rejection of `Program SmallSignature Type` in the algebra battery) | align the three universes; add only explicit, law-carrying lifts later |
| `E4-ALG-CE-006` | MOVED | A fixed-fuel evaluator admits a bind/composition law | moved to lean4-effects `v0.1.0` (`5611c3a`), `test/counterexamples/REGISTER.md` there (origin status PINNED; was: Foldlab `Universal.lean:894–910`, `run_has_no_composition_law`, same pin) | state composition at `interpret`/big-step, never at one fixed fuel |
| `E4-ALG-CE-007` | MOVED | The higher-order proof carrier is canonical first-order program content | moved to lean4-effects `v0.1.0` (`5611c3a`), `test/counterexamples/REGISTER.md` there (origin status PINNED; was: Foldlab `Prog.lean:12–15`: continuations are host functions and are not serializable) | keep `Program` as proof syntax; introduce a distinct checked first-order flow with an explicit embedding |
| `E4-ALG-CE-008` | MOVED | Inductiveness makes every `Program` a globally finite tree | moved to lean4-effects `v0.1.0` (`5611c3a`), `test/counterexamples/REGISTER.md` there (origin status SEEDED; was: `Effect4Test/Algebra/ExtractionContract.lean`: a `Nat`-indexed injective continuation has distinct children of unbounded finite depth) | describe `Program` as a well-founded higher-order W-tree; claim only selected-branch well-foundedness, not finite branching or a uniform depth bound |
| `E4-FLOW-CE-001` | SEEDED | Duplicate block identifiers may be resolved by first-row/last-row lookup or normalization | lean4-effects `EffectsTest/Counterexamples/Flow/FlowV2.lean`, `duplicateBlockFlow` (v0.4.0; the v1 battery retired with the Flow v2 re-freeze) | reject with `.duplicateBlockId` before canonical-order checking; preserve one unambiguous block table |
| `E4-FLOW-CE-002` | SEEDED | All dangling references can share a clause-only or block-only diagnostic | lean4-effects `EffectsTest/Counterexamples/Flow/FlowV2.lean`, `danglingRootFlow` and `danglingJump` | distinguish exact root-row and successor-slot sites and retain the missing target payload |
| `E4-FLOW-CE-003` | SEEDED | Resolved block references imply operation closure | lean4-effects `EffectsTest/Counterexamples/Flow/FlowV2.lean`, `unknownOperationFlow` | check every `perform` against the supplied closed alphabet before local term typing |
| `E4-FLOW-CE-004` | SEEDED | A known operation and resolved target suffice for term typing | lean4-effects `EffectsTest/Counterexamples/Flow/FlowV2.lean`, `answerSlotFlow` | require the last parameter of the target block to equal the operation answer type (clause `argumentTypeMismatch`, site `.argument`) and report expected/actual types |
| `E4-FLOW-CE-005` | SEEDED | Admission must reject cycles to retain an inductive meaning | superseded in v0.4.0 by lean4-effects `EF-FLOW-CE-002`: lean4-effects `EffectsTest/Counterexamples/Flow/FlowV2.lean`, `unchosenLoop` rejected with `unchosenCycle` and `chosenLoop` admitted | admit a closed cycle only through a `choose` (`CyclesWF`, decided by `cyclesChoose`), so a finite decision tape bounds every run; divergence stays out of graph admission |
| `E4-FLOW-CE-007` | SEEDED | Soundness/completeness pins which of several defects is reported | lean4-effects `EffectsTest/Counterexamples/Flow/FlowV2.lean`, `firstDiagnosticFlow` | freeze packet-owned clause order and prove the returned diagnostic is the first condemning clause |
| `E4-FLOW-CE-013` | SEEDED | A raw first-order branch may store a Lean continuation | compile-negative in lean4-effects `EffectsTest/Flow/FlowV2Contract.lean` (retained E4-FLOW-CE-013 section) | require nominal child `BlockId`s only; keep host functions outside canonical content |
| `E4-FLOW-CE-014` | SEEDED | Reference closure may ignore unreachable blocks, or admission may require every block reachable and acyclic | lean4-effects `EffectsTest/Counterexamples/Flow/FlowV2.lean`, `unreachableDanglingFlow`; since v0.4.0 an unreachable cycle must also pass a `choose` (`unreachableUnchosenLoop` rejected at `.block 1`) | allow unreachable cycles that pass a `choose`, and reject every dangling successor in the whole raw document |
| `E4-FLOW-CE-015` | SEEDED | Public proof projections are enough reason to expose an unchecked `CheckedFlow` constructor | direct-name and record-literal compile-negatives in lean4-effects `EffectsTest/Flow/FlowV2Contract.lean` (retained E4-FLOW-CE-015 section), reachable from `EffectsTest.lean` | keep `CheckedFlow.mk` module-private and require every external checked value to cross `admit` |
| `E4-FLOW-CE-016` | SEEDED | A failed clause licenses any site/payload, or only clause-level firstness needs proof | lean4-effects `EffectsTest/Counterexamples/Flow/FlowV2.lean`, `duplicateDecisionFlow` and `nestedSourceOrderFlow` plus the rejected `.flow/.none` witness | require `Diagnostic.Valid`, prove source-index and nested-order precision for every returned diagnostic, and expose `admit_error_valid` as the observational no-fallback law |
| `E4-FLOW-CE-017` | SEEDED | Fuel exhaustion may be reported as a failure (`done (failure _)`), giving a finite approximation a denotation it does not have | `Effect4Test/Counterexamples/Flow/Runner.lean`: with fuel 0 the run is `.frontier (.fuel ⟨0⟩)`, the log is `[frontier]` with no `done` row, and the same run with fuel finishes | DB-04: fuel and tape exhaustion are live frontiers (`Effect4.Frontier`), never outcomes; `run_fuel_mono` says more fuel changes nothing about a run that did not exhaust |
| `E4-FLOW-CE-018` | SEEDED | The head tape entry may be consumed whatever site it names | `Effect4Test/Counterexamples/Flow/Runner.lean`: a tape `[⟨2, true⟩]` at a `choose` of site 1 yields `.refusedSite ⟨1⟩ ⟨2⟩` with an empty log; an empty tape yields the unanswered frontier instead | R6: the tape is consumed by occurrence with a site check (`Tape.read`); a mismatch is a refusal, exhaustion an unanswered frontier |
| `E4-FLOW-CE-019` | SEEDED | A release that fails while a region closes with a failure replaces the failure | `Effect4Test/Flow/RegionRunnerContract.lean`, `releaseFails` and `nested`: the run's failure is the body's `"boom"`, and a release failure on a clean leave is the run's failure only because nothing failed before it; host: `generated/traces/flow/regionNested.empty` and `regionTwoFail.empty` agree under `m1` | `Exit.mergeFinalizer` and `Exit.asVoidAll` put the body's cause first; the runner keeps the first failure (`closeFrame` reports the first release failure, `unwind` never replaces the failure it closes with) |
| `E4-FLOW-CE-020` | SEEDED | Releases may run in registration order or with their own region's exit | `RegionRunnerContract.lean`: on a clean leave the second-registered release runs first and the first-registered release still sees `success 5` after the second failed; `Scope.closeOrder` and `Scope.closeExits` receipts; host: `regionTwoFail.empty` under `m2` shows both `finalizer 1 {"failure":"boom"}` rows latest-first | DISCHARGED both sides (spike S4, 2026-09-03). Runner half unchanged: releases run latest-first with the closing exit (`Frame.releases` latest first; `closeFrame` hands every release the same exit). Compile half repaired by P1: `Effect4.RegionSimulation.compileRegion` gives a region **one** scope frame (`Prim.onSuccessAndFailure`, names `RegionName.regionCont`/`RegionName.close`) and each registered release an `onExit` frame whose `finalizerExit` is `Exit.success ()` (`regionInterp_finalizerExit`), so the order half and the exit half now both hold on the machine: `close_success_region_compiled` and `unwind_to_frame_region` carry no premise, and `Effect4Test/Counterexamples/Target/RegionSimulationBoundary.lean` `repaired_frames_use_original_exit` pins the literal. Spike S4b (2026-09-03) adds the *order* half's other end: `closeWalk_agrees_regionLoop` proves the registration list the compile closes against is the runner's `Frame.releases` — same entries, same latest-first order — for every flow, so the `Scope.closeOrder` agreement is no longer only an evaluated receipt at five fixtures |
| `E4-FLOW-CE-021` | SEEDED | A close that merges several failures reports only the first and forgets the rest | `Effect4/Flow/Region.lean`: `closeFrame_failure_merge` and `runRegionsCause` keep every failure of a close in close order (the merged cause), while `runRegions` and every `leave`/`finalizer`/`done` row project the first; `Effect4Test/Semantics/ApproximationContract.lean` pins a two-failure carrier `[boom, boom]` with wire result `failed boom`. The host cannot observe the merge: a fallible release has no lowering (`E4-TARGET-CE-012`) | repair: the runner carries the cause and the wire is a projection of it, pinned by `Effect4/Flow/Region.lean` |
| `E4-FLOW-CE-022` | SEEDED | An interrupt is delivered wherever the tape answers it, so a mask either drops the delivery or interrupts inside an uninterruptible region | `Effect4Test/Counterexamples/Flow/Interrupt.lean`: one flow, one tape, two runs — unmasked the point at site `1000005` closes region 1 with `interrupted`; with region 1 masked the same tape lets the `perform` run, closes the region with `success 5`, and the run is interrupted only at the first point outside it; host: `generated/traces/flow/interrupt/interruptMasked.tsv` agrees under every mask | a mask defers (rc.112 `_deferredInterrupt`): `Effect4.Flow.interruptPoint` folds a delivered answer into `IState.pending` and returns `false` while `isMasked`, and the first unmasked point delivers it (`interruptPoint_masked_defers`, `interruptPoint_unmasked_delivers`) |
| `E4-FLOW-CE-023` | SEEDED | `Outcome.interrupted` carries no payload, so an interrupted close may hand its releases the exit the body would have had, or the failure arm | `Effect4Test/Counterexamples/Flow/Interrupt.lean`: the delivered run's only `finalizer` row is `interrupted` and no `finalizer` row carries a success, a failure or a defect, while the same release on an uninterrupted run sees `success 5`; host: `generated/traces/flow/interrupt/interruptFinalizer.tsv` shows `finalizer 2 {"interrupted":true}` and `finalizer 1 {"interrupted":true}` | an interrupted close is `closeFrame … .interrupted`, so `closeFrame_interrupted_log` puts one `finalizer region interrupted` row before each release's own rows, and `frameExit .interrupted` is the one-reason interrupt cause with no interruptor |
| `E4-FLOW-CE-024` | SEEDED | Passing the masked M2 golden proves that the host delivered and deferred an interruption under a real runtime mask | `harness/trace/interrupt-mask-boundary.mjs`, ten finite rc.112 runs at thresholds 1000000 and 3 over historical generated/tail bytes at `5b29edb`: the baseline withholds the request and passes; direct delivery without a mask interrupts too early; real `Effect.uninterruptible` retains the cause and delivers on restoration without the golden's outside decision | append-only M2 boundary amendment in `test/contracts/flow-regions-runner.contract.md`: emit a real mask around the scope and cleanup, send the request while masked, assert pending cause and exact rows; `_interruptedCause` is the masked pending cause, while `_deferredInterrupt` is the separate running/unmasked injection latch; this supersedes CE-022's host-boundary claim |
| `E4-FLOW-CE-025` | SEEDED | A pending request may wait for another outside interrupt point, including when the outside continuation returns immediately | `Effect4Test/Counterexamples/Flow/InterruptMaskBoundary.lean`, three corrected-boundary red guards and five positive controls: historical `maskedReturn` is admitted but reports success; `harness/trace/interrupt-mask-boundary.mjs` observes successful cleanup followed by interruption under the real mask, including direct return | deliver pending interruption after successful cleanup restores interruptibility and before any outside action, without another tape read or decision row; retain successful closing exits; nested masks and failing-exit/cause behavior remain explicit general obligations, not consequences of these finite controls |
| `E4-FLOW-CE-026` | SEEDED | A region flow can act on an operation's failure, so retry-or-give-up after a failing job is expressible over an aborting operation | `Effect4Test/Counterexamples/Flow/JobRunner.lean`, `abortFlow`: the graph is admitted, and the run ends `failed` at the abort with no row from the successor block, while the same graph over a non-aborting operation reaches it; the packet's own `jobPoison` golden is the same fact at scale (`generated/traces/job/jobPoison.poison.tsv`) | state a recoverable failure in the data reading of the error (`Except ε α` as the answer, rc.112 `Result`), which a `choose` can branch after, or add a handler former to `RawTerm`/`RegionTerm` and a catch arm to both runners; until then, say plainly that an aborting operation ends a flow run **Repaired 2026-09-03 (Flow v3, lean4-effects v0.7.0):** `RawTerm.performCatch` names a failure successor that receives the environment and the error; the runner continues there, no region unwinds, and the trace writes `failed` then the successor's rows. Positive control `caughtFlow` in `Effect4Test/Counterexamples/Flow/JobRunner.lean`; the job runner's `retry` and `requeue` goldens exercise it on the host |
| `E4-FLOW-CE-027` | SEEDED | A flow tests the answer it just received, so a queue-empty check is `next` answering 0 and a retry bound is a carried counter reaching zero | `Effect4Test/Counterexamples/Flow/JobRunner.lean`, `branchFlow`: two services differing only in what `ping` answers produce different logs and the same branch under one tape, and the same service under two tapes takes two paths; `Effect4Test/Flow/JobRunnerContract.lean` shows the job runner attempting job 0 on an empty queue | add a data-dependent terminator (a branch on a variable against an alphabet-declared decidable predicate) with arms in both runners and all three lowerings, or state everywhere that every branch of an admitted flow is a tape question and a graph's control is independent of its service **Repaired 2026-09-03 (Flow v3):** `RawTerm.branch` is taken by the value of a boolean test operand and is still a decision site — the tape entry is read, the `decide` row written, and a run whose tape disagrees with the value is refused (`RunResult.refusedValue`, since 2026-09-03 a constructor of its own), so every cycle stays tape-bounded and `Flow.denote` fuel-free. Positive control `valueFlow` in the same battery; the job runner's queue-empty test and retry bound are branches on the atoms `nonEmpty`/`positive` |
| `E4-FLOW-CE-028` | SEEDED | With an arity-aware request spelling a flow can perform any operation of the family: the request slot holds the tuple, so a graph assembles one from the values it is carrying | `Effect4Test/Counterexamples/Target/JobRequest.lean`, `unpairedRaw`: admission refuses a `perform` of the two-parameter row on a `number` slot, and no term of the flow language builds the tuple it wants -- `perform` names one request `Var`, a literal answers a constant and an atom is a unary wire function (`OpKind.atom`, `tableService`). The job runner is shaped by it: `Jobs.next` answers a ticket rather than a job id so that `run`, `ack` and `requeue` have a slot to be performed from, and the `snd` atom takes the ticket apart again for the one-parameter `attempt` (`harness/trace/Generate.lean`, `generated/traces/job/`) | add a pairing former to `RawTerm`/`RegionTerm` -- `pair (left right : Var) (target : BlockId)`, answering the product of the two slots' types -- with arms in both runners, all three lowerings and the admission clause that types it; or state everywhere that a flow performs a multi-parameter operation only on a tuple it received |
| `E4-FLOW-CE-029` | SEEDED | A Flow v3 `branch` is answered by the tape alone when its test operand has no boolean reading, because admission types the slot `boolTy` | `Effect4Test/Counterexamples/Flow/BranchValue.lean`, `probeBranch`: the flow is admitted (`BranchTestWF` sees the spelling `"boolean"`), a service answers `.nat 0`, and the run takes whichever edge the tape names -- both tapes are accepted and the two runs differ only in the `decide` row the tape dictated. The host has no such mode: `decisions.report` compares the reported test to the tape entry with `!==` (`harness/trace/tracer.ts:327-335`), so a non-boolean test dies as `TapeValueMismatch` | `plan`'s `branch` clause refuses when the test has no boolean reading agreeing with the tape (`Effect4/Semantics/Runs.lean`, `| none => .mismatch site site`, now one rule: `if testValue env test = some answer then .choose ... else .mismatch site site`), and `Skel.execControl`'s `branchIf` mirrors it. `plan_checked` is unaffected -- `PlanSized.mismatch` already discharges the case, so the module header's claim that the fallthrough is what keeps `stuck` unreachable was false. Positive controls in the same battery. **Repaired 2026-09-03:** the rule landed in `plan`, `Skel.execControl`, `plan_choose_inv`/`plan_mismatch_inv` and both block laws; `docs/TRACE-DAG.md` rows `flow-runner` and `structured-agreement` carry the ruling |
| `E4-SCHEMA-CE-001` | RESERVED | Overlapping `anyOf` members may be treated as unordered success | `schema/ATTACKS.md`; executable overlap witness assigned to the denotation packet | retain source member order and define `anyOf` observation as the first successful member |
| `E4-SCHEMA-CE-002` | RESERVED | `oneOf` may accept the first success even when another member succeeds | `schema/ATTACKS.md`; executable overlap witness assigned to the denotation packet | reject multiple successes and distinguish zero, one, and many successful members |
| `E4-SCHEMA-CE-003` | RESERVED | Enum encode is injective even when distinct names alias one value | `schema/ATTACKS.md`; alias witness assigned to the codec packet | qualify injectivity by a uniqueness condition or define an explicit canonical-name policy |
| `E4-SCHEMA-CE-004` | RESERVED | rc.112 document-codec field admission and semantic tuple well-formedness are the same judgment | `schema/ATTACKS.md`; executed rc.112 field-shape acceptance of optional-before-required plus a later semantic tuple witness | `FieldAdmissible` retains and accepts the authored order; the full tuple/denotational admission judgment separately rejects an optional member followed by a required member |
| `E4-SCHEMA-CE-005` | RESERVED | Every transformation has an unqualified encode/decode round trip | `schema/ATTACKS.md`; trim witness assigned to the transformation packet | state directional or normalized round-trip laws with their exact domain assumptions |
| `E4-SCHEMA-CE-006` | RESERVED | Decode-only service requirements also constrain encoding | `schema/ATTACKS.md`; split-environment witness assigned to the transformation packet | track decode and encode requirements separately and join only where both are used |
| `E4-SCHEMA-CE-007` | RESERVED | Transformation encodings compose in decoder order | `schema/ATTACKS.md`; non-commuting pair assigned to the transformation packet | freeze encoding composition direction with a theorem over an order-sensitive witness |
| `E4-SCHEMA-CE-008` | RESERVED | A declaration reference may decode without a registered reviver | `schema/ATTACKS.md`; missing-identity witness assigned to the registry packet | refuse an unresolved declaration identity at the checked registry boundary |
| `E4-SCHEMA-CE-009` | RESERVED | Duplicate reviver identities may use first- or last-row lookup | `schema/ATTACKS.md`; duplicate-identity witness assigned to the registry packet | reject duplicate identities before lookup so revival has one meaning |
| `E4-SCHEMA-CE-010` | RESERVED | A live-only local symbol may enter portable wire data | `schema/ATTACKS.md`; host-symbol witness assigned to payload and codec packets | exclude local symbols from checked portable keys and prove lowering cannot reintroduce them |
| `E4-SCHEMA-CE-011` | RESERVED | All host annotations survive portable serialization | `schema/ATTACKS.md`; non-JSON annotation witness assigned to the codec packet | specify and test the exact pruning or refusal policy for non-portable annotations |
| `E4-SCHEMA-CE-012` | RESERVED | Parsing object entries into a map preserves duplicate-key evidence | `schema/ATTACKS.md`; duplicate-key witness assigned to the document packet | retain ordered raw entries until duplicate detection, then construct a checked map |
| `E4-SCHEMA-CE-013` | RESERVED | A bare self-reference is a guarded recursive representation | `schema/ATTACKS.md`; self-cycle witness assigned to reference admission | reject reference cycles with no `Suspend` guard on the cycle path |
| `E4-SCHEMA-CE-014` | RESERVED | Every recursive reference cycle must be rejected | `schema/ATTACKS.md`; guarded-cycle witness assigned to reference admission | admit a closed cycle when every recursive path crosses the required `Suspend` guard |
| `E4-SCHEMA-CE-015` | RESERVED | One bounded fan-graph probe proves acceptable guardedness complexity | `schema/ATTACKS.md`; adversarial family assigned to reference admission | require memoized graph analysis and report only measured finite bounds until a complexity theorem exists |
| `E4-SCHEMA-CE-016` | RESERVED | Middleware and value getters are one declaration shape | `schema/ATTACKS.md`; signature witness assigned to the getter packet | keep middleware composition and value projection as distinct typed interfaces |
| `E4-SCHEMA-CE-017` | SEEDED | Identical persisted field shapes permit semantic collapse; or the flat API follows as a theorem from field shape | `Effect4Test/Counterexamples/Schema/SemanticTagSeparation.lean`, plus the exact recursor snapshot in `Effect4Test/Schema/RepresentationContract.lean` | preserve all 22 nominal values; independently freeze the flat carrier as a native minimality/API ruling without minting a duplicate `KeywordKind` alphabet |
| `E4-SCHEMA-CE-018` | SEEDED | A Lean constructor name determines the persisted `_tag` spelling | `Effect4Test/Counterexamples/Schema/WireSpellingDrift.lean` | pin the case-sensitive wire string per tag and prove injectivity plus a partial inverse |
| `E4-SCHEMA-CE-019` | SEEDED | `census.length = 22` establishes the census | `Effect4Test/Counterexamples/Schema/CensusCoverage.lean`, `decoyCensus` with 22 entries, one repeat, one omission | require length, duplicate freedom, and coverage as three separate theorems, coverage by case analysis |
| `E4-SCHEMA-CE-020` | SEEDED | Enum member values and literal payloads may share one kind alphabet | `Effect4Test/Counterexamples/Schema/KindAlphabetSeparation.lean` | keep `EnumValueKind` and `LiteralKind` separate and relate them by an explicit non-surjective kind-level embedding |
| `E4-SCHEMA-CE-021` | SEEDED | `null` is a persisted literal payload kind | compile-negative in `Effect4Test/Counterexamples/Schema/NoNullLiteralKind.lean`, plus the 4-entry census | enforce the exclusion by absent constructor, not by a later admission rule; keep the `Null` representation tag distinct |
| `E4-SCHEMA-CE-022` | SEEDED | Object property keys may include live-only local symbols | compile-negative in `Effect4Test/Counterexamples/Schema/NoLocalSymbolPropertyKey.lean`, plus the 3-entry census | admit only string, number, and global-symbol keys; necessary condition for reserved `E4-SCHEMA-CE-010`, not a discharge |
| `E4-SCHEMA-CE-023` | PINNED | The `number` spelling denotes one numeric domain across `Literal`, `Enum`, and property keys | pinned rc.112 `SchemaRepresentation.ts` (SHA-256 `a0a7a153...ae69bc`): `Literal` uses `Schema.Finite` at `:1005`, `Enum` uses `Schema.Number` at `:999`/`:1020`; executed in `test/fixtures/schema-representation/` §2a-§2c | keep the kind map, total injective raw `EnumValue.toLiteralValue` embedding, and literal field admission as three distinct claims: non-finite bits survive the raw embedding but fail literal admission. The wire packet separately owns `number \| "NaN" \| "Infinity" \| "-Infinity"` — **not** one JSON type with a range predicate. Four `number` domains, not two; see `E4-SCHEMA-CE-028` |
| `E4-SCHEMA-CE-024` | PINNED | A non-empty references table means the document is recursive | sealed rc.112 pin `SchemaReferencesPin.test.ts` (SHA-256 `73b28e60...4106cb`): a shared non-recursive name allocates a table entry with zero `Suspend` nodes | never infer recursion from table non-emptiness; recursion is a `Suspend` on a reference path, not a populated table |
| `E4-SCHEMA-CE-025` | PINNED | An Effect4 admission refusal is an rc.112 refusal | same sealed pin, **at the document-codec layer only** (`readsBack` at `:67-75` wraps `SchemaRepresentation.fromJson`): `fromJson` accepts a dangling `$ref`, a self alias, a two-step alias cycle, an unguarded structural cycle, and a dead table entry | state `SC-CAS-*` compatibility directionally **and name the host layer**; Effect4-admitted implies host-accepted, never the converse. The dangling-`$ref` acceptance holds at `fromJson` and fails at revival; see `E4-SCHEMA-CE-040` |
| `E4-SCHEMA-CE-026` | SEEDED | A correct 22-member tag alphabet licenses a payload carrier that merges the twelve identical-shape keyword nodes | `Effect4Test/Schema/PayloadContract.lean`: 22 constructor ascriptions, `Representation.cases_census`, and the `Representation.keyword` compile-negative | keep twelve payload constructors; `E4-SCHEMA-CE-017` binds the tag layer only and says nothing about the payload carrier |
| `E4-SCHEMA-CE-027` | SEEDED | A payload carrier cannot drift from the frozen tag alphabet, public declaration surface, or downward module ownership | `Effect4Test/Schema/PayloadContract.lean`: without `Representation.tag`, a 21- or 23-constructor carrier typechecks and every census theorem in `RepresentationContract.lean` still passes; an existential census also cannot reject an uninhabited extra constructor or a constructor permutation; mere reachability does not prove which module owns D2-D3 | require `tag`, its 22 `rfl` equations, `tag_surjective`, and `cases_census`, plus the elaborated-surface reaction gate for four declaration mutations and the frozen `payloadBoundaryImportProbe`/expected-owner rows proving D0-D3 reachable through `Effect4.Schema.Payload`, D4-D7 unreachable there, D0-D1 owned by `Data.Json`, D2-D3 owned by `Schema.Payload`, and no upward import |
| `E4-SCHEMA-CE-028` | PINNED | One numeric domain and one direction-insensitive policy serve `Literal`, `Enum`/property keys, representation/check payloads, and ordinary annotation bags | pinned `SchemaRepresentation.ts` `:1005`/`:999`/`:919`/`:939-948` and `SchemaAST.ts` (SHA-256 `7f7cb036...2275d8`) `:1448-1456`, `:3341`, `:4271-4274`; executed in `test/fixtures/schema-representation/` §2a, §2b/2c, §2f, §4a, §4e | carry four source positions directionally: the total raw enum-to-literal embedding preserves non-finite bits but literal field admission rejects them; enum/property non-finites round-trip through three escapes; representation/check payloads and retained ordinary bags require finite JSON on decode; only ordinary bags prune wider live values on encode |
| `E4-SCHEMA-CE-029` | PINNED | A non-finite number cannot survive JSON encoding, so `Schema.Number` at the enum and property-key sites is effectively finite on the wire | `SchemaAST.ts:1448-1456` replaces a plain `Number`'s encoding with `numberToJson` (`:3343-3349`) targeting `Union([finite, nonFiniteLiterals])`, `nonFiniteLiterals` (`:3085-3089`) being exactly `"Infinity" \| "-Infinity" \| "NaN"`; executed round trip in `test/fixtures/schema-representation/` §2b/2c/2e | reject the refuse-or-lossy dichotomy: the persisted form is a discriminated string escape, lossless, with exactly three spellings; the ruling's non-finite bullet needs no scope qualifier |
| `E4-SCHEMA-CE-030` | PINNED | Every references-table key is a possible field-admissible `$ref` | `Reference.$ref` is `Schema.NonEmptyString` (`:1068`) while a table key is plain `Schema.String` (`:1096`); executed acceptance of an unreferenced empty-string key in `test/fixtures/schema-representation/` §5a | admit the empty table key in both `Document` and `MultiDocument`, retain the raw empty `$ref` so field admission can reject it, and record that no field-admissible reference can name the empty-key entry; later dead-entry policy belongs to `SC-DOC-*` |
| `E4-SCHEMA-CE-031` | PINNED | `Suspend`'s empty-`checks` constraint is decorative, or the field may be dropped from the carrier | codec `checks: Schema.Tuple([])` (`:987`) is present-and-exactly-empty; executed refusal `Expected no excess property` in `test/fixtures/schema-representation/` §5b | keep the field and reject a non-empty one by admission; it is the only per-tag field constraint separating `Suspend` from the twelve keyword-shaped tags |
| `E4-SCHEMA-CE-032` | PINNED | Declarations may carry referenced schemas, so one annotation record serves both | `RepresentationAnnotation` (`:25-28`) has no `schemas`; only `CheckRepresentationAnnotation` (`:36-38`, codec `:922-925`) does, and `Declaration` uses the former (`:146`, codec `:979`) | keep two nominally distinct annotation records; a compile-negative on `RepresentationAnnotation.schemas` enforces the absence |
| `E4-SCHEMA-CE-033` | PINNED | `Representation` and `Check` recurse only through `FilterGroup.checks`, so `Filter` is a leaf | the two recursive field forms create three routes: required `Filter.representation.schemas`, optional `FilterGroup.representation.schemas`, and `FilterGroup.checks`; rc.112's lowering visits schemas at `internal/schema/toRepresentation.ts:174` before checks at `:175`; `harness/schema-annotations/EmitFieldAdmissionFixture.lean` generates the buried empty `$ref`, its Lean result, and the TypeScript oracle comparison | enumerate all three routes; the Lean battery and generated host fixture bury an empty `$ref` under `Filter.representation.schemas`, which an admission following only `FilterGroup.checks` never inspects |
| `E4-SCHEMA-CE-034` | PINNED | `FilterGroup` carries no annotations, because the ruling's census table does not mention them | `FilterGroup.annotations` at `:460`, codec `:965` | read silences against the pinned bytes; a census-table omission is not a field absence |
| `E4-SCHEMA-CE-035` | PINNED | Admission should reject duplicate enum values, duplicate enum names, or an optional array element before a required one | rc.112 persists `enums` and `elements` as plain `Schema.Array`s (`:1018-1021`, `:1036`) with no such check; executed acceptances in `test/fixtures/schema-representation/` §5a | keep admission no stronger than the pin: an over-strict clause makes the reserved `E4-SCHEMA-CE-003` and `E4-SCHEMA-CE-004` witnesses unspellable, which is the one failure mode that destroys reserved rows silently |
| `E4-SCHEMA-CE-036` | PINNED | An omitted-when-empty annotations key and an absent one are the same raw value | `pruneAnnotations` (`:927-937`) returns `Option.none()` when nothing survives and `AnnotationsSchema` encodes through `Schema.optionalKey` (`:940`), while decode is `passthroughSubtype` (`:941`) and passes `{}` through; executed in `test/fixtures/schema-representation/` §4c | carry `Option (List AnnotationEntry)` so `none ≠ some []`; without the distinction `SC-WIRE-04` has nothing to normalize and `SC-WIRE-03` is unstatable |
| `E4-SCHEMA-CE-037` | PINNED | Effect4's unique-property-key requirement is an rc.112 field-admission failure, or structural binary64 equality may silently choose property-key equivalence | `propertySignatures` is a plain `Schema.Array` (`:1057`); executed duplicate-key acceptance plus the unresolved `+0`/`-0`/NaN numeric-key equivalence cases | keep duplicates field-admissible and defer `PropertyKeysUnique` until a key-equivalence relation is frozen; do not put it in this battery or assume `List.Nodup` over structural `Float64` equality |
| `E4-SCHEMA-CE-038` | SEEDED | `Document` is a `MultiDocument` with one root | `Effect4Test/Schema/PayloadContract.lean`: exact `toMulti_mk`, injectivity, and an admissible two-root value outside the image | keep the two shapes nominally distinct; prove non-surjectivity with two roots rather than a D7-rejected empty-root artifact |
| `E4-SCHEMA-CE-039` | SEEDED | A Lean field or binder name determines a persisted key spelling, or the payload carrier owns persisted field keys | `Effect4Test/Schema/PayloadContract.lean` compile-negatives on `Representation.persistedFieldName` and `Representation.fieldKeyName`; `$ref` (`:1068`) is not a legal Lean identifier and the scalar envelope adds `type`/`value` keys and five discriminator values (`:992`, `:998-1008`, `:1043`) | assign every persisted key spelling to the wire-profile packet (`SC-WIRE-01`/`SC-WIRE-02`); until it exists `SC-REP-FIELD-PIN` stays a single-route lexical extraction with no Lean cross-check |
| `E4-SCHEMA-CE-040` | PINNED | `E4-SCHEMA-CE-025`'s acceptance list holds at every rc.112 layer | the sealed pin's `readsBack` (`:67-75`) is `SchemaRepresentation.fromJson` only; at revival, `resolveReference` (`internal/schema/fromRepresentation.ts`, SHA-256 `0b95c360...36d82e`, `:64-87`) throws `Invalid reference <key>` at `:67-68` | split the claim by layer: a dangling `$ref` is accepted by `fromJson` and refused by `fromRepresentation`; the other four are accepted at both, cycles surviving because `ReferenceSlot` (`:19-31`) returns a `Schema.suspend` wrapper on re-entry (`:76-77`) |
| `E4-SCHEMA-CE-041` | SEEDED | Structural equality on the payload's binary64 datum is the host's `===`, signed zero may be normalized, or five named cases characterize finiteness | `Effect4Test/Schema/PayloadContract.lean`: bit-pattern bijection, `Float64.negZero_ne_zero`, and all-bit exponent-field finiteness characterizations; executed `-0` behaviour in `test/fixtures/schema-representation/` §2g | name structural equality and its three non-claims, preserve signed zero, and characterize finiteness for every `UInt64` pattern rather than only the named NaN/infinity/zero constants |
| `E4-SCHEMA-CE-042` | PINNED | Every value of rc.112's own `Representation` type is encodable by its own codec | `toRepresentation` omits the `representation` key for a live filter with no representation annotation (`internal/schema/toRepresentation.ts`, SHA-256 `677449c7...0d614a2`, `:351-357`), and `FilterSchema` requires it (`:958`), so `toJson` throws `Missing key`; executed refusals for both `Declaration` and `Filter` without `representation` in `test/fixtures/schema-representation/` §5b | do not inherit interface optionality: model the codec's requirements, keep `Declaration.representation` and `Filter.representation` non-`Option`, and never state "an rc.112 `Representation` is encodable" without this exclusion |
| `E4-SCHEMA-CE-043` | SEEDED | Traversing direct `Representation` children and `FilterGroup.checks` is a complete general recursion over the payload family | `Effect4Test/Counterexamples/Schema/RecursiveElimination.lean`: a root with one filter and one nested annotation schema has shallow trace `[String, Filter]` and complete trace `[String, Filter, Number]`; the twelve-label witness gives every recursive route a distinct observation, detecting omission, duplication, and reordering | expose one two-sorted nondependent fold whose algebra receives recursively substituted `CheckRepresentationAnnotationOf`, `ElementOf`, `PropertySignatureOf`, and `IndexSignatureOf` values, plus mapped representation/check lists; retain all 24 public constructor equations and both rebuild identity theorems |
| `E4-SCHEMA-CE-044` | SEEDED | A `Reference` with no annotation field is indistinguishable from a node whose present annotation field stores `none` | `Effect4Test/Counterexamples/Schema/AnnotationDataPlane.lean`, `collapsedNodeAnnotations` and `no_focus_ne_present_none` | expose `Representation.nodeAnnotations` as an `Optional`: `Reference` has no focus, while every other node has `some annotations`, including `some none` |
| `E4-SCHEMA-CE-045` | SEEDED | Looking up the first annotation key, or first collapsing the bag to a map, is enough for annotation updates | `Effect4Test/Counterexamples/Schema/AnnotationDataPlane.lean`, three ordered duplicate `k` entries around an unrelated entry | keep `Annotations` as the existing optional ordered list and make `payloadsAt` visit and update every matching payload in place without reordering, deduplicating, or touching unrelated entries |
| `E4-SCHEMA-CE-046` | SEEDED | `decode (encode value) = some value` is sufficient for an exact typed annotation view | `Effect4Test/Counterexamples/Schema/AnnotationDataPlane.lean`, `canonicalUnit` decodes every string but re-encodes `"authored-spelling"` as `"canonical"` | require both `decode_encode` and `encode_decode`; prove `entry_of_decodeEntry` so identity modification reconstructs the exact existing `AnnotationEntry` without a second Schema carrier |
| `E4-SCHEMA-CE-047` | SEEDED | Traversing only node annotation fields covers the recursive annotation data plane | `Effect4Test/Counterexamples/Schema/AnnotationDataPlane.lean`, `nestedRoutes`: the shallow result is `[arrays]`, while the exact nine-label result includes group/filter schemas, nested checks, an element bag and type, and a property bag and type | derive `Representation.annotationBags` and `Check.annotationBags` from the closed two-sorted fold; preserve structural preorder across every recursive route and every annotation-bearing child record |
| `E4-SCHEMA-CE-048` | SEEDED | A document annotation traversal may follow resolved or reachable references and skip the rest of the reference table | `Effect4Test/Counterexamples/Schema/AnnotationDataPlane.lean`, a root with no references plus three dead entries, including a duplicate key | traverse the root(s), then every `ReferenceEntry.representation` in stored list order; do not resolve keys or filter dead, duplicate, dangling, or unreachable entries in the structural data plane |
| `E4-SCHEMA-CE-049` | SEEDED | Typed annotation lookup is sufficient for effectful-field admission, so malformed and duplicate raw occurrences may be filtered away | `Effect4Test/Counterexamples/Schema/EffectfulField.lean`, one duplicate-valid bag and one malformed-plus-valid bag | derive admission from the exact raw `Annotations.payloadsAt` occurrence list; require one canonical payload and reject every other multiplicity or malformed spelling |
| `E4-SCHEMA-CE-050` | SEEDED | Equal read and write operation numbers identify an effectful field across alphabets | `Effect4Test/Counterexamples/Schema/EffectfulField.lean`, `operations` versus `otherAlphabet` | require alphabet, read-operation, and write-operation identities to agree; attach no service or refusal meaning at this layer |
| `E4-SCHEMA-CE-051` | SEEDED | Effectful `modify` may transform `Lens.get source` and skip its declared read operation | `Effect4Test/Counterexamples/Schema/EffectfulField.lean`, `field.modify` starts at `Op.read` while `staleModify` starts at `Op.write 11` | generate `modify` as effectful read followed by write and local replacement; the source lens value is not the read result |
| `E4-SCHEMA-CE-052` | SEEDED | Effectful `set` may return the pure lens replacement without performing the declared write | `Effect4Test/Counterexamples/Schema/EffectfulField.lean`, root operation of `field.set` versus the pure replacement program | perform the existing signature operation before returning the lens-replaced source and prove the same order through interpretation |
| `E4-SCHEMA-CE-053` | SEEDED | An effectful-field marker on a representation node marks an object property | `Effect4Test/Counterexamples/Schema/EffectfulFieldProperties.lean`, `nodeOnly` versus `propertyOnly` | inspect only the existing `PropertySignatureOf.annotations` bag for direct discovery; node, check, element, and property-type annotations do not mark the property |
| `E4-SCHEMA-CE-054` | SEEDED | Scanning only the current object's property list inventories effectful fields recursively | `Effect4Test/Counterexamples/Schema/EffectfulFieldProperties.lean`, `recursiveWitness` with fourteen exact discoveries through object properties, property types, index positions, arrays, templates, unions, suspend, and both check-schema routes | derive one collector from the existing `Representation.fold`; preserve the fold's structural preorder and reuse its closed recursion proof graph |
| `E4-SCHEMA-CE-055` | SEEDED | A map or set is an adequate effectful-field inventory because property names identify occurrences | `Effect4Test/Counterexamples/Schema/EffectfulFieldProperties.lean`, the two stored `duplicate` properties retain read-operation identities `21` then `31` | return an ordered list of the existing full property paired with its existing spec; preserve duplicate names, multiplicity, pairing, and authored order |
| `E4-ENV-CE-001` | SEEDED | A context key carries its service type as a Lean type index, so `Environment` is a dependent map | `Effect4Test/Environment/ContextKeyContract.lean`: `@Effect4.ServiceKey : Type` plus the exact `ServiceKey.rec` snapshot, and the `ServiceKey.Service`/`ServiceKey.universe` compile-negatives | keep the key first-order: a nominal `ServiceName` paired with a first-order `ServiceTypeCode`, with the reading of a code as a Lean type supplied separately by `ServiceUniverse`; `DecidableEq` alone does not exclude the indexed family and is not cited as if it did |
| `E4-ENV-CE-002` | SEEDED | A key's nominal name determines its service, so name equality is key equality | `Effect4Test/Environment/ContextKeyContract.lean`: `ServiceKey.conflict_iff`, one conflicting and two non-conflicting ground pairs, and the same-name disequality stated without `Conflict` | make the whole pair the identity and freeze nominal collision as a named decidable relation at the key node; leave to `Context/Environment` whether a colliding pair may be held |
| `E4-ENV-CE-003` | SEEDED | A first-order service type code is a faithful name for a Lean type, so type equality recovers code equality | `Effect4Test/Environment/ContextKeyContract.lean`, `ServiceUniverse.exists_carrier_collision`, plus the `ServiceUniverse.code` compile-negative | prove the collision rather than warn about it; route every typing statement through the code, never through `Type` equality, and supply no inverse interpretation |
| `E4-ENV-CE-004` | SEEDED | `DecidableEq` on keys is enough for a requirement row, as the `Context/Key -> Context/Requirement` edge description states | `Effect4Test/Environment/ContextKeyContract.lean`: `ServiceKey.lt_iff`, `lt_irrefl`, `lt_trans`, `lt_trichotomy`, a synthesized `Decidable (a < b)`, and four kernel-decided ground comparisons | freeze a decidable strict linear order at the key node so `Context/Requirement` neither mints a second canonical-order notion nor falls back to authored insertion order; state no `DATA-ROW` law here |
| `E4-ENV-CE-005` | SEEDED | Any decidable strict linear order on keys serves equally, so the three order laws settle the canonical spelling | `Effect4Test/Environment/ContextKeyContract.lean`: `ServiceKey.lt_iff` and the ground comparison `⟨⟨0⟩, ⟨9⟩⟩ < ⟨⟨1⟩, ⟨0⟩⟩`, which is false under a service-major order | pin the exact name-major lexicographic relation, not only its three laws; record contiguity of nominally equal keys as the reason for the direction and prove no adjacency theorem at `L0` |
| `E4-ENV-CE-006` | SEEDED | A service value may cross service type codes without an equality proof | `Effect4Test/Environment/ContextKeyContract.lean`: the ascribed signature of `ServiceKey.transport`, `ServiceKey.transport_rfl`, and the `ServiceKey.cast` compile-negative | supply exactly one transport, with the code equality as an explicit argument and no instance argument that would let a default value replace the transported one |
| `E4-TARGET-CE-001` | SEEDED | Emitters may pre-escape strings, or the printer may omit escaping for its delimiter, backslash, or source-line controls | `Effect4Test/Counterexamples/Target/TypeScriptRender.lean`, `hostileString` and its exact rendered-byte guard | make `Render.escapeString` the sole escaping owner and retain the exact quote/backslash/newline/carriage-return/tab reduction |
| `E4-TARGET-CE-002` | SEEDED | Object layout may depend on formatter width or source-line length | `Effect4Test/Counterexamples/Target/TypeScriptRender.lean`, `longField` rendered through both `object` and `objectML` | make layout an explicit constructor choice plus child-newline inspection, with no width input |
| `E4-TARGET-CE-003` | RESERVED | Every retained `Decl` is checked TypeScript syntax or is in the image of Effect4 lowering | `Effect4Test/Counterexamples/Target/TypeScriptRender.lean`, `rawEscapeHatch`; exclusion proof assigned to the typed-lowering packet | retain `raw` only as a compatibility arm and make checked lowering unable to produce it, or replace it with an admitted structured helper form before target cutover |
| `E4-TARGET-CE-004` | SEEDED | Quoting every data key makes a JavaScript object literal faithful to every JSON object | `Effect4Test/Counterexamples/Target/TypeScriptRender.lean`, exact `__proto__` lowering guard plus the host Schema generation harness | lower an admitted `__proto__` key through `Object.fromEntries`; reject duplicates before target construction so no object operation silently collapses evidence |
| `E4-TARGET-CE-005` | SEEDED | Typed annotation lookup is sufficient at the TypeScript generation boundary | `Effect4Test/Counterexamples/Target/EffectfulField.lean`, malformed-plus-valid and duplicate-valid raw bags | call the existing exact `EffectfulFieldSpec.check` on `PropertySignature.annotations`; do not reconstruct admission from typed values |
| `E4-TARGET-CE-006` | SEEDED | A target binding with the right local operation number may cross alphabets | `Effect4Test/Counterexamples/Target/EffectfulField.lean`, a read binding with alphabet `8` against annotated alphabet `7` | require both target operation bindings to agree with the existing spec alphabet and their directional operation IDs before lowering |
| `E4-TARGET-CE-007` | SEEDED | Generated field effects may merge directional error/service rows or use `Effect.Requirements` | `Effect4Test/Counterexamples/Target/EffectfulField.lean` string receipts plus `harness/schema-effectful-field/` exact `Success`/`Error`/`Services` assertions and three language-service mutants | emit exact read, write, and read-then-write rows using rc.112 `Effect.Services`; gate with direct TypeScript and exact `effect-tsgo` diagnostic names |
| `E4-TARGET-CE-008` | SEEDED | A generated effectful-field declaration may use the retained `Decl.raw` compatibility arm | `Effect4Test/Counterexamples/Target/EffectfulField.lean`, `decl?_never_raw` | add one structured `Decl.effectfulField` syntax constructor and prove checked lowering never returns `.raw` |
| `E4-TARGET-CE-009` | SEEDED | An `Except E A` answer may be rendered as `Effect.Effect<A, E>` | `harness/trace`: `recover` (answer `tryGet` carries `[false, "boom"]` and the program continues to `done {"success":0}`) and `fallible` (`failed get "boom"`, `done {"failure":"boom"}`) are two goldens with two rule sets; conflating them would make one of them diverge on the host | two row shapes: `Except E A` spells `Result.Result<A, E>` as data; an aborting operation is declared `!! E`, its method is `Effect.Effect<A, E>` (rule `error-abort`) and its handler kind is `X.Service (ExceptT E M)` |
| `E4-TARGET-CE-010` | SEEDED | The trace of a failing run is discarded with the failure | the carrier `StateT log (ExceptT ε M)` loses the log on error; `Family.Service.tracedExcept` (Effects v0.3.1) keeps the log outside the error and `fallible.empty` records the write before the failing read | `ExceptT ε (StateT log M)` with `interpret_tracedExcept_fst`; goldens of failing runs must show the operations before the failure |
| `E4-TARGET-CE-011` | SEEDED | Block parameters may be passed on a self-edge by assigning in source order | `Effect4Test/Target/TypeScript/FlowLowerContract.lean`, `swap?`: the chosen self-loop `choose ⟨1⟩ ⟨1⟩ ⟨2⟩ [⟨1⟩, ⟨0⟩]` lowers with `let m0 = b1p1; let m1 = b1p0` before the assignments, and the exit edge without temporaries; the host runs `flow/swap.once` and `flow/swap.twice` against the Flow-runner goldens | `param-move` (`FlowLower.lean`) is a sequentialized parallel move: temporaries on a self-edge, plain assignments otherwise, so the dispatch form and the runner agree on every swap |
| `E4-TARGET-CE-012` | SEEDED | A region may lower to JS `try/finally`, or a fallible release to `Effect.acquireRelease` | the syntax has no `try` arm (R4; a yielded failure never runs a generator `finally`, `docs/TRACE-DAG.md` separation 8); `Effect4Test/Target/TypeScript/RegionLowerContract.lean`, `fallibleRelease`: admitted, runs in Lean, `Region.lowerDispatch` returns `none` | regions lower to `Effect.scoped(Effect.onExit(…))` with `Effect.acquireRelease` inside; a release with an error row is refused by the lowering because the host types releases `Effect<unknown, never, R>` |
| `E4-TARGET-CE-013` | SEEDED | A structured lowering may wrap loop headers only where they are dominator children, leaving an entry that is its own loop header with a `continue` to no loop | the property corpus (seed 2026, `f0`): `continue W0` at the top level, refused by the host's parser; `TypeScriptTest/StructureSmoke.lean` `entryLoop` (lean4-typescript v0.4.1) | `Structure.emitWith` wraps an entry that is a loop header in its `while (true)`; the corpus runs the structured module through `property-structured-tail.ts` so the shape is checked on every generated flow; since 2026-09-03 the shape is a Lean theorem, `Effect4.Target.Structured.emitWith_wellScoped` / `structuredBody_wellScoped` (`Effect4/Target/TypeScript/StructureLaws.lean`), which is false for the pre-fix emission |
| `E4-TARGET-CE-014` | SEEDED | A scope with one finalizer may be reasoned about through the finalizer loop (latest-first, merged exits) | the patched rc.112 copy: `regionBothSucceed` records `scope.close-inline` and no `scope.close-lifo` row; `regionNested` records two inline closes and none through the loop (`harness/trace/receipts/patched/`, `scripts/check-trace-patched.sh`) | rc.112 `scopeCloseUnsafe` closes an inline finalizer and a single registered finalizer directly, and only two or more through `scopeCloseFinalizers`; the reified `ScopeState.openInline` and the three arms of `Scope.closeResult` already say so, and the runner's `closeFrame` is arm-independent (one release sees the closing exit either way) |
| `E4-TARGET-CE-015` | SEEDED | Every `Effects.Trace.Val.nat` has a host counterpart | a JavaScript number is an IEEE-754 double, so above `2^53 - 1` it is not the integer it prints; golden emission refuses such a log (`harness/trace/Generate.lean` `admitted`, planted by the `admission-probe` arm in `scripts/test-trace-goldens-gate.sh`) and the host tracer marks the run INVALID, never pass or fail (`harness/trace/tracer.ts` `wire`; planted `overflow` in `harness/trace/wire-tail.ts`) | repair: the wire is exact on the host-safe range and refuses beyond it on both faces |
| `E4-TARGET-CE-016` | SEEDED | The `Program` value and the `Script` that one `effect_program` declaration emits can be related by an equation between them (`denoteScript rows name.script = name`) | the two land in different signatures over different carriers: `denoteScript` (`Effect4/Target/TypeScript/ScriptDenotation.lean`) is a `Program (Sig (tableAlphabet id table)) Val`, every request and answer a wire `Effects.Trace.Val`, while `incr : Nat -> Program Cell.Sig Nat` answers `Cell.Answer .get = Nat`; the equation does not typecheck, and making it typecheck needs a decoding `Val -> X.Answer name` that no admitted Stratum V spelling carries (`Effects.Trace.ToVal` is the encoding half only, and a decoding is partial). `E4-TARGET-CE-015` is claimed by the wire packet in `Effect4/Target/TypeScript/Trace.lean` and `harness/trace/wire-tail.ts` but has no row here yet | relate the two faces by what they do, not by what they are: `effect_program` emits `performedNames X.Name.spelling X.answerDefault (name default) = name.script.operationNames`, checked by `rfl` at the declaration site (`Effect4/Meta/Derive.lean`), pinned per program in `Effect4Test/Target/TypeScript/ScriptDenotationContract.lean`; the wire-level equation stays owed until a `FromVal` with a stated partiality joins the DSL |
| `E4-TARGET-CE-017` | SEEDED | A defect may render as an empty failure | before lean4-effects v0.6.0 a `Die` cause rendered `{"failure":[]}`, byte-identical to a unit failure; `Outcome.defect` (A1) renders `{"defect":d}` on both faces (`Effect4/Target/TypeScript/Trace.lean`, `harness/trace/tracer.ts` `outcomeWire`), and the planted `die` program in `harness/trace/wire-tail.ts` diverges from a golden still claiming the old form | repair: `Outcome.defect` with its own wire form |
| `E4-TARGET-CE-018` | SEEDED | A generic body that preserves scoping for all supplied transfers must use only the graph's declared edges, so closedness and reducibility suffice for `BreakScopedStatement` | `Effect4Test/Counterexamples/Target/BreakScoped.lean`, `frozen_statement_false`: a closed reducible diamond and `illicitBody` satisfy the original premises but emit an orphan `break L3`; the theorem proves `¬ BreakScopedStatement` in the kernel | require the body to preserve scoping when supplied only the current node's declared-edge guarantees; `actualBody_scoped_on_edges` proves this obligation for the actual lowerer, while the corrected generic scope law and full T4 remain open |
| `E4-TARGET-CE-019` | SEEDED | Registrations sharing one scope can compile to independent nested `onExit` frames even when a release fails | `Effect4Test/Counterexamples/Target/RegionSimulationBoundary.lean`, `failing_release_diverges` and `same_scope_closing_exit`, reusing `Effect4Test.Flow.RegionRunnerContract.releaseFails`; `test/counterexamples/target/region-simulation-boundary.mjs` reproduces the distinction in pinned rc.112 with a type-valid defecting release and a successful-cleanup control | DISCHARGED (spike S4, 2026-09-03, packet P1). `Effect4/Semantics/RegionSimulation.lean` now gives a region one scope, not a nest: `compileRegion`'s `enter` arm emits a single `Prim.onSuccessAndFailure` whose cause name is `RegionName.close region enterPoint`, `regionInterp.finalizerExit` is constantly `Exit.success ()` so no release's outcome is threaded into the exit another release observes (`regionInterp_finalizerExit`), and the whole close is `closeExit` — `ScopeMachine`'s zero/one/many policy over `Exit.asVoidAll` of the registered releases' exits (`closeFinish_nil`/`_single`/`_many`, `closeFinish_eq_result?`, `closeExit_reasons`), applied once per scope and restored once. `unwind_failure`, `close_success` and the new `unwind_to_frame` carry a per-name premise which `regionInterp` discharges outright, so `hfin` is gone. The witnesses are retained and inverted: `same_scope_closing_exit` unchanged, `repaired_frames_use_original_exit` and `failing_release_agrees` replace `nested_frames_thread_failure`/`failing_release_diverges`; `regions_simulate_regionReleaseFails`, `T5_regionReleaseFails` and `T6_regionReleaseFails` are the same fixture as an instance of the general equation. Spike S4b (2026-09-03) removes the last evaluated step from this row: `closeExit` is applied to `oracle.registrations`, and `closeWalk_agrees_regionLoop` now proves that list *is* the runner's `Frame.releases` at the moment the frame closes, for every admitted flow rather than at the five fixtures. rc.112 basis: `internal/effect.ts:3815-3827` (one close, one exit, combine after the loop), `:3826`, `:2025-2038` (`exitAsVoidAll`), `:3971-3987` (`acquireRelease` registers and pushes no frame). Fence C amended; generic `onExit` unchanged |
| `E4-TARGET-CE-020` | SEEDED | Compiling exhausted source fuel to an empty failure preserves a live region run after frontier rows are masked out | `Effect4Test/Counterexamples/Target/RegionSimulationBoundary.lean`, `source_fuel_zero_diverges` and `source_fuel_after_acquire_diverges`: the runner remains live; the current machine emits interruption completion and, after acquisition, runs a finalizer | PARTIAL (spike S4, 2026-09-03, packet P2). Finalisation half DISCHARGED: `compileRegion` no longer emits `Prim.failure` anywhere — `compileRegion_not_failure`/`compileRegion_never_fails` prove it for every flow, fuel, block, environment and tape — and every frontier residual is `Prim.suspend point` whose `suspendBody` is itself, so the machine stays `FrameStep.running` at every machine fuel with an empty trace (`step_suspend_fixed`, `run_suspend_fixed`, `compileAt_zero_fuel_live`; DB-04, `Runtime.lean:2435-2439`). The suspended continuation and the open registrations stay on the stack: `source_fuel_zero_machine`, `source_fuel_zero_live`, `source_fuel_after_acquire_machine`, `source_fuel_after_acquire_live` and the matching `_agrees` theorems replace the two `_diverges` witnesses. Still owed: the general finite-prefix and residual-state simulation (T8, packet P5), and the classification of refusal against live suspension, which this compile still conflates (`mismatched_decision_machine_is_live`). Spike S4b (2026-09-03) sharpens what "still owed" means here. The unrestricted **trace** equation is no longer refuted: S4's counterexample `tapeAfterRegion_diverges` is repaired and replaced by `tapeAfterRegion_agrees`, `unrestricted_finite_agreement_false` is deleted, and `AllFiniteInputsAgree` is retained as the `Prop` naming the obligation (its Lean form is `RegionSimulation.RegionsSimulateAll`/`RegionsSimulateExitAll`), with `finite_agreement_has_content` showing it is not vacuous. What survives as an unrestricted refutation is the endpoint half only: `unrestricted_finite_classification_false`, from `mismatched_decision_classification_diverges` |
| `E4-TARGET-CE-021` | SEEDED | An unanswered region decision may become an empty failure without changing finalizer/outcome observations | `Effect4Test/Counterexamples/Target/RegionSimulationBoundary.lean`, `unanswered_decision_diverges` and `unanswered_after_choice_diverges`: a resource-bearing admitted cycle waits at site 7, while compiled frames finalize and report interruption; the false-decision completion control agrees and mismatch remains a refusal on the runner side | PARTIAL (spike S4, 2026-09-03, packet P2). Finalisation half DISCHARGED: an unanswered decision compiles to `Prim.suspend`, so the acquired resource stays held and nothing is finalised — `unanswered_machine`, `unanswered_live`, `unanswered_decision_agrees`, `unanswered_after_choice_agrees`; the completion control `completed_decision_control` still agrees. Unconsumed tape is preserved because the residual carries the whole `Config`, tape included. Still owed, and now stated rather than implied: the general relation must distinguish live suspension, refusal and completion — this compile does not (`mismatched_decision_is_refusal` on the runner against `mismatched_decision_machine_is_live` on the machine), because `Flow.RunResult.refusedSite` has no `Exit` image; that is T8, packet P5. **The leave-configuration half is DISCHARGED (spike S4b, 2026-09-03).** S4 still resumed a region's continuation with the fuel and tape held at the *enter*, and `tapeAfterRegion_diverges` was the minimal witness. S4b repairs the compile instead of restricting the flows: `RegionSimulation.closeWalk` is rewritten as the runner is written (one unit of fuel per block, an open-region stack, `acquire` registering on the innermost frame, `leave` popping one), it reports a `LeavePoint` carrying the configuration the runner resumes at, `leaveConfig` is that configuration, and `regionInterp.contA` compiles the region's continuation there (`regionInterp_regionCont_resume`). `closeWalk_agrees_regionLoop` proves the walk is the runner by induction on the runner's fuel with one lemma per runner arm, and `leaveConfig_agrees_runRegions` is its corollary at an `enter`; the only premise is that the service is stateless, which is D2's standing `stateless`. So `RegionOracleAgrees.registrations` is no longer an owed induction but a definition check on the oracle. Witness inverted: `tapeAfterRegion_machine` now reads `some [.done (.success (.nat 5))]`, `tapeAfterRegion_agrees` and `tapeAfterRegion_agrees_other_branch` replace `tapeAfterRegion_diverges`, and the leave configurations of the five fixtures are pinned in `RegionSimulationContract.lean` (`leaveConfig_regionBothSucceed` against `enterPoint_regionBothSucceed`: fuel 2 at block 3, not the enter's 4) |
| `E4-TARGET-CE-022` | SEEDED | Every operation an `effect_signature` admits can be performed by a flow: `familyTable` spells every row's request and the dispatch lowering calls every row | `Effect4Test/Counterexamples/Target/JobRequest.lean`. The attack, as it fired: `familyTable` wrote the string `"unsupported"` for a two-parameter row, admission accepted a graph over it, and the emitted module declared `run` with two parameters and called it with one (`let b0p0!: unsupported`, `yield* paired.run(b0p0)`) | **Repaired 2026-09-03**, by lifting the restriction rather than refusing the row: `familyTable` spells a request of `n` parameters as the right-nested product `Effects.Trace.ToVal` builds (`requestSpelling`, the spelling `Spelling.prod` renders), `OpSpec` carries the row's parameters so `OpSpec.arity` is the call's argument count, and `Lowering.callOf` destructures the one request slot at the call through `Lowering.tupleArgs` (`lowering: rule.perform-tuple`, `Rule.all` 26 -> 27). `OpRow.answerArity` carries a tuple answer's arity to `harness/trace/tracer.ts`, which cannot parse `readonly [JobQueue, number]` back into a pair -- the handle target is outside the wire grammar on purpose. The witness now pins the repair; `Effect4Test/Target/TypeScript/MultiArgContract.lean` pins the emitted bytes, and the six job goldens exercise it against rc.112. What the repair does not reach is `E4-FLOW-CE-028` |
| `E4-TARGET-CE-024` | SEEDED | A host answer can be encoded from the value alone, so the wire form need not consult the declared answer type | `Effect4Test/Counterexamples/Target/AnswerProfile.lean`: the one host array `[[1, 2]]` has two readings, `List (Nat × Nat)` rendering `[[1, 2], []]` and `List (List Nat)` rendering `[[1, [2, []]], []]`; the bytes differ and `Trace.agree m2` of the two answer events is false, while `harness/trace/tracer.ts` read every array as a list | reify the answer-type profile (`Spelling`, `Effect4/Target/TypeScript/EffectV4.lean`) and make the host encode at the spelling the row declares (`parseSpelling`, `wireTyped`); corpus witness `generated/traces/probe.empty.tsv` with answer `Option.Option<Result.Result<number, string>>` |
| `E4-TARGET-CE-025` | SEEDED | A pure atom may be declared once per face: a Lean function, a hand-written `AtomTable` row, and a hand-written `const` in `atoms.ts` | `Effect4Test/Counterexamples/Target/AnswerProfile.lean`: `Script.toFlow` refuses a script whose atom the table does not carry, and that refusal is the only signal a hand-written table gives; a name missing from `atoms.ts` is a host `ReferenceError` no Lean receipt sees | declare an atom once with `effect_atoms` (`Effect4/Meta/Derive.lean`) and derive every face from the row list: the Lean function, `AtomTable`, the wire dispatcher, and the generated `atoms.ts`, byte-compared by `scripts/check-trace-host.sh` |
| `E4-TARGET-CE-026` | SEEDED | Destructuring a tuple request at the call is total: `Lowering.tupleArgs` may take a `TypeScript.Expr` and project the identifier case, answering the request itself for every other shape | `Effect4Test/Counterexamples/Target/TupleRequest.lean`: the retired clause, kept as `retired`, answers a one-element argument list for `Expr.member (.ident "a4") "success"` at arity 2 and at arity 3 while projecting an identifier of the same arity correctly -- a two- or three-parameter row called with one argument, `E4-TARGET-CE-022`'s own defect behind a fallthrough. It never fired only because every caller passed `Slot.expr`, which was `.ident` for every slot | repair: the shape is unrepresentable rather than refused. `Lowering.tupleArgs` takes the request slot's spelling (`String -> Nat -> List Expr`) and `Lowering.callOf` takes a `Slot` (`ServiceRow -> OpSpec -> Slot -> Expr`), so every request has a path to project; `tupleArgs_length` proves one argument per parameter at every arity above zero (`[propext, Quot.sound]`), and the compile-negative names the rejected term |
| `E4-DATA-CE-001` | SEEDED | A row is a raw list, or equal raw membership implies raw-list equality, so canonicality may remain an external agreement check | `Effect4Test/Data/RowContract.lean`: exact proof-carrying `Row.mk`, `Row.eq_of_mem_iff`, absent `Row.ofList`, and the raw witnesses that `[1,2]` and `[2,1]` have equal membership but unequal spellings while canonicality and agreement are independent | retain one proof-carrying `Row`; make noncanonical construction unspellable and restrict membership extensionality to checked rows |
| `E4-DATA-CE-002` | SEEDED | Order-preserving duplicate removal is normalization | `Effect4Test/Data/RowContract.lean`: `ascending_iff`, `Row.mem_normalize`, and ground reductions requiring `normalize [3,1,2] = [1,2,3]` and `normalize [2,1,2] = [1,2]`; Foldlab late `s2/attack-EC1-T012.lean` supplies the adopted failure shape | normalize by structurally recursive sorted insertion against the ambient standard lawful order, never by first-occurrence dedup |
| `E4-DATA-CE-003` | SEEDED | Normalization retains multiplicity or is injective | `Effect4Test/Data/RowContract.lean`: `Row.normalize_duplicate` and the ground equality `normalize [1,1] = normalize [1]`, while raw list inequality is immediate | record presence rather than multiplicity and expose duplicate erasure as a theorem so no consumer relies on a converse |
| `E4-DATA-CE-004` | SEEDED | `Data.Row` may bundle or invent an order/comparator independent of the element owner's frozen relation | `Effect4Test/Data/RowContract.lean`: every operation quantifies over `LE`/`LT`/`DecidableEq`/`DecidableLT`/`Std.IsLinearOrder`/`Std.LawfulOrderLT`; a test-only reverse Nat instance changes canonical spelling; absence guards reject `Effect4.RowOrder` and `Row.Comparator` | consume Lean's standard lawful-order classes and the element owner's existing relation; mint no Effect4 comparator or order-law carrier |
| `E4-DATA-CE-005` | SEEDED | Concatenation or a left-favouring merge is row union | `Effect4Test/Data/RowContract.lean`: `Row.mem_union`, both operand-order reductions, the commutative/idempotent/identity laws, and absent `Row.append` | define union through canonical normalization and lift membership equality through `Row.eq_of_mem_iff` |
| `E4-DATA-CE-006` | SEEDED | Row membership may be a test defined independently of the stored canonical elements | `Effect4Test/Data/RowContract.lean`: `Row.mem_def`, synthesized decidable membership, and every insert/normalize/union theorem stated through the same `Membership` instance | freeze one membership observation and prove it is exactly underlying-list membership |
| `E4-DATA-CE-007` | SEEDED | Weakening is spelling containment, or subset direction may be silently reversed | `Effect4Test/Data/RowContract.lean`: `Row.subset_iff`, decidability, `subset_refl`, `subset_trans`, `subset_union_left`, `subset_union_right`, and the positive ground subset | use conventional member inclusion: `Subset r s := ∀ a, a ∈ r → a ∈ s`; state both union inclusions explicitly and leave program-level provision meaning to Context |
| `E4-DATA-CE-008` | SEEDED | An opaque or classical decision procedure is acceptable because it inhabits `Decidable` | `Effect4Test/Data/RowContract.lean`: explicit `DecidableEq`/`DecidableLT` binders plus kernel reductions for insert, normalization, duplicate erasure, union, and subset; the satisfiable reference reaches `[propext, Quot.sound]` and not `Classical.choice` | implement structural sorted insertion that reduces in the kernel and keep every exported theorem within the no-choice axiom ceiling |
| `E4-DATA-CE-009` | SEEDED | Normalization idempotence or membership preservation establishes denotational preservation | `Effect4Test/Data/RowContract.lean`: `normalize_idempotent`, `normalize_duplicate`, and the absent `normalization_preserves_denotation`; `[1,1]` and `[1]` collapse despite distinct authored multiplicity | report only canonical row equalities; require a later calculus to prove its observation multiplicity-insensitive before relating normalization to denotation |

| `E4-SEM-CE-001` | SEEDED | A tree-shaped cause with sequential and parallel nodes faithfully refines rc.112's cause | `Effect4Test/Counterexamples/Semantics/CauseExit.lean`, `tree_invents_structure` and `tree_empty_not_unique` | keep one flat ordered reason list; `Cause.ext` makes the list the whole content and `Cause.combine_no_new_reason` forbids a node |
| `E4-SEM-CE-002` | SEEDED | `causeCombine` is concatenation | `Effect4Test/Counterexamples/Semantics/CauseExit.lean`, `append_duplicates_reasons` and `append_breaks_self_combine` | deduplicate by first occurrence; freeze both `Cause.dedup` equations, `Cause.combine_reasons`, `Cause.combine_order`, and the `Cause.combine_self` short circuit |
| `E4-SEM-CE-003` | SEEDED | `causeSquash` may report whichever reason comes first, or may prefer an earlier defect over a typed error | `Effect4Test/Counterexamples/Semantics/CauseExit.lean`, `squash_must_partition_before_choosing`, `squash_interrupt_does_not_shadow_defect`, `squash_empty_is_a_fourth_arm` | partition before choosing: a `Fail` anywhere beats a `Die` anywhere, the first `Fail` wins among fails, and `emptyCause` is a distinct fourth arm |
| `E4-SEM-CE-004` | SEEDED | Annotation merge may let the incoming map win, or may move an overwritten key to the end | `Effect4Test/Counterexamples/Semantics/CauseExit.lean`, `annotate_must_not_overwrite_by_default`, `annotate_overwrite_keeps_position`, `annotate_empty_is_identity` | freeze `ReasonAnnotations.annotate_entries`; keep existing values unless `overwrite := true`, keep the key's slot when overwriting, and make `annotate` with the empty map the identity |
| `E4-SEM-CE-005` | SEEDED | A finalizer failure may be discarded in favour of a successful exit, or the merge itself re-raises the original exit cause | `Effect4Test/Counterexamples/Semantics/CauseExit.lean`, `finalizer_failure_stands_alone` and `successful_finalizer_under_failed_exit` | four exact `Exit.mergeFinalizer_*` arms from `internal/effect.ts:3800-3804`, plus three `Exit.restoreAfterFinalizer_*` arms for the caller-side composite at `:4023-4028` |
| `E4-SEM-CE-006` | SEEDED | `exitAsVoidAll` fails whenever any input exit is a `Failure` | `Effect4Test/Counterexamples/Semantics/CauseExit.lean`, `empty_cause_failure_joins_to_success` and `join_is_not_combine` | test the concatenated reasons, not the exits: `Exit.asVoidAll_reasons`, `asVoidAll_empty_cause`, `asVoidAll_keeps_duplicates` |
| `E4-SEM-CE-007` | SEEDED | `Effect4.Data.Row`, the canonical finite-set carrier, can carry a cause | `Effect4Test/Counterexamples/Semantics/CauseExit.lean`, `union_is_not_commutative` and `order_changes_squash` | retain an ordered `List`; state `Cause.combine_order` as an operand-order-preserving union and record the `Row` separation in `docs/CAUSE-DAG.md` |
| `E4-SEM-CE-008` | SEEDED | A host may spell its own mask table, or compare unprojected traces | `scripts/test-trace-goldens-gate.sh`, mutant `removed-masks`: a mask table without rows is refused as drift; `EffectsTest/Counterexamples/Trace/Around.lean`, `agreement_is_per_mask` | `generated/traces/masks.tsv` is generated from `Effect4.Trace.maskTable`; `effect4-trace` reads only that table; agreement is always per named mask |
| `E4-SEM-CE-009` | SEEDED | A host answer can be encoded from its runtime value | first host run of the trace harness: rc.112's `Ref.set` returns the mutable ref under a declared `Effect<void>`, the untyped encoder died and the trace ended `done {"failure":[]}` (`docs/TRACE-DAG.md` separation 7) | answers are recorded as typed: `ServiceRow.rowsDecl` carries each answer spelling and `wireAnswer` encodes a `void` answer as unit |
| `E4-SEM-CE-012` | SEEDED | An operation that cannot answer has failed, so a pending `Deferred.await` may be reported through the declared error channel and end the run in an outcome | `Effect4Test/Counterexamples/Flow/Deferreds.lean`, `stall_as_failure_is_indistinguishable`, `stall_as_failure_agrees_under_every_mask`, `frontier_separates_under_every_mask`, `frontier_log_has_no_outcome` | a pending await writes the `op` row and stops: no `answer`, no `failed`, no `done`, and a `frontier` instead (`deferredsTraced` in `harness/trace/Generate.lean` refuses `Family.Service.tracedExcept` for exactly this reason; the host reaches the same row through `RunOptions.stallMs` in `harness/trace/tracer.ts`) |
| `E4-SEM-CE-013` | SEEDED | The sequential projection's step is a function of its table, so a golden that stops at a frontier records everything the program can do | `Effect4Test/Counterexamples/Flow/Deferreds.lean`, `the_child_changes_the_answer` and `no_state_function_matches_both`: the same table answers `none` parked and `some 7` once another fiber completes the cell | state the frontier as a limit of the projection, never of the program; the two-fiber golden that would record the resumption is owed until `effect_program` can bind two families and `Fibers` has a former for a child that performs operations of its own (note on `DeferredEntry`, `harness/trace/Generate.lean`) |
| `E4-SEM-CE-014` | SEEDED | `harness/trace/tracer.ts`'s first-seen handle indexing *is* the meaning of a handle, so a tail may brand as many handle types as it likes | `harness/trace/ref-probe.mjs` at rc.112: `nextHandleIndex` is one module-global counter over every branded object, so with both `~effect/Ref` and `~effect/Scope` branded it prints `first scope -> host handle index 1`, `next ref -> host handle index 2`, where a per-family Lean store names that ref `0`. Lean half in `Effect4Test/Counterexamples/Runtime/Refs.lean`: `Refs` and `ERefs` each name their first cell `0` from their own store, `twoRefs` names two cells `0, 1` in creation order, and the wire form of a handle is an injective bare index | first-seen order equals creation order only *within* one branded type. `harness/trace/ref-tail.ts` brands exactly one (`~effect/Ref`), and `Effect4/Stateful/RefFamily.lean` records the constraint as a refusal beside the family. A tail branding two handle types must reconcile the counters before its goldens mean anything |
| `E4-SEM-CE-015` | SEEDED | A `modify`-shaped operation is pinned by its declared type, so the golden need not separate its answer from its new state | rc.112's `Ref.modify(self, f)` takes `f : (a) => readonly [B, A]`; with `B = A = number` the tuple order is invisible to the compiler. `harness/trace/ref-tail.ts` mutated to `Ref.modify(ref, (current) => [current + amount, current])` is accepted by the pinned unpatched compiler (`tsc.original` exit 0) with zero `effect-tsgo` diagnostics, and `generated/traces/ref/modifyOld.tsv` then diverges at `answer modify` (expected 7, actual 12) under `m1`/`m2` and at `done` under `outcome`. `Effect4Test/Counterexamples/Runtime/Refs.lean` carries the same attack as a handler, plus `modifyZero`: naming `fnNoChange`, whose two tuple components coincide, the attacked and repaired handlers agree row for row under every mask. Since lowering lane L3 the read-modify-write argument is a *named* function (`RefFn`) and not an amount, which is DB-02's spelling of the same request and changes nothing about the attack | when the answer and the new state share a type the golden is the only separation: `refModifyOld` names `fnTakeAndBump` (`a ↦ [a, a + 1]`), `refGetAndSetOld` writes a different value, and both read the cell afterwards, so both values reach the wire in the order the family fixes. `ERefs.tryTake` is the contrasting half — its components have different types, the compiler pins the order, and no golden carries that claim alone |
| `E4-SEM-CE-016` | SEEDED | A memoized rebuild is a second construction: `build` runs the layer every time, answers a new handle and raises the count | `Effect4Test/Counterexamples/Semantics/Layers.lean`: a memo hit constructs nothing — the construction count, the live set and the layer scopes do not move, and only the context handle and the entry's observer count do; the `buildMemo` rows show two builds and one construction, while `freshRebuild` shows `Layer.fresh` building through a private memo map, constructing twice, counting both against the layer it wraps and leaving the caller's ambient memo map empty | `build` answers the memo map whenever the layer is memoized there and the scope is open (rc.112 `layer.memo-build-once`, `layer.memo-get-or-else` replay the built context); only a construction raises `provideCount`, takes a service handle or joins the live set, and `Layer.fresh` calls the layer's own build with a brand new memo map (`Layer.ts:3851`, `layer.fresh-drops-memoization`) |
| `E4-SEM-CE-017` | SEEDED | Layers are released in construction order, or the release order is unstated because "a scope closes what it owns" | `Effect4Test/Counterexamples/Semantics/Layers.lean`: `close` answers `live.reverse`, checked against `[0,1,2]`; the `releaseOrder` rows answer `[6, [3, []]]` and the `freshRelease` rows `[7, [3, []]]`, both last constructed first. The eight goldens under `generated/traces/layer/` are owed a regeneration after lane L3 (a layer is a handle now, so every index moved) and the host agreement is owed with them | release is reverse construction order: rc.112 registers each memo entry's finalizer on the caller scope before running the construction (`layer.memo-build-once`) and a scope runs its finalizers in reverse registration order (`internal/effect.ts` `scopeCloseFinalizers`) |
| `E4-SEM-CE-018` | SEEDED | `close` only drops the memo table, so a later build behaves like the first: constructed, memoized, and live until the next close | `Effect4Test/Counterexamples/Semantics/Layers.lean`: after `close`, a build raises the count, takes a new handle, and joins neither the memo table nor the live set; the `rebuildAfterClose` golden answers `close []` the second time | `close` is terminal. A closed scope forks closed (`internal/effect.ts` `scopeForkUnsafe`), so the construction lands in an already-closed layer scope and is released inside its own build; the handler carries `closed` |
| `E4-SEM-CE-019` | SEEDED | The agreement of `generated/traces/layer/` is evidence about observer counting or concurrent builds | refusal row; the only witness is what the alphabet *cannot* carry. A memoized rebuild's finalizer decrements `entry.observers` and releases nothing (`layer.memo-reuse-observer-count`, `layer.memo-finalizer-last-observer`), so one release per construction reaches the wire and never one per build; `memoMapBuild` installs a `Deferred` before the construction, so a second fiber awaits rather than builds, and this handler is single-fiber. `Effect4Test/Counterexamples/Semantics/Layers.lean` pins the single-fiber statement (two builds, one live entry, one release) beside the exact observer count the store carries. **The third clause — layer composition — is lifted by lowering lane L3**: a layer crosses as a `Handle "Layer.Layer<number>"` answered by the constructor that made it, so `Layer.provide`, `Layer.merge`, `Layer.mergeAll` and the forked memo map's parent lookup are rows of the family, not prose | the row is registered and `docs/TRACE-DAG.md` lists observer counting and concurrent builds among the things agreement does not establish; a witness for either needs a concurrent host program and a scheduler tape, which this phase has not. `observers` is a probe of the store and not an rc.112 call, so `harness/trace/layer-tail.ts` owes an observer counter of its own before any golden compares it — that is the forgetful direction of the join. What is left of the composition clause is `LAYER-FB-LAYER-IDENTITY`: rc.112 keys the memo `Map` on the layer *object* (`Layer.ts:411`, `:438`) and an index is only a stand-in, the `SCOPE-FB-KEY-IDENTITY` shape |

| `E4-RUN-CE-001` | SEEDED | Scope close may run its finalizers and then write the `Closed` state | `Effect4Test/Counterexamples/Runtime/Scope.lean`, `close_must_write_state_first` and `close_state_is_independent_of_finalizers` | write the state first; `Scope.closeState` takes no `run` argument, `Scope.close_state_independent_of_run` holds by construction, and `Scope.close_reentrant_add` states what a re-entrant finalizer observes |
| `E4-RUN-CE-002` | SEEDED | Scope finalizers run in registration order | `Effect4Test/Counterexamples/Runtime/Scope.lean`, `close_order_is_lifo` and `close_order_changes_the_cause` | reverse the materialised list; `Scope.closeOrder_eq`, `Scope.closeOrder_last_first`, `Scope.closeExits_reverse` |
| `E4-RUN-CE-003` | SEEDED | Closing a scope twice re-runs its finalizers, or overwrites the stored closing exit | `Effect4Test/Counterexamples/Runtime/Scope.lean`, `close_is_idempotent` and `close_guard_prevents_double_run` | guard on `Closed` first; `Scope.close_idempotent`, `Scope.close_twice`, `Scope.closeState_idempotent`, `Scope.closeResult_closed` |
| `E4-RUN-CE-004` | SEEDED | Adding a finalizer to a `Closed` scope registers it, or silently drops it | `Effect4Test/Counterexamples/Runtime/Scope.lean`, `add_after_closed_runs_now` and `add_after_closed_registers_nothing` | run it now with the stored closing exit and register nothing; `Scope.addExit_closed`, `Scope.addExit_closed_registers_nothing`, `Scope.addUnsafe_closed` |
| `E4-RUN-CE-005` | SEEDED | Finalizer removal may normalise every scope into a keyed map, and a cleared inline slot is just the empty map | `Effect4Test/Counterexamples/Runtime/Scope.lean`, `remove_leaves_non_open_untouched`, `cleared_inline_slot_is_its_own_state`, `remove_inline_miss_is_a_no_op` | leave a non-`Open` scope untouched and keep `empty`, `openEmpty`, and `openMap []` three distinct states; `Scope.removeUnsafe_not_open`, `ScopeState.openEmpty_ne_openMap_nil`, `Scope.addUnsafe_openEmpty` |
| `E4-RUN-CE-006` | SEEDED | A child scope forked from a `Closed` parent is a fresh `Empty` scope | `Effect4Test/Counterexamples/Runtime/Scope.lean`, `fork_of_closed_parent_is_born_closed` | born `Closed` carrying the parent's exit; `Scope.fork_closed_parent`, `Scope.fork_closed_parent_child_exit` |
| `E4-RUN-CE-007` | SEEDED | The parent-side and child-side fork finalizers may each mint their own key | `Effect4Test/Counterexamples/Runtime/Scope.lean`, `fork_link_needs_one_shared_key` and `fork_registers_the_same_key_on_both_sides` | one shared key on both sides so the child's finalizer detaches the parent exactly; `Scope.fork_shared_key`, `Scope.fork_detach` |
| `E4-RUN-CE-008` | SEEDED | A failing finalizer aborts the sequential scope close | `Effect4Test/Counterexamples/Runtime/Scope.lean`, `sequential_close_captures_failures` and `short_circuit_loses_a_reason` | capture each finalizer's exit and keep going; `Scope.closeExits_length`, `Scope.closeResult_reasons` |
| `E4-RUN-CE-009` | SEEDED | Scope close always merges its finalizer exits through `exitAsVoidAll` | `Effect4Test/Counterexamples/Runtime/Scope.lean`, `single_finalizer_is_not_merged`, `many_finalizers_are_merged_flat`, `merge_is_concatenation_not_union` | keep three result arms: nothing, the single finalizer's own exit, and the merge; `Scope.closeResult_nil`, `Scope.closeResult_single`, `Scope.closeResult_many` |
| `E4-RUN-CE-010` | SEEDED | `getCont` runs a frame's `contAll` only when that frame answers the demanded arm | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `contAll_runs_on_skipped_frames`, `contAll_skipped_frame_is_still_popped` | run the ensure hook on every frame passed; `FrameFiber.popFrom_ranContAll`, `FrameFiber.getCont_ranContAll` |
| `E4-RUN-CE-011` | SEEDED | A user `catchCause` handler catches an interruption like any other cause | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `interrupt_skips_user_handlers`, `skip_requires_a_pending_cause` | keep popping `contE` frames while the fiber is interruptible with a pending cause; `FrameFiber.interrupt_skips_every_handler`, `FrameFiber.getCont_skip_of_no_pending_cause` |
| `E4-RUN-CE-012` | SEEDED | The handler skip is decided once, before the pop loop | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `mask_frame_stops_the_skip` | re-read the flag after every `contAll`, so a mask frame stops the skip; `FrameFiber.getCont_mask_stops_skip` |
| `E4-RUN-CE-013` | SEEDED | A `SetInterruptible` frame's `contAll` only assigns the flag | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `set_interruptible_substitutes_with_a_pending_cause`, `set_interruptible_without_a_cause_substitutes_nothing` | with a pending cause it returns `failCause(cause)` for either arm; `Prim.ensure_setInterruptible_substitutes`, `Prim.answerOf_replacement` |
| `E4-RUN-CE-014` | SEEDED | An `OnExit` frame's `contAll` leaves the interruptible flag alone | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `on_exit_finalizer_runs_masked`, `on_exit_told_not_to_mask` | clear the flag and push `SetInterruptible(true)` unless told not to; `Prim.ensure_onExit_masks`, `Prim.ensure_onExit_told_not_to` |
| `E4-RUN-CE-015` | SEEDED | Either `OnExit` arm returns whatever the finalizer produced | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `on_exit_restores_the_original_exit` | run the finalizer, then restore the original exit; `Prim.armA_onExit`, `Prim.onExit_finalizer_success_restores` |
| `E4-RUN-CE-016` | SEEDED | When both the computation and its finalizer fail, one cause is dropped | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `finalizer_failure_merges_by_combine`, `finalizer_failure_under_success_stands_alone` | merge by `causeCombine` on the failure arm only; `Prim.onExit_finalizer_failure_merges`, `Prim.onExit_success_finalizer_failure` |
| `E4-RUN-CE-017` | SEEDED | The continuation arms live on the op prototype, as `evaluate` does | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `arms_are_assigned_per_instance`, `the_three_frames_answer_different_arms` | store the continuation name per frame; `Prim.onSuccess_arm_is_per_instance`, `Prim.onFailure_arm_is_per_instance`, `Prim.onSuccessAndFailure_arms_are_per_instance` |
| `E4-RUN-CE-018` | SEEDED | An `Exit` yielded by a generator is returned as the next primitive like any other effect | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `iterator_folds_exits_inline`, `iterator_pushes_only_for_a_real_effect` | fold the inline run inside `contA`, so no `getCont` checkpoint sits between the folded values; `Prim.iterator_folds_inline`, `Prim.armA_iterator_resume` |
| `E4-RUN-CE-019` | SEEDED | `While`'s `contA` steps the cursor and runs the body again without re-testing | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `while_retests_through_contA`, `while_steps_before_it_retests` | step, re-test the stepped cursor, and only then push and run the body; `Prim.armA_whileLoop_continue`, `Prim.armA_whileLoop_stop` |
| `E4-RUN-CE-020` | SEEDED | Producing a value with an empty stack is a defect, or the value is dropped | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `empty_stack_yields_the_exit`, `empty_stack_yields_a_failure_unchanged` | yield the Exit the fiber produced; `FrameFiber.resumeValue_empty`, `FrameFiber.resumeCause_empty`, `FrameFiber.step_ofExit_finishes` |
| `E4-RUN-CE-021` | SEEDED | `withFiber` and `YieldableError` can be given behavioural models of the host object | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `with_fiber_cannot_see_the_raw_fiber`, `yieldable_error_cannot_see_the_host_class`, `yieldable_error_evaluates_to_its_own_failure` | refuse both identities by theorem while keeping the behaviours; `Prim.withFiber_refused`, `Prim.yieldableError_host_class_refused` |
| `E4-RUN-CE-025` | SEEDED | Restoring interruptibility replaces any completed scope exit with its pending interruption, including an existing cleanup failure | `Effect4Test/Counterexamples/Runtime/ScopeRestorationBoundary.lean`: `pending_does_not_replace_cleanup_failure`, `overwrite_failure_is_observably_wrong`; `harness/trace/scope-restoration.mjs`: real pending requests, body/cleanup failure and legal defect cases at two thresholds | existing FrameFiber failure-arm skipping retains the incoming failure through the restoration frame and delegates the arbitrary tail to its real step; `test/contracts/scope-restoration.contract.md` freezes the full cause, tail and event equation without changing generic Runtime |
| `E4-RUN-CE-026` | SEEDED | Once inner restoration delivers interruption, later outer cleanup can discard its failures or see a predicted successful inner exit | `Effect4Test/Counterexamples/Runtime/ScopeRestorationBoundary.lean`: `outer_cleanup_retains_delivered_interrupt`, `dropping_outer_cleanup_is_observably_wrong`, `outer_cleanup_receives_actual_outgoing_exit`; `harness/trace/scope-restoration.mjs`: full Interrupt/Fail/Die reason order and retained state | start the actual outer ScopeMachine with the inner frame step's outgoing full Exit and service state; retain all actual responses, then use existing zero/one/many folding and Cause.combine; fallible-release JavaScript probes and legal release defects remain distinct host evidence classes |
| `E4-RUN-CE-022` | SEEDED | Answer shape or final exit alone identifies a full continuation-pop observation | `Effect4Test/Counterexamples/Runtime/LiveStack.lean`, `arm_shape_does_not_identify_payload`, `answer_only_misses_current`, `cause_tags_do_not_identify_annotations`, `answer_only_misses_event_order` | retain complete primitive payloads, current program, ordered/repeated cause reasons and annotations, popped frames, events and all returned fiber fields; the live-stack contract requires whole `FramePop` equality |
| `E4-RUN-CE-023` | SEEDED | A pop may decide interruption before its hook, or a failure entry may discard every deferred answer solely because skipping was requested | `Effect4Test/Counterexamples/Runtime/LiveStack.lean`, `post_hook_mask_stops_discard`, `stale_decision_positive_control`, `masked_deferred_distinguishes_inner_and_legacy`, `legacy_discards_deferred_event` | re-read interruption after hooks; keep a masked deferred answer and retain the event when an unmasked answer is discarded; old-entry compatibility is guarded by no deferred interruption, with separate unrestricted false-skip equality; representable-state evidence does not establish source reachability |
| `E4-RUN-CE-027` | SEEDED | A generator's progress is a function of its name and the last answer, so the `Iterator` frame can be pushed back unchanged | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `same_name_reenters_the_first_yield`, `advanced_name_reaches_done` | `IterStep.resume next continueAs` names the advanced generator and `Prim.armA` pushes `iterator continueAs cursor` back (`Prim.armA_iterator_resume`, `iterator_folds_inline` restated); found 2026-09-04 by writing a two-yield `Effect.gen` as `Effect4/Syntax/Eff.lean` data, `docs/research/2026-09-04-eff-compile.md` §0; no battery had run a two-yield generator — `docs/research/2026-09-04-stress-plan.md` |
| `E4-RUN-CE-028` | SEEDED | `While`'s step is a function of the body's answer alone, so the frame's stored cursor can be dropped at `contA` | `Effect4Test/Counterexamples/Runtime/Frames.lean`, `answer_only_step_cannot_count`, `cursor_step_counts` | `PrimInterp.loopStep : ν → β → β → β` reads the stored cursor and the answer, as rc.112's `step(value)` reads the closure it mutates (`internal/effect.ts:4624-4645`); `Prim.armA_whileLoop_continue`/`_stop` restated; found 2026-09-04 with a counting `Effect.whileLoop` as `Eff` data, `docs/research/2026-09-04-eff-compile.md` §4 G3 |
| `E4-RUN-CE-029` | SEEDED | The interrupt-children middleware is installed separately from `fork`, so a tracked (non-daemon) child can outlive its parent's exit when nothing installs it | `Effect4/Deep/Witnesses.lean`, `w5_fork_latches_the_middleware` (a tape with nothing but the root's start: the fork sets the latch and the parent's exit interrupts the child) and `w5_daemon_child_survives_parent_exit` (the child that survives is the daemon one, host golden `test/fixtures/traces/fiber-m3/daemonSurvivesParentExit.tsv`); `Effect4Test/Syntax/CompileContract.lean`, `pMiddleware` on the bare tape and `pDaemon` | `forkChild` calls `interruptChildrenPatch()` (`internal/effect.ts:5253`, `:6656-6658`), so the `fork` arm with `daemon = false` sets `middlewareInstalled` (`withFiber_fork` restated); `RunDecision.installMiddleware` remains for installs that are not forks; the retired witness `w5_no_middleware_leaves_children` had pinned the opposite (`docs/research/2026-09-04-deep-review-2.md`, R2-6, found 2026-09-04 by the second review) |
| `E4-RUN-CE-030` | SEEDED | An interrupt one fiber requests on another (`fiberInterrupt`, `fiberInterruptAll`, the exit path's children, a race's settle) carries no annotations beyond the target's own stack | `Effect4/Deep/Witnesses.lean`, `w6_close_interrupt_carries_closer_annotations` (the closer's stack key sits on top of the child's own, and a tape interrupt with empty annotations leaves only the child's), `w5_middleware_interrupts_children`, `w3_immediate_success_stops_launch`, `w6_link_then_close` (all restated with `interruptedWith … (stores.stackAnnotations ⟨0⟩)`) | rc.112 passes the *caller's* `fiberStackAnnotations` (`internal/effect.ts:880-883`, `:892-895`, `:910-913`): `interruptEach` takes the caller's annotations and every site passes `interp.stackAnnotations caller` (`interruptEach_known`, `interruptThenJoin_eq`, `withFiber_interruptAll`, `drive_launch_skipped` restated); `forkIn`'s closed-scope arm (M10) was the only site that had them (`docs/research/2026-09-04-deep-review-2.md`, R2-5) |
| `E4-RUN-CE-031` | SEEDED | A `Sync` may pop its continuation before running what its thunk owes synchronously, and may pop it through the frame machine's pure finalizer stand-in | `Effect4/Deep/Witnesses.lean`, `w13_completion_pop_sees_the_waiter_interrupt` (a completer under `onExit` whose resumed waiter interrupts it: the finalizer program runs once with the *interrupt* exit, not the thunk's success) and `w13_sync_meets_the_finalizer_program` (a `sync` under an `onExit` whose finalizer parks: the program runs and the fiber parks in it); the first shape was pinned nowhere and the second ran no program at all | `Sync[evaluate]` is thunk then `getCont` (`internal/effect.ts:931-935`), and `doneUnsafe` resumes every waiter inside the thunk (`Deferred.ts:1655-1658`): the sync arm answers `Outcome.answered` with the thunk's value as `current` and `drainDue` nested, and the pop is `Cmd.deliver` — `evaluatePrim` on the answer, no loop top, no op count — run after the nested commands, so its `getCont` sees the recorded interrupt (`:680-683`) and an `OnExit` frame's program runs through `finalizerOr`; `settle` and `Cmd.finish` make the loop's outcome dispatch first-order (`evaluatePrim_sync_answers`, `settle_answered`, `drive_deliver`, `drive_finish`); `docs/research/2026-09-04-deep-review-2.md`, R2-1 |
| `E4-RUN-CE-032` | SEEDED | A join, await or await-all park needs no cleanup: an interrupted awaiter can leave its observer on the target | `Effect4/Deep/Witnesses.lean`, `w14_join_cleanup_drops_the_observer` (the parked joiner holds one frame, the target one observer; the interrupt fails through the frame and the observer is gone, the target untouched); before the repair no frame was pushed and the observer stayed | `fiberJoin`/`fiberAwait`/`fiberAwaitAll` are `callback`s whose registration returns the splice-out cleanup (`internal/effect.ts:773`, `:812`, `:821`), pushed as an `AsyncFinalizer` frame at park time (`:1128-1141`) and run by its `contE` under the mask (`:1155-1159`): the join arm and `countdownPark` push `asyncFinalizer (cancelName parkCancelName fiber token)`, whose program is the new `WithFiberAction.dropObservers token` (`evaluatePrim_join_live`, `countdownPark_parks`, `withFiber_dropObservers`); `docs/research/2026-09-04-deep-review-2.md`, R2-3 |
| `E4-RUN-CE-033` | SEEDED | `fiberAwaitAll` observes every target at once and answers the exits in completion order | `Effect4/Deep/Witnesses.lean`, `w12_awaitAll_input_order` (the second child exits first; the answer lists the first child's exit first, and while the first is live it is the only fiber observed); `w12_awaitAll_answers_the_exits` had pinned "the order the countdown collected them" | `fiberAwaitAll` walks its fibers in input order, one observer at a time (`internal/effect.ts:794-808`): `countdownWalk`, `Pending.waitingOn`/`remaining`, `fireObserver_countdown_done`/`_next`; R2-4 |
| `E4-RUN-CE-034` | SEEDED | A racing host interrupted while entrants are live leaves them running, and a settled race interrupts the losers on nobody's stack, unmasked | `Effect4/Deep/Witnesses.lean`, `w3_host_interrupt_cancels_entrants` (the host holds the race's cleanup frame; interrupted, it interrupts the live entrant with its own id and annotations) and `w3_settle_interrupts_the_parked_loser` (the winner's settle resumes the host with the cleanup program, and the host interrupts the parked loser itself); `w3_empty_until_interrupted` had checked only the host | the race is a `callback` whose registration returns `fiberInterruptAll(fibers)` (`internal/effect.ts:1530`) — pushed as `asyncFinalizer (cancelName (raceCancelName race) host token)`, program `WithFiberAction.cancelRace race` (R2-13) — and the winner resumes the host with `flatMap(uninterruptible(fiberInterruptAll(live)), () => exit)` (`:1510-1514`): `RunInterp.raceSettle`, `Stores.raceSettleProgram`, `settleRace_eq` (R2-12) |
| `E4-RUN-CE-035` | SEEDED | A race's entrants all exist before any is launched, so an entrant the settle overtakes is a fiber that is interrupted and kept (M8) | `Effect4/Deep/Witnesses.lean`, `w3_immediate_success_stops_launch` (two fibers, one entrant program left unlaunched, nothing interrupted); host golden `test/fixtures/traces/fiber-m3/raceImmediateSuccessStopsLaunch.tsv`, `answer started [0, []]` — M8 rested on the *Lean-face* golden (`docs/research/2026-09-04-spike-a0-avatar.md`) | `raceAll` forks one entrant per iteration of its register loop and breaks once done (`internal/effect.ts:1520-1528`): `Race.programs` holds the entrants still to fork, `Cmd.launch race` is one iteration (`launchEntrant`, then `evaluate` and the next launch), and a launch after acceptance forks nothing (`drive_launch_done`); `RunEvent.raceSkipped` is retired; `docs/research/2026-09-04-deep-review-2.md`, R2-11 |
| `E4-RUN-CE-036` | SEEDED | `forkIn` links the child to the scope before running it, so an immediately finished child is linked and then unlinked | `Effect4/Deep/Witnesses.lean`, `w6_child_exit_drops_key` (no `scopeLinked` row for an immediately finished child) and `w6_deferred_child_is_linked` (a child started on the parent's dispatcher is linked, and its later exit drops the key) | `forkIn` forks and, when immediate, runs the child first, and links only `if (!fiber._exit)` (`internal/effect.ts:5366-5376`): the link is `Cmd.link` after the start commands (`withFiber_forkIn`, `drive_link`), and `linkScope` skips an exited target (R2-9); R2-8 |
| `E4-RUN-CE-037` | SEEDED | `awaitAllChildren` awaits the new children only when its body succeeds | `Effect4/Deep/Witnesses.lean`, `w5_await_all_children_on_failure` (a failing body: the parent parks until the new child exits, then fails with its own cause; the child ends by its own exit) and `w5_await_all_children_awaits_only_new` (the body's own value is answered) | `awaitAllChildren` is `onExit(self, …)` (`internal/effect.ts:5314-5333`): `Name.snapshotThen` compiles to `Prim.onExit body (finalizerName (FinName.awaitNewChildren snapshot)) false`, whose program is `WithFiberAction.awaitNewChildren`, run under the finalizer mask on any exit; `Name.awaitNew` is retired; R2-7 |
| `E4-RUN-CE-038` | SEEDED | `runSyncExit` flushes every armed dispatcher, so a program whose child yielded completes under `runSync` | `Effect4/Deep/Witnesses.lean`, `w8_sync_child_yield_is_async` (a root joining an immediate child that yields: the entry answers `AsyncFiberError` while the child's callback is scheduled, and a later host flush completes the root) | `runSyncExitWith` flushes the *root's* dispatcher only (`fiber._dispatcher?.flush()`, `internal/effect.ts:5542`) and dispatchers are per fiber (`:552-554`; a yield schedules on the yielding fiber's own, `:986`): `stepDecision.flushRoot` (`flushRoot_idle`, `flushRoot_round`), `runSyncExit_exited`/`_survives` restated; `docs/research/2026-09-04-deep-review-2.md`, R2-14 |
| `E4-RUN-CE-039` | SEEDED | The host runs armed dispatchers in fiber order ("assumption, recorded") | `Effect4/Deep/Witnesses.lean`, `w15_flush_runs_callbacks_in_arming_order` (fiber `2` arms before fiber `1`; the flush resumes `2` first) | a dispatcher arms itself with `setImmediate` (or the sync scheduler's microtask) the first time a task is scheduled on it, and later tasks join the armed callback (`Scheduler.ts:207-212`); the host runs the callbacks in arming order: `RunMachine.armed`, `arm` at every scheduling site (`start`, `injectYield`, `yieldNowWith`), `disarm` in `fire`, `flushAll` over the head of the schedule (`RunMachine.arm_new`/`_known`, `flushAll_round`); R2-15 |
| `E4-RUN-CE-024` | SEEDED | The detached-stack extension to source AsyncFinalizer is safe because it selects the same next continuation or eventually returns the same exit | `Effect4Test/Counterexamples/Runtime/LiveStack.lean`, `current_profile_no_answer_keeps_stack` (every constructor but `asyncFinalizer`) and `asyncFinalizer_pushes_without_answering` (the shape the row named, reachable since the run-loop packet made `AsyncFinalizer` a `Prim` constructor); `Effect4/Runtime/Runtime.lean`, `FrameFiber.popFrom_asyncFinalizer_pops_its_push` (the divergence: the pushed restoring frame is popped inside the same pop, answering `replacement (failCause)`) and `popFrom_pass_no_push` (the fused reading survives for every frame that pushes nothing); independent source prefix regression still required by `test/contracts/live-stack.contract.md` | repaired 2026-09-03 (spike S1, `docs/research/2026-09-03-spike-s1-prim-parking.md` §2): the attack held — the fused loop did **not** agree with rc.112 on a successful async resume — so `popFrom` is unfused, structurally, with `passPushed` draining the frame a hook pushed before the frames below it; the executing-finalizer relation is the fiber machine's (`workshop/Deep/Fibers.lean`) |
| `E4-SURFACE-CE-001` | SEEDED | `Kind.text` is a struct of primitives, read loosely enough that a nested object counts as a property like any other | `Effect4Test/Counterexamples/Surface/Kind.lean`, `textWithNestedObject` (admitted as `struct` and as `json`, refused as `text`) | `kindCheck .text` inspects property types: string, number, boolean, a string or number literal, an `anyOf` of those, an optional of those, and nothing else |
| `E4-SURFACE-CE-002` | SEEDED | `Kind.struct` is any `objects` node | `Effect4Test/Counterexamples/Surface/Kind.lean`, `structWithIndexSignature` (JSON-representable, and not enumerable) | require string property keys and an empty index-signature list, so an entity key list and a tool parameter list can be checked at all |
| `E4-SURFACE-CE-003` | SEEDED | `Kind.void` marks a no-content response, read as any representation with no fields | `Effect4Test/Counterexamples/Surface/Kind.lean`, `voidLookalikes` (an empty struct, `null`, `never`, `unknown`, `undefined`, all refused) | `kindCheck .void` admits exactly the representations whose tag is `.void`; `kindCheck_void_iff` states it and `void` is disjoint from the other kinds |
| `E4-SURFACE-CE-004` | SEEDED | Every `Representation` an emitter receives is JSON-representable because the estate only builds JSON schemas | `Effect4Test/Counterexamples/Surface/Kind.lean`, `noJsonInhabitant` (`undefined`, `bigint`, `symbol`, a global symbol, a bigint literal, a template literal) and `duplicateKeys` | `JsonRepresentable = kindCheck refs 64 .json` is a precondition of every emitter, over the refusal set `Effect4.Arch.Accepts` already names |
| `E4-SURFACE-CE-005` | SEEDED | `kindCheck refs fuel k rep = false` means `rep` does not have kind `k` | `Effect4Test/Counterexamples/Surface/Kind.lean`, `fuelFrontier`: `reference "User"` answers `false` at fuel 0 and 1, `true` at 64; `reference "Warehouse"` answers `false` at every fuel | fuel exhaustion is a frontier of the check and never a refusal: no `Refusal` constructor, error value or theorem may name it, and `kindCheck_fuel_mono` is how a caller raises the budget |
| `E4-SURFACE-CE-006` | SEEDED | `toJsonSchema` and `ofJsonSchema` round-trip a representation, stated without a quotient | `Effect4Test/Counterexamples/Surface/JsonSchema.lean`, `annotatedUserId` (a `Check.pattern` survives) and `unspellableCheck` (`Check.trimmed` makes the forward direction `none`) | name the quotient: `annotationErasure` is the exact information dropped, `ofJsonSchema_toJsonSchema` is stated against it, and an inexpressible check answers `none` rather than being dropped |
| `E4-SURFACE-CE-007` | SEEDED | A `$ref` names an entry of the document's `$defs` | `Effect4Test/Counterexamples/Surface/JsonSchema.lean`, `foreignPointers` (`#/definitions/User`, `#/components/schemas/User`, a remote URL, a cross-document pointer, a deep pointer, and bare `#`) | refuse every pointer that is not exactly `#/$defs/<segment>` with `Refusal.unsupportedRefTarget` carrying it verbatim; resolve no base URI and fetch no remote document |
| `E4-SURFACE-CE-008` | SEEDED | The decoder reads the keywords it models and ignores the rest | `Effect4Test/Counterexamples/Surface/JsonSchema.lean`, `unmodeledKeywords` (`contentEncoding`, `patternProperties`, `not`, `unevaluatedProperties`, `dependentRequired`, `$dynamicRef`) | refuse with `Refusal.unknownKeyword` naming the keyword; a sibling keyword that parsed does not suppress it |
| `E4-SURFACE-CE-009` | SEEDED | The key is a list of property names, with no condition on optionality | `Effect4Test/Counterexamples/Surface/Entity.lean`, `optionalKey` (`email` is a property, and optional, so the weaker check passes) | every key member is a *required* property name, and `Entity.key_subset_props` is stated against `Entity.requiredPropertyNames` |
| `E4-SURFACE-CE-010` | SEEDED | An entity is a named struct, with the key list left free | `Effect4Test/Counterexamples/Surface/Entity.lean`, `emptyKey` | `keyEmpty` is a clause: an entity with no identity has no store path and no client cache handle |
| `E4-SURFACE-CE-011` | SEEDED | The key names the identity fields, checked against nothing | `Effect4Test/Counterexamples/Surface/Entity.lean`, `keyNotAProperty` | `keyNotAProperty` is a clause, so a generator cannot read an absent field |
| `E4-SURFACE-CE-012` | SEEDED | The key is a list of required property names, with duplicates admitted | `Effect4Test/Counterexamples/Surface/Entity.lean`, `duplicateKey` | `keyDuplicate` is a clause: a repeated key makes a composite identity of the wrong arity |
| `E4-SURFACE-CE-013` | SEEDED | `Domain.refs` is the entities as reference entries, with no distinctness condition | `Effect4Test/Counterexamples/Surface/Entity.lean`, `twoUsers` (both entries are in the table, so `reference "User"` is not a function) | `entityNameDuplicate` refuses the domain rather than resolving by first match |
| `E4-SURFACE-CE-014` | SEEDED | An entity refers to another by `Schema.reference name`, and the domain is the closed world | `Effect4Test/Counterexamples/Surface/Entity.lean`, `userWithDanglingRef` and `openWorld` (no `$defs` target, no TypeScript module) | `referenceUnresolved` is checked by `Domain.check`, not by the emitter |
| `E4-SURFACE-CE-015` | SEEDED | An entity carries its domain, with the field never read | `Effect4Test/Counterexamples/Surface/Entity.lean`, `foreignEntity` and `mixedDomain` | `entityDomainMismatch` is a clause; v1 models no cross-domain reference at all |
| `E4-SURFACE-CE-016` | SEEDED | `params` decodes the path variables, with the two lists never compared | `Effect4Test/Counterexamples/Surface/Api.lean`, `pathParamNoDecoder` | compare `path.paramNames` with `params`'s property names in both directions, and state it as `Endpoint.wellFormed_params_match` |
| `E4-SURFACE-CE-017` | SEEDED | Containment of path variables in `params` is enough | `Effect4Test/Counterexamples/Surface/Api.lean`, `decoderNoPathParam` (a decoder for a variable the path does not carry) | set equality, not containment: `schemaParamWithoutPath` is its own clause |
| `E4-SURFACE-CE-018` | SEEDED | The path's variables are its `param` segments, with no distinctness condition | `Effect4Test/Counterexamples/Surface/Api.lean`, `duplicatePathParam` (`/:id/x/:id`) | `pathParamDuplicate` is a clause: the second binding silently wins in every router |
| `E4-SURFACE-CE-019` | SEEDED | An endpoint may declare a payload | `Effect4Test/Counterexamples/Surface/Api.lean`, `getWithPayload`, against the control `postWithPayload` | `Method.allowsPayload` is false for `get`, `head`, `options`, and `payloadOnBodylessMethod` is the clause |
| `E4-SURFACE-CE-020` | SEEDED | The bodyless rule is about `GET` | `Effect4Test/Counterexamples/Surface/Api.lean`, `headWithPayload` | the same clause covers `HEAD`, whose response has no body either |
| `E4-SURFACE-CE-021` | SEEDED | The bodyless rule is about `GET` and `HEAD` | `Effect4Test/Counterexamples/Surface/Api.lean`, `optionsWithPayload` | the same clause covers `OPTIONS` |
| `E4-SURFACE-CE-022` | SEEDED | Endpoint ids are distinct, so the API is unambiguous | `Effect4Test/Counterexamples/Surface/Api.lean`, `adminGroup` and `collidingApi`: two distinct ids and two distinct `path` fields whose *group prefixes* make the full paths equal | `Api.check` compares `(method, fullPath)` over `api.pathPrefix ++ group.pathPrefix ++ endpoint.path` with `routeCollision`, and `Api.routes_nodup` states it |
| `E4-SURFACE-CE-023` | SEEDED | An endpoint has one success and a list of errors, with the two never compared | `Effect4Test/Counterexamples/Surface/Api.lean`, `successIsAlsoError` (rc.112 `validateResponseExclusivity`, `HttpApiEndpoint.ts:1258-1299`, is the check that would have caught it) | `statusCollision` ranges over successes and errors together |
| `E4-SURFACE-CE-024` | SEEDED | Error statuses need no distinctness check | `Effect4Test/Counterexamples/Surface/Api.lean`, `duplicateErrorStatus` | the same `statusCollision` clause; two schemas on one status make the client's decode undecidable |
| `E4-SURFACE-CE-025` | SEEDED | A status is a `Nat` | `Effect4Test/Counterexamples/Surface/Api.lean`, `statusOutOfRange` (999, and an error at 600) | `statusOutOfRange` over `100..599`, inclusive at both ends |
| `E4-SURFACE-CE-026` | SEEDED | A status below 600 is a status | `Effect4Test/Counterexamples/Surface/Api.lean`, `statusBelowRange` (0, the value a partially initialised record carries, and 42) | the range is two-sided, and both boundaries are pinned as admitted |
| `E4-SURFACE-CE-027` | SEEDED | rc.112 `HttpApiEndpoint.ts:1134`, multiple payload encodings for one content type | `Effect4Test/Counterexamples/Surface/Api.lean`, `twoJsonPayloads`, against the control that admits one JSON and one multipart payload | clause 8, `payloadEncodingDuplicate`, over `Payload.encoding`'s content type |
| `E4-SURFACE-CE-028` | SEEDED | rc.112 `:1137`, multiple multipart payloads for one content type | `Effect4Test/Counterexamples/Surface/Api.lean`, `twoMultipartPayloads` | clause 9, `multipartPayloadDuplicate`, distinct from clause 8 for the reason rc.112 has a distinct throw |
| `E4-SURFACE-CE-029` | SEEDED | rc.112 `:1180`, a streaming schema in an error response | `Effect4Test/Counterexamples/Surface/Api.lean`, `streamingError`, against the control that admits the same stream body on a success | clause 16, `errorBodyStreams`, naming the status |
| `E4-SURFACE-CE-030` | SEEDED | rc.112 `:1201`, multiple streaming success responses | `Effect4Test/Counterexamples/Surface/Api.lean`, `twoStreams` | clause 13, `streamSuccessDuplicate` |
| `E4-SURFACE-CE-031` | SEEDED | rc.112 `:1206`, no-content combined with a streaming success, stream recorded first | `Effect4Test/Counterexamples/Surface/Api.lean`, `streamThenVoid` | clause 14, `streamWithVoidStatus`, naming the status |
| `E4-SURFACE-CE-032` | SEEDED | rc.112 `:1209`, buffered and streaming success of one content type, stream recorded first | `Effect4Test/Counterexamples/Surface/Api.lean`, `streamThenBuffered` | clause 15, `streamWithBufferedStatus`; the clause compares content types, so a stream and a buffered body of different content types is a different question |
| `E4-SURFACE-CE-033` | SEEDED | rc.112 `:1219`, the `:1206` message from the buffered-first branch | `Effect4Test/Counterexamples/Surface/Api.lean`, `voidThenStream` | clause 14 again; the row is separate because the rc.112 path is separate and a half-repair must stay visible |
| `E4-SURFACE-CE-034` | SEEDED | rc.112 `:1225`, the `:1209` message from the buffered-first branch | `Effect4Test/Counterexamples/Surface/Api.lean`, `bufferedThenStream` | clause 15 again, for the same reason |
| `E4-SURFACE-CE-035` | SEEDED | rc.112 `:1290`, multiple responses with headers for one status | `Effect4Test/Counterexamples/Surface/Api.lean`, `twoHeaderResponsesOneStatus`, against the control that admits one response with headers per status | clause 17, `responseHeadersDuplicate`; `Response.headers` exists so the clause has operands |
| `E4-SURFACE-CE-036` | SEEDED | rc.112 `:1303`, a HEAD endpoint with a streaming success | `Effect4Test/Counterexamples/Surface/Api.lean`, `headStreams`, with `Method.allowsPayload .head = false` pinned beside it | clause 18, `streamOnHeadMethod`; both facts are pinned so removing either is visible |
| `E4-SURFACE-CE-037` | SEEDED | rc.112 `:1306`, the reserved SSE event name `effect/httpapi/stream/failure` | `Effect4Test/Counterexamples/Surface/Api.lean`, `reservedSseName`, against the control that admits any other event name | clause 19, `sseEventNameReserved`; the event names are data on `ResponseBody.stream`, which is why the clause can see them |
| `E4-SURFACE-CE-038` | SEEDED | A tool name is a legal identifier, reusing `TypeScript.targetIdentifier` or a similar convention | `Effect4Test/Counterexamples/Surface/Agent.lean`, `illegalToolNames`, with `lookup-user` admitted (a hyphen is legal for MCP and not for a TypeScript identifier) | `Tool.nameLegal` decides `^[A-Za-z0-9_-]{1,64}$` itself, and `toolNameIllegal` is the clause |
| `E4-SURFACE-CE-039` | SEEDED | The MCP length bound is a formality | `Effect4Test/Counterexamples/Surface/Agent.lean`, `name64` admitted and `name65` refused | the bound is 1..64 and the count is characters, not UTF-8 bytes |
| `E4-SURFACE-CE-040` | SEEDED | A toolkit is a list of tools | `Effect4Test/Counterexamples/Surface/Agent.lean`, `twoToolsOneName` (each tool well formed on its own) | `toolNameDuplicate` on the server: a client indexes `tools/call` by name |
| `E4-SURFACE-CE-041` | SEEDED | Resources are distinct because their names are | `Effect4Test/Counterexamples/Surface/Agent.lean`, `twoResourcesOneUri`, against the control that admits one name on two URIs | `resourceUriDuplicate`: the URI is the key a client reads |
| `E4-SURFACE-CE-042` | SEEDED | One distinctness clause covers resources and prompts | `Effect4Test/Counterexamples/Surface/Agent.lean`, `twoPromptsOneName` | `promptNameDuplicate` is its own clause, because prompts have no URI and the two clauses read different fields |
| `E4-SURFACE-CE-043` | SEEDED | Bindings are a list | `Effect4Test/Counterexamples/Surface/Deploy.lean`, `duplicateBindingNames` (the collision is across two different tables, which a per-table check misses) | `bindingNameDuplicate` across all tables: bindings become properties of one `env` object |
| `E4-SURFACE-CE-044` | SEEDED | A binding name is a string | `Effect4Test/Counterexamples/Surface/Deploy.lean`, `illegalBindingNames` (`MY-DB` is accepted by wrangler's own schema and unreachable from generated code without bracket access) | `bindingNameIllegal` decides `^[A-Za-z_][A-Za-z0-9_]*$` |
| `E4-SURFACE-CE-045` | SEEDED | `main` is optional | `Effect4Test/Counterexamples/Surface/Deploy.lean`, `workerWithoutMain` and `staticWithMain` | the host decides in both directions: `mainMissing` and `mainForbidden` |
| `E4-SURFACE-CE-046` | SEEDED | A deployment that is well formed can serve the APIs it mounts | `Effect4Test/Counterexamples/Surface/Deploy.lean`, `providesNothing` (well formed, and `satisfies` refuses) and the wrong-service-name variant | `Deployment.satisfies` is a separate judgment over the API list with `requirementUnprovided`; the emitted worker module is `none` when it fails |
| `E4-SURFACE-CE-047` | SEEDED | `provides` maps a service name to a binding name, with the binding never looked up | `Effect4Test/Counterexamples/Surface/Deploy.lean`, `providesAbsentBinding` | `providedBindingAbsent` is a `check` clause, because it reads the deployment alone |
| `E4-SURFACE-CE-048` | SEEDED | The compatibility date is a string | `Effect4Test/Counterexamples/Surface/Deploy.lean`, `badCompatibilityDates` (`2026-13-01` and `2026-09-32` pass a digits-and-dashes shape check) | `compatibilityDateIllegal` checks the shape and the month and day ranges; Cloudflare changes runtime behaviour across the date, so a malformed one is a runtime difference rather than an error |
| `E4-SURFACE-CE-049` | SEEDED | The worker name is the deployment's name | `Effect4Test/Counterexamples/Surface/Deploy.lean`, `illegalWorkerNames` (uppercase, underscore, empty, 64 characters) | `workerNameIllegal` decides `^[a-z0-9-]{1,63}$`, checked at both ends of the bound |
| `E4-SURFACE-CE-050` | SEEDED | Pages are a list, and a route is a `Path` | `Effect4Test/Counterexamples/Surface/Site.lean`, `duplicateRoutes`, plus two segment lists that render one string | `routeDuplicate` compares `Path.render`, not the `Path` value |
| `E4-SURFACE-CE-051` | SEEDED | A page names the endpoints it uses | `Effect4Test/Counterexamples/Surface/Site.lean`, `usesAbsentEndpoint`, plus the wrong-group and wrong-API variants | `Site.resolves` is a separate judgment over the APIs with `usedEndpointAbsent` naming the whole triple; a search across groups would silently repair a rename |
| `E4-SURFACE-CE-052` | SEEDED | A form names an endpoint | `Effect4Test/Counterexamples/Surface/Site.lean`, `formWithoutPayload`, distinguished from the unresolvable-reference case | `formEndpointWithoutPayload`, checked after the reference resolves; it is the browser-side twin of `payloadOnBodylessMethod` |
| `E4-SURFACE-CE-053` | SEEDED | The OpenAPI decoder reads `requestBody` | `Effect4Test/Counterexamples/Surface/Ingest.lean`, `multipartOperation` | `unsupportedContentType` carries the content type verbatim and refuses the whole document; only `application/json` is in the fragment |
| `E4-SURFACE-CE-054` | SEEDED | An OpenAPI parameter has a name and a schema | `Effect4Test/Counterexamples/Surface/Ingest.lean`, `cookieParameter` | `unsupportedParameterLocation`; filing a cookie parameter as a query parameter would put a session token in a URL |
| `E4-SURFACE-CE-055` | SEEDED | A wrangler file is its known binding tables | `Effect4Test/Counterexamples/Surface/Ingest.lean`, `unknownBindingTables` (`hyperdrive`, `ai`, `vectorize`, `analytics_engine_datasets`, `browser`) and the two `missingField` rows | `unsupportedBindingKind` carries the table key; a decoder that read only the six it knows would make the round trip a lie on the configurations that matter most |
| `E4-SURFACE-CE-056` | SEEDED | An MCP tool has an `inputSchema` | `Effect4Test/Counterexamples/Surface/Ingest.lean`, `nonObjectInputSchema` (a string, and the boolean `true` that draft 2020-12 admits) | `unsupportedShape "inputSchema"`; an empty parameter struct is a valid `Kind.struct`, so the bad row would look modeled |
| `E4-SURFACE-CE-057` | SEEDED | An ingested row is a row like any other | `Effect4Test/Counterexamples/Surface/Ingest.lean`, the stance census over every entity of every decoded domain | every decoder writes `stance := .ingested`; a canonical-looking ingested row would be regenerated over its own source |
| `E4-SURFACE-CE-058` | SEEDED | A rule is `modeled` when its emitter is right | `Effect4Test/Counterexamples/Surface/Emit.lean`, `landingStances` (every rule `emitted`, every receipt `none`) | `Rule.modeled_has_receipt` makes the flip cost a receipt name in the same change; rightness is not an observation |
| `E4-SURFACE-CE-059` | SEEDED | `Rule.all` lists the emitters | `Effect4Test/Counterexamples/Surface/Emit.lean`, `ids` and the `ofId?` round trip; the source-tag half is the shell census that reuses `scripts/check-lowering-coverage.sh`'s tokenizer | `Rule.mem_all` and `Rule.all_nodup` close the Lean half; the `-- surface: rule.<id>` tag census in both directions closes the other, and neither alone is enough |
| `E4-SURFACE-CE-060` | SEEDED | The refusal alphabet is an implementation detail | `Effect4Test/Counterexamples/Surface/Facts.lean`, the registry census in both directions and `Refusal.clause` on four families | `Facts.registry` has one row per `Refusal` constructor and a `#guard` keeps them equal; a clause with no row could refuse with a name no `#surface_fit` table prints |
| `E4-SURFACE-CE-061` | SEEDED | `WellFormed` is the conjunction of its clauses, so proving three of them proves part of it | `Effect4Test/Counterexamples/Surface/Facts.lean`, `user_clauses_from_wellFormed` and `user_wellFormed_from_clauses` | `wellFormed_iff` per carrier, proved by unfolding `check`; without it a derivation theorem stated from three clauses is unprovable and the only close is a `decide` over the derived value |
| `E4-SURFACE-CE-062` | SEEDED | Annotations are metadata, so a row without them is still a row | `Effect4Test/Counterexamples/Surface/Entity.lean`, `undescribedEntity` | `descriptionMissing` is a clause of `Entity.check` (plan §15.2); every emitter reads semantics from the bag and has no field to fall back on, so the omission would be silent in every output at once |
| `E4-SURFACE-CE-063` | SEEDED | An entity's `identifier` can be derived from its name at emission | `Effect4Test/Counterexamples/Surface/Entity.lean`, `unidentifiedEntity`, and the both-missing case that pins the clause order | `identifierMissing` is the first clause; rc.112 names a `$defs` entry and labels a type-level failure from `identifier` (`Schema.ts:17105`) |
| `E4-SURFACE-CE-064` | SEEDED | Descriptions are for the surface, not for its fields | `Effect4Test/Counterexamples/Surface/Entity.lean`, `undescribedProperty` refused in `shop` and admitted in an inactive domain | `propertyDescriptionMissing` fires exactly when `dom.active`: the source of truth documents its fields, an ingested copy is not held to it |
| `E4-SURFACE-CE-065` | SEEDED | An endpoint's annotations are optional metadata | `Effect4Test/Counterexamples/Surface/Api.lean`, `undescribedEndpoint`, plus the ordering pin against a structural failure | clauses 1 and 2 of `Endpoint.check` run before every structural clause, so a user reads the semantic failure first |
| `E4-SURFACE-CE-066` | SEEDED | A tool's description is documentation | `Effect4Test/Counterexamples/Surface/Agent.lean`, `undescribedTool`, with the emitter answering `none` | `descriptionMissing "tool"`; for MCP the description is what a model reads to decide whether to call the tool |
| `E4-SURFACE-CE-067` | SEEDED | A deployment is infrastructure, so the semantic layer does not reach it | `Effect4Test/Counterexamples/Surface/Deploy.lean`, `undescribedDeployment`, with `wranglerJson` answering `none` | clauses 1 and 2 of `Deployment.check`; it is also the row that exposes the ingest quotient, since a wrangler file has no description field |
| `E4-SURFACE-CE-068` | SEEDED | A site is a route table, so it needs no description | `Effect4Test/Counterexamples/Surface/Site.lean`, `undescribedSite` | clauses 1 and 2 of `Site.check` |
| `E4-SURFACE-CE-069` | SEEDED | Ingest carries the source's descriptions where the source has them, and that is enough | `Effect4Test/Counterexamples/Surface/Ingest.lean`, `undescribedOperation`, the undescribed MCP tool, and `decodedWorker` (which decodes and is then refused by `Deployment.check`) | `descriptionMissing` from `ofOpenApi` and `ofMcpToolsList`; for `ofWrangler` the wire form has no description field at all, so the refusal lands on the decoded row and the quotient is stated |
| `E4-SURFACE-CE-070` | SEEDED | An emitter given a shape it does not support emits what it can | `Effect4Test/Counterexamples/Surface/Emit.lean`, `streamingEndpoint` and `multipartEndpoint`: well formed, and every emitter answers `none` | emitters answer `none` and `Rule.refuses` names the shape; skipping an endpoint would ship a module that typechecks and is missing a route, and a `Decl.raw` escape would reopen `E4-TARGET-CE-003` |
| `E4-SURFACE-CE-071` | SEEDED | A capability's proof fields can be discharged by whatever tactic the field needs | `Effect4Test/Counterexamples/Surface/Derive.lean`, the `badKey` compile-negative recorded verbatim, `identifiedByHand` as the hand-written control, and the refusal pinned as data | `by decide` is the only admitted auto-param tactic (plan §14.6 excludes instance-resolved facts, a proof cache and searching tactics); a user who cannot decide writes the term by hand and the API is unchanged |
| `E4-SURFACE-CE-072` | SEEDED | A capability may carry the data a derivation finds convenient | `Effect4Test/Counterexamples/Surface/Derive.lean`, the `noKey` compile-negative, and the derived path read out of the derivation rather than the capability | every capability field is the carrier value or a proof; a stored route would be a second canonical spelling next to `Path` |
| `E4-SURFACE-CE-073` | SEEDED | The derived endpoint is well formed, and a `#guard` on the fixture shows it | `Effect4Test/Counterexamples/Surface/Derive.lean`, `identifiedAddress` (a two-field key) beside `identifiedUser` (a one-field key): a builder emitting a fixed `{ id }` params schema passes on one and fails on the other | `getEndpoint_wf` and `getEndpoint_paramsMatchPath` are proved by unfolding the builder, and the batteries use them rather than `decide` |
| `E4-SURFACE-CE-074` | SEEDED | The semantic layer is checked on what a derivation produces | `Effect4Test/Counterexamples/Surface/Derive.lean`, the `undescribed` compile-negative and the derived endpoint's non-empty bag | `Entity.Described` is a field of `Identified`, so the semantic layer is a precondition of the derivation and `getEndpoint` writes the bag from the entity's own |
| `E4-SURFACE-CE-075` | SEEDED | A derivation may land with a `#guard` while its theorem is owed | `Effect4Test/Counterexamples/Surface/Derive.lean`, every derivation named by the contract paired with its theorem in the same file | a derivation with no theorem is an owed row in `test/contracts/surface-derive.contract.md`, not a landed function: the universal claim is the derivation's only value over a hand-written endpoint |
| `E4-SURFACE-CE-088` | SEEDED | `Handler.fits e h := typeOf h.sig h.body = some e.effTy` (plan §13.2), together with the same section's typed stub `Effect.fail(new NotImplemented())` "under the same declared type, so the module typechecks either way" | `Effect4Test/Counterexamples/Surface/Handler.lean`, `stubRefusedByEquality`: the stub types as `⟨.never, NotImplemented, ∅⟩` and `getUser` demands `⟨User, NotFound, {Db}⟩`, so `fitsExactly` is `false`; kernel-checked at the landed `Effect4.Syntax.typeOf` before the module exists | `Handler.fits` is subsumption on the three covariant parameters of `Effect<out A, out E, out R>` (`Effect.ts:117`, `:154-157`; `HttpApiEndpoint.ts:584-586`); `Handler.fitsExactly` keeps the equality and `Handler.fitsExactly_fits` relates them |
| `E4-SURFACE-CE-089` | SEEDED | A handler whose program does not have the endpoint's `Effect<A, E, R>` is refused, so an infallible implementation of a fallible endpoint is refused | `Effect4Test/Counterexamples/Surface/Handler.lean`, `infallibleHandler`: error `never` against the endpoint's `NotFound`, `fitsExactly` false and `fits` true | the error channel is a containment: `E` at `HttpApiEndpoint.ts:586` is a free parameter, and the client's error type comes from the endpoint's rows, never from a handler's |
| `E4-SURFACE-CE-090` | SEEDED | `requires` is the service keys of the named services, compared against the handler's whole `typeOf` requirement | `Effect4Test/Counterexamples/Surface/Handler.lean`, `scopedHandler`: `acquireRelease` and a scoped row put `sig.scopeKey` in the requirement, which `Endpoint.requires` never names | `Alphabet.provided` is rc.112's `HttpRouter.Provided` (`HttpRouter.ts:853-857`), subtracted by `Handler.requires` before clause 11; rc.112 removes it at `HttpApiEndpoint.ts:720-724` and again at `HttpApiBuilder.ts:140` |
| `E4-SURFACE-CE-091` | SEEDED | A handler must require exactly what the endpoint declares | `Effect4Test/Counterexamples/Surface/Handler.lean`, `pureHandler`: requires nothing where the endpoint declares `[Db]`, admitted by rc.112 because `HandlerRequirements` (`HttpApiBuilder.ts:169-179`) only adds | clause 11 is one-sided `Row.Subset`; an endpoint service no handler uses is a deployment fact caught by `requirementUnprovided` |
| `E4-SURFACE-CE-092` | SEEDED | `errorTy` is the join of the error entities' handles, with no law on `Ty.join`, while `fits` compares the fold by `DecidableEq` | `Effect4Test/Counterexamples/Surface/Handler.lean`, `errorsForward` and `errorsReversed`; the instance receipts in `Effect4Test/Surface/HandlerContract.lean` are green today against the landed `Effect4/Syntax/Eff.lean`, which proves exactly one theorem and none about `join` | the seven `Ty.join` and `Ty.sub` laws of `test/contracts/surface-handler.contract.md` are owed rows against the `Syntax` lane; until they land the fold's order-independence is evidence at instances, named as such |
| `E4-SURFACE-CE-093` | SEEDED | A sorted join is enough for order-independence | `Effect4Test/Counterexamples/Surface/Handler.lean`, `duplicatedErrorEntity`: one entity at `404` and at `410` must give one union member, since `Ty.union NotFound NotFound` is a different value from `Ty.handle "NotFound"` | `Ty.join` deduplicates by structural equality in `Ty.insertMember` before ordering, and `Ty.join_idem` is one of the owed laws |
| `E4-SURFACE-CE-094` | SEEDED | "one success: the entity handle `Ty.handle name`", for a success body that is any `Sch refs .json` | `Effect4Test/Counterexamples/Surface/Handler.lean`, `inlineSuccessBody` and `inlineSuccessApi`: an inline struct body has no entity, and `Ty` has no node for a struct, so an invented handle or a widening to `.string` would make two different bodies one type and stop `fits` discriminating | `ResponseBody.ty?` is partial, `Endpoint.answerTy?`/`errorTy?` propagate the `none`, and clauses 7 and 8 refuse by name; the endpoint stays well formed and simply has no handler type |
| `E4-SURFACE-CE-095` | SEEDED | `answerTy` for zero successes | `Effect4Test/Counterexamples/Surface/Handler.lean`, `noSuccess`: `Endpoint.success` is a list (plan §13.1), the empty join is `.never`, and only the typed stub would fit such an endpoint | the clause is `Endpoint.check` clause 10 `successEmpty` (already in the alphabet), not a second emptiness decision in `answerTy?`, which stays total (plan §13.6 rule 2) |
| `E4-SURFACE-CE-096` | SEEDED | "several successes: their `Ty.join`" | `Effect4Test/Counterexamples/Surface/Handler.lean`, `twoSuccesses`, `branchingBody` and `unionHandler`: `EffTy.joinAnswer` (`Effect4/Syntax/Typing.lean:38-42`) is `none` on two distinct handles, so `branch`, `catchCause`, `matchCause`, `choose`, `raceAll` and a two-`return` `gen` all refuse; only a row that already answers the union inhabits the type | ruling needed: either one success per endpoint in v1 (plan §12 open question 2), or `answerTy?` for several successes is not their `Ty.join` and `fits` is per status; the packet freezes §13.2 as written and pins the consequence so either ruling is a visible change |
| `E4-SURFACE-CE-097` | SEEDED | "the service alphabet of a handler is the `effect_signature` families the endpoint requires", with `fits` as the evidence | `Effect4Test/Counterexamples/Surface/Handler.lean`, `lyingHandler`: a `Row` carries a `spelling` beside its types with no relation between them, so a row spelled `totallyUnrelated.thing` types identically to `db.getUser` and `fits` cannot tell them apart | clauses 5 and 6 tie the alphabet's service *names* to `e.requires`, which is as far as the rows reach; `fits` is a typing claim and the contract says so in words rather than in a clause |
| `E4-SURFACE-CE-098` | SEEDED | `structure Handler (refs) (Op : Type) where sig : Signature Op` (plan §13.2) is a surface carrier | `Effect4Test/Counterexamples/Surface/Handler.lean`, the two `BEq` receipts, and the ascribed slot definitions in `Effect4Test/Surface/HandlerContract.lean`: `Signature` has two Lean function fields (`rowOf`, `atomOf`), so the structure has no `DecidableEq`, no `Json` view, no store address and no `#guard`, and the `Op : Type` index cannot be a registry row | `Alphabet` is first-order (`List ServiceOp`, `List AtomSig`, one `ServiceKey`), `Op` is `Nat`, and `Alphabet.signature` derives the `Signature` at the call to `typeOf`; nothing stores a function (`AGENTS.md` "Representation rules", plan §4) |
| `E4-SURFACE-CE-099` | SEEDED | `requires` is a list of names with no distinctness clause, and the alphabet is indexed by service name | `Effect4Test/Counterexamples/Surface/Handler.lean`, `duplicateOpName`, beside the green receipt that `Requirement.ofList [k, k] = Requirement.single k` | the requirement row is safe because `Requirement` is `Effect4.Data.Row ServiceKey`; the damage is two alphabet ops of one service and one name, which the printer spells identically, so clause 3 `alphabetOpDuplicate` is the clause and the `requires` duplicate deliberately has none |
| `E4-SURFACE-CE-100` | SEEDED | `Handler.endpoint : String` with nothing checking it names anything | `Effect4Test/Counterexamples/Surface/Handler.lean`, `wrongGroup` and `wrongEndpoint`: rc.112 makes both type errors (`handle<Identifier extends keyof EndpointsByIdentifier>`, `HttpApiBuilder.ts:283-296`; `groupIdentifier`, `:126-131`), and `fits` never looks at the api | `Handler.check` takes the whole `Api` and clauses 1 and 2 are lookups, before any typing clause runs |
| `E4-SURFACE-CE-101` | SEEDED | A group's handlers are a list, so one endpoint may be handled twice | `Effect4Test/Counterexamples/Surface/Handler.lean`, `doubledHandlers`: rc.112 intersects the identifier with `never` through `NotHandledIdentifier` (`HttpApiBuilder.ts:219-221`, applied `:286`, `:320`), and at run time the `Map<string, HandlerRuntime>` (`:277`) would let the second silently win | `Handlers.check` clause 2, `handlerDuplicate group endpoint` |
| `E4-SURFACE-CE-102` | SEEDED | A group with no handlers still typechecks because of the stub | `Effect4Test/Counterexamples/Surface/Handler.lean`, `emptyHandlers` and `partialHandlers`: rc.112 replaces the return type with the string literal `` `Endpoint not handled: ${Missing & string}` `` (`HttpApiBuilder.ts:246-252`), a message rather than an error with a location | `Handlers.check` clause 3 refuses by name and `Handlers.complete` fills the gaps with `Endpoint.stub`; completion is a fill, not a replacement |
| `E4-SURFACE-CE-103` | SEEDED | `body : Eff Op` with `Op` the alphabet's positions, once `Op` is `Nat` | `Effect4Test/Counterexamples/Surface/Handler.lean`, `outOfAlphabet`: a `perform` at index 7 of a three-op alphabet would be typed by whatever default row `rowOf` returns, and the handler would fit while naming an operation that does not exist | `Handler.check` clause 4 walks the body and refuses the first out-of-range index by number, before any typing clause, so the default row is never load-bearing |
| `E4-SURFACE-CE-104` | SEEDED | "the rows of the services the endpoint `requires`, from their `ServiceRow`s", with the two never compared | `Effect4Test/Counterexamples/Surface/Handler.lean`, `extraService` and `missingService`: an alphabet for a service the endpoint does not require imports a class no deployment need bind; a required service with no row makes the handler unwritable and the omission invisible | clauses 5 and 6, set equality in both directions, in that order, before any typing clause |
| `E4-SURFACE-CE-105` | SEEDED | "emission is `Syntax.Print.print` of the body into the `handle` slot", with `fits` as the precondition | `Effect4Test/Counterexamples/Surface/Handler.lean`, `unprintableBody`: `choose` and the five internal fiber actions are well typed and refused by `Effect4/Syntax/Print.lean`, so a handler fits perfectly and emits nothing | `Handler.printBody` returns the printer's own `PrintRefusal` and `handlerBodyUnprintable` is in the alphabet for the emitter; the check does not refuse an unprintable handler, matching the `multipart`/`stream` stance of `surface-emit.contract.md` |
| `E4-SURFACE-CE-106` | SEEDED | The endpoint's answer type is the handler's answer type | `Effect4Test/Counterexamples/Surface/Handler.lean`, the `Ty.sub` receipts: rc.112's answer is `SuccessType<~Success> \| HttpServerResponse` (`HttpApiEndpoint.ts:586`) and `handleRaw` (`HttpApiBuilder.ts:317-325`) is a second way in, while `Ty` has no node for a response | recorded non-claim, not a clause: every handler this packet admits rc.112 admits, and some rc.112 admits are unrepresentable here; a clause would assert the surface had looked at something it cannot spell |
| `E4-SURFACE-CE-107` | SEEDED | `Handler.fits : … → Bool` is the packet's observation | `Effect4Test/Counterexamples/Surface/Handler.lean`, `wrongAnswer`, `illTyped`, `undeclaredError` and the inline-success api: one `false` collapses four distinct failures, and `surface-facts.contract.md` says a Boolean receipt passes for a carrier that refused the right term for the wrong reason | `Handler.check : Except Refusal Unit` is the observation every negative receipt pins, with four distinct clause names; `fits` is a Bool projection used only for positive facts |
| `E4-SURFACE-CE-108` | SEEDED | Plan §13.2 places `Endpoint.effTy : Endpoint refs → EffTy` in `Effect4/Surface/Handler.lean`, while plan §2 forbids `Effect4.Surface.*` from importing `Effect4.Syntax.*` or `Effect4.Deep.*` | `Effect4Test/Counterexamples/Surface/Handler.lean`, `endpointTypeIsEffTy`: `EffTy` is `Effect4.Syntax` and its `requires` field is `Effect4.Deep.Env.Requirement`, so §13.2 crosses the fence twice; reported by the wave-2a builder as the reason `EndpointTy` was not built | coordinator ruling owed (finding H-1). The whole packet is stated in namespace `Effect4.Surface`, which every candidate resolution can honour, so a ruling changes the batteries' owed `import` line and nothing else; the one fact true either way is that the type is the landed `EffTy` and no second copy is admissible (plan §13.6 rule 2) |
| `E4-SURFACE-CE-109` | SEEDED | "their `ServiceRow`s already carry the TypeScript spellings", read as a source of typing rows | `Effect4Test/Counterexamples/Surface/Handler.lean`, the `Ty.render` receipt: `Effect4.Target.EffectV4.OpRow` stores `answer`/`tsAnswer` as rendered strings while `Effect4.Syntax.Row` needs `Ty` and `ServiceKey`s, and `Ty.render` is not injective (`Ty.nat` and `Ty.int` both render `"number"`) | `ServiceOp` carries a `Syntax.Row` written beside the `ServiceRow`, and the agreement of the two spellings is an owed row, not a theorem this packet can state |
| `E4-SURFACE-CE-110` | SEEDED | "`requires` as the service keys of the named services", from `Endpoint.requires : List String` alone | `Effect4Test/Counterexamples/Surface/Handler.lean`, the `Alphabet.requirementOf` receipts: a `ServiceKey` is two `Nat`s and `Effect4/Context/Key.lean` states outright that codes are never minted from names, so no name-to-key function exists in the estate | `Endpoint.effTy?` takes the `Alphabet`, whose rows already carry their `ServiceKey`s, and `Alphabet.requirementOf` is the only bridge; the refusal spells the offending key as two `Nat`s (`handlerRequirementUnprovided endpoint name code`) |

Area-specific attack shapes are in
[`algebra/ATTACKS.md`](algebra/ATTACKS.md),
[`flow/ATTACKS.md`](flow/ATTACKS.md),
[`schema/ATTACKS.md`](schema/ATTACKS.md),
[`environment/ATTACKS.md`](environment/ATTACKS.md),
[`semantics/ATTACKS.md`](semantics/ATTACKS.md),
[`runtime/ATTACKS.md`](runtime/ATTACKS.md),
[`target/ATTACKS.md`](target/ATTACKS.md), and
[`data/ATTACKS.md`](data/ATTACKS.md). Fired implementation attacks
add a `BROKE / LAW / WITNESS / CLASS / FIXED-BY` record to the owning contract
packet without deleting the stable row.

## History

Rows `E4-CONC-CE-001`–`026` were retired on 2026-09-04 with the scheduler and supervision calculi; the behaviours they guarded are witnessed on the reference machine by `Effect4/Deep/Clauses.lean` and `Effect4/Deep/Witnesses.lean` through `Effect4Test/Audit/RuntimeCoverage.lean` (`docs/research/2026-09-04-retire-old-machines.md`).
