# Rows 42 and 43: polymorphic `Ref`/`Deferred` and function-taking rows (plan, 2026-09-18)

Ruled by the owner 2026-09-18: do both; the language stays a first-order stored program, the
power stays in the builders, the rows and the types widen where Effect's semantics are wanted;
land it with the meta infrastructure. Design authority for the cut: `docs/core/language-cut.md`.

## 1. The design

- **`Ty.refOf (value : Ty)` and `Ty.deferredOf (value error : Ty)`**, dedicated constructors
  as `fiberOf` is. Tried first: `Ty.app (target) (args : List Ty)`; the compiler refused it at
  once, since a `List Ty` argument makes `Ty` a nested inductive, which loses the derived
  decidable equality and every `induction` on `Ty` (three in `Ty.lean` alone). The tree's idiom
  for parametrised handles is one constructor per handle sort, and row 3's `Ty.app` waits for
  the foreign-schema need it was proposed for. `handle t` stays for the argument-less handles.
  `Val.hasTy` at both is what it is at `handle`: coarse by kind (row 44; the world tables type
  the contents). `normalize` normalises the arguments; `sub` is covariant in them pointwise, as
  `fiberOf` is (sound because the checker never writes through a widened handle without
  re-matching the template); `key` gets one code each.
- **`Ty.var (index : Nat)`**: a row template's parameter, never a program's type. `normalize` is
  the identity, `hasTy` is `false`, `render` prints `A`/`E` by index for row declarations.
- **Rows as templates**: `Row.params : Nat := 0`; the thirteen `Ref` rows and the six `Deferred`
  rows use `var 0`/`var 1`. `Ty.instantiate (args : List Ty) : Ty → Ty` substitutes;
  `Ty.matchTemplate (template request : Ty) : Option (List Ty)` binds each variable at its first
  occurrence and checks the rest by `sub`, so the checker's `perform` rule becomes: match the
  request against `row.request`, instantiate `answer` and `error`. A row whose variables the
  request does not fix carries them on the operation: `NativeOp.deferredMake (value error : Ty)`,
  as `Deferred.make<number, number>()` carries them in rc.112 (`E4-CHECK-CE-013`).
- **Function-taking rows by binder terms** (row 43): `NativeOp.refUpdate (f : Term)` and its
  six siblings carry a term evaluated at `env ++ [current]`, typed by the checker at
  `tys ++ [A]` for the request's `Ref<A>`, exactly as `iterate`'s `step` is. `FnName` retires
  from the rows. The store op carries the function as data: `SyncOp.refUpdate (cell) (f : Term)
  (env : List Val)`, and `refStep` evaluates it, so `update` stays one atomic store step (a
  `get`; compute; `set` sequence could be interleaved by a yield). `Term`, `Lit`, `NativeAtom`
  and `evalTerm` therefore move below `Machine` (`Effect4/Store/Term.lean`, namespace unchanged);
  they are first-order data over `Val` and belong with it.
- **Faces**: the printer prints `refOf`/`deferredOf` as `Ref.Ref<A>`/`Deferred.Deferred<A, E>` and a binder term as `(x) => …`; the
  reader reads both (a lambda skeleton in the template table); `typeArgs` on rows is derived
  from the instantiated arguments, not spelled.

## 2. The order, each step built narrowly and committed

1. `Ty.refOf`, `Ty.deferredOf`, `Ty.var`: the compiler lists every non-exhaustive match; the sixteen folded
   traversals (`Program/Folds/Ty.lean`) get their arms and `fold_of` regenerates each connector;
   the `Fold` group regenerates `TyAlgebra` (`make gen-derived`); two wire tags appended
   (`tools/Effect4Gen/wire-tags.json`, stable and append-only); `Ty`'s laws (`key` injective,
   `sub` reflexive and transitive, the join) get the two arms.
2. `Row.params`, `Ty.instantiate`, `Ty.matchTemplate` with their laws (`instantiate` respects
   `sub`; a match is sound: `sub request (instantiate args template)`); the checker's `perform`
   rule; `inv_perform` restated; `CheckSound`; `Progress.answer_typed` per instantiation.
3. `Term` below `Machine`; `SyncOp` ref rows carry `Term × env`; `refStep` evaluates;
   `NativeOp` rows for `Ref`/`Deferred` rewritten (`deferredMake` with its arguments,
   `refUpdate` and siblings with a term); `FnName` retired with its service entries.
4. The straight soundness (`MeaningSound.sound`, `LoopSound.soundB`): the heap column
   `HeapNat` becomes the per-cell table `Ρ` of the typed world, threaded through `TypedAt`,
   with `HandleFit` for cells; the `perform` arms restated once against the template rule.
   This is layer 1's world entering the straight proof, which is why the rows come before S1.
5. Faces: printer, reader, TS profile (`make gen-ts`), OCaml `eff_*` (`make gen-eff`), the
   corpus goldens (`.ty` verdicts change from `handle "Ref.Ref<number>"` to `refOf nat`),
   `check-gen`, `check-host`.

## 2a. Step 1 landed (2026-09-18, the same night)

`Ty.refOf`, `Ty.deferredOf`, `Ty.var` and `Ty.closed`; wire tags 16–18; arms in `renderRaw`,
`members`, `key`, `isNever`, `isMember`, `sub`, `normalize`, `Normal`, `findInt`,
`rawSupportedErrTy`, `ofNormalized`, `Val.hasTy`, the schema bridge both ways, the OCaml
alphabet emitter, the profile writer, the goldens and the metadata; `key_injective`,
`normal_normalize`, `hasTy_sub`, `hasTy_normalize`, `hasTy_mono`, `normal_children`,
`sub_antisymm_normal` and the admission proofs extended, with every catch-all alternative
rewritten as a wildcard (the owner's rule: a new constructor must never touch a proof).
`ofSchema_schema` gains `t.closed = true`: a template parameter has no schema. The `Fold` and
`Program` groups regenerated; the hermetic groups regenerated (fourteen generated files, the
alphabet only). Semantics unchanged: no row uses the constructors yet; the corpus's typing
verdicts are the same. The whole tree builds (`Effect4`, `Effect4Laws`, `Test`, `OCaml5`,
`Tools`). Three findings for the meta estate, all applied: the trust gate refuses `partial`,
`unsafe`, `implemented_by` and `initialize` throughout `src`, so the census walks are fuel-bounded
and the source table is read as an expression (`Laws/Auto/TypedSources.lean`), not run; the
module-closure gate refuses a test module outside `Test.All`, so the skeleton's emit driver lives
in `scripts/lean/`; a `List Ty` argument would make `Ty` a nested inductive, hence dedicated
constructors.

## 2b. Step 2 landed (2026-09-18), and the probe of steps 3–5

The template calculus and the checker's rule, with no row a template yet, so the corpus's
verdicts are unchanged: `Ty.Subst`, `Ty.instantiate`, `Ty.infer`, `Ty.matchTemplate`
(`Ty.lean`, after `sub`); `rowTy` (`Typing/Rules.lean`), which the checker's `perform` arm and
`HasTy.perform` both name, so `inv_perform` (both levels), `CheckSound` and `MeaningSound`
restate against one helper; `Laws/Program/Template.lean` holds the laws, twelve of fourteen by
one `aesop` call each (`infer_closed` is a 400-case split): `closed_members`/`factors`/
`ofMembers`/`normalize`, `instantiate_closed`, `infer_closed`, `matchTemplate_sound`,
`matchTemplate_closed`, `rowTy_closed`, `rowTy_closed_some`, `NativeOp.row_closed`,
`nativeSignature_row_closed`. Three corrections to §1, made by the code:

1. **No `Row.params`.** A row is a function of the operation's data, so `deferredMake (value
   error : Ty)` instantiates its own row as `scopeMake strategy` and `refUpdate f` already do;
   the only variables left are the ones the request fixes. `Row.typeArgs` becomes derived at
   step 5.
2. **Match = infer, then guard.** The law is the guard (`sub request (instantiate σ template)`),
   so inference can only lose completeness, never soundness. A parameter the request leaves
   unbound is `never` — what TypeScript infers for a parameter the arguments do not constrain,
   which happens only on the dead branch a `never` request marks — so there is no new refusal.
   `matchTemplate` takes a seed: step 3's function-taking rows extend the same bindings with
   the term's type (`modify : A → [B, A]` binds `B` from the term), and `rowTy`'s consumers
   do not move.
3. **Variance (decisions row 55).** rc.112 declares `Ref<in out A>` and `Deferred<in out A,
   in out E>`; step 1's `sub` was covariant at both, which step 4's per-cell table would have
   found unsound (widen a `Ref<number>` to `Ref<number | string>` through an `iterate` cursor
   annotation, write a string, read it back at `number` through the first alias). `sub` is
   invariant there now; `fiberOf` stays covariant, as `Fiber<out A, out E>`. The three
   `TypeAlgebra` proofs that enumerate handle arms (`sub_trans_core`, `sub_antisymm_normal`,
   `sub_normalize_of_sub`) each gained the invariant alternatives — the same chore three
   times, so a `sub` induction principle that hides the arm shapes is owed to the bank.

What the probe found for the remaining steps:

- **Step 3, layering.** `refStep` and `syncOpStep` live in `Machine/Stores.lean`, and the four
  cause-query atoms of `NativeAtom.eval` read the cause image (`Val.cause?`, `Stores.lean:430`),
  whose alphabet (`Err`, `Defect`, `Ann`, `causeImage`, `ExitV`, `exitImage`; `Stores.lean:55-562`)
  is in the same module. For a term to be evaluated inside a store step that block moves below
  the stores (`Machine/Alphabets.lean`); then `Term`, `Lit`, `Terms`, `scoped`, `Lit.toVal`,
  `NativeAtom` (the inductive, names, arity, `eval`) and `evalTerm` sit in `Machine/Term.lean`
  in namespace `Effect4.Program` (the name the wire tags, the generator's `Effect4.Program.Term`
  checks and every consumer use), while `NativeAtom.typeOf`/`mono`/`constGeneric` stay in
  `Program/NativeAtom.lean` (they need `Ty`). `refStep` is not parameterised by an
  interpretation: the machine state stays first-order and the step total.
- **Step 3, atoms.** The five `FnName`s as binder terms need `ite : bool → A → A → A` (typed at
  the join), `some : A → option A`, `none : option never` (nullary) and `mul`;
  `zeroWhenPositive = ite (lt 0 a) (some 0) none`, `noChange = none`, `takeAndBump = pair a
  (succ a)`, `double = mul a 2`. Each is one `NativeAtom` row (name, arity, `eval`, `typeOf`)
  and one prelude entry; the language cut §1 gains them.
- **Step 3, rows.** `SyncOp.refUpdate cell (f : Term) (env : List Val)` and six siblings;
  `refStep` evaluates `evalTerm (env ++ [a]) f`, decodes `some`/`none` for the partial rows and
  `list [b, a']` for `modify`; a term that does not evaluate, or decodes to the wrong shape, is
  the step's `none` (a frontier, as a dangling key is). `NativeOp.syncOpOf` takes the
  environment. `Row.fn : Option (Ty × Ty)` (parameter and result templates) and
  `Signature.termOf : Op → Option Term` let `rowTy` type the term at `env ++ [instantiate σ
  param]` and extend σ from its type by the same `matchTemplate`.
- **Step 3, faces.** `Val.hasTy`'s `.handle` arm for `.cell`/`.promise` retires with
  `refTarget`/`deferredTarget` (a cell is only a `refOf`); `LambdaShape`/`atom?`
  (`Codegen/Styles.lean`, `Forms.lean`) become the printing of a binder term `(a) => body` and
  its reading; `nativeServiceTypes` code 7 becomes `refOf nat`; `Test/Program/Gen.lean` and
  `Test/Machine/Fuzz.lean` generate terms instead of names; `Progress.lean`'s `FnName.*_hasTy_nat`
  become the term typing already proved (`evalTerm_hasTy`).
- **Step 4.** `Val.hasTy` is a fold with one coarse handle arm; the per-cell table wants the
  same fold with the arm reading the world. One definition `hasTyWith (oracle : HandleKind →
  Nat → Ty → Bool)`, `hasTy := hasTyWith coarse`, `hasTyIn W := hasTyWith W.oracle`, monotone
  in the oracle, so the existing laws (`hasTy_sub`, `hasTy_normalize`, `hasTy_mono`) generalise
  once and the current ones are instances. `TypedAt` carries `Ρ : List Ty` (allocation order,
  prefix-monotone) beside the stores.
- **Step 5.** `Row.typeArgs` (spelled strings through `parseLegacy`) becomes derived from the
  instantiated answer through `ofNormalized` (`Codegen/Types.lean`); the TS profile and reader
  (`ts/eff/profile.gen.ts`, `read.ts`) must accept `Ref.Ref<A>` generics; there is no
  independent TS checker to mirror (`ts/eff/check.ts` runs the corpus through the profile).

## 2c. The language push (owner, 2026-09-18: "land all the language improvements we possibly can in this push")

Every improvement the cut (`docs/core/language-cut.md`) and the decisions name, in the order
their dependencies allow, each a narrow build and a commit. Rows 42/43's steps 3–5 are inside
it; step 4 (the per-cell world) comes before the rows become templates, because the rows'
answer types can only be stated per instantiation against a world that types each cell.

| # | slice | what it is | needs |
| --- | --- | --- | --- |
| L1 (landed 2026-09-18) | the alphabet, once, below the stores | delete the dead `Env` copy of `Err`/`Defect`/`Ann`/`causeImage`/`ExitV` (`Machine/Context.lean:282-398`, no consumer, the S5 spike's residue — the cut's §3 read *this* copy and so says `Err := boom \| tag`, which is stale: the live alphabet has `tagged` and `text`); move the live block (`Stores.lean:55-102`, `:235-336`, `:428-435`, `:554-562`) to `Machine/Alphabets.lean`; then `Lit`, `Term`, `Terms`, `scoped`, `Lit.toVal`, `Val.tuple`, `NativeAtom` (inductive, names, arity, `eval`) and `evalTerm` to `Machine/Term.lean` in namespace `Effect4.Program`; the cause queries (`ErrorQueries.lean`, retired) to `Term`; `NativeAtom.lean` keeps the typing half and `causeInputError?`; `Native.lean` imports `Typing` directly (it had it through the split modules) | nothing |
| L2 | the world types each cell | `Val.hasTyWith (oracle)` with `hasTy := hasTyWith coarse`; `Ρ : List Ty` in `TypedAt` (prefix order); `HeapNat` becomes `HeapFits Ρ`; `MeaningSound`/`LoopSound`/`Progress` restated with the rows still closed (so `Ρ` is `nat` at every cell and the proofs change shape, not content) | L1 |
| L3 | the atoms | `ite`, `some`, `none`, `mul`, `sub`, `div`, `mod`, `le`, `nil`, `cons`, `head`, `tail`, `length`, `append`, `get`, `isEmpty`, `concat`: each a `NativeAtom` row (`name`, `arity`, `constGeneric`, `mono`, `eval`, `typeOf`), its typing-soundness case (`evalTerm_hasTy`), its prelude entry on the TS side, the profile regenerated | L1 |
| L4 | rows as templates; function-taking rows | the `Ref`/`Deferred` rows over `var 0`/`var 1`; `deferredMake (value error : Ty)`; `SyncOp.refUpdate cell (f : Term) (env)` and six siblings with `refStep` evaluating; `Row.fn`, `Signature.termOf`, `rowTy` typing the term; `FnName` deleted with `fnNames`, `LambdaShape`, `atom?`, the `.handle` cell/promise arms of `hasTy`, `refTarget`/`deferredTarget`; `nativeServiceTypes` 7 at `refOf nat`; `Progress.step_typed` per instantiation; the generators (`Test/Program/Gen.lean`, `Fuzz.lean`) over terms | L2, L3 |
| L5 (landed 2026-09-18, before L2–L4: it needs none of them) | a top type; the release as rc.112 | `Ty.unknown` (rows 46, 47): `hasTy v unknown = true`, `sub t unknown = true`, canonical (absorbs in a union), key 19, schema `unknown`, printed and read; the checker's `.handle "unknown"` placeholder retired; the release's parameter at `exitOf unknown unknown` and its error column `never` | L4 |
| L6 | the faces | `Row.typeArgs` derived from the instantiated answer; printer `Ref.Ref<A>`/`(a) => body`; reader; TS profile and reader on generics; OCaml `eff_*`; corpus `.ty` goldens; `check-gen`, `check-host` | L4, L5 |
| L7 | user service carriers | the six type codes (`nativeServiceTypes`) become a service table beside the program, as the row table is (`Signature.serviceTy` already abstracts it); the printer spells the tag's type from the table | L6 |
| — | records and variants (row 2) | needs a ruling: a `List (String × Ty)` field makes `Ty` nested (the `Ty.app` trap of step 1); the idiom is a mutual `Fields` list, which restructures every `Ty` induction (`sub_trans_core`, `hasTy_sub`, `normal_normalize`, …). Recommended after this push, as its own plan | owner |
| — | `int`/`float` values | a carrier change (`Store.Val`, the bytes boundary, the OCaml `Val`), not a language change; refuse by name in the profile until the boundary work | owner |
| — | `Err.value` | refused by the basis (`DESIGN-BASIS.md:640`); the live alphabet already carries `tagged`/`text` payloads; records (row 2) are the real gap | — |

## 2d. The research seats' findings against the ledger (2026-09-18; the owner's hold)

Four read-only Opus seats wrote `docs/research/2026-09-18-research-{type-algebra,atoms-terms,
lcnf-reification,proof-engineering}.md`. The owner ruled: hold at a stopping place (L5 committed)
and re-plan against them before L2–L4, L6, L7. What they change, item by item:

**Immediate, small, safe (do first after the hold):**
- `Schema/Codec.lean:45-49` `Codec.isSupported` ends in `| _ => true`, so `refOf`, `deferredOf`,
  `var` and `unknown` are classified supported while `encodeRaw` refuses them (types seat §7.3).
  Nothing consumes it; still a false claim. Rule for the whole tree: **a wildcard is right in a
  proof and wrong in a classifier** — classifiers list their positive arms and close with an
  explicit `false`. The traversal census names every `Ty` reader to audit.
- `Ty.matchTemplate`'s guard compares a canonical request with a non-normal instantiated
  template; normalise it (`sub r (instantiate σ t).normalize`), strictly more complete, sound by
  `sub_normalize_of_sub` (types §3.6). Add `templateAdmissible` (no variable under a union) and
  `Row.wellScoped` (every answer/error variable bound by the request or the operation) by
  `decide` beside `NativeOp.row_closed` (types §3.5); the `never`-for-unbound choice then becomes
  provably unreachable. Correction to §2b(2): TypeScript infers `unknown`, not `never`, for an
  unconstrained parameter; `never` stays right because it only makes the guard harder.
- `ocaml/gen/NOTES.md:255` and `ocaml/engine/externs.txt:157-162` say `Nat.div`/`mod` by zero is
  unguarded; `Translate.builtin?` guards both (atoms §4). `Nat.mul` lowers to unclamped OCaml `*`
  (`OCaml5/Lcnf/Translate.lean:161`) — clamp it with the `mul` atom (atoms §5).
- Root sets of the LCNF outputs live inside the generated files' own headers and
  `scripts/generate.py:110-114` reads them back; retiring `FnName` (L4) would break regeneration.
  Move the root sets to one tracked input before L4 (lcnf §3c). Commit a closure manifest
  (`ocaml/gen/closure-<output>.tsv` from `LcnfGen`'s `translated` list) so a new runtime entrant
  is a diff (lcnf §3f).
- Name the aesop banks: the checker's own definitions are registered in aesop's *default* rule
  set (`Laws/Program/Typing/CheckInversion.lean:39-42`), a cost every one of ~390 `aesop` calls
  pays; `declare_aesop_rule_sets [Effect4.Inversion] (default := true)` and named
  `Effect4.TyOrder/TypedState/Rows/Atoms/Reader/Checker` (proof-engineering §2; exact syntax
  verified against the pinned aesop).

**L2 (the world) — re-cut.** Keep the runtime `Val.hasTy` a concrete match (it is on the
engine's external-answer admission path); define `hasTyWith (oracle)` for the proofs and connect
the two by a lemma, or `@[specialize]` one definition and *check* the closure report for the
specialised twin (lcnf §3e). The oracle must be a plain first-order arrow (a dependent one becomes
`lcnf_unknown`). The `List Ty` ghost column cannot enter the closure while it is `Prop`-only; the
closure manifest is the check that it stays so.

**L3 (atoms) — re-cut.** Reuse the template calculus for atom schemes (`List Ty × Ty` over
`Ty.var`) with one generalisation, `Ty.inferJoin` (join at a repeated parameter; `ite` needs it).
Land the four atoms L4 needs first (`ite`, `some`, `none`, `mul`) in the existing shape, then the
`atomSound` bank, then the `Spec`/`Scheme` table as a refactor of a working thing. Twelve atoms,
not seventeen (`le`/`gt`/`head`/`isEmpty` derivable; `strLength` waits on UTF-16 agreement). Per-
atom soundness is not `decide`-able; the shape assignment is `rfl` and the content goes through
`sound_of_mono`/`sound_of_shape`/`sound_of_poly`. `AtomSpec` may hold no function fields (`eval`
stays a match: the LCNF mono phase). List atoms need `Val.asList?` + `Val.hasTy_list_inv` first:
`hasTy v (list t)` accepts the fiber-snapshot frame, so a naive `length` breaks `evalTerm_isSome`.

**L4 (rows) — re-cut.** Two new obligations: the generated `Eff.scopedAt` `perform` arm ignores
the operation (`Program/Scoped.lean:26`), silently wrong once `NativeOp` carries a `Term` — fix in
`tools/Effect4Gen/Authoring.lean` with `Signature.scopedOp` and a red control; and inference must
be anchored (`Ref.setAndGet`'s template `prod (refOf (var 0)) (var 0)` repeats a variable), so
`collect`/`solve` with the anchored-completeness theorem is due at L4 (types §3.4). Binder terms
close finding F3 (`harness/truth/prelude.ts:24-30`: `Ref.modify` printed the wrong lambda shape).
Capture is impossible by construction (de Bruijn levels).

**Proofs that never see constructors (after the push, with row 2).** A generated relational view
of `Ty` (children with variance) and `AdmitsSub`, so `sub`'s reflexivity, transitivity,
antisymmetry and `hasTy_sub` are proved once (`hasTy_sub` is 256 lines with sixteen copies of one
block today); no hand-written induction principle (the compiler's `Ty.sub.induct_unfolding` is
the artefact); a custom eliminator only for `Ty.Normal`; `#exhaustive_gate` reads catch-alls off
the matcher's own type. Records and variants as a mutual `Fields` spine (not `List`, not
`Ty.app`); `Ty.foreign id args` supersedes row 3.

**The reification's own list (lcnf seat N1–N10; the crown jewel, shored up).** N1 root sets out
of the generated headers into `ocaml/gen/roots.json` — a blocker for L4. N2 re-seed the case-site
policy and sign the diff (done with L5). N3 the frozen engine's `of_ty`: a named failure now, an
arity assertion so the next append fails at build time (the runner plan deletes the file). N4 one
`AtomRow`, `eval` and `ofName?` as matches, beside L3 (the lookup is on the store step's path
after L4). N5 split `NativeOp.kind` off `row` so `Ty` leaves the runtime closure and L4's
templates are provably invisible to the engine. N6 a committed closure manifest per LCNF output.
N7 Lean's run answer in the corpus index, gating the cross-face check. N8 wire the 20,387-vector
differential lane (it has drivers no target calls) and make `Ml.checkModule` fatal; amend
`docs/core/lcnf-route.md:58-59` and `docs/GENERATED.md:48`, which overclaim. N9 make the engine
mirror refuse an append shortage unless allowed, and order `gen-lcnf` before `gen-eff` so one
`make gen` reaches its fixpoint. N10 `make gen-lcnf` in the `check-ocaml` job. Two pipelines, not
one: `OCaml5/Eff/*` is reflection plus partial evaluation of Lean values; `OCaml5/Lcnf/*` is the
LCNF translator, and only it earns the name.

**Rakes met landing L5, now rules.** A `Ty` constructor is appended, never inserted (the engine
structure mirror pins declaration order; `unknown` moved last). `dune` is not on `PATH`: the
engine builds by `cd ocaml && opam exec --switch=effect4 -- dune build` (step 1's engine check was
a false green). `make check-cases` builds `tools/Conform/Effect4/Lcnf{Ml,Semantics}.lean`, which
hand-match on `Ty` outside the `Tools` target, and its policy covers need re-seeding and signing.

## 3. What the meta estate does for it

The compiler's exhaustiveness check is the arm list; `#traversal_census Effect4.Program.Ty`
names every definition that reads `Ty` and says whether it is still a fold after the arms;
`fold_of` regenerates the sixteen connectors at `[propext]` with no hand proof; the generator
regenerates `TyAlgebra`, the lenses and the `Canonical` instances from the manifest; the
position census is unchanged (`Val` does not change) and the totality gate stays green; the
reader/printer template table takes the lambda skeleton as one row.

## 2e. The pause (owner, 2026-09-18 evening): synthesis before L2–L7

The owner stopped implementation at L5 (applied, green through the engine, uncommitted; `make
check-cases` red on the stale policy; DI-95 filed) and asked for a synthesis pass with the
tooling first: the patterns behind the recurring chores, the instruments that retire them, the
research read in full and reasoned through, the sources it points at, and a plan that makes the
build faster rather than adding gates. That note is
`docs/research/2026-09-18-synthesis-tooling-first.md`; a review cycle follows it, and the ledger
of §2c–§2d is re-cut on its tiers (its §5, "The language ledger, re-cut on top"). Nothing in
L2–L7 moves before the review.
