# Clock analysis budget correction

The actual flat and engine producer stages now pass in no-write diagnostics under the unchanged 200000-heartbeat budget. The compiler lease is released; canonical generation remains the coordinator's next step.

The earlier successful closure-only probe was insufficient. Clock analysis ran as pure code and used 566805 heartbeats before the next `whnf` checked the budget. The exact driver-copy diagnostic retained here imports the same environment, uses `Core.toIO`, performs carrier inference, type generation, collision retranslation, emission, manifest generation, module checking, extern-ledger checking and rendering. Its two output writes are replaced by byte counts. It makes no artifact.

Stage measurements isolated 549207 heartbeats in callable resolution. The old pass rescanned all declarations 22 times and used linear membership checks for growing origin lists. Hash sets alone still exceeded the cap. A finite worklist now enqueues each new value/origin fact once, installs dependency edges once and propagates existing facts when an edge is discovered. Both measured implementations reached 76712 facts on the flat API graph; the callable stage fell to 43894 heartbeats. This is measured finite evidence, not a formal equivalence proof.

The engine's carrier inference had separately validated four intermediate translations, using 255305 heartbeats before type generation. Intermediate collection is now private. Both public translation functions validate their final returned closure; unsettled carrier inference still refuses. Two negative controls cover the moved validation boundary.

The engine's extra generic exports also contaminated the concrete interpreter's shared return summaries. The retained trace starts at an opaque comparator in `Supervision.interruptCause._redArg`, then passes through specialized interruption and decision helpers into `replayEval`. Reachability now identifies all public roots with a clock ingress, a clock-typed return, or a higher-order clock result. Their full reachable flow union and opaque inputs are checked together. Independent exports with no such boundary do not seed unrelated opaque arguments into shared helpers. The generic exports remain emitted. Two mixed-root controls retain the unknown clock-reaching export while accepting an irrelevant generic callback export beside a closed clock root.

Validation was serial, `LEAN_NUM_THREADS=1`. Narrow builds: `lake build OCaml5.Lcnf.ClockFlow`, then `OCaml5.Lcnf.Translate`, then `Test.Audit.ClockLowering`; all passed. The latter has 22 finite controls. No proof statements or theorem axioms changed in this correction.

Full diagnostics used `lake env lean -DwarningAsError=true -M4096 --run /tmp/m1-tools/clock-timeout/FullStage.lean` and the exact flat/engine roots, type requests, extern table and prelude in `ocaml/gen/roots.json`. The engine argument vector is retained in `engine-command.json`. Flat: 664 declarations, zero todos, module check passes, 124584/200000 heartbeats after rendering. Engine: 658 declarations, zero todos, module and extern-ledger checks pass, 158907/200000 after rendering. No budget reset, budget increase, producer invocation or generated-file write was performed by this seat.

The checks cover these finite source graphs and the existing target/transcription boundaries. They are not generated OCaml compilation or execution. Those remain the canonical producer and runtime checks. The original callable-origin negative tests remain in place. The failed merged-root full-driver log was overwritten by its final successful run; its separately retained source trace and root-separated probes are the durable evidence for that seam, rather than a claimed retained full failure log.
