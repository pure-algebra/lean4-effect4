The store bank now closes the existing owed-code handle bound. Its pending marker was stale and caused the dependent frame build to fail. Removing that one marker passes a fresh Handles build in 101 seconds. The theorem body and frozen statement are unchanged. The local ledger moves from eight open to seven open, with its ceiling still nine; the origin ledger remains nine open. The full Handles fill and after-census remain pending. The archive retains both source snapshots, the dependency failure, and the successful narrow build. No generator ran.
