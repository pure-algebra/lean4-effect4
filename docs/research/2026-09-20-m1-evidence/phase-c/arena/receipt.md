Phase C Arena slice

The final narrow build passes. Both ledgers report zero open: the ten frozen Arena obligations and three support obligations. The original ten statements remain unchanged. The proofs cover the lawful list instance, list projection for lookup/update/allocation, and the three DeferredStore adapters. Reusable facts are registered in Effect4.Stores; extensionality remains local. New helpers have retained Obligation and wanted-marker snapshots from before proof attempts.

The coordinator reran the audit on the final source after the four searched-proof rewrites. It returned 0 and printed 27 proof dependencies, all within [propext, Quot.sound]. The final 20,000-heartbeat census reports 3/24 with plain aesop and 1/24 with the Stores bank, all reported closures trust-admissible. The before-fill report was 0/10. The population grew, and these figures do not claim a same-population improvement. The unchanged instrument excludes proofs referring to their own or later declarations. The full-radius comparison remains separate work.

The lossless archive contains all three coordinator initial probes and failed logs, all seven delegated attempt sources and build logs, both post-proof census/axiom logs, the helper wanted snapshot, Phase B source, final source, scope-only diff, and original baseline row/log. Initial probes each exited 1; builds 2, 5, 6 and 7 exited 0; both audits exited 0. The interim README and JSON receipt remain byte-for-byte under scratch/arena and predate the final audit; this package records the later result explicitly. Exact commands, confirmed exits, source versions, and per-file hashes are in manifest.json.

Every archive member was decompressed and compared byte-for-byte. The final source matches the snapshot used for build 7. The earlier source matches Phase B commit 3f893074. Frozen-statement comparison, forbidden-tactic checks, and scoped git diff --check passed. Packaging ran no compiler and changed no live source. No staging, commit, or downstream build was performed.

Ready to copy to docs/research/2026-09-20-m1-evidence/phase-c/arena. Root owns the compiler lease and commit.
