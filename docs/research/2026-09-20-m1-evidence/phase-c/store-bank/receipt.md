Phase C store-bank slice

The final narrow build and bank census returned 0. Of the original two theorems, search now closes 1/2 with Effect4.Stores, compared with 0/2 using plain aesop in the corrected baseline. Four new Owed.mapCode helpers expand the current module to six theorems; its final census closes 4/6, all four within the allowed axiom set and reporting only [propext]. The denominator increase is separate from the one additional closure on the original population. The census does not close store_lookup_lt or mapCode_id_fun; this does not negate their separately compiled proofs.

The bank adds cancel, wakeBatch and mapCode normalization, plus code projection, identity, composition and the identity-function equation. All four support statements have retained pre-proof Obligation/#proof_wanted probes. The first identity probe failed because id resolved to an existing obligation name; qualifying _root_.id fixed it. Both versions and their logs remain archived. The complete probe context includes scratch map examples, but this package claims no live Refinement build or completion.

Final ledgers report Core 0 open / 5 proved, CompletionSupport 0 open / 2 proved (ceiling 2→0), and OwedMapWanted 0 open / 4 proved. The existing poll control and its plain-aesop negative control are unchanged and pass the final narrow build. The printed poll proof depends on [propext].

The archive preserves the initial and three-helper builds/censuses, the final build/census, all relevant wanted probes, the failed naming attempt, exact Phase B and final CompletionData sources, the unchanged control, and the slice diff. Earlier package records are retained under chronology/. Commands, confirmed exits and hashes are in manifest.json. All 23 archive members were read back and compared byte-for-byte.

Ready to copy under docs/research/2026-09-20-m1-evidence/phase-c/store-bank. Packaging ran no compiler and changed no live source. Remaining Phase C proofs and the full authorized-radius final-bank census are separate work.
