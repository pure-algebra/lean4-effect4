# Phase B instrument correction — proposed receipt

The finite controls distinguish the old search-budget defect from the corrected behavior, and the empty-module census now passes with warnings treated as errors. Ordinary unreachable proof tactics still fail. These results concern the instrument controls listed below; they are not a universal theorem about search or a replacement for the full trust sweep.

The source snapshot was captured at commit `05417cc614c6e0cefc11fe2f9bda91fe8b9c6fae`, with Phase B changes still in the working tree. This packaging step ran no Lean process and changed no live repository file. `commands-and-results.json` records the exact commands and process statuses confirmed by the coordinator; earlier Search logs retain compiler output but not their exact outer invocation/status, so those fields remain null. No execution timestamps were inferred.

`ProofGraph.search` now installs the requested heartbeat cap in both Lean's option and cached `Core.Context.maxHeartbeats`, using `Core.getMaxHeartbeats` for the unit conversion, then resets the allocation-counter origin for the attempt. Zero retains Lean's unlimited setting. The rollback, proof validation, publication, and search logic are unchanged. Previous census counts remain evidence from their then-inherited cap; they are not relabeled as measurements under the requested cap.

The old Search control failed specifically with `search did not install its requested cached heartbeat limit`; its subsequent unknown-constant message is a consequence of the earlier command failure. The corrected build ran the finite requested-40000 check, finite zero-cap check, forced-2000 timeout, same-command recovery, and speculative declaration/context rollback checks. Its published `Test.ProofGraphSearch.closed` theorem reports no axioms. The existing ProofGraph controls also passed and their searched theorem reports no axioms. The additional existing obligation-ledger control build passed, with its displayed checked theorem depending on `[propext]`.

The first empty-census attempt was a setup failure: `Effect4.Laws.Auto.RuleSets` had not been explicitly imported. It contributes no evidence about the linter defect. After adding the import, the same zero-theorem census printed its correct `0 of 0` report and then failed on `this tactic is never executed`. The corrected `Test.Audit.ProofGraph` build passed its exact zero-report guard. The separate `OrdinaryUnreachableControl.lean` still failed on the same unreachable-tactic diagnostic, under `-DwarningAsError=true`.

The census correction registers `autoCensus` through Batteries' documented `addIgnoreTacticKind` API. Its tactic argument is deferred speculative syntax, so the command can legitimately enumerate no statements without executing that syntax. This classification applies to all census arguments, not only empty invocations: the unreachable-tactic source linter does not inspect dead branches inside a census argument. Ordinary proof syntax remains linted. No linter option is disabled, no synthetic execution metadata is fabricated, and no dummy proof search is introduced. The census enumeration, search calls, heartbeat choice, totals, and report are untouched. The obligation-ledger command needs no corresponding registration: it errors on an empty goal set, and each successful nonempty check invokes search.

The live diff reviewed here matches that scope: one cached-budget wrapper correction, one documented census syntax registration, the new finite Search control, its leading import into the existing ProofGraph test, and an exact zero-report census guard. `git diff --check` returned 0 for those files. The snapshots, base versions, raw logs, reviewed diff, and SHA-256 manifest are retained in this bundle. No full `Test.All`, architecture, or `make check` result is claimed here.
