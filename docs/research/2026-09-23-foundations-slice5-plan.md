# Slice 5 implementation from the reviewed divergence

Base: `fc638550f1b1790b016324661ccfaae8d34bd30d`, branch `codex/foundations-slice-5`.
The retargeted 2026-09-21 slice 5 brief and packet own the statements and finish criteria.
Runtime source, generated predicates and the protocol rows stay fixed.

First elaborate the R3 frame contract and recursive hook protocols, then state the generic
stack-walk and delivery obligations. Prove the unchanged hard statements under HookLaws
and InterruptProvenance without a run premise. Assemble the strong stored-state leaves and
active token correlation, state the initialization and hook debts, and declare each required
M6 actual-delivery obligation with the answer-gate join. The Keeps ladder remains unary and
imports only Machine/Fibers for runtime semantics. Focused positive and negative controls
precede the full packet checks; each proof has a transitive axiom report.

Done means the six hard results are checked at the existing trust ceiling, the three new
open gates have exact ceilings, all nine historical open obligations remain, the packet's
four final checks pass, and a receipt lists the exact statements, counts and evidence.
A checked false frozen statement stops the dependent work with its smallest amendment.
