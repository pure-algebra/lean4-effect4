# M1 completion-as-data

M1 is in progress on the owner's current checkout. Base: `6dcfe79a1cdf5515a9dea8743d773777d14c9423` (`refactor/phase1-phase3`). Nothing is pushed. The unused isolated worktree was created before the owner directed work back to this checkout; it is not used.

## Contract and proof plan

The kickoff §6/§6a and plan §14 D2 govern this slice. Deferred cells and owed resumes hold `Completion`; each consumer interprets it. Poll retains its nested option and DI-97 boolean answer. Memo entries retain their cell identity, scope, observer count and finalizer. Their removed copy is replaced by cell-based witnesses and a named observation connector. Missing-reference counterexample E4-STORES-CE-003 remains. No placement moves.

The connector must distinguish a missing cell, a pending cell, and a completed cell. It relates the former coherent memo observation to the cell projection; it does not assert equality of arbitrary old concrete stores. The old memoComplete overwrote the duplicate even after a second completion was rejected by the cell.

The proof order is storage, consumers, store/handle laws, reduced simulation and guard graph, typed census, generators and full check. InterpR imports StoresLaws through Behaviour, so its narrow build requires the mechanical StoresLaws statement migration. Remove shape lemmas before rebuilding their callers. StoresOk becomes a one-field structure for ScopeKeysFresh so #frame_rules can generate its frames.

The owner approved keeping tooling out of the runtime root: core proofs remain local; their obligations and search evidence live in a Laws-side companion. Laws proofs use the required search/register/retry loop. New helper facts carry the Stores bank. Changed statements are recorded with Obligation and #proof_wanted before proof attempts, then markers are removed as the exact statements close. The final ledger ceiling is zero.

## Baseline

`lake env lean docs/research/2026-09-20-m1-evidence/CensusBefore.lean`: exit 0; 246/1593 theorems closed across 38 modules. Output is retained in the evidence directory. The scratch census emits unreachable-tactic warnings for modules with no theorems; these are measurement-command warnings, not library-build evidence. Six further Guard consumers were discovered by following the removed InternalCodeNoRace conjuncts and are measured separately before editing.

## Storage slice

`lake build Effect4.Machine.Stores`: exit 0, 20 jobs. One unused simp lemma was removed after the first narrow build rejected it. `lake env lean /tmp/m1-core-axioms.lean`: exit 0; all five retyped witnesses were checked. Exact output is retained in `2026-09-20-m1-evidence/core-axioms.log`. The core wanted statements were recorded before changing the proof bodies in `CoreWanted.lean`.
