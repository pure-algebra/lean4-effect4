# Seat W1: the avatar as a mechanical port of the `Effect4/Deep` representation

Status: **checkpoint 1 of 4**, 2026-09-04, on `main` at `e1cad67`. Nothing committed.
Owner files: `workshop/OCaml5/avatar/deep_*.ml`, the fixtures and runners in `avatar/`,
`workshop/OCaml5/Render.lean` (additive, one delimited section at the end), this report.

Read first: `2026-09-04-spike-a0-avatar.md` (the avatar as A0 left it, and its eight
divergences), `2026-09-03-spike-p5-fuzz.md` §11–12 (the generator), `2026-09-04-ocaml5-state-and-paths.md`
§3 path B.

---

## 0. Headline

`Effect4/Deep/Stores.lean` is ported. `workshop/OCaml5/avatar/deep_stores.ml` (1,220 lines) is
one OCaml declaration per Lean declaration, in the Lean order: **every carrier is generated**
from a description in `Render.lean` and diffed back against the file, and every function is
hand-written arm for arm with the `Stores.lean` line it is the port of. The three stores the
avatar improvised as hashtables of ints — the Ref heap, the Deferred store, the ScopeStore —
are now the `Stores.lean` carriers, and `Effect4.Scope`'s five-state machine actually runs
underneath the `scope` family.

`RunInterp` is a record (`deep_fibers.ml`), which closes half of A0's DIVERGENCE 5, and the
machine reaches its store through it rather than through fields of its own. `deep_fibers.ml`
now holds no store at all.

Everything the avatar did before, it still does, on the same rows:

```
=== families with goldens in this checkout: ref deferred scope layer
hosts … AGREE                       (25 goldens + 6 extra, bytecode = native = js_of_ocaml)
compare[lean]  ref 21   deferred 18   scope 12   layer 24    0 failed
  …and again at EFFECT4_MAX_OPS=3 (the yield-every-op form): 21 / 18 / 12 / 24, 0 failed
rc.112 face vs lean golden, through the estate's own runner: all ok
compare[rc112] extra: 12 ok, 0 unclassified, 0 classified
corpus: avatar ok=158 bad=0 ; rc112 ok=149 skipped=5 nonterminating=4
        hosts agree: 158, disagree: 0
        compare[rc112]: 435 ok, 0 unclassified, 12 classified
$ echo $?    # build-avatar.sh
0
```

**A finding about the checkout, before any number is compared to A0's.** A0's headline is
"102/102 golden-mask pairs". That cannot be reproduced here and it is not a regression:
commit `98dcd20` ("Deep promoted, cleanup cut 1") **deleted `generated/traces/fiber/`**, so the
nine fiber goldens no longer exist in the tree. The families with goldens are `ref` (7),
`deferred` (6), `scope` (4) and `layer` (8) = 25 goldens = 75 mask pairs, and the avatar is
100% on all 75, twice (plain and yield-every-op). `build-avatar.sh` now discovers the families
that have goldens instead of hard-coding five, so it exits 0 rather than dying on `awk`.
Whether the fiber goldens should come back is an open question for the coordinator (§7 Q1).

---

## 1. What the port is, mechanically

| | |
| --- | --- |
| one OCaml module per Lean module | `deep_stores.ml` ← `Effect4/Deep/Stores.lean`; `deep_layer.ml` ← `Layer.lean` (checkpoint stub, §5) |
| link order | `deep_fibers.ml`, `deep_stores.ml`, `deep_layer.ml`, … — Lean's import direction (`Stores.lean` imports `Fibers.lean`) |
| carriers | generated by `OCaml5.Ml.Deep.Stores` in `Render.lean`, rendered by `avatar/render-deep.sh`, diffed against the file |
| functions | hand-written, one OCaml declaration per Lean declaration, in the Lean order, each citing its `Stores.lean` line |
| not ported | calls `Deep_fibers.refuse "<Lean name>"`, which raises `Not_ported`; never silent |

### The generator

`Render.lean` gained one delimited section at the end (`SEAT W1` banner) so seat W3's refactor
of `OCaml5.Ml` can re-export the names without merging into it. It adds:

* `Ml.typeName'` — `typeName` with `mangleField`'s keyword escape. `typeName "Val" = "val"`,
  which is an OCaml keyword; the file's own rule (`exit` → `exit_`) gives `val_`. `Ml.typeName`
  does not apply it because no `Fibers.lean` carrier needed it. `#guard`ed.
* `Ml.renameDecl` — renames the single type a `StructDesc.decl`/`InductiveDesc.decl` makes.
  Additive: neither description structure is changed, so W3 sees them as they were.
* `Ml.Deep.Stores` — a 40-entry substitution table, 9 `StructDesc`s, 13 `InductiveDesc`s, four
  tuple aliases, and `generated : List Decl` in dependency order.
* 22 `#guard`s: the constructor count of every `Stores.lean` inductive against the Lean file,
  the field order of every structure, the mangling round-trip on every field name, that no
  description erases an argument (`Stores.lean` is arity for arity, unlike `Fibers.lean`), and
  the exact OCaml names `deep_stores.ml` uses.

`lake build OCaml5.Render` is green. Render and diff with:

```
$ ./workshop/OCaml5/avatar/render-deep.sh stores
…/deep_stores.ml: carriers IDENTICAL to OCaml5.Ml.Deep
```

`render-deep.sh --write` rewrites the block. `render-deep.lean` is the `lake env lean --run`
driver; it lives under `avatar/`, which is not a `srcDir` of any `lean_lib`, so lake never
builds it. `tools/fuzz.sh avatar` still reports the five `Fibers.lean` carriers IDENTICAL.

### One `#guard` is a finding

`ActionName` (`Stores.lean:377`) and `WithFiberAction` (`Fibers.lean:258`) have the **same 17
constructor names in different orders**: `setInterruptible` is `WithFiberAction`'s twelfth and
`ActionName`'s sixteenth. A0 §3 lists no such row. `deep_stores.ml` follows the `Stores.lean`
order, because that is the file it is the port of; the `#guard` pins both facts.

---

## 2. `Effect4/Deep/Stores.lean` → `deep_stores.ml`, declaration by declaration

8 structures, 12 inductives, 7 abbrevs, 50 defs, 61 theorems. Theorems have no OCaml
counterpart and are not in the table; they are §6.

### Carriers — all generated, none refused

| Lean | Line | OCaml | State |
| --- | --- | --- | --- |
| `RefKey` | :56 | `ref_key` | generated |
| `DeferredKey` | :62 | `deferred_key` | generated |
| `Err` | :73 | `err`, prefix `E` | generated |
| `Defect` | :81 | `defect`, prefix `X` | generated |
| `Ann := Unit` | :89 | — | substituted: `ReasonAnnotations Ann` is the avatar's `string list` of keys |
| `FnName` | :94 | `fn_name`, prefix `Fn` | generated |
| `FinName` | :110 | `fin_name`, prefix `Fin` | generated |
| `Ctx` | :130 | `ctx` | generated (unused by the machine: `χ` is `unit`, A0 §18) |
| `Val` | :141 | — | **substituted** by the wire alphabet `value` (`deep_fibers.ml:64`), row W1-1 |
| `CauseV` / `ExitV` / `VoidExitV` | :171–:177 | `cause` / `exitv` / `exitv` | substituted (`deep_fibers.ml:77,96`) |
| `Completion` | :184 | `completion`, prefix `Co` | generated |
| `SyncOp` (23) | :193 | `sync_op`, prefix `S` | generated |
| `RaceName` (4) | :248 | `race_name`, prefix `Rn` | generated |
| `ProgName` (22) | :265 | `prog_name`, prefix `P` | generated |
| `Name` (20) | :321 | `name`, prefix `N` | generated |
| `ActionName` (17) | :377 | `action_name`, prefix `A` | generated |
| `Thunk` (4) | :403 | `thunk`, prefix `T` | generated |
| `Program` | :415 | `program = unit -> value` | substituted, DIVERGENCE 1 |
| `RefHeap` | :420 | `value list` | generated (as `stores.refs`) |
| `DeferredCell` | :653 | `deferred_cell` | generated, both fields `mutable` (DIVERGENCE 3) |
| `DeferredStore` | :663 | `deferred_store` | generated |
| `ScopeV` | :850 | `scope` | generated (from `Scope.lean:86`) |
| `ScopeEntry` | :859 | `scope_entry` | generated |
| `ScopeStore` | :867 | `scope_store` | generated |
| `Stores` | :1002 | `stores` | generated |

Plus, from `Effect4/Runtime/Scope.lean` (no OCaml module of its own in this pass — the seat's
frame is one module per `Effect4/Deep` module — so they are rendered into `deep_stores.ml`
under their own banner): `FinalizerStrategy` (`Scope.lean:39`) → `finalizer_strategy` prefix
`Fs`; `ScopeState` (`:71`) → `scope_state` prefix `Ss`; `Scope` (`:86`) → `scope`.
And, hand-written above the generated block because `ProgName`/`ActionName` name them:
`Supervision.MaskMode`/`ForkOptions` (`Supervision.lean:15-21`) → `mask_mode`/`fork_options`.

### Functions

| Lean | Line | OCaml | State |
| --- | --- | --- | --- |
| `refPeek` | :424 | `ref_peek` | hand-ported |
| `refPoke` | :427 | `ref_poke` | hand-ported |
| `FnName.total` | :431 | `fn_name_total` | hand-ported |
| `FnName.partialUpdate` | :438 | `fn_name_partial_update` | hand-ported |
| `FnName.modify` | :445 | `fn_name_modify` | hand-ported |
| `FnName.modifySome` | :450 | `fn_name_modify_some` | hand-ported |
| `refStep` (13 arms) | :457 | `ref_step` | hand-ported, arm for arm |
| `DeferredStore.make` | :673 | `deferred_store_make` | hand-ported |
| `DeferredStore.cellAt` | :677 | `deferred_store_cell_at` | hand-ported |
| `DeferredStore.setCell` | :681 | `deferred_store_set_cell` | **identity**, DIVERGENCE 3 (a cell is a record the store already holds) |
| `DeferredStore.isDone` | :685 | `deferred_store_is_done` | hand-ported |
| `DeferredStore.poll` | :689 | `deferred_store_poll` | hand-ported; answers `Completion option option`, not `Program option option` (row W1-2) |
| `DeferredStore.register` | :694 | `deferred_store_register` | hand-ported |
| `DeferredStore.cancel` | :705 | `deferred_store_cancel` | hand-ported |
| `DeferredStore.complete` | :715 | `deferred_store_complete` | hand-ported |
| `DeferredStore.drainDue` | :727 | `deferred_store_drain_due` | hand-ported |
| `finExit` | :854 | `fin_exit` | hand-ported |
| `ScopeStore.entryAt` | :875 | `scope_store_entry_at` | hand-ported |
| `ScopeStore.setEntry` | :879 | `scope_store_set_entry` | **identity**, DIVERGENCE 3 |
| `ScopeStore.make` | :883 | `scope_store_make` | hand-ported |
| `ScopeStore.addFinalizer` | :888 | `scope_store_add_finalizer` | hand-ported |
| `ScopeStore.removeFinalizer` | :897 | `scope_store_remove_finalizer` | hand-ported |
| `ScopeStore.forkChild` | :905 | `scope_store_fork_child` | hand-ported |
| `ScopeStore.status` | :919 | `scope_store_status` | hand-ported |
| `ScopeStore.closeOrderOf` | :924 | `scope_store_close_order_of` | hand-ported |
| `ScopeStore.closeState` | :931 | `scope_store_close_state` | hand-ported |
| `ScopeStore.closeResultOf` | :939 | `scope_store_close_result_of` | hand-ported |
| `voidAllOf` | :948 | `void_all_of` | hand-ported |
| `mergeExits` | :954 | `merge_exits` | hand-ported |
| `reifyExitVal` | :965 | `reify_exit_val` | hand-ported; the wire has no `exitOk`/`exitErr` pair, so it is `exit_value _ MAwait` and loses the typed error (A0 DIVERGENCE 6) |
| `exitsVal` | :971 | `exits_val` | hand-ported; **disagrees with the machine's own `resume_prim RexitsValue`** (row W1-7) |
| `reasonsOfVal` | :976 | `reasons_of_val` | hand-ported but **lossy** at this alphabet (row W1-8) |
| `Stores.empty` | :1016 | `stores_empty` | hand-ported; `store` is the one live instance |
| `raceEntrants` | :1026 | `race_entrants` | hand-ported |
| `finProgram` (5 arms) | :1036 | `fin_program` | 3 ported, 2 REFUSE (`closeChildScope`, `parkThen`) |
| `completionPrim` | :1052 | `completion_prim` | 1 ported, `ofRefGet` REFUSES |
| `progOf` (22 arms) | :1057 | `prog_of` | 11 ported, 11 REFUSE |
| `cancelProgram` | :1107 | `cancel_program` | hand-ported |
| `closeSeqChain` | :1115 | `close_seq_chain` | hand-ported; the Lean `Prim` frame chain is the OCaml `try`, so `Name.closeSeq` is a loop accumulator rather than a name |
| `closeParChain` | :1126 | `close_par_chain` | **REFUSES** (needs a fork over a `ProgName`) |
| `storesCloseScope` | :1143 | `stores_close_scope` | hand-ported (the `parallel` branch refuses through `closeParChain`) |
| `syncOpStep` (23 arms) | :1160 | `sync_op_step` | hand-ported, arm for arm |
| `contAOf` | :1193 | `cont_a_of` | **REFUSES**, structurally (row W1-9) |
| `contEOf` | :1225 | `cont_e_of` | **REFUSES**, structurally |
| `actionOf` | :1236 | `action_of` | **REFUSES**, structurally |
| `defaultBudget` | :1263 | `default_budget` | hand-ported |
| `emptyCtx` | :1266 | `empty_ctx` | hand-ported |
| `stackKey` | :1270 | `stack_key` | hand-ported |
| `stackAnnotationsOf` | :1283 | `stack_annotations_of` | hand-ported |
| `stores : RunInterp` (40 fields) | :1293 | `stores : run_interp` | 6 fields ported as a record; 34 are the avatar's own control (row W1-10) |

Also hand-ported, from `Effect4/Runtime/Scope.lean`, because `ScopeStore` calls them:
`ScopeState.entries` (`:98`), `.isOpen` (`:106`), `.isClosed` (`:114`), `.closingExit?` (`:122`),
`tableInsert` (`:326`), `tableRemove` (`:334`), `addState` (`:470`), `removeState` (`:661`),
`Scope.make` (`:241`), `.finalizers` (`:250`), `.isClosed` (`:262`), `.closingExit?` (`:265`),
`.addUnsafe` (`:481`), `.addExit` (`:613`), `.removeUnsafe` (`:672`), `.closeState` (`:779`),
`.closeOrder` (`:785`), `.closeExits` (`:790`), `.closeResult` (`:796`), `.fork` (`:1036`).
Twenty declarations, all ported, none refused.

**Counts.** 27 carriers generated (25 `Stores.lean` + 3 `Scope.lean`, with `Val`, `Ann`,
`Program` and the four `Cause`/`Exit` abbrevs substituted); 70 functions hand-ported
(50 from `Stores.lean`, 20 from `Scope.lean`); **21 refusal sites**, listed in §4.

---

## 3. The divergence table (rows W1-1 … W1-11)

Each row is a place the OCaml differs from the Lean spelling. A0's eight rows still stand and
are not repeated.

| # | Where | Divergence |
| --- | --- | --- |
| **W1-1** | `Val` (`:141`) | Substituted by the avatar's wire alphabet `value` (`deep_fibers.ml:64`). The two are different alphabets, and the map is not injective either way: `Val.fiber`/`.cell`/`.promise`/`.scopeHandle` all become one `Vhandle` under the wire's first-seen handle counter (`handle_index`, `deep_fibers.ml:371`); `Val.exitNil`/`.exitCons` become one `Vlist`; `Val.exitOk`/`.exitErr` become `Vsome`/`Vnone` (see W1-8); `Val.context` has no image (`χ` is `unit`). Conversely the wire's `Vpair`, `Vstring` and `Vsome`/`Vnone` have no Lean arm — they are the *fixture's* shapes (`harness/trace/atoms.ts`). A relation stated on `Val` needs this table as its clause. |
| **W1-2** | `DeferredCell.completion` (`:655`) | Lean stores `Option Program`; the avatar stores `Option Completion`. `Program` is an OCaml closure (DIVERGENCE 1), and the fixture's `poll`/`awaitError` rows read the *exit* back out of the slot, which a closure cannot answer. `completionPrim` is the map Lean applies at store time and the avatar applies at read time; both are declared, so the two spellings are visible. |
| **W1-3** | the harness fixture's alphabet | The `ref`/`deferred`/`scope` families' rows are **not** `SyncOp` arms and cannot be: `Ref.update(r, n => n + a)` is a JavaScript closure over `a`, and `FnName` (`:94`) has five *named* functions and no parameterised one. The fixture's rows run on the same `RefHeap`/`DeferredStore`/`ScopeStore` through the same `refPeek`/`refPoke`/`DeferredStore.*`/`ScopeStore.*` primitives `refStep` and `syncOpStep` are built from, declared as a second family of `store_request` constructors. So `refStep` and `syncOpStep` are ported and *not on the golden path*; they are exercised by `deep_witnesses.ml` (checkpoint 4). |
| **W1-4** | `SyncOp.deferredPoll` (`:224`) | Lean answers `Val.bool slot.isSome`; the fixture's `poll` row answers the completion's exit (`Vnone` / `Vsome (Vpair (bool, value))`). Both are in the file: `sync_op_step` answers the bool, the `Rdef_poll` arm answers the exit. |
| **W1-5** | `Supervision.ForkOptions` | `deep_fibers.ml` carries neither `MaskMode` nor `ForkOptions`: its `spawn` (`:631`) takes `~daemon` and inherits the parent's mask, i.e. `⟨startImmediately := false, maskMode := inherit⟩`. Both are declared in `deep_stores.ml` because `ProgName`/`ActionName` name them; the machine still reads only `daemon`. |
| **W1-6** | the wire handle decoding | `ref_key_of_handle`/`deferred_key_of_handle`/`scope_key_of_handle` read the handle's owner index **without checking its kind**, which is what the three `*_cell` lookups they replace did. Three of P5's 220 generated programs (`p400029`, `p400160`, `p400173`) pass a handle of one kind to an operation of another and die on a kind check. Kept, so those three keep running; it is a defect of the wire decoding, not of the store, and the fix belongs with P5's generator. |
| **W1-7** | `exitsVal` (`:971`) | `exits_val` is `Vlist (map reify_exit_val)`, i.e. a list of `Vsome`/`Vnone`; the machine's own spelling, `resume_prim RexitsValue` (`deep_fibers.ml:568`), drops the `Vsome` wrapper because `WithFiberAction.awaitAll`'s wire row carries the raw values. **The avatar has two meanings for one Lean function.** Not reconciled: the machine's is on the golden path and this one is not, so changing either would be a behaviour change made blind. Open (§7 Q3). |
| **W1-8** | `reasonsOfVal` (`:976`) | Lossy at this alphabet, because `reifyExitVal` mapped every failure to `Vnone`. Lean's `reasonsOfVal_exitsVal` (`:984`) — "the M6 channel loses nothing" — therefore has **no avatar counterpart**, and `Name.mergeAwaitedExits` cannot be given its Lean meaning here. This is a consequence of W1-1, not a coding choice. |
| **W1-9** | `contAOf`/`contEOf`/`actionOf` | Refuse *structurally*, not for want of writing. A continuation name means "the `Prim` the frame machine pushes"; the avatar's frame is the OCaml stack and its continuation is the `continue k` the arm already holds (DIVERGENCE 1), so there is nothing for these three to return that the machine would read. They are the three `Stores.lean` declarations the substitution makes unreachable. |
| **W1-10** | `RunInterp` (`Fibers.lean:386`) | Now a record in `deep_fibers.ml` with **six** slots — `store_arm`, `due_resumes`, `scope_status`, `scope_link_fiber`, `drop_finalizer`, `close_scope` — each citing its Lean field. That closes half of A0 DIVERGENCE 5: the machine calls its interp rather than its own fields, and `deep_stores.ml` defines `stores : run_interp` as the port of `Stores.lean:1293`. The other 34 fields remain handler-arm code, which is the half W1-9 says cannot change. `syncState` and `registerAsync` collapse into `store_arm` because `σ` is `store_request`, an *extensible* variant the machine never inspects — which is what a type parameter amounts to in OCaml. `St` is `unit`: the store is a module-level record (DIVERGENCE 3). |
| **W1-11** | link order | `deep_stores.ml` links after `deep_fibers.ml`, matching Lean's import direction, and `deep_layer.ml` after that. Inside a module, declaration order is dependency order and not Lean order (`FinalizerStrategy`/`ScopeState`/`Scope` are hoisted above `SyncOp`); P5 §11.7 finding 4 already says the sort is over the substitution table, not the Lean file. |

---

## 4. What refuses, exactly

`Deep_fibers.refuse "<Lean name>"` raises `Not_ported "not ported: <Lean name>"`. Every site,
with the Lean declaration it names:

```
Effect4.Deep.completionPrim (Completion.ofRefGet)
Effect4.Deep.finProgram (FinName.closeChildScope)
Effect4.Deep.finProgram (FinName.parkThen)
Effect4.Deep.progOf (ProgName.intoDeferred)
Effect4.Deep.progOf (ProgName.intoBody)
Effect4.Deep.progOf (ProgName.onExitOf)
Effect4.Deep.progOf (ProgName.forkThen)
Effect4.Deep.progOf (ProgName.forkOnly)
Effect4.Deep.progOf (ProgName.forkInScope)
Effect4.Deep.progOf (ProgName.runInScope)
Effect4.Deep.progOf (ProgName.forkScopedOf)
Effect4.Deep.progOf (ProgName.raceOf)
Effect4.Deep.progOf (ProgName.closeScopeOf)
Effect4.Deep.progOf (ProgName.awaitAllNew)
Effect4.Deep.closeParChain
Effect4.Deep.contAOf
Effect4.Deep.contEOf
Effect4.Deep.actionOf
Effect4.Deep.stores.syncState (no arm)
Effect4.Deep.Completion.ofRefGet (poll)
Effect4.Deep.stores (unknown store_request)
Effect4.Deep.Layers (unknown store_request)
```

Three groups:

1. **Structural** (W1-9): `contAOf`, `contEOf`, `actionOf`. These do not become writable by
   more work; they become writable only by giving the avatar a `Prim` tree, which is the
   thesis A0 rejected.
2. **Needs a fork over a `ProgName`** — `progOf`'s six fork arms, `closeParChain`,
   `finProgram (closeChildScope)`. `deep_fibers.ml`'s `arm_fork` (`:1108`) takes an `int` code
   into the fixture's root table; a `Prim`-alphabet fork needs `spawn m f (prog_of child) ~daemon`
   with a `fork_options` mask. **This is the precise next step for checkpoint 3** and it is
   small: generalise `arm_fork` to take a `(string * value) -> (unit -> value)` pair instead of
   a code, keep the code-taking arm as a wrapper so the golden rows are unchanged, and add
   `Rfork of prog_name * fork_options` to `store_request`.
3. **Needs a machine arm A0 already lists as absent**: `intoDeferred`/`intoBody` (`Prim.exitFrame`),
   `onExitOf` (`Prim.onExit`), `parkThen` (`Prim.async` at an external slot), `Completion.ofRefGet`.

`Effect4.Deep.Layers (unknown store_request)` and `Effect4.Deep.stores (unknown store_request)`
are the chain's tails — an unclaimed request refuses with the module that did not claim it.

---

## 5. Where each deliverable stands

| # | Deliverable | State |
| --- | --- | --- |
| 1 | `deep_stores.ml` | **DONE and verified.** Carriers generated and diffed; 70 functions hand-ported; the ref/deferred/scope faces are 21/18/12 on the Lean goldens under every mask, and again at `MAX_OPS=3`; the rc.112 face is unchanged; the 158-program corpus is byte-identical on three hosts with 435 ok / 0 unclassified / 12 classified, exactly A0 round five. |
| 2 | `deep_context.ml`, `deep_layer.ml` | **NOT STARTED.** `deep_layer.ml` exists but is a *checkpoint stub*: A0's round-three `layer_state` hashtables moved out of `deep_fibers.ml` to their proper module and re-registered as `Rlayer_*` store requests, so that the machine module holds no store. The layer face is 24/24, unchanged. The `Layer.lean` carriers (`LayerId`, `MemoMapId`, `CombineMode`, `Construction`, `LayerDesc`, `MemoEntry`, `MemoMap`, `MemoWorld`, `St`, the sixteen `LayerTable` rows) are **not** generated yet, and the file says so at the top. `deep_context.ml` does not exist. |
| 3 | `deep_forkflow.ml` + the rest of `Fibers.lean` | **NOT STARTED**, except that `fire_observer`'s `DropScopeFinalizer` arm (`deep_fibers.ml`) now calls `RunInterp.dropFinalizer` and halts only when the scope is genuinely unknown, instead of halting unconditionally. That is the machine half of W6. |
| 4 | `deep_witnesses.ml` | **NOT STARTED.** W6 still has no member: the *observer* arm is fixed but `forkIn`/`forkScoped`/`runIn` still refuse in `progOf`, which is group 2 of §4. |
| 5 | re-run the batteries | **DONE for this checkpoint**, numbers in §0 and §6. |
| 6 | the report | this file. |

### Next step, precisely

Checkpoint 2 is `deep_layer.ml` proper, and it is the same shape as this one:

1. add `Ml.Deep.Layer` to the seat-W1 section of `Render.lean` — `LayerId`, `MemoMapId`,
   `DeferredKey`, `CombineMode`, `Construction`, `LayerDesc`, `FinName`, `SyncOp`, `ProgName`,
   `Name`, `ActionName`, `Thunk`, `DeferredCell`/`DeferredStore`, `ScopeEntry`/`ScopeStore`,
   `MemoEntry`, `MemoMap`, `MemoWorld`, `St` — note that `Layer.lean` **re-declares** its own
   `DeferredKey`/`DeferredStore`/`ScopeStore`/`FinName`/`SyncOp`/`ProgName`/`Name`/`ActionName`/`Thunk`
   at `:63`–`:355`, distinct from `Stores.lean`'s, so they need their own prefixes and their
   own OCaml names (`layer_sync_op`, …). That collision is the first thing to settle;
2. add a `"layer"` case to `avatar/render-deep.lean`, render, `render-deep.sh layer --write`;
3. hand-port `MemoWorld.*` (`:512`–`:556`), `syncStep` (`:698`), the sixteen `layerTable` rows
   and `St.seeded` (`:637`), and replace `layer_state` with them;
4. `build-avatar.sh` and confirm the layer face is still 24/24.

`deep_context.ml` is independent of that and is the smaller job: `Context.lean` is mostly
theorems (its `Context` is an insertion-ordered `List (Service U)` with a `Nodup` proof field —
the proof field has no OCaml counterpart and is the module's one interesting divergence), and
**there is no context family in this checkout** (A0 §18), so nothing executes it yet.

---

## 6. Evidence

```
$ lake build OCaml5.Render                      ✔  (22 new #guards)
$ ./workshop/OCaml5/avatar/render-deep.sh stores
  …/deep_stores.ml: carriers IDENTICAL to OCaml5.Ml.Deep
$ ./workshop/OCaml5/tools/fuzz.sh avatar
  ocamlc OK / ocamlopt OK / js_of_ocaml OK
  run_fiber, frame_fiber, observer, run_event, run_decision: IDENTICAL to deep_fibers.ml
$ ./workshop/OCaml5/avatar/build-avatar.sh ; echo $?
  … (numbers in §0) …
  0
```

Both full `build-avatar.sh` sweeps (before and after the W1-6 repair) produced the identical
numbers in §0 and exited 0; `run-corpus.sh` runs inside it and is the last three lines of §0.

All three modules (`deep_fibers.ml`, `deep_stores.ml`, `deep_layer.ml`) compile under
`ocamlc`, `ocamlopt` and `js_of_ocaml --enable effects`, and every one of the 31 golden
programs and 158 corpus programs is byte-identical across `ocamlrun`, `ocamlopt` and jsoo.

**`run-fuzz.sh` (not a gate; P5's 220 generated programs).**

```
fuzz: avatar ok=220 bad=0 ; rc112 ok=220 bad=0
compare[rc112]: 517 ok, 143 unclassified, 0 classified
```

A0 §24's was `avatar ok=218 bad=2`. **All 220 now run**, and the two extra are a real
improvement rather than a masked failure: `DeferredStore.register` on an unknown cell answers
`(self, none)` in Lean (`Stores.lean:698`), i.e. the awaiting fiber parks, where the
hashtable it replaces raised `Failure "unknown deferred handle"`. The avatar moved *toward*
the Lean model on a path no golden reaches. (Before the W1-6 repair the count was 217/3: the
kind check `handle_target` performs, which the code it replaced did not, killed
`p400029`/`p400160`/`p400173`.)

`compare[rc112]: 517 ok, 143 unclassified` is **not** comparable with A0 §24's `497 ok, 159
unclassified`, because A0 §24 states its numbers predate the `Ref.set` repair in
`fuzz_rc112_tail.ts`. The 143 are still the contract mismatches A0 §24 names (`yieldNow` vs
`yield`, `awaitAll`'s answer, and the residue), none of them a machine divergence, and the
count went up by four because four more programs now produce rows at all.

**The 61 `Stores.lean` theorems** have no OCaml counterpart and none is claimed. Two are worth
naming because the port makes them *false* here rather than merely unstated:
`reasonsOfVal_exitsVal` (`:984`) and `mergeAwaited_eq_mergeExits` (`:994`), by W1-8.

---

## 7. Open questions

1. **The fiber goldens are gone.** `98dcd20` deleted `generated/traces/fiber/`. The avatar's
   fiber family, `fibers_fixture.ml`, the `extra` family and A0's headline all target it, and
   the corpus's `iW*` members are mirrors of it. Should they be regenerated
   (`scripts/generate-trace-goldens.sh`), or is the fiber face now the corpus only? Until this
   is answered, "the avatar is 100% on the goldens" means 75 mask pairs, not 102.
2. **Two families of store requests.** W1-3: the harness fixture's alphabet and `Stores.lean`'s
   `SyncOp` are different profiles of the same stores, and `deep_stores.ml` carries both. Is
   that the intended end state, or should `harness/trace/Generate.lean` eventually lower to
   `SyncOp` arms (which needs a parameterised `FnName` in `Stores.lean` — a Lean edit)?
3. **`exitsVal` has two meanings** (W1-7). The machine's `resume_prim RexitsValue` and the
   ported `exits_val` disagree on a `Vsome` wrapper. Which is `WithFiberAction.awaitAll`'s
   answer? A0 §24 already routed the same question to P5 from the other side ("P5's two
   renderings disagree with each other on this row").
4. **`Effect4/Runtime` has no OCaml module.** `Scope.lean`'s twenty declarations are ported
   *inside* `deep_stores.ml`. `Layer.lean` will want the same ones, and `ForkFlow.lean` will
   want `Supervision`. Should there be a `deep_runtime.ml`/`deep_supervision.ml`, breaking the
   "one module per `Effect4/Deep` module" rule, or should each Deep module carry its own copy?
   Right now `Layer.lean` re-declaring its own `ScopeStore` means a copy is at least *faithful*.
5. **Files touched outside `avatar/` and `Render.lean`**: `workshop/OCaml5/tools/fuzz.sh`, two
   lines, both stale module lists (`corpus-smoke`'s `ocamlc -c` now names `deep_stores.ml` and
   `deep_layer.ml`). Flagged in case seat W3 or P5 owns that file.

---

## 8. Files changed (nothing committed)

```
 workshop/OCaml5/Render.lean                    +~330  (one delimited section at the end)
 workshop/OCaml5/avatar/deep_stores.ml          NEW   1220
 workshop/OCaml5/avatar/deep_layer.ml           NEW    130  (checkpoint stub, §5)
 workshop/OCaml5/avatar/render-deep.lean        NEW     16
 workshop/OCaml5/avatar/render-deep.sh          NEW     40
 workshop/OCaml5/avatar/deep_fibers.ml          -260 +90   (RunInterp record; the stores go)
 workshop/OCaml5/avatar/store_fixtures.ml       22 call sites → Op_store
 workshop/OCaml5/avatar/fibers_fixture.ml       1 call site
 workshop/OCaml5/avatar/corpus_run.ml           22 call sites
 workshop/OCaml5/avatar/build-avatar.sh         family discovery, module list
 workshop/OCaml5/avatar/run-fuzz.sh             module list
 workshop/OCaml5/tools/fuzz.sh                  module list (see §7 Q5)
 docs/research/2026-09-04-seat-w1-deep-port.md  NEW  (this file)
```

---

# Seat F1 (checkpoint 2), 2026-09-04

Base `b60fe28` (W3's `Ml/` API and W2's daemon committed). Nothing committed by F1; the
coordinator holds the WIP save. Every file below compiles and every gate that was green is
green: `build-avatar.sh` exit 0 with the numbers of §0 unchanged (75+75 lean pairs, 12 extra,
corpus 435 ok / 0 unclassified / 12 classified, hosts agree 31 + 158), plus the new
witness/clause report; `lake build OCaml5.Render` untouched this round (no Render edit landed).

## F1.1 What landed

| File | State |
| --- | --- |
| `avatar/deep_fibers.ml` | the run-loop top split as `dd1bc93` split it (`runloop_top`, `count_op`, `yield_verdict`, `inject_yield`; `guard` is their composition); `interrupt_each`, `exit_interrupt_children`/`exit_store`, `link_scope`, `spawn_child`/`spawn` over `fork_options` (moved here from `deep_stores.ml`, W1-5 closed), `race_entrant`, `replay_eval`, `squash`; `answer` gains `Aexits` (W1-7 closed: `resume_prim RexitsValue` answers the exits, each site projects); `'a kops`; `RunInterp.stackAnnotations` slot; the probe `on_event`; the Lean alphabet's own effects and arms: `Op_fork_prog`, `Op_fork_in`, `Op_fork_scoped` (dies `notImplemented`: `χ = unit`), `Op_run_in`, `Op_join_on`, `Op_await_fibers` (exits-typed), `Op_interrupt_fiber`, `Op_interrupt_scoped`, `Op_race_all_prog`, `Op_close_scope` (installs the program in `current_program`, the fiber-side wrapper runs it: DIVERGENCE 1's spelling of `current := program`), `Op_get_id`, `Op_on_exit_enter` (`Prim.onExit`'s `ensure`) |
| `avatar/deep_stores.ml` | **every `progOf` arm ported** (22/22), `finProgram` 5/5, `closeParChain` ported, `on_exit_program`/`masked_region` (M2), M3 cancel on an interrupted `Deferred.await` park, `Rinterrupt_deferred` drains on the spot (M1). Refusals left: `contAOf`/`contEOf`/`actionOf` (structural, W1-9), `Completion.ofRefGet` (2 sites), unknown `store_request` tails |
| `avatar/deep_census.ml` + `extract-census.py` | the census join as data: the 80 of 130 `RuntimeCoverage.lean` rows naming an `Effect4.Deep.*` witness, extracted mechanically, `--check` diffs (IDENTICAL) |
| `avatar/deep_clauses.ml` | **107 entries = 107 `Clauses.lean` theorems, same names, same order**, each with its line and census tags; `Pure`/`Run`/`Both`/`Refused` checks |
| `avatar/deep_witnesses.ml` | **53 entries = 53 `Witnesses.lean` theorems**, same order; every `def` ported (`replay` over a seeded store and a decision tape) |
| `avatar/avatar_witnesses.ml`, `run-witnesses.sh` | the report on three hosts, byte-identical; wired into `build-avatar.sh` (module list updated for W2's daemon) |
| `avatar/dune-project`, `effect4-avatar.opam`, `dune`, `build-dune.sh` | the dune project in the `effect4` switch (5.1.1, dune 3.24.2, jsoo 5.7.1 *from the switch*): library + 2 executables × {byte, exe, js}; **green; its witness report is byte-identical to the shell build's**. No ppx yet (see F1.4) |
| `avatar/deep_layer.ml` | unchanged (checkpoint stub); collision settled on paper only (F1.4) |

## F1.2 The clause transfer, counts

```
$ ./workshop/OCaml5/avatar/run-witnesses.sh
hosts witnesses AGREE (bytecode = native = js_of_ocaml)
clauses: 107 entries = 107 Clauses.lean theorems
witnesses: 53 entries = 53 Witnesses.lean theorems
deep_census.ml: IDENTICAL to RuntimeCoverage.lean (80 of 130 rows)
clauses    107  holds 104  fails 0  not-portable 3
witnesses   53  holds  51  fails 0  not-portable 2
run-clauses      holds  26  fails 0        (the step checks on the runs that exercise them)
```

Not portable, with why: `drive_loop_parked`, `drive_loop_continues` (`Cmd.loop`, DIVERGENCE 2);
`withFiber_forkScoped_ambient` (`χ = unit`, no ambient scope; the `_none` arm holds);
witnesses `forbidden_setInterruptible_as_current` (no `Prim` in `current`, DIVERGENCE 1) and
`toSched_computes` (`toSched` not ported). Every other clause and witness holds on the avatar,
including all of W6 (scope linkage: `forkIn`/`runIn`/`closeScope` no longer refuse) and W6b
(both close strategies), W10, W11, W12, W13. Statements that depend on the value alphabet are
stated at the avatar's and cite the row: W1-1 (`Val.fiber` = wire handle, `exitErr` = `Vnone`),
W1-2 (`Completion` in the slot), `Err.boom` = code 100, `Defect.asyncFiber` = the string.

## F1.3 New divergence rows

| # | Where | Divergence |
| --- | --- | --- |
| F1-1 | `deep_witnesses.ml` `scope_rows` | `ScopeLinked` erases the mode (`Avatar.runEvent.erasures`), so a linked row is `[0; scope; key; fiber]` |
| F1-2 | `resume_prim (RcontinueWith e)` | the restoring name applied to the collected exits is the restored exit delivered directly (DIVERGENCE 1); clause `resumePrim_continueWith` holds in that spelling |
| F1-3 | `Op_close_scope` | `current := program` is `current_program` + the fiber-side `take_current_program` |
| F1-4 | `cause_keys` | the avatar annotates the cause, not each reason: one key list, not one per reason |

## F1.4 Exact next steps

1. **Re-cite `Witnesses.lean` lines in `deep_witnesses.ml`**: the file changed on disk during
   this round (harness lines shifted by ~3); the `lean` fields were taken from the earlier read.
2. **`deep_layer.ml`, collision settled**: types keep the Lean names inside their own module
   (`Deep_layer.sync_op` vs `Deep_stores.sync_op`, as `Effect4.Deep.Layers` vs `Effect4.Deep`);
   constructors take Layer prefixes (`Lc/Lk/Ld/Lfin/Ls/Lp/Ln/La/Lt/Lu/Lx`, `Lss` for the
   `ScopeState` copy) so a file opening both modules is unambiguous; the SHIM `DeferredKey`/
   `DeferredCell`/`DeferredStore` are *substituted* by `Deep_stores`' (one store, as the landing
   intends); `ScopeEntry`/`ScopeStore`/`Scope`/`ScopeState` are rendered as Layer's own
   monomorphic copies over Layer's `FinName` (generator has no type parameters here; row).
   Counts pinned from `Layer.lean`: CombineMode 2, Construction 4, LayerDesc 6, FinName 11,
   SyncOp 10, ProgName 27, Name 47, ActionName 12, Thunk 3, ContextUpdate 3, Defect 5; `Ctx`
   is `(service_key * value) list`, `service_key = {name; type_code}`. Then add `Ml.Deep.Layer`
   to the SEAT W1 section, a `"layer"` case to `render-deep.lean`, port `MemoWorld.*`,
   `St.empty/seeded`, the value helpers and `syncStep` (10 arms), keep `layer_state` for the
   golden family as W1-3's analogue.
3. **`[@@deriving]` on generated carriers** (dune, ppx_jane v0.16): `TypeDecl.derivers` exists
   (W3); the obstacle is that generated carriers reference hand-written `deep_fibers.ml` types
   with closures (`run_fiber.frame.control`, `kops`) — derive `deep_fibers.ml`'s data carriers
   first with `[@sexp.opaque]`/`[@compare.ignore]` on the closure fields, then emit the
   attributes through the descriptions.
4. `deep_context.ml`, `deep_forkflow.ml`: not started.
