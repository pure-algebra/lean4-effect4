--- PDF page 1 ---
Logical Methods in Computer Science
Vol. 9(4:23)2013, pp. 1–36
www.lmcs-online.org
Submitted Oct. 20, 2011
Published Dec. 17, 2013
HANDLING ALGEBRAIC EFFECTS ∗
GORDON D. PLOTKIN a AND MATIJA PRETNAR b
a Laboratory for Foundations of Computer Science, School of I nformatics, University of Edinburgh,
Scotland
e-mail address : gdp@inf.ed.ac.uk
b Faculty of Mathematics and Physics, University of Ljubljan a, Slovenia
e-mail address : matija.pretnar@fmf.uni-lj.si
Abstract. Algebraic eﬀects are computational eﬀects that can be repre sented by an equa-
tional theory whose operations produce the eﬀects at hand. T he free model of this theory
induces the expected computational monad for the correspon ding eﬀect. Algebraic eﬀects
include exceptions, state, nondeterminism, interactive i nput/output, and time, and their
combinations. Exception handling, however, has so far rece ived no algebraic treatment.
We present such a treatment, in which each handler yields a mo del of the theory for ex-
ceptions, and each handling construct yields the homomorph ism induced by the universal
property of the free model. We further generalise exception handlers to arbitrary algebraic
eﬀects. The resulting programming construct includes many previously unrelated exam-
ples from both theory and practice, including relabelling a nd restriction in Milner’s CCS,
timeout, rollback, and stream redirection.
Introduction
In seminal work [13], Moggi proposed a uniform representati on of computational eﬀects
by monads [1]. For example, working in the category of sets, a computation that returns
values from a set A is modelled by an element of T A for a suitable monad T . Examples
of such eﬀects include exceptions, state, nondeterminism, i nteractive input/output, time,
continuations, and combinations of them. Later, Plotkin an d Power proposed to represent
eﬀects by
(1) a set of operations that represent the sources of eﬀects; a nd
(2) an equational theory for these operations that describe s their properties [19].
The basic operational intuition is that each computation ei ther returns a value or per-
forms an operation with an outcome that determines a continu ation of the computation.
The arguments of the operation represent the possible conti nuations. For example, using a
2012 ACM CCS: [Theory of computation ]: Semantics and reasoning—Program constructs; Semantics
and reasoning—Program semantics—Algebraic semantics.
Key words and phrases: algebraic eﬀects, exception handlers, generalised handle rs.
∗ A preliminary version of this work was presented at ESOP 2009 , see [21].
a This research was supported by EPSRC grant GR/586371/01 and by a Royal Society-Wolfson Award.
LOGICAL METHODS
/D0 IN COMPUTER SCIENCE DOI:10.2168/LMCS-9(4:23)2013
c⃝ G. D. Plotkin and M. Pretnar
CC⃝ Creative Commons

--- PDF page 2 ---
2 G. D. PLOTKIN AND M. PRETNAR
binary choice operation choose, a computation that nondeterministically chooses a boolea n
is:
choose(return true, return false)
The outcome of making the choice is binary: either to continu e with the computation given
by the ﬁrst argument or else to continue with that given by the second.
A computation that returns values from a set A is modelled by an element of the
free model F A, generated by the equational theory. In the case of nondeter minism, these
equations state that choose is a semilattice operation. Modulo the forgetful functor, t he
free model functor is exactly the monad proposed by Moggi to m odel the corresponding
eﬀect [17]. Eﬀects whose monad can be obtained by such an equati onal presentation are
called algebraic; the relevant monads are exactly the ranked ones. With the no table excep-
tion of continuations [4, 6], all of the above eﬀects are algeb raic, and, indeed, have natural
equational presentations.
The algebraic view has given ways of combining eﬀects [7] and r easoning about them [20].
However, exception handlers provided a challenge to the alg ebraic approach.
The monad − +exc for a given set of exceptions exc is presented by a nullary exception
raising operation raisee() for each e ∈ exc and no equations. The operation raisee() takes
no arguments as there is no continuation immediately after a n exception has been raised.
The question then arises how to deal with exception handling . One approach would be
to consider a binary exception handling construct
handlee(M, N )
which proceeds as M unless the exception e is raised, when it proceeds as N . This construct
has a standard interpretation as a binary operation using th e exception monad; however,
as explained in [18], it lacks a certain naturality property characterising equationally speci-
ﬁed operations. In programming terms this corresponds to th e operation commuting with
evaluation contexts E[− ]. For example we would expect each of the following two equat ions
to hold:
E[choose(M, N )] = choose(E[M ], E[N ]) E[raisee()] = raisee()
but not:
E[handlee(M, N )] = handlee(E[M ], E[N ])
Since the naturality property is common to all equationally speciﬁed operations, it follows
that no alternative (ranked) monad will suﬃce either.
In this paper we give an algebraic account of exception handl ing. The main idea is that
(1) handlers correspond to (not necessarily free) models of the equational theory; and
(2) the semantics of handling is given using unique homomorp hisms that target such models
and are induced by the universal property of the free model.
The usual exception handling construct corresponds to the a pplication of the unique ho-
momorphism that preserves returned values. We, however, ad opt a more general approach,
suggested by Benton and Kennedy [2], and which was an inspira tion for the present work. In
Benton and Kennedy’s approach returned values are passed to a user-deﬁned continuation;
this amounts to an application of an arbitrary induced homom orphism to a computation.
As we shall see, this idea generalises to all algebraic eﬀects , yielding a new programming
concept enabling one to handle any algebraic eﬀect. Examples include relabelling and
restriction in CCS [12], timeout, rollback, stream redirec tion of shell processes, and many
others. Conceptually, algebraic operations and eﬀect handl ers are dual: the former could

--- PDF page 3 ---
HANDLING ALGEBRAIC EFFECTS 3
be called eﬀect constructors as they give rise to the eﬀects; the latter could be called eﬀect
deconstructors as the computations they provide proceed according to the eﬀe cts already
created. Filinski’s reﬂection and reiﬁcation operations p rovide general eﬀect constructors
and deconstructors in the context of layered monads [3].
In Section 1, we illustrate the main semantic ideas via an inf ormal discussion of excep-
tion handlers. Then, in Section 2, we give a calculus extendi ng Levy’s call-by-push-value [9]
with operations, handler deﬁnitions, and an eﬀect handling c onstruct, which handles com-
putations using a given handler. In Section 3, we give some ex amples that demonstrate the
versatility of our handlers. Next, in Section 4, we provide a denotational semantics, and
deﬁne a notion of handler correctness; an informal introduc tion to handler correctness is
given in Section 1.3. The denotational semantics is given in terms of sets and functions
although a more general categorical semantics should also b e possible.
In Section 5, we sketch some reasoning principles for handle rs, and then, in Section 6, we
give some results on the diﬃculty of deciding handler correc tness. In Section 7, we describe
the inclusion of recursion; to do this we switch from the cate gory of sets and functions to
that of ω -cpos (partial orders with suprema of increasing countable chain s) and continuous
functions (monotone functions preserving such suprema). In the concl usion, we list open
questions and brieﬂy discuss some possible answers. At vari ous points in the paper we use
operational ideas to aid understanding; we do not however pr esent a formal operational
semantics of eﬀect handlers.
1. Exception Handlers
We start our study with exception handlers, both because the y are an established concept [2,
10] and also since exceptions are the simplest example of an a lgebraic eﬀect. To focus on
the exposition of ideas, we write this section in a rather inf ormal style, mixing syntax and
semantics.
We consider a ﬁnite set of exceptions exc. Computations returning values from a
set A are modelled by elements of the exception monad T A def= A + exc. This has unit
ηA
def= in 1 : A → A + exc, and the computation return V is interpreted by ηA(V ) = in 1(V ),
while raisee() is interpreted by in 2(e).
1.1. Simple handling construct. Fixing A, the simple, standard, handling construct is
M handled with {raisee() ↦→ Me}e∈ exc
where {· · ·}e∈ exc represents a set of computations, one for each exception e ∈ exc. The
construct proceeds by carrying out the computation M ∈ A + exc, intercepting raised
exceptions e ∈ exc by carrying out predeﬁned computations Me ∈ A + exc instead. If we
choose not to handle a particular exception e, we take Me to be raisee(). The handling
construct satisﬁes two equations:
return V handled with {raisee() ↦→ Me}e∈ exc = in 1(V )
raisee′() handled with {raisee() ↦→ Me}e∈ exc = Me′
From an algebraic point of view, the computations Me give a new model M for the
theory of exceptions. The carrier of this model is A + exc as before; however, for each e,

--- PDF page 4 ---
4 G. D. PLOTKIN AND M. PRETNAR
raisee() is instead interpreted by Me. We then see from the above two equations that
h(M ) def= M handled with {raisee() ↦→ Me}e∈ exc
is the unique homomorphism (a map preserving operations) fr om A+exc to M that extends
the map in 1 : A → A + exc, i.e., so that the following diagram commutes:
A
A + exc
ηA
❄
............
h
✲ M
in1
✲
The idea is therefore to obtain such a homomorphism h using the freeness of the model
A+ exc, used for computations, and then interpret the handling con struct as an application
of h to the computation being handled. The existence of h, in turn, requires a model M
on A + exc for its target; that model is supplied via the handling const ruct.
1.2. Extended handling construct. Benton and Kennedy [2] generalised the handling
construct to one of the form
M handled with {raisee() ↦→ Ne}e∈ exc to x : A. N (x)
(written using our syntax). Here returned values are passed to a user-deﬁned continuation,
a map N : A → B + exc, where B is a set that may diﬀer from A; handling computations
Ne return values in B, if they do not themselves raise exceptions: thus Ne ∈ B +exc. These
two facts can be expressed equationally:
return V handled with {raisee() ↦→ Ne}e∈ exc to x : A. N (x) = N (V )
raisee′() handled with {raisee() ↦→ Ne}e∈ exc to x : A. N (x) = Ne′
As discussed in [2], this construct captures a programming i diom that was cumbersome
to write with the simpler construct, allows additional prog ram optimisations, and has a
stack-free small-step operational semantics.
Algebraically we again have a model M, this time on B + exc, interpreting raisee() by
Ne. The handling construct can be interpreted as h(M ), where h : A + exc → M is the
unique homomorphism that extends N , i.e., such that the following diagram commutes:
A
A + exc
ηA
❄
............
h
✲ M
N
✲
Note that all the homomorphisms from the free model to a model on a given car rier are
obtained in this way. So Benton and Kennedy’s handling const ruct is the most general one
possible from the algebraic point of view.

--- PDF page 5 ---
HANDLING ALGEBRAIC EFFECTS 5
1.3. Handling arbitrary algebraic eﬀects. We can now see how to give handlers for
other algebraic eﬀects. A model of an equational theory is an i nterpretation, i.e., a set and
a set of maps, one for each operation, that satisﬁes the equat ions; handlers give deﬁnitions
of such interpretations. As before, computations are inter preted in the free model and
handling constructs are interpreted by the induced homomor phisms. Where exceptions
were replaced by handling computations, operations are now replaced by the handling maps;
as computations are built from combinations of operations, handling a computation may
involve several such replacements.
Importantly however, not all interpretations yield a model of the theory, and so, in
that sense, not all handlers need be correct (see Section 4). If a handler is not correct then
handling constructs using it have no meaning. Even more, whe ther or not a handler is
correct may be undecidable (see Section 6).
We can see two general approaches to this diﬃculty when desig ning programming lan-
guages with facilities to handle eﬀects. One is to make the lan guage designer responsible:
the deﬁnable families should be restricted so that only mode ls can be deﬁned; this is the
approach taken in [21]. Another, the one adopted in this pape r, is to allow complete free-
dom in the language: all possible deﬁnitions are permitted. In this case not all handlers are
correct, and it is not the responsibility of the language des igner to ensure that all handlers
deﬁned are correct.
One can envisage responsibility being assigned variously t o the language designer, to
the compiler, to the programmer, to a protocol for establish ing program correctness, or, in
varying degrees, to all of them. For example, some handlers m ay be “built-in”, and so the
responsibility of the language designer, while others may b e deﬁned by the programmer,
and so their responsibility.
2. Syntax
2.1. Signatures. Signature types α, β are given by:
α, β :: = b |1 |α × β |∑
ℓ∈ Lα ℓ
where b ranges over a given set of base types, where L ranges over ﬁnite subsets of labels
(taken from a ﬁxed set Lab of all possible labels); we do not sp ecify the set of labels in
detail, but assume such ones as needed are available. We spec ify a subset of the base types
as arity base types , and say that a signature type is an arity signature type if the only base
types it contains are arity ones.
To represent ﬁnite data such as booleans bool = {true, false}, ﬁnite subsets of inte-
gers n = {1, 2, . . . , n }, the empty set 0, characters chr, or memory locations loc, we may
confuse a ﬁnite set L with the type ∑
ℓ∈ L1. For inﬁnite sets, such as natural numbers nat
or strings, we take base types as needed.
Next, we assume given a set of typed function symbols f : α → β . These represent pure
built-in functions, for example arithmetic function symbo ls, such as + : nat × nat → nat,
or function symbols for arithmetic relations, such as < : nat × nat → bool.
Finally, we assume given a ﬁnite set of typed operation symbols op : α /rightarrowtriangleβ , where each
such β is an arity signature type, and an eﬀect theory T . The operations represent sources
of eﬀects and the eﬀect theory determines their properties. In order to to focus on handlers,
we postpone the consideration of eﬀect theories to Section 4. 1.

--- PDF page 6 ---
6 G. D. PLOTKIN AND M. PRETNAR
The typing op : α /rightarrowtriangleβ indicates that the operation represented by op accepts a param-
eter of type α and, after performing the relevant eﬀect, its outcome, of typ e β , determines
its continuation; we say that op is parameterised on α and has arity β , or is β -ary. In case
α = 1, we may just write op : β .
The given sets of base types, arity base types, typed functio n symbols, and typed
operation symbols constitute a signature; the syntax is parameterised by the choice of such
a signature. The choice of signature (and theory) that we tak e evidently depends on the
eﬀects we want to represent.
We now give some examples, taken from [7].
Examples 2.1.
Exceptions: We take a single nullary (i.e., 0-ary) operation symbol raise : exc /rightarrowtriangle0, param-
eterised on exc, for raising exceptions. Here exc is a ﬁnite set of exceptions; we could
instead take it to be a base type if we wanted an inﬁnite set of e xceptions. The opera-
tion symbol is nullary as there is no continuation after rais ing an exception: instead the
exception has to be handled.
State: We take an arity base type nat for natural numbers and read and write operation
symbols get : loc /rightarrowtrianglenat and set : loc× nat /rightarrowtriangle1, where loc is a ﬁnite set of locations. The
idea is that there is a state holding natural numbers in the lo cations, and get retrieves
the number from a given location, while set sets a given location to a given number and
returns nothing.
Read-only state: Here we only take an arity base type nat and a read operation symbol
get : loc /rightarrowtrianglenat.
(Binary) nondeterminism: We take a binary (i.e., 2-ary) operation symbol choose for
nondeterministic choice. The operation symbol is binary as the outcome of making a
choice is to decide between one of two choices.
Interactive input and output (I/O): We take a ﬁnite set chr of characters, and oper-
ation symbols read : chr, for reading characters, and write : chr /rightarrowtriangle1, for writing them.
The operation symbol read is chr-ary, as the outcome of reading is to obtain a character;
the operation symbol write is unary as there will be just one continuation after writing
a character.
2.2. Types. Our language follows Levy’s call-by-push-value approach [ 9] and so has a strict
separation between value types A, B and computation types C
. These types are given by:
A, B :: = b |1 |A × B |∑
ℓ∈ LAℓ |U C
C :: = F A |∏
ℓ∈ LC ℓ |A → C
The value types extend the signature types, as there is an add itional type constructor U − .
The type U C classiﬁes computations of type C that have been thunked (or frozen) into
values; such computations can be passed around and later forced back into evaluation.
The computation type F A classiﬁes the computations that return values of type A.
The product computation type ∏
ℓ∈ LC ℓ classiﬁes ﬁnite indexed products of computations,
of types C ℓ, for ℓ ∈ L. These tuples are not evaluated sequentially as in a call-by -value
setting; instead, a component of a tuple is evaluated only on ce it is selected by a projection.
Finally, the function type A → C classiﬁes computations of type C parametric on values of
type A.

--- PDF page 7 ---
HANDLING ALGEBRAIC EFFECTS 7
2.3. T erms. The terms of our language consist of value terms V , W , computation terms M ,
N , and handler terms H. They are given by:
V, W :: = x |f(V ) | ⟨⟩ | ⟨V, W ⟩ | ℓ(V ) |thunk M
M, N :: = match V with ⟨x, y ⟩ ↦→ M |match V with {ℓ(xℓ) ↦→ Mℓ}ℓ∈ L |force V |
return V |M to x : A. N | ⟨Mℓ⟩ℓ∈ L |prjℓ M |λx : A. M |M V |
opV (x : β. M ) |k(V ) |M handled with H to x : A. N
H :: = {opx : α (k : β → C) ↦→ Mop}op : α /rightarrowtriangleβ
where x, y, . . . range over an assumed set of value variables , and k ranges over an assumed
set of continuation variables . Here {· · ·}ℓ∈ L represents a set of computations, one for each
label ℓ ∈ L; similarly, {· · ·}op : α /rightarrowtriangleβ represents a set of computations, one for each operation
symbol op : α /rightarrowtriangleβ .
We may omit type annotations in bindings if it does not cause a mbiguity; we may also
speak of values, computations, or handlers instead of value terms, computation terms, or
handler terms, respectively.
All the syntax is standard from call-by-push-value, other t han that for the handler
terms and the last line of the computation terms. Value terms are built from value vari-
ables, the usual constructs for ﬁnite products, constructs for indexed sums, and thunked
computations. Note that value terms only involve construct ors, for example, pairing ⟨V, W ⟩,
while the corresponding destructor terms, for example matc hing match V with ⟨x, y ⟩ ↦→ M ,
are computations.
Next, there are constructor and destructor computation ter ms for computation types.
Perhaps the most interesting ones are the ones for the type F A. The constructor compu-
tation return V returns the value V , while the sequencing construct M to x : A. N evaluates
M , binds the result to x, and proceeds as N .
Next, there is an operation application computation term:
opV (x : β. M )
This ﬁrst triggers the operation op with parameter V and then binds the outcome to x,
proceeding as the continuation M .
Examples 2.2.
• The computation
read⟨⟩(c : chr. writec(x : 1. writec(y : 1. return ⟨⟩)))
reads a character c, entered by the user, prints it out twice, and returns the uni t value.
• The computation
getl(n : nat. set⟨l,n +1⟩(x : 1. return n))
increments the number x, stored in location l, and returns the old value.
Next, a handler term
{opx : α (k : β → C
) ↦→ Mop}op : α /rightarrowtriangleβ
is given by a ﬁnite set of handling operation deﬁnitions
opx : α (k : β → C) ↦→ Mop
one for each operation symbol op. The handling terms Mop are dependent on their pa-
rameters, captured in their parameter variables x, and on the continuations of the handled

--- PDF page 8 ---
8 G. D. PLOTKIN AND M. PRETNAR
operations, captured in their continuation variables k. Note that continuation variables
never appear independently, only in the form k(V ), where they are applied to a value V .
Examples 2.3.
• The exception handler
{raisee() ↦→ Ne}e∈ exc
given in Section 1, can be written as:
Hexc = {raisey : exc(k : 0 → C
) ↦→ match y with {e(z) ↦→ Ne}e∈ exc}
where y is the raised exception and k is the continuation. As exc = ∑
e∈ exc1, we match
y against all possible cases e(z), where z is a dummy variable of type 1. Note that we
do not use the continuation k in the handling term. Indeed, raise is a nullary operation
symbol and there are no values of type 0 we could feed to the continuation, hence we
cannot use it.
• Even though we cannot modify read-only state, we can still ev aluate a computation with
the state temporarily set to a diﬀerent value. To do so, we use t he temporary-state handler
that is dependent on a variable n : nat; it is
Htemporary = {getl : loc(k : nat → C) ↦→ k(n)}
Finally, we have the handling computation term
M handled with H to x : A. N
This evaluates the computation M , handling all operation application computations accord-
ing to H, binds the result to x and proceeds as N . In more detail, handling works as follows.
Assume that M triggers an operation application opV (y. M ′) and that the corresponding
handling term is opz(k) ↦→ Mop ∈ H. Then, the operation is handled by evaluating Mop
instead, with the parameter variable z bound to V and with each occurrence of a term of
the form k(W ) in Mop replaced by
M ′[W/y ] handled with H to x : A. N
Thus, the continuation k receives an outcome W , determined by Mop, and is handled in
the same way as M . The handling term Mop may use the continuation k any number of
times and the behaviour of the handling construct can be very involved. Note that while
continuations are handled by H, the handling term Mop itself is not. Any operations it
triggers or values it returns escape the handler. They could however be handled by an
enclosing handler.
We remark that sequencing M to x : A. N is equivalent to the special case of handling in
which H handles all operations by themselves: see the discussion of this point in Section 5.
Example 2.4. The simplest use for the handling construct is handling exce ptions. Using
handlers, we would write the computation
M handled with {raisee() ↦→ Ne}e∈ exc to x : A. N
given in Section 1, as
M handled with Hexc to x : A. N
In this case, the behaviour matches the one given by Benton an d Kennedy. If the com-
putation M returns a value V then N [V /x ] is evaluated. If, instead, the computation M
triggers an exception e then the replacement term Ne is evaluated instead; so, if a value is

--- PDF page 9 ---
HANDLING ALGEBRAIC EFFECTS 9
then returned, that value is the ﬁnal result of the entire com putation and is not bound in
N .
Remark 2.5. The syntax of our handling construct diﬀers from that of Bento n and
Kennedy:
try x : A ⇐ M in N unless {e ⇒ Me}e∈ exc
They noted some programming concerns regarding their synta x [2]. In particular, is not
obvious that M is handled but N is not; this is especially the case when N is large and the
handler is obscured. An alternative they propose is:
try x : A ⇐ M unless {e ⇒ Me}e∈ exc in N
but then it is not obvious that x is bound in N but not in the handler. The syntax of our
construct M handled withH to x : A. N addresses both those issues. It also clariﬁes the order
of evaluation: M is handled with H and its results are bound to x and then used in N .
In the next example we use a standard let binding abbreviatio n, deﬁned as follows:
let x : A be V in M def= ( λx : A. M ) V
Example 2.6. Consider the temporary state handler Htemporary given in Example 2.3.
Then the computation
let n : nat be 20 in
getl(x : nat. getl(y : nat. return x + y))
handled with Htemporary to z : A. return z + 2
involving the stateful computation of Example 2.2 evaluate s as follows: the ﬁrst get opera-
tion is handled, with an outcome 20 bound to x; the second get operation in the continuation
is handled in the same way, again with an outcome 20 bound to y; next, return 20 + 20 is
handled with result 40; this is substituted for z in return z + 2, and the ﬁnal result is 42.
As remarked in Section 1.3, the language considered in [21] h as restricted facilities
for deﬁning handlers. In more detail, two levels of language are considered there. In the
ﬁrst there are no handlers, and so no handling constructs. Th e ﬁrst level is used to deﬁne
handlers, which, if they give models, are then used in handli ng constructs in the second
level (which has no facilities for the further deﬁnition of h andlers). This can be considered
a minimal approach in contrast to that considered here, which can rath er be considered
maximal as handlers and handling can be nested arbitrarily deeply. T he advantage of the
maximal approach is that it accommodates all possible ways o f treating the problem of
ensuring that handlers give models.
2.4. Typing judgements. All typing judgements are made in value contexts
Γ = x1 : A1, . . . , x m : Am
of value variables xi bound to value types Ai and continuation contexts
K = k1 : α 1 → C
1, . . . , k n : α n → Cn
of continuation variables kj bound to continuation types α j → Cj. Continuation types have
the form α → C and type continuations k that accept a value of an arity signature type α
and proceed as a computation of type C. Values are typed as Γ |K ⊢ V : A, computations

--- PDF page 10 ---
10 G. D. PLOTKIN AND M. PRETNAR
are typed as Γ |K ⊢ M : C, and handlers are typed as Γ |K ⊢ H : C handler. Values are
typed according to the following rules:
Γ |K ⊢ x : A (x : A ∈ Γ) Γ |K ⊢ V : α
Γ |K ⊢ f(V ) : β (f : α → β ) Γ |K ⊢ ⟨⟩ : 1
Γ |K ⊢ V : A Γ |K ⊢ W : B
Γ |K ⊢ ⟨ V, W ⟩ : A × B
Γ |K ⊢ V : Aℓ
Γ |K ⊢ ℓ(V ) : ∑
ℓ∈ LAℓ
(ℓ ∈ L)
Γ |K ⊢ M : C
Γ |K ⊢ thunk M : U C
Next, computations are typed according to the following rul es:
Γ |K ⊢ V : A × B Γ, x : A, y : B |K ⊢ M : C
Γ |K ⊢ match V with ⟨x, y ⟩ ↦→ M : C
Γ |K ⊢ V : ∑
ℓ∈ LAℓ Γ, x ℓ : Aℓ |K ⊢ Mℓ : C (ℓ ∈ L)
Γ |K ⊢ match V with {ℓ(xℓ) ↦→ Mℓ}ℓ∈ L : C
Γ |K ⊢ V : U C
Γ |K ⊢ force V : C
Γ |K ⊢ V : A
Γ |K ⊢ return V : F A
Γ |K ⊢ M : F A Γ, x : A |K ⊢ N : C
Γ |K ⊢ M to x : A. N : C
Γ |K ⊢ Mℓ : Cℓ (ℓ ∈ L)
Γ |K ⊢ ⟨ Mℓ⟩ℓ∈ L : ∏
ℓ∈ LC ℓ
Γ |K ⊢ M : ∏
ℓ∈ LC ℓ
Γ |K ⊢ prjℓ M : C ℓ
(ℓ ∈ L)
Γ, x : A |K ⊢ M : C
Γ |K ⊢ λx : A. M : A → C
Γ |K ⊢ M : A → C Γ |K ⊢ V : A
Γ |K ⊢ M V : C
Γ |K ⊢ V : α Γ, x : β |K ⊢ M : C
Γ |K ⊢ opV (x : β. M ) : C (op : α /rightarrowtriangleβ ) Γ |K ⊢ V : α
Γ |K ⊢ k(V ) : C (k : α → C ∈ K)
Γ |K ⊢ M : F A Γ |K ⊢ H : C handler Γ, x : A |K ⊢ N : C
Γ |K ⊢ M handled with H to x : A. N : C
Finally, handlers are typed according to the following rule :
Γ, x : α |K, k : β → C ⊢ Mop : C (op : α /rightarrowtriangleβ )
Γ |K ⊢ { opx : α (k : β → C) ↦→ Mop}op : α /rightarrowtriangleβ : C handler
Observe that K may contain more than one continuation variable when the han dler being
deﬁned is used in handling deﬁnitions of other handlers.
2.5. Abbreviations. Before we continue, let us introduce a few abbreviations to h elp make
examples more readable. First, we obtain arbitrary ﬁnite pr oducts from binary products:
A1 × · · · ×An
def= ( A1 × · · · ×An− 1) × An (n ≥ 3)
⟨V1, . . . , V n⟩ def= ⟨⟨V1, . . . , V n− 1⟩, V n⟩ (n ≥ 3)

--- PDF page 11 ---
HANDLING ALGEBRAIC EFFECTS 11
understanding binary product, where n = 2, as before, the unit product, where n = 1 as
simply A1, and the empty product, where n = 0, as 1.
The main use of products is to pass around multiple values as o ne, so we set:
f(V1, . . . , V n) def= f(⟨V1, . . . , V n⟩)
ℓ(V1, . . . , V n) def= ℓ(⟨V1, . . . , V n⟩)
opV1,...,V n(x : β. M ) def= op⟨V1,...,V n⟩(x : β. M )
k(V1, . . . , V n) def= k(⟨V1, . . . , V n⟩)
Further, where possible, we omit empty parentheses in value s and write:
f def= f()
ℓ def= ℓ()
k def= k()
We also adapt the tuple destructor to tuples of arbitrary ﬁni te size. Using this de-
structor, we allow multiple variables in binding construct s such as sequencing or handler
deﬁnitions. For example, we set
opx1,...,x n(k) ↦→ M def= opx(k) ↦→ (match x with ⟨x1, . . . , x n⟩ ↦→ M )
Similarly, we omit empty parentheses in binding constructs that bind no variables. For the
set of booleans, we set:
if V then M else N def= match V with {true ↦→ M, false ↦→ N }
We may use inﬁx or other suitable notation when writing funct ion applications. We may
assume additional function symbols if appropriate deﬁning terms are available. Examples
include logical function symbols, such as or : bool × bool → bool, and relations such as
inequality on locations, ̸=loc : loc × loc → bool.
For operation symbols op : α /rightarrowtrianglen, we deﬁne the usual ﬁnitary operation applications
by:
opV (M1, . . . , M n) def= opV (x : n. match x with {i ↦→ Mi}i∈ n)
and write handling deﬁnitions as:
opx(k1, . . . , k n) ↦→ Mop
where in Mop, we write ki instead of k(i) for 1 ≤ i ≤ n. In particular, we have:
opV () def= opV (x : 0. match x with {})
and the handling deﬁnitions for nullary operations do not co ntain the corresponding con-
tinuation variable. This agrees with the discussion given i n Examples 2.3.
Operation applications can be somewhat cumbersome for writ ing programs, and instead
we may write computations using generic eﬀects [18]. The generic eﬀect corresponding to
an operation symbol op : α /rightarrowtriangleβ is deﬁned by:
op def= λx : α. opx(y : β. return y) : α → F β

--- PDF page 12 ---
12 G. D. PLOTKIN AND M. PRETNAR
Computations
op V to y : β. M and opV (y : β. M )
behave equivalently, in that both ﬁrst trigger the operatio n with parameter V , bind the
outcome to x, and proceed as M .
Example 2.7. The computations of Example 2.2 could be written using gener ic eﬀects as
follows:
read ⟨⟩ to c : chr. write c; write c
and
get l to x : nat. set⟨l, x + 1⟩; return x
Here M ; N is the usual abbreviation for a sequencing M to x : 1. N , where the useless result
of M is bound to a dummy variable x.
When a handler term contains handling terms only for operati on symbols from a sub-
set Θ of the set of operation symbols, we assume that the remai ning operations are handled
by themselves (so they are “passed through”). Such a handler is deﬁned by:
{opx(k) ↦→ Mop}op∈ Θ
def=
{
opx(k) ↦→
{
Mop (op ∈ Θ)
opx(y : β. k (y)) ( op /∈ Θ)
}
op
Sometimes we do not wish to write a value continuation in hand lers. Then, we use the
following abbreviation:
M handled with H def= M handled with H to x : A. return x
which employs a standard value continuation — the identity o ne. This abbreviation can be
considered as a generalisation to arbitrary algebraic eﬀect s of the simple exception handling
construct discussed in Section 1.
There is a diﬀerence between
M handled with H to x : A. N
which is the full handling construct,
(M handled with H) to x : A. N
which takes the result of the handled computation and binds i t to x in N , and
(M to x : A. N ) handled with H
which handles the computation that evaluates M and binds the result to x in N . Both
the ﬁrst and the second computation handle only eﬀects trigge red by M , while the third
computation handles eﬀects triggered by both M and N . Furthermore, in the ﬁrst com-
putation, x binds the value returned by M , while in the second computation, x binds the
value returned by M once handled with H.
Example 2.8. To see the diﬀerence between the ﬁrst two computations and the third, set:
H def= {raisee() ↦→ return 10}
M def= return 5
N def= raisee()

--- PDF page 13 ---
HANDLING ALGEBRAIC EFFECTS 13
Then the ﬁrst two computations raise exception e, while the third one returns 10. To see
the diﬀerence between the second computation and the other tw o, set:
H def= {raisee() ↦→ return 10}
M def= raisee()
N def= return 5
Then the second computation returns 5, while the other two re turn 10.
3. Examples
We now give some more examples to further demonstrate the sco pe of handlers of alge-
braic eﬀects. As before such examples can be understood using the informal operational
understanding of handlers given above. The question of the c orrectness of our examples is
addressed in Remark 4.6, after the notions of eﬀect theories a nd handler correctness have
been presented.
3.1. Explicit Nondeterminism. The evaluation of a nondeterministic computation usu-
ally takes only one of all the possible paths. An alternative is to take all the paths in some
order and allow the possibility of a path’s failing. This kin d of nondeterminism is repre-
sented slightly diﬀerently from binary nondeterminism. In a ddition to the binary operation
symbol choose, we take a nullary operation symbol fail, representing a path that failed. This
interpretation of nondeterminism corresponds to Haskell’ s nondeterminism monad [14].
We consider a handler which extracts the results of a computa tion into a list (which can
then be operated on by other computations). Since our calcul us has no polymorphic lists
— although they could easily be added — we limit ourselves to l ists of a single base type
b. We take a base type listb and function symbols: nil : 1 → listb, cons : b × listb → listb,
append : listb × listb → listb.
Then, all the results of a computation Γ |K ⊢ M : F b can be extracted into a returned
value of type F listb by
Γ |K ⊢ M handled with Hlist to x : b. return cons(x, nil) : F listb
where Hlist is the handler given by:
Γ |K ⊢ {
fail() ↦→ return nil,
choose(k1, k 2) ↦→ k1 to l1 : listb. k 2 to l2 : listb. return append(l1, l 2)
} : F listb handler

--- PDF page 14 ---
14 G. D. PLOTKIN AND M. PRETNAR
3.2. CCS. To represent (the ﬁnitary part of) Milner’s CCS [12] we take a type name
of (channel) names and an equality function symbol = name : name × name → name;
we write lab for a type of labels, abbreviating name+ + name− , and act for a type of
actions, abbreviating 1τ + lablab. We further take three operation symbols representing
combinators which we consider as eﬀect constructors: deadlo ck nil : 1 /rightarrowtriangle0, action preﬁx
preﬁx : act /rightarrowtriangle1, and sum choose : 1 /rightarrowtriangle2. We use the usual notation and write P instead of
M for processes, a.P instead of preﬁxa(P ), and P1 + P2 instead of choose(P1, P 2). Processes
P do not terminate normally, only in deadlock, hence we repres ent them as computations
P : F 0.
We consider the other CCS combinators as eﬀect deconstructor s. Both relabelling and
restriction can be represented using handlers. In order to w rite these handlers we assume
available an equality function symbol = act : act × act → act on actions and a “dual”
function symbol · : lab → lab: both have evident deﬁnitions. We also feel free to omit
evident conversions, from name to label, and from label to act.
Relabelling P [m/l ] replaces all actions with label l (respectively l) in P by actions with
label m (respectively m); it can be represented using the following handler:
Γ, l : lab, m : lab |K ⊢ { a.k ↦→ if a = τ then k else
if a = l then m.k else
if a = l then m.k else a.k }
: F 0 handler
One can deal with more general versions of renaming involvin g ﬁnitely given functions from
labels to labels similarly.
Restriction P \ n blocks all actions with name n in P ; it can be represented using the
following handler:
Γ, n : name |K ⊢ { a.k ↦→ if a = n or a = n then nil() else a.k } : F 0 handler
One can deal with more general versions of restriction invol ving ﬁnite sets of names sim-
ilarly. The two handlers give handling terms only for preﬁx because our convention for
handling omitted operations gives exactly the expected str uctural behaviour of relabelling
and restriction on nil and choose.
We do however, not know how to represent the ﬁnal CCS combinat or — parallel, written
P |Q. Parallel is also an eﬀect deconstructor, but unlike relabel ling and restriction, which
are both unary deconstructors, it is a binary deconstructor as it reacts to actions of both
its arguments. For a discussion of the diﬃculties in the trea tment of such deconstructors,
see [24].
The next few examples concern the use of parameter-passing h andlers. We sometimes
wish to handle diﬀerent occurrences of the same operation diﬀe rently, depending on the
value of some parameter passed between the diﬀerent occurren ces. Although each handler
prescribes a ﬁxed handling term for each operation, we can us e handlers on function types
P → C to obtain C handlers that pass around parameters of value type P . Each handling
term then has the type P → C, rather than C, and captured continuations k have the type
α → (P → C), rather than α → C.
3.3. Interactive input and output (I/O). Suppose we wish to suppress output after a
certain number of characters have been printed out. For any c omputation type C, we deﬁne

--- PDF page 15 ---
HANDLING ALGEBRAIC EFFECTS 15
a character-suppressing handler Hsuppress by:
Γ, n max : nat |K ⊢ {
writec(k : 1 → (nat → C)) ↦→ λn : nat.
if n < n max then writec(k() (n + 1)) else k() nmax
} : nat → C handler
The handling term for write is dependent on n : nat, the number of characters printed out. If
this number is less than nmax, the maximum number of characters we want to print out, we
write out the character and then handle the continuation, bu t now passing an incremented
parameter to it. But if n ≥ nmax, we do not perform the write operation, but continue with
the handled continuation. It does not matter exactly which p arameter we pass to it, as long
as it at least nmax. Still, we call the continuation as it may return a value or tr igger other
operations. (Using a convention introduced above regardin g operations of type α /rightarrowtrianglen, the
deﬁnition of Hsuppress could have been written a little more elegantly, taking k : nat → C,
and so on.)
Since the type of the handler is not of the form F Ahandler, we need to specify a term
for “handling” values in the handling construct, also depen dent on the current value of the
parameter. For example, if we wish to handle M : F A with Hsuppress, we write
Γ |K ⊢ M handled with Hsuppress to x : A. λn : nat. return x : nat → F A
This means than no matter what the value of parameter n is, we return the value x. The
handled computation has type nat → F A and so, in order to obtain a computation of type
F A, we need to apply it to the initial parameter 0 : nat as:
Γ |K ⊢ (M handled with Hsuppress to x : A. λn : nat. return x) 0 : F A
Remark 3.1. In the presence of parameters, the convention of handling om itted operations
by themselves is still valid (this convention was used above for the input operation read).
What one wishes to do in the case of an operation that is not han dled is to pass an unchanged
parameter to the continuation, that is:
opy(k) ↦→ λp : P. opy(x : β. k (x) p)
Since operations are deﬁned pointwise on the function type, this is equivalent to
opy(k) ↦→ opy(x : β. λp : P. k (x) p)
which, by η-equality, is equivalent to
opy(k) ↦→ opy(x : β. k (x))
and is exactly what our convention assumes. The two equaliti es we have used here are
discussed further in Section 5.

--- PDF page 16 ---
16 G. D. PLOTKIN AND M. PRETNAR
3.4. Timeout. The timeout handler furnishes another example of parameter-passing. T his
runs a computation and waits for a given amount of time. If the computation does not
complete in given time, it terminates it, returning a defaul t value x0 instead.
We represent time using a single operation delay : nat /rightarrowtriangle1, where delayt(M ) is a com-
putation that stalls for t units of time, and then proceeds as M . For any value type A, the
timeout handler Htimeout is given by:
Γ, x 0 : A, t wait : nat |K ⊢ {
delayt(k : nat → F A) ↦→ λt spent : nat.
if t + tspent ≤ twait
then delayt(k(t + tspent))
else delaytwait− tspent (return x0)
} : nat → F A handler
(now making use of the above convention introduced regardin g operations op : α /rightarrowtrianglen). The
handler is used on a computation M : F A as follows:
Γ, x 0 : A, t wait : nat |K ⊢ (M handled with Htimeout to x : A. λt : nat. return x) 0 : F A
Note that the handling term preserves the time spent during t he evaluation of the handled
computation.
3.5. Rollback. When a computation raises an exception while modifying the m emory, for
example, when a connection drops halfway through a database transaction, we may want to
revert all modiﬁcations made during the computation. This b ehaviour is termed rollback.
Assuming, for the sake of simplicity, that there is only a sin gle location, which is given
by a term l0, an appropriate rollback handler Hrollback is given by:
Γ, n init : nat |K ⊢ { raisee(k) ↦→ setl0,n init(M ′e)} : C
handler
where M ′: exc → C. To evaluate a computation M , rolling back to the initial state ninit in
the case of exceptions, we write:
Γ |K ⊢ getl0(ninit : nat. M handled with Hrollback) : C
An alternative is a parameter-passing handler, which does n ot modify the memory,
but keeps track of all the changes to the location l0 in the parameter n. Then, once the
handled computation has returned a value, meaning that no ex ceptions have been raised,
the parameter is committed to the memory. This handler Hparam-rollback is:
Γ |K ⊢ {
getl0(k : nat → (nat → C)) ↦→ λn : nat. k (n) n
setl0,n ′(k : 1 → (nat → C)) ↦→ λn : nat. k () n′
raisee() ↦→ λn : nat. M ′e
} : nat → C handler
It is used on a computation M as follows:
getl0(ninit : nat. (M handled with Hparam-rollback to x : A. λn : nat. setl0,n (return x)) ninit)

--- PDF page 17 ---
HANDLING ALGEBRAIC EFFECTS 17
Here the initial state is read, obtaining ninit, which is passed to the handler as the initial
parameter value. If no exception is raised and a value x is returned, the state is updated
to reﬂect the ﬁnal value n of the parameter.
3.6. Stream redirection. Let us conclude with a practical example of processes in a Unix-
like operating system. These processes read their input and write their output through
standard input and output channels. These channels are usua lly connected to a keyboard
and a terminal window. However, an output of one process can b e piped to the input of
another one, allowing multiple simple processes to be combi ned into more powerful ones.
We present a simpliﬁed model of ﬁles and devices. We take a bas e type ﬁle to represent
ﬁles and two operation symbols: read : channel /rightarrowtrianglechr and write : chr× channel /rightarrowtriangle1. Here,
channel is an abbreviation for the sum 1std + ﬁleﬁle, where std represents the standard
input and output channel, and ﬁle( f ) represents the ﬁle f . A more realistic model would
have to include an operation that allows one to open a ﬁle and t hus obtain a needed channel,
and a similar operation for closing an opened ﬁle. In our mode l, one may instead assume
that all ﬁles have already been opened for reading and writin g.
We begin with the redirection p > out which takes the output stream of a process p
and writes it to a ﬁle out. This is used to either automatically generate ﬁles or to log the
activity of processes. The redirection is written as:
Γ |K ⊢ let f : ﬁle be out in p handled with H> : C
where the handler H> is given by:
Γ, f : ﬁle |K ⊢ {
writec,ch (k) ↦→ match ch with {
std ↦→ writec, ﬁle(f )(k),
ﬁle(f ′) ↦→ writec, ﬁle(f ′)(k)
}
} : C handler
Unix allows some devices to present themselves as ordinary ﬁles. This, for example,
allows a program to print out a document by simply redirectin g its output to a ﬁle that
corresponds to the printer. A general treatment of devices i s beyond the scope of this paper,
but some simple devices can be modelled with handlers.
An example is the null device /dev/null, which discards everything that is written to
it. The command p > /dev/null hence eﬀectively suppresses the output of the process p.
The same behaviour can be achieved using the handler H> /dev/null, given by:
Γ |K ⊢ {
writec,ch (k) ↦→ match ch with {
std ↦→ k,
ﬁle(f ′) ↦→ writec, ﬁle(f ′)(k)
}
} : C handler

--- PDF page 18 ---
18 G. D. PLOTKIN AND M. PRETNAR
A similar redirection p < in reads the ﬁle in and passes its contents to the process p.
This redirection can be represented using a handler that now replaces the standard input
with a given ﬁle in all read operations. It is given by:
Γ, f : ﬁle |K ⊢ {
readch(k) ↦→ match ch with {
std ↦→ readﬁle(f )(c : chr. k (c)),
ﬁle(f ′) ↦→ readﬁle(f ′)(c : chr. k (c)))
}
} : C handler
Both redirections can be combined so (p < in) > out reads the input ﬁle and writes the
processed contents to the output ﬁle.
We next consider Unix pipes p1 | p2, where the output of p1 is fed to the input of p2.
Using handlers we can express simple cases of the pipe combin ator |. For example, consider
the pipe yes | p, where the process yes outputs an inﬁnite stream made of a predetermined
character (the default one being y). Such a pipe then gives a way of routinely conﬁrming a
series of actions, for example deleting a large number of ﬁle s. (This is not always the best
way, since processes usually provide a safer means of doing t he same thing, but is often
useful when they do not.) This particular pipe may be written using the following handler:
Γ, c : chr |K ⊢ { readstd(k) ↦→ k(c)} : C handler
The general pipe combinator p1 | p2 is harder to represent, because like the CCS parallel
combinator, and unlike the above redirections and the simpl e pipe, it is a binary rather
than a unary deconstructor.
It would be very interesting to investigate to what extent th e eﬀects supported by Unix
can be realistically modelled using the methods of the algeb raic theory of eﬀects: as well as
ﬁles and devices there are, for example, signals or process s cheduling.
4. Semantics
We now introduce eﬀect theories and their interpretations an d then give the denotational
semantics of our language for handling algebraic eﬀects. For the sake of simplicity, we largely
limit ourselves to sets, but show in Section 7 how everything adapts straightforwardly to
ω -cpos.
4.1. Eﬀect theories. We describe properties of eﬀects with equations between templates T .
These describe the general shape of all computations, regar dless of their type. Assuming a
given signature, templates are given by:
T :: = z(V ) |match V with ⟨x, y ⟩ ↦→ T |match V with {ℓ(xℓ) ↦→ Tℓ}ℓ∈ L |opV (x : β. T )
where z ranges over a given set of template variables .
In templates, we limit ourselves to signature values. These are values that can be typed
as Γ ⊢ V : α , where Γ is a context of value variables bound to signature ty pes; the typing
rules for this judgement are, with the omission of continuat ion contexts and the rule for
typing thunks, the same as those for values.

--- PDF page 19 ---
HANDLING ALGEBRAIC EFFECTS 19
We build templates in a context of value variables, bound to s ignature types, and a
template context
Z = z1 : α 1, . . . , z n : α n
of template variables zj, bound to arity signature types α j. Note: zj : α j does not represent
a value of type α j, but a computation dependent on such a value. As templates de scribe
common properties of computations, we do not assign them a ty pe, but only check whether
they are well-formed, relative to a value context and a templ ate context. The typing
judgement for being a well-formed template is Γ |Z ⊢ T ; it is given by the following rules:
Γ ⊢ V : α
Γ |Z ⊢ z(V ) (z : α ∈ Z) Γ ⊢ V : α × β Γ, x : α, y : β |Z ⊢ T
Γ |Z ⊢ match V with ⟨x, y ⟩ ↦→ T
Γ ⊢ V : ∑
ℓ∈ Lα ℓ Γ, x ℓ : α ℓ |Z ⊢ Tℓ (ℓ ∈ L)
Γ |Z ⊢ match V with {ℓ(xℓ) ↦→ Tℓ}ℓ∈ L
Γ ⊢ V : α Γ, x : β |Z ⊢ T
Γ |Z ⊢ opV (x : β. T ) (op : α /rightarrowtriangleβ )
An eﬀect theory T over a given signature is a ﬁnite set of equations Γ | Z ⊢ T1 = T2,
where T1 and T2 are well-formed relative to Γ and Z.
Some examples follow, continuing on from the corresponding signatures given in Exam-
ple 2.1 and Section 3. We make use of abbreviations for handli ng tuples, etc., in templates
that are analogous to those introduced above for the other ki nds of terms.
Examples 4.1.
Exceptions: The eﬀect theory is the empty set, as exceptions satisfy no non trivial equa-
tions.
State: The eﬀect theory consists of the following equations (for rea dability, we write this
and other theories without contexts):
getl(x. z ) = z
getl(x. setl,x (z)) = z
setl,x (setl,x ′(z)) = setl,x ′(z)
setl,x (getl(x′. z (x′)) = setl,x (z(x))
getl(x. getl(x′. z (x, x ′))) = getl(x. z (x, x ))
setl,x (setl′,x ′(z)) = setl′,x ′(setl,x (z)) ( l ̸= l′)
setl,x (getl′(x′. z (x′)) = getl′(x′. setl,x (z(x′))) ( l ̸= l′)
getl(x. getl′(x′. z (x, x ′))) = getl′(x′. getl(x. z (x, x ′)) ( l ̸= l′)
We found it convenient to write the last three equations with a side condition l ̸= l′. This
still remains within the scope of our deﬁnition of an eﬀect the ory, reading T1 = T2 (l ̸= l′)
as an abbreviation of the equation
T1 = (if l ̸=loc l′ then T2 else T1)
There is some redundancy in these equations, with (see [17, 1 1]) the ﬁrst, ﬁfth, and
eight being consequences of the others for a suitable notion of equational consequence (a

--- PDF page 20 ---
20 G. D. PLOTKIN AND M. PRETNAR
semantic such notion can be provided using the interpretati ons of eﬀect theories deﬁned
in Section 4.2 below).
Read-only state: The eﬀect theory consists of the following equations:
getl(x. z ) = z
getl(x. getl(x′. z (x, x ′))) = getl(x. z (x, x ))
getl(x. getl′(x′. z (x, x ′))) = getl′(x′. getl(x. z (x, x ′)) ( l ̸= l′)
Nondeterminism: The eﬀect theory consists of the following equations:
choose(z, z ) = z
choose(z1, z 2) = choose(z2, z 1)
choose(choose(z1, z 2), z 3) = choose(z1, choose(z2, z 3))
Explicit nondeterminism: The eﬀect theory consists of the following equations:
choose(choose(z1, z 2), z 3) = choose(z1, choose(z2, z 3))
choose(z, nil()) = z
choose(nil(), z ) = z
I/O, Stream redirection: The eﬀect theory is (again) the empty set.
Time: The eﬀect theory consists of the following equations:
delay0(z) = z
delayt1(delayt2(z)) = delayt1+t2(z)
CCS: We take the eﬀect theory for CCS to be the union of the eﬀect theor ies for nonde-
terminism and explicit nondeterminism, i.e., it consists o f the following equations:
choose(z, z ) = z
choose(z1, z 2) = choose(z2, z 1)
choose(choose(z1, z 2), z 3) = choose(z1, choose(z2, z 3))
choose(z, nil()) = z
choose(nil(), z ) = z
These equations are those axiomatising strong bisimulatio n; for weak bisimulation one
would add Milner’s τ laws [12].
Destructive exceptions: The signature of destructive exceptions is that of state tog ether
with that of exceptions, and its theory consists of all the eq uations for state and the
following two equations:
getl(x. raisee()) = raisee()
setl,x (raisee()) = raisee()
As raise is nullary, we have written raisee() instead of raisee(x : 0. T ), emphasising that
the continuation never gets evaluated.

--- PDF page 21 ---
HANDLING ALGEBRAIC EFFECTS 21
The equations imply that a memory operation followed by rais ing an exception is the
same as just raising the exception. Eﬀectively, this implies that all memory operations
are moot if an exception occurs, hence the terminology of des tructive exceptions. Ob-
serve that the ﬁrst equation is an instance of one occurring i n the eﬀect theory for state.
The theory of destructive exceptions is discussed as that of “rollback” in [7], where it is
given as an example of the tensor of two theories.
4.2. Interpreting eﬀect theories. We assume given an eﬀect theory T . We begin by
interpreting each signature type α by a set /llbracketα /rrbracket.
Suppose we are given an assignment /llbracketb/rrbracketof a set to each base type b, such that /llbracketb/rrbracketis
countable in the case that b is an arity base type. Finite products and sums of signature
types are then interpreted using the corresponding set oper ations, and base types are inter-
preted by the assigned sets. Note that /llbracketα /rrbracketis then a countable set for any arity signature
type α ; this will allow us to interpret eﬀects in terms of countable e quational theories.
An interpretation of T then consists of such an assignment together with a map
/llbracketf/rrbracket: /llbracketα /rrbracket→ /llbracketβ /rrbracketfor each function symbol f : α → β . The intended interpretations for each
of our examples should be clear. For instance /llbracketlistb/rrbracketis the set of lists of elements of /llbracketb/rrbracket,
/llbracketappend/rrbracket: /llbracketlistb/rrbracket→ /llbracketlistb/rrbracketis list concatenation, and /llbracketname/rrbracketis a given set with equality
function /llbracket=name/rrbracket:/llbracketname/rrbracket× /llbracketname/rrbracket→ /llbracketname/rrbracket.
We assume given such an interpretation. We can then interpre t each well-typed signa-
ture value Γ ⊢ V : α in an evident way by a map /llbracketV /rrbracket: /llbracketΓ/rrbracket→ /llbracketα /rrbracket, where value contexts Γ
are interpreted component-wise by setting /llbracketx1 : α 1, . . . , x m : α m/rrbracket= /llbracketα 1/rrbracket× · · · ×/llbracketα m/rrbracket.
Take a set |M|and an operation
opM : /llbracketα /rrbracket× |M|/llbracketβ /rrbracket→ |M|
for each operation symbol op : α /rightarrowtriangleβ . Then, we can interpret a well-formed template
Γ | Z ⊢ T by a map /llbracketΓ | Z ⊢ T /rrbracket: /llbracketΓ/rrbracket× /llbracketZ/rrbracket→ |M| , where template contexts Z are
interpreted component-wise by /llbracketz1 : α 1, . . . , z n : α n/rrbracket= |M|/llbracketα 1/rrbracket× · · · × |M|/llbracketα n/rrbracket.
We can then interpret templates:
/llbracketΓ |Z ⊢ zj(V )/rrbracket(a, b ) = pr j(b)(/llbracketV /rrbracket(a)) (1 ≤ j ≤ n)
/llbracketΓ |Z ⊢ match V with ⟨x, y ⟩ ↦→ T /rrbracket(a, b ) = /llbracketT /rrbracket(⟨a, pr1(/llbracketV /rrbracket(a)), pr2(/llbracketV /rrbracket(a))⟩, b )
/llbracketΓ |Z ⊢ match V with {ℓ(xℓ) ↦→ Tℓ}ℓ∈ L/rrbracket(a, b ) = /llbracketTℓ/rrbracket(⟨a, x ⟩, b ) (if /llbracketV /rrbracket(a) = ℓ(x))
/llbracketΓ |Z ⊢ opV (x : β. T )/rrbracket(a, b ) = opM(/llbracketV /rrbracket(a), /llbracketT /rrbracket(⟨a, −⟩ , b ))
where on the right, pr i : A1 × · · · × An → Ai is the i-th projection, and we have abbrevi-
ated evident typing judgements. We also used a standard nota tion for functions, writing
/llbracketT /rrbracket(⟨a, −⟩ , b ) for the function x :/llbracketβ /rrbracket↦→ /llbracketT /rrbracket(⟨a, x ⟩, b ).
Deﬁnition 4.2. A model M of the eﬀect theory T is a set |M|, called the carrier of the
model, together with an operation
opM : /llbracketα /rrbracket× |M|/llbracketβ /rrbracket→ |M|
for each operation symbol op : α /rightarrowtriangleβ , such that /llbracketT1/rrbracket= /llbracketT2/rrbracketholds for all the equations
Γ |Z ⊢ T1 = T2 in T .
A homomorphism h : M1 ⊸ M2 is a map h : |M1| → |M 2| such that
h ◦ opM1 = opM2 ◦ (id/llbracketα /rrbracket× h/llbracketβ /rrbracket)

--- PDF page 22 ---
22 G. D. PLOTKIN AND M. PRETNAR
holds for all operation symbols op : α /rightarrowtriangleβ .
The models of T and the homomorphisms between them form a category ModT . This
category is equipped with a forgetful functor U : ModT → Set, where U M def= |M|and
U h def= h.
Proposition 4.3. The functor U : ModT → Set has a left adjoint F : Set → ModT .
Proof. The existence of the adjoint functor follows from the corres ponding result in the
context of countable equational theories [5].
First we obtain a countable equational theory Tω from T . The signature of Tω consists
of operations opa of arity |/llbracketβ /rrbracket| for each operation symbol op : α /rightarrowtriangleβ and each a ∈ /llbracketα /rrbracket. As
β is an arity signature type, /llbracketβ /rrbracketis a countable set, hence each operation has a countable
arity.
Next, for each well-formed template
Γ |z1 : α 1, . . . , z n : α n ⊢ T
and each c ∈ /llbracketΓ/rrbracket, we get a Tω term Z ′: T c, where the context Z ′ consists of variables za
i
for each 1 ≤ i ≤ n and each a ∈ /llbracketα i/rrbracket. Since all the α i are again arity signature types, the
sets /llbracketα i/rrbracketare countable, and so Z has countably many variables. The term T c is deﬁned
recursively by:
(zi(V ))c = z/llbracketV /rrbracket(c)
i
(match V with ⟨x, y ⟩ ↦→ T )c = T ⟨c, pr1(/llbracketV /rrbracket(c)), pr2(/llbracketV /rrbracket(c))⟩
(match V with {ℓ(xℓ) ↦→ Tℓ}ℓ∈ L)c = T ⟨c,a ⟩
ℓ′ (/llbracketV /rrbracket(c) = in ℓ′(a))
(opV (x : β. T ))c = op/llbracketV /rrbracket(c)(T ⟨c,b ⟩)b∈ /llbracketβ /rrbracket
We then take Tω to be the equational theory generated by all equations Z ′ ⊢ T c
1 = T c
2 ,
where Γ | Z ⊢ T1 = T2 is a T equation and c ∈ /llbracketΓ/rrbracket. It is not diﬃcult to see that the
category of models of Tω is equivalent to ModT , with the equivalence being consistent with
the forgetful functors; the result follows.
The left adjoint F is called the free model functor; for a set A, the model F A is called the
free model over A; and for a map f : A → U M, the adjoint homomorphism f †M : F A ⊸ M
is called the homomorphism induced by f . We write ηA : A → F A for the unit, as usual.
4.3. Interpreting values and computations. We assume given a signature (so that
value and computation terms are determined), an eﬀect theory T over that signature, and
an interpretation of the eﬀect theory (so that the category of models, etc., is determined).
We can then interpret value types A by sets /llbracketA/rrbracketand computation types C by models
/llbracketC/rrbracketof the given eﬀect theory T . Value types are interpreted in the same way as signature
types, except for U C, which is interpreted as the carrier of /llbracketC/rrbracket. The computation type F A
is interpreted by the free model on /llbracketA/rrbracket. Products of computation types are interpreted by
product models and function types are interpreted by expone nt models. These are deﬁned
as follows:
Deﬁnition 4.4. For any family of models Mℓ, where ℓ ranges over the index set L, the
product model ∏
ℓ∈ L Mℓ is the model with carrier ∏
ℓ∈ L |Mℓ| and component-wise deﬁned

--- PDF page 23 ---
HANDLING ALGEBRAIC EFFECTS 23
operations. For any model M and any set A, the exponent model MA is the model with
carrier |M|A and pointwise deﬁned operations.
Contexts are again interpreted component-wise, with Γ = x1 : A1, . . . , x m : Am being
interpreted by /llbracketΓ/rrbracket=def /llbracketA1/rrbracket× · · · × /llbracketAm/rrbracket, and K = k1 : β1 → C1, . . . , k n : βn → C n being
interpreted by /llbracketK/rrbracket=def U /llbracketC1/rrbracket/llbracketβ 1/rrbracket× · · · ×U /llbracketCn/rrbracket/llbracketβ n/rrbracket.
We would like to interpret well-typed value terms Γ |K ⊢ V : A by maps of the form
/llbracketΓ |K ⊢ V : A/rrbracket: /llbracketΓ/rrbracket× /llbracketK/rrbracket→ /llbracketA/rrbracket
well-typed computation terms Γ |K ⊢ M : C by maps of the form
/llbracketΓ |K ⊢ M : C/rrbracket: /llbracketΓ/rrbracket× /llbracketK/rrbracket→ U /llbracketC/rrbracket
and well-typed handler terms Γ |K ⊢ H : C handler by maps of the form
/llbracketΓ |K ⊢ H : C handler/rrbracket: /llbracketΓ/rrbracket× /llbracketK/rrbracket→
∏
op : α /rightarrowtriangleβ
/llbracketα /rrbracket× U /llbracketC/rrbracket/llbracketβ /rrbracket→ U /llbracketC/rrbracket
such that, for every c ∈ /llbracketΓ/rrbracket× /llbracketK/rrbracket, the set U /llbracketC/rrbracket, together with the operations pr op(/llbracketH/rrbracket(c)),
forms a model of T .
However, as discussed informally above, not all handlers re ceive an interpretation. As
handler terms occur in computation terms and computation te rms occur in value terms,
computation terms and value terms may also not receive an int erpretation. So, instead, we
interpret well-typed value terms Γ |K ⊢ V : A by partial maps
/llbracketΓ |K ⊢ V : A/rrbracket: /llbracketΓ/rrbracket× /llbracketK/rrbracket⇀ /llbracketA/rrbracket
on /llbracketΓ/rrbracket× /llbracketK/rrbracket, and similarly for computation and handler terms.
Fix contexts Γ = x1 : A1, . . . , x m : Am and K = k1 : β1 → C 1, . . . , k n : βn → Cn. Value
terms are interpreted as follows:
/llbracketΓ |K ⊢ xi : Ai/rrbracket(a, b ) ≃ pri(a) (1 ≤ i ≤ m)
/llbracketΓ |K ⊢ f(V ) : β /rrbracket(c) ≃ /llbracketf/rrbracket(/llbracketV /rrbracket(c)) ( f : α → β )
/llbracketΓ |K ⊢ ⟨⟩ : 1/rrbracket(c) ≃ ∗
/llbracketΓ |K ⊢ ⟨ V, W ⟩ : A × B/rrbracket(c) ≃ ⟨ /llbracketV /rrbracket(c), /llbracketW /rrbracket(c)⟩
/llbracketΓ |K ⊢ ℓ(V ) : ∑
ℓ∈ LAℓ/rrbracket(c) ≃ inℓ(/llbracketV /rrbracket(c))
/llbracketΓ |K ⊢ thunk M : U C/rrbracket(c) ≃ /llbracketM /rrbracket(c)
In the above, we have used Kleene equality e ≃ e′ which holds if either both the expressions
e and e′ are deﬁned and equal, or both are undeﬁned. Further, typing j udgements are
abbreviated on the right-hand side, ∗ is the unique element of the one-element set /BD and
inℓ : Aℓ → ∑
ℓ∈ L Aℓ is the ℓ-th injection into a disjoint sum.

--- PDF page 24 ---
24 G. D. PLOTKIN AND M. PRETNAR
Next, computation terms, other than the handling construct (for which see below) are
interpreted as follows:
/llbracketΓ |K ⊢ match V with ⟨x, y ⟩ ↦→ M : C
/rrbracket(a, b )
≃
{
/llbracketM /rrbracket(⟨a, pr1(/llbracketV /rrbracket(a, b )), pr2(/llbracketV /rrbracket(a, b ))⟩, b ) (if ∀x ∈ /llbracketA/rrbracket, y ∈ /llbracketB/rrbracket. /llbracketM /rrbracket(⟨a, x, y ⟩, b ) ↓)
undeﬁned (otherwise)
(where Γ |K ⊢ V : A × B and Γ , x : A, y : B |K ⊢ M : C)
/llbracketΓ |K ⊢ match V with {ℓ(xℓ) ↦→ Mℓ}ℓ∈ L : C/rrbracket(c)
≃
{
/llbracketMℓ(⟨a, x ⟩, b )/rrbracket(if /llbracketV /rrbracket(a, b ) ≃ ℓ(x) and ∀ℓ′ ∈ L, x ℓ′ ∈ /llbracketAℓ′/rrbracket. /llbracketMℓ′/rrbracket(⟨a, x ℓ′⟩, b ) ↓)
undeﬁned (otherwise)
(where Γ |K ⊢ V : ∑
ℓ∈ LAℓ and Γ , x ℓ : Aℓ |K ⊢ Mℓ : C, for ℓ ∈ L)
/llbracketΓ |K ⊢ force V : C/rrbracket(c) ≃ /llbracketV /rrbracket(c)
/llbracketΓ |K ⊢ return V : F A/rrbracket(c) ≃ ηA(/llbracketV /rrbracket(c))
/llbracketΓ |K ⊢ M to x : A. N : C/rrbracket(a, b )
≃
{
U (/llbracketN /rrbracket(⟨a, −⟩ , b )†/llbracketC /rrbracket)(/llbracketM /rrbracket(a, b )) (if ∀x ∈ /llbracketA/rrbracket. /llbracketN /rrbracket(⟨a, x ⟩, b ) ↓)
undeﬁned (otherwise)
/llbracketΓ |K ⊢ ⟨ Mℓ⟩ℓ∈ L : ∏
ℓ∈ LC ℓ/rrbracket(c) ≃ ⟨ /llbracketMℓ/rrbracket(c)⟩ℓ∈ L
/llbracketΓ |K ⊢ prjℓ M : Cℓ/rrbracket(c) ≃ prℓ(/llbracketM /rrbracket(c))
/llbracketΓ |K ⊢ λx : A. M : A → C/rrbracket(a, b )
≃
{
/llbracketM /rrbracket(⟨a, −⟩ , b ) (if ∀x ∈ /llbracketA/rrbracket. /llbracketM /rrbracket(⟨a, x ⟩, b ) ↓)
undeﬁned (otherwise)
/llbracketΓ |K ⊢ M V : C/rrbracket(c) ≃ /llbracketM /rrbracket(c)(/llbracketV /rrbracket(c))
/llbracketΓ |K ⊢ opV (x : β. M ) : C/rrbracket(a, b )
≃
{
op/llbracketC/rrbracket(/llbracketV /rrbracket(a, b ), /llbracketM /rrbracket(⟨a, −⟩ , b )) (if ∀x ∈ /llbracketβ /rrbracket. /llbracketM /rrbracket(⟨a, x ⟩, b ) ↓)
undeﬁned (otherwise)
/llbracketΓ |K ⊢ kj(V ) : C i/rrbracket(a, b ) ≃ prj(b)(/llbracketV /rrbracket(a, b )) (1 ≤ j ≤ n)
In the above, we have used existence assertions e ↓ which hold if, and only if, the expression e
is deﬁned. Typing judgements are again abbreviated on the ri ght-hand side and we used
the evident tupling and projections associated to labelled products.
Consider a handler Γ |K ⊢ H : C handler where
H = {opx : α (k : β → C) ↦→ Mop}op : α /rightarrowtriangleβ
The interpretations of the handling terms Mop in H yield partial maps
/llbracketΓ, x : α |K, k : β → C ⊢ Mop : C/rrbracket: ( /llbracketΓ/rrbracket× /llbracketα /rrbracket) × (/llbracketK/rrbracket× U /llbracketC/rrbracket/llbracketβ /rrbracket) ⇀ U /llbracketC/rrbracket

--- PDF page 25 ---
HANDLING ALGEBRAIC EFFECTS 25
and by suitable rearrangement and transposition, one obtai ns (total) maps
opH : /llbracketΓ/rrbracket× /llbracketK/rrbracket→ (/llbracketα /rrbracket× U /llbracketC/rrbracket/llbracketβ /rrbracket⇀ U /llbracketC/rrbracket)
Hence, for any c ∈ /llbracketΓ/rrbracket× /llbracketK/rrbracket, we obtain partial maps opH(c) on U /llbracketC/rrbracket.
Deﬁnition 4.5. A handler Γ |K ⊢ H : C handler is correct at c if the opH(c) are all total
and the set U /llbracketC/rrbrackettogether with the opH (c) forms a model of T . The handler is correct if
it is correct at all c.
We can then give the interpretation of handlers:
/llbracketΓ |K ⊢ H : C handler/rrbracket(c) ≃
{
⟨opH(c)⟩op (if Γ |K ⊢ H : C handler is correct at c)
undeﬁned (otherwise)
and the interpretation of the handling construct follows th e lines discussed informally above:
/llbracketΓ |K ⊢ M handled with H to x : A. N : C/rrbracket(a, b )
≃



U (/llbracketN /rrbracket(⟨a, −⟩ , b )†⟨U /llbracketC
/rrbracket,/llbracketH/rrbracket(a,b)⟩)(/llbracketM /rrbracket(a, b )) ( ∀x ∈ /llbracketA/rrbracket. /llbracketN /rrbracket(⟨a, x ⟩, b ) ↓
and H is correct at ⟨a, b ⟩)
undeﬁned (otherwise)
The existence conditions adopted above for the existence of term denotations are quite
strict. For example, in the case of the ﬁrst matching constru ct one might instead have
written:
/llbracketΓ |K ⊢ match V with ⟨x, y ⟩ ↦→ M : C/rrbracket(a, b ) ≃ /llbracketM /rrbracket(⟨a, pr1(/llbracketV /rrbracket(a, b )), pr2(/llbracketV /rrbracket(a, b ))⟩, b )
and there are similar alternatives for terms of any of the for ms matchV with{ℓ(xℓ) ↦→ Mℓ}ℓ∈ L
or M to x : A. N or M handled with H to x : A. N . Our choices followed the principle that
the denotation of a term should exist if, and only, that of all its subterms exist (taking due
account of variable binding occurrences).
The correctness of handlers is clearly of central concern. I f the eﬀect theory is empty,
then any handler is correct, but, in general — and unsurprisi ngly — correctness is undecid-
able. Indeed, as will be discussed in Section 6, even the deci dability of the correctness of
very simple handlers is a Π 2-complete problem.
Remark 4.6. All the handlers, given in Examples 2.3 and Section 3 are corr ect. In partic-
ular, the exception handler and the stream redirection hand lers are trivially correct as the
corresponding eﬀect theories are empty. Also, the CCS relabe lling and restriction handlers
are correct for both the weak and the strong bisimulation the ories.
Both the rollback handlers are also correct in the presence o f destructive exceptions,
given in Examples 4.1. The standard exception handler, howe ver, is not correct for de-
structive exceptions, as it intercepts an exception and pro vides a replacement computation
instead, but does not correct any modiﬁcations made to the st ate. In particular, the handler
does not respect the equation
setl,x (raisee()) = raisee()
Another possible approach that may well be worth investigat ing to giving our language
a denotational semantics would be to consider only terms tha t are guaranteed to receive an
interpretation under suitable assumptions. One could intr oduce conditional judgements of
the form Γ |K |Φ ⊢ V : A (and similarly for computations and handlers), where Φ is a l ist
of universally quantiﬁed equations guaranteeing that hand lers in V respect the equations of

--- PDF page 26 ---
26 G. D. PLOTKIN AND M. PRETNAR
the eﬀect theory. One would also have a logic for establishing the assertions (the logic for
algebraic eﬀects of [20] should be helpful here). Thus the typ ing judgement and the logic
would be integrated in a way somewhat reminiscent of Martin- L¨ of type theory.
5. Reasoning about handlers
The semantics we have just given allows us to reason about han dlers. We are interested in
two questions in particular: which computations are equal, and which handlers are correct?
We content ourselves in this section with some initial obser vations, and do not attempt to
provide a full-ﬂedged logic for algebraic eﬀects and handler s.
We ﬁx a signature, and an eﬀect theory T and its interpretation, and consider as
well-formed formulas Γ | K ⊢ ϕ those that can be built up from atomic formulas by the
usual boolean connectives and universal and existential qu antiﬁcation over value types, e.g.,
∀x : A. ϕ , and continuations, e.g., ∀k : α → C. ϕ . As atomic formulas we take existence
(or deﬁnedness) and Kleene equality assertions for value te rms, V ↓ and V ≃ W (with
evident well-formedness conditions), and for computation and handler terms. In the case
of a handler H the existence assertion H ↓ can equivalently be understood as that of the
correctness of H. There is a convenient “Kleene inequation” V ≲ W , which abbreviates
V ↓ ⇒ V ≃ W (and similarly for computation and handler terms).
There is an evident inductively deﬁned satisfaction relati on c |=Γ|K ϕ for c ∈ /llbracketΓ/rrbracket× /llbracketK/rrbracket
and ϕ well-formed relative to Γ and K. In particular c |=Γ|K V ↓ holds if, and only if,
/llbracketV /rrbracket(c) ↓ and c |=Γ|K V ≃ W holds if, and only if, /llbracketV /rrbracket(c) ≃ /llbracketW /rrbracket(c), and similarly for
computation and handler terms. We say that |=Γ|K ϕ holds if, and only if c |=Γ|K ϕ holds
for all c ∈ /llbracketΓ/rrbracket× /llbracketK/rrbracket; below we generally just say that ϕ holds, and the intended contexts
Γ and K will be clear.
We ﬁrst consider equations. Versions of Levy’s call-by-pus h-value equations [9] hold.
For example we have a Kleene β -inequality for the sequencing construct:
return x to x : A. M ≲ M
and a Kleene η-equality for functions:
λx : A. M x ≃ M
Next, we have equations that describe the component-wise an d pointwise behaviour of
operations on product and function types, respectively:
opV (x : α. ⟨Mℓ⟩ℓ∈ L) ≃ ⟨ opV (x : α. M ℓ)⟩ℓ∈ L : ∏
ℓ∈ LC ℓ
opV (x : α. λy : A. M ) ≃ λy : A. opV (x : α. M ) : A → C
We also have an equation giving the commutativity between op erations and sequencing:
opV (x : α. M ) to y : A. N ≃ opV (x : α. M to y : A. N ) : C
This equation holds as sequencing is deﬁned using the homomo rphism induced by the uni-
versality of the free model, thus it maps an operation on F A to an operation on C. The
equations given by Levy [9] for two speciﬁc operations: prin ting and divergence (divergence
is considered in Section 7), are instances of the above three equations.
Further equations are inherited from from the eﬀect theory T . Given a a template
x1 : α 1, . . . , x m : α m |z1 : β1, . . . , z n : βn ⊢ T

--- PDF page 27 ---
HANDLING ALGEBRAIC EFFECTS 27
and distinct continuation variables k1, . . . , k n we write T [k1/z 1, . . . , k n/z n] for the computa-
tion term obtained by replacing each occurrence of a zi in T by one of the corresponding ki.
If Γ contains the type assignments x1 : α 1, . . . , x m : α m and K contains the type assignments
k1 : β1 → C, . . . , k n : βn → C then Γ |K ⊢ T [k1/z 1, . . . , k n/z n] : C holds. We then have that
T1[k1/z 1, . . . , k n/z n] ≃ T2[k1/z 1, . . . , k n/z n]
holds for every equation
x1 : α 1, . . . , x m : α m |z1 : β1, . . . , z n : βn ⊢ T1 = T2
in T and for every type assignment k1 : β1 → C, . . . , k n : βn → C.
We next give two Kleene inequations for the handling constru ct that state the universal
properties of the induced homomorphism: it extends the indu cing map on values, and it
acts homomorphically on operations. For any handler H = {opy(k) ↦→ Mop}op : α /rightarrowtriangleβ , the
inequations are:
return x handled with H to x : A. N ≲ N
opy(x′: β. M ) handled with H to x : A. N ≲ Mop[x′: β. M handled with H to x : A. N/k ]
where substitutions of the form x′: β. M ′/k replace each occurrence of a computation k(V )
by M ′[V /x ′] — recall that continuation variables always appear only in the form k(V ).
As mentioned in Section 2.3, we have:
M to x : A. N ≃ M handled with {} to x : A. N
stating that sequencing is equivalent to the special case of handling in which all operations
are handled by themselves. One can then observe that (given t he existence assertions
below) the two handler inequations generalise the formulas given above for the sequencing
construct, viz. the β -inequality and the commutativity of operations and sequen cing.
There are other equations for exception handlers, given by B enton and Kennedy [2],
and Levy [10], and one would wish to generalise these to our ge neral handlers. It turns out
that these equations fail for general handlers, but do hold f or particular classes of handlers,
see: [22]. We do not consider this issue further here.
We next consider existence assertions. They all spell out th e conditions for term inter-
pretations to exist. For example the following hold:
return V ↓ ⇔ V ↓
M to x : A. N ↓ ⇔ M ↓ ∧ ∀ x : A. N ↓
λx : A. M ↓ ⇔ ∀ x : A. M ↓
opV (x : α. M ) ↓ ⇔ V ↓ ∧ ∀ x : α. M ↓
Turning to handler existence, or correctness, we ﬁrst need t o be able to replace oper-
ations in templates by their corresponding deﬁnitions in ha ndlers. So, let us consider a
handler Γ |K ⊢ H : C handler, where H is {opx : α (k : β → C) ↦→ Mop}op : α /rightarrowtriangleβ , and a tem-
plate variable context Z. Let K ′ be the continuation context with a continuation variable
kz : α → C for each template variable z : α ∈ Z. Then, for every template term Γ ′ |Z ⊢ T

--- PDF page 28 ---
28 G. D. PLOTKIN AND M. PRETNAR
we recursively deﬁne a computation term Γ , Γ′ |K, K ′ ⊢ T H : C by:
z(V )H = kz(V )
(match V with ⟨x1, x 2⟩ ↦→ T )H = match V with ⟨x1, x 2⟩ ↦→ T H
(match V with {ℓ(xℓ) ↦→ Tℓ}ℓ∈ L)H = match V with {ℓ(xℓ) ↦→ T H
ℓ }ℓ∈ L
opV (y : β. T )H = Mop[V /x, y : β. T H/k ]
The following then holds:
H ↓ ⇔
⋀
{∀x : α. M op ↓ | op : α /rightarrowtriangleβ } ∧
⋀
{T H
1 ≃ T H
2 | Γ′ |Z ⊢ T1 = T2 ∈ T }
asserting that a handler is correct when its operations are d eﬁned and respect the equations
of the eﬀect theory. In particular, {} ↓ holds, where {} is the empty handler, the one deﬁning
all operations by themselves. Regarding the handling const ruct itself, the following holds:
M handled with H to x : A. N ↓ ⇔ M ↓ ∧ H ↓ ∧ ∀ x : A. N ↓
6. Deciding handler correctness
The decidability of handler correctness is an interesting q uestion and one potentially per-
tinent for compiler writers. We now give some results on the d ecidability of some natural
classes of handlers; their proofs are given in Appendix A.
Deﬁnition 6.1. A handler Γ |K ⊢ H : C
handler over a given signature is simple if
• up to reordering, Γ has the form
x1 : α 1, . . . , x m : α m, f 1 : U (β1 → C), . . . , f n : U (βn → C)
• up to reordering, K has the form
k′
1 : β ′
1 → C, . . . , k ′
p : β ′
p → C
and
• for each op : α /rightarrowtriangleβ , there is a template
x1 : α 1, . . . , x m : α m, x : α |z1 : β1, . . . , z n : βn, z ′
1 : β ′
1, . . . , z ′
p : β ′
p, z : β ⊢ Top
such that the handling term
Γ, x : α |K, k : β → C ⊢ Mop : C
is obtained by the substitution of Top that replaces each xi by itself, x by itself, each zj
by ( yj : βj. (force fj)yj), each z′
l by ( y′
l : β ′
l. k ′
l(y′
l)), and z by ( y : β. k (y)).
In essence, simple handlers deﬁne handling computations in terms of algebraic constructs
only. The temporary-state handler, the CCS relabelling and restriction handlers, and stream
redirection handlers are all simple; none of the parameter- passing handlers are simple as
they all contain lambda abstractions in their handling term s. The exception handler
{raisey : exc(k : 0 → C) ↦→ match y with {e(z) ↦→ Ne}e∈ exc}
is also not simple, as computation terms Ne may be arbitrary. One can instead use the
simple handler
f : U (exc → C) ⊢ { raisee : exc(k : 0 → C) ↦→ (force f ) e}

--- PDF page 29 ---
HANDLING ALGEBRAIC EFFECTS 29
Then, letting f be the thunk of
λy : exc. match y with {e(z) ↦→ Ne}e∈ exc
one obtains exactly the same behaviour.
A signature is simple if it has no base types or function symbols. In that case there is
a unique interpretation, the trivial one, and we will omit me ntion of it. Simple signatures
and theories are equivalent to ones in which all operation sy mbols are n-ary for some n.
The signatures given above for exceptions (over a ﬁnite set) , read-only state, I/O, and
nondeterminism are all simple, but none of the others are.
Theorem 6.2. The problem of deciding, given a simple signature, eﬀect theo ry, and closed
simple handler ⊢ H : F 0 handler over the given signature, whether the handler is correct,
is Π2-complete.
The polymorphic nature of template variables means that a te mplate can deﬁne a whole
family of handlers, one for each computation type. This lead s one to the deﬁnition of a
uniformly simple family of handlers:
Deﬁnition 6.3. A family of handlers {ΓC
|KC ⊢ HC : C handler}C over a given signature,
where C ranges over all computation types, is uniformly simple if
• up to reordering, Γ C is
x1 : α 1, . . . , x m : α m, f 1 : U (β1 → C), . . . , f n : U (βn → C)
• up to reordering, KC is
k′
1 : β ′
1 → C, . . . , k ′
p : β ′
p → C
and
• for each op : α /rightarrowtriangleβ , there is a template
x1 : α 1, . . . , x m : α m, x : α |z1 : β1, . . . , z n : βn, z ′
1 : β ′
1, . . . , z ′
p : β ′
p, z : β ⊢ Top
such that for each computation type C, the handling term
ΓC, x : α |KC, k : β → C ⊢ Mop : C
is obtained by the substitution of Top that replaces each xi by itself, x by itself, each zj
by ( yj : βj. (force fj)yj), each z′
l by ( y′
l : β ′
l. k ′
l(y′
l)), and z by ( y : β. k (y)).
If a family of handlers is uniformly simple then, as is eviden t, any member of the family
is simple. Conversely, because of the polymorphic nature of templates, any simple handler
can be generalised to obtain a uniformly simple one. Of the si mple handlers given above,
the temporary-state handler, the stream redirection handl ers, and the alternative exception
handler are also uniformly simple; the CCS relabelling and r estriction handlers are not as
we deﬁne them only for the computation type F 0.
Note that a uniformly simple family of handlers is determine d by the various variables,
types and template terms discussed in the above deﬁnition. A s there are ﬁnitely many of
these altogether, uniformly simple families of handlers ca n be ﬁnitely presented and so it is
proper to ask decidability questions about them.
Deﬁnition 6.4. A family of handlers {ΓC
|KC ⊢ HC : C handler}C over a given signature,
where C ranges over all computation types, is correct if each handler in the family is correct.

--- PDF page 30 ---
30 G. D. PLOTKIN AND M. PRETNAR
Since a uniformly simple family of handlers cannot use prope rties of a speciﬁc compu-
tation type, it cannot be as contrived as an arbitrary family of handlers; we may therefore
expect it to be easier to decide its correctness. As we now see , correctness can become
semidecidable:
Theorem 6.5. The problem of deciding, given a simple signature, eﬀect theo ry, and a
uniformly simple family of closed handlers {⊢ HC : C handler}C over the given signature,
whether the family of handlers is correct, is Σ1-complete.
Eﬀect theories with a simple signature correspond (see the pr oof of Proposition 4.3) to
ordinary ﬁnite equational theories, i.e., those with ﬁnite ly many ﬁnitary function symbols
and ﬁnitely many axioms. Consequently notions such as decid ability can be transferred to
them. (All the above examples of simple signatures and theor ies are decidable in this sense.)
With this understanding, we have:
Theorem 6.6. The problem of deciding, given a simple signature, decidable eﬀect theory,
and a uniformly simple family of closed handlers {⊢ HC : C handler}C over the given
signature, whether the family of handlers is correct, is dec idable.
7. Recursion
We now sketch some changes to the syntax and semantics that en able the incorporation of
recursion. We begin with syntax. Signatures are as before ex cept that we always assume an
additional operation symbol div : 1 /rightarrowtriangle0 representing nontermination. To provide a recursion
facility, we follow [9] and add a ﬁxed-point constructor rec x : U C. M , typed as follows:
Γ, x : U C |K ⊢ M : C
Γ |K ⊢ rec x : U C. M : C
Turning to semantics, templates are deﬁned as before, but eﬀe ct theories now consist
of inequations Γ | Z ⊢ T1 ≤ T2 rather than equations. We always assume that the eﬀect
theory contains the inequation · |z : 1 ⊢ div() ≤ z, stating that div is the least element. As
examples one can take those of Section 4 with this additional inequation, reading equations
as conjunctions of two inequations; see [7] for further exam ples.
In order to interpret recursion, we move, as remarked above, from the category of
sets to ω Cpo, the category of ω -cpos and continuous functions. First we assume given an
assignment of ω -cpos /llbracketb/rrbracketto base types b, such that /llbracketb/rrbracketis ﬂat and countable in the case
that b is an arity base type; this yields interpretations of signat ure types α as ω -cpos /llbracketα /rrbracket,
which are ﬂat and countable in the case of arity signature typ es. (A ω -cpo is ﬂat if no two
distinct elements are comparable.) An interpretation of an eﬀect theory then consists of
such an assignment and an assignment /llbracketf/rrbracket: /llbracketα /rrbracket→ /llbracketβ /rrbracketof continuous functions to function
symbols f : α → β , and well-formed base values Γ ⊢ V : α are interpreted as continuous
functions /llbracketV /rrbracket: /llbracketΓ/rrbracket→ /llbracketα /rrbracketmuch as before.
Similarly, given an ω -cpo |M|together with operations, that is, continuous functions
opM : /llbracketα /rrbracket× |M|/llbracketβ /rrbracket→ |M| , for each operation symbol op : α /rightarrowtriangleβ , well-formed templates
Γ |Z ⊢ T can be interpreted as continuous functions /llbracketT /rrbracket: /llbracketΓ/rrbracket× /llbracketZ/rrbracket→ |M| .
With that, one can deﬁne models M of eﬀect theories T as ω -cpos together with
operations for each operation symbol such that such that /llbracketT1/rrbracket≤ /llbracketT2/rrbracketholds for all the
inequations Γ |Z ⊢ T1 ≤ T2 in T . Deﬁning homomorphisms in the evident way, one obtains

--- PDF page 31 ---
HANDLING ALGEBRAIC EFFECTS 31
a category of models ModT , equipped with a forgetful functor U : ModT → ω Cpo. As we
discuss below, the forgetful functor has a left adjoint F : ω Cpo → ModT . Both functors
have continuous strengths and are locally continuous, by wh ich is meant that they act
continuously on the hom ω -cpos. It is also important to note that the unique continuou s
homomorphism f †M : F A ⊸ M induced by a continuous map f : A → U M is itself a
continuous function of f . As eﬀect theories assume least elements, carriers of models are
pointed ω -cpos (i.e., ω -cpos with a least element) and homomorphisms are strict con tinuous
functions (i.e., continuous functions preserving least el ements).
The monads U F obtained in this way are the standard ones that occur in seman tics. For
example, the monad obtained from the theory for exceptions t ogether with a least element
is P ↦→ (P + exc)⊥ , where the lifting Q⊥ is the ω -cpo obtained from Q by adding a new
least element; the monad for nondeterminism, together with a least element, is P ↦→ P (P⊥ ),
where P is the convex powerdomain monad; and the monad for state, tog ether with a least
element and inequations sating that the state operations ar e strict, is P ↦→ (S × P )S
⊥ . See [7]
for further discussion and references.
To show the existence of the free model functor and its proper ties, one can proceed
along related lines to before. One again has families opa of operations of countable arity,
but now parameterised by elements a of given ω -cpos P , and one considers inequations
instead of equations between the resulting inﬁnitary terms . Operations are interpreted by
continuous functions varying continuously over the parame ter ω -cpos; this corresponds to
the fact that one is working with continuous functions opM : /llbracketα /rrbracket× |M|/llbracketβ /rrbracket→ |M| with /llbracketα /rrbracket
an ω -cpo.
One thereby obtains continuously parameterised countably inﬁnitary inequational theo-
ries; these can be shown to be equivalent to the discrete coun table Lawvere ω Cpo-theories:
see [8], and see too [15] for further discussion of parameter ised equational logic. The exis-
tence of the free model functor and its continuity propertie s follow from the fact that ω Cpo
is locally countably presentable as a cartesian closed cate gory (see [8]).
Value types are now interpreted by ω -cpos and computation types are interpreted by
models whose carriers are pointed ω -cpos, again making use of product and exponentiation
models. Values, computations (including the handling cons truct) and handlers are inter-
preted by partial maps analogously to before, except that as well as requiring functions be
total, one requires that they are also continuous.
The ﬁxed-point constructor is interpreted using the usual l east ﬁxed-point interpreta-
tion:
/llbracketΓ |K ⊢ rec x : U C
. M : C/rrbracket(a, b )
≃
{
µx :/llbracketU C/rrbracket. /llbracketM /rrbracket(⟨a, x ⟩, b ) ( ∀x ∈ /llbracketU C/rrbracket. /llbracketM /rrbracket(⟨a, x ⟩, b ) ↓)
undeﬁned (otherwise)
where, as usual, µx : P. f (x) is the least ﬁxed-point of a continuous function f on a pointed
ω -cpo P .
One can show by structural induction that the sets of element s of /llbracketΓ/rrbracket× /llbracketK/rrbracketat which
the denotations /llbracketΓ |K ⊢ V : A/rrbracketof value terms exist form a sub-cpo of /llbracketΓ/rrbracket× /llbracketK/rrbracketand that
the denotations are continuous when restricted to that sub- cpo, and, further, that the same
holds for computation terms and handlers. (One may then obse rve that, as a consequence,
the above continuity requirement is redundant.)

--- PDF page 32 ---
32 G. D. PLOTKIN AND M. PRETNAR
Correct handlers cannot redeﬁne div as the theory of div ﬁxes it uniquely. The handlers
given in the various examples above, as detailed in Remark 4. 6, remain correct in the
presence of recursion, understanding them as deﬁning div by itself (and with the addition
of the inequation stating that div is the least element to the corresponding theories).
Conclusion
The current work opens some immediate questions. The most im portant is how to simulta-
neously handle two computations to describe parallel combi nators, e.g., that of CCS or the
Unix pipe combinator. Understanding this would bring paralleli sm within the ambit of the
algebraic theory of eﬀects.
Next, the logical ideas of Section 5 should be worked out more fully and merged with
the general logic for algebraic eﬀects [20, 22]. There is a clo se correspondence between the
handling construct and the free model principle in the logic , which should be examined in
detail.
It would be worthwhile to extend the results of Section 6 furt her, whether to wider
classes of signatures, handlers, or theories. One would als o like to have analogous results
for (the interpretation over) ω -cpos.
Only correct handlers can be interpreted, but, as we have see n, obtaining mechanisms
that ensure correctness is hard. One option would be to drop e quations altogether, when
handlers are interpreted as models of absolutely free theor ies (i.e., those with no equations).
This would be correctly implementable if one removed the con nection with the equalities
expected for the real eﬀects between handled computations.
More routinely, perhaps, the work done on combinations of eﬀe cts in [7] should be ex-
tended to combinations of handlers, and there should be a gen eral operational semantics [16]
which includes that of Benton and Kennedy [2].
In so far as possible, one would like to work in a general categ orical setting as regards
both the denotational semantics and the logic. Considering only the denotational semantics,
it may be possible to generalise the above semantics to work o ver any category V that is
locally countably presentable as a cartesian closed catego ry. Presumably one would use a
more abstract notion of the eﬀect theories of Section 4 above, based on Lawvere V-theories
(see [19]) or, perhaps, just discrete Lawvere V-theories [8]. As one needs to deal with
partiality when handlers are incorrect, one would require a suitable factorisation system.
Finally, one should develop the programming language aspec ts further. For example,
while call-by-push-value serves well as a fundamental calc ulus and as an intermediate lan-
guage, it may be more realistic, or at least more in accordanc e with current practice, to
ﬁnd a formulation of handlers in a call-by-value context (an d call-by-name also has some
interest). It would also be important to increase ease of pro gramming, for example by al-
lowing abstraction on handlers, rather than, as above, maki ng use of their global variables;
one would also like syntactic support for parametric handle rs; some ideas along these lines
can be found in [21]. In general, perhaps handlers could beco me more ﬁrst-class entities.
Acknowledgments
We thank Andrej Bauer, Andrzej Filinski, Ohad Kammar, Paul L evy, John Power, Mo-
jca Pretnar, Alex Simpson, and an anonymous referee for thei r insightful comments and
support.

--- PDF page 33 ---
HANDLING ALGEBRAIC EFFECTS 33
References
[1] Nick Benton, John Hughes, and Eugenio Moggi. Monads and e ﬀects. In APPSEM 2000 , volume 2395
of Lecture Notes in Computer Science , pages 42–122, 2000.
[2] Nick Benton and Andrew Kennedy. Exceptional syntax. Journal of Functional Programming, 11(4):395–
410, 2001.
[3] Andrzej Filinski. Representing layered monads. In 26th Symposium on Principles of Programming Lan-
guages, pages 175–188, 1999.
[4] Cormac Flanagan, Amr Sabry, Bruce F. Duba, and Matthias F elleisen. The essence of compiling with
continuations. In Conference on Programming Language Design and Implementat ion, pages 237–247,
1993.
[5] George A. Gr¨ atzer. Universal Algebra. Springer, 2nd edition, 1979.
[6] Martin Hyland, Paul Blain Levy, Gordon D. Plotkin, and Jo hn Power. Combining algebraic eﬀects with
continuations. Theoretical Computer Science , 375(1-3):20–40, 2007.
[7] Martin Hyland, Gordon D. Plotkin, and John Power. Combin ing eﬀects: Sum and tensor. Theoretical
Computer Science , 357(1-3):70–99, 2006.
[8] Martin Hyland and John Power. Discrete Lawvere theories and computational eﬀects. Theor. Comput.
Sci., 366(1-2):144–162, 2006.
[9] Paul Blain Levy. Call-by-push-value: Decomposing call -by-value and call-by-name. Higher-Order and
Symbolic Computation , 19(4):377–414, 2006.
[10] Paul Blain Levy. Monads and adjunctions for global exce ptions. Electronic Notes in Theoretical Com-
puter Science , 158:261–287, 2006.
[11] Paul-Andr´ e Melli` es. Segal condition meets computat ional eﬀects. In LICS, pages 150–159. IEEE Com-
puter Society, 2010.
[12] Robin Milner. Communication and Concurrency . International Series in Computer Science. Prentice
Hall, 1989.
[13] Eugenio Moggi. Notions of computation and monads. Information And Computation , 93(1):55–92, 1991.
[14] Simon L. Peyton Jones. Haskell 98. Journal of Functional Programming , 13(1):0–255, 2003.
[15] Gordon D. Plotkin. Some varieties of equational logic. In Essays Dedicated to Joseph A. Goguen , volume
4060 of Lecture Notes in Computer Science , pages 150–156, 2006.
[16] Gordon D. Plotkin and John Power. Adequacy for algebrai c eﬀects. In 4th International Conference
on Foundations of Software Science and Computation Structu res, volume 2030 of Lecture Notes in
Computer Science , pages 1–24, 2001.
[17] Gordon D. Plotkin and John Power. Notions of computatio n determine monads. In 5th International
Conference on Foundations of Software Science and Computat ion Structures , volume 2303 of Lecture
Notes in Computer Science , pages 342–356, 2002.
[18] Gordon D. Plotkin and John Power. Algebraic operations and generic eﬀects. Applied Categorical Struc-
tures, 11(1):69–94, 2003.
[19] Gordon D. Plotkin and John Power. Computational eﬀects and operations: An overview. Electronic
Notes in Theoretical Computer Science , 73:149–163, 2004.
[20] Gordon D. Plotkin and Matija Pretnar. A logic for algebr aic eﬀects. In 23rd Symposium on Logic in
Computer Science , pages 118–129, 2008.
[21] Gordon D. Plotkin and Matija Pretnar. Handlers of algeb raic eﬀects. In ESOP 2009 , volume 5502 of
Lecture Notes in Computer Science , pages 80–94, 2009.
[22] Matija Pretnar. The Logic and Handling of Algebraic Eﬀects . PhD thesis, School of Informatics, Uni-
versity of Edinburgh, 2010.
[23] Terese. Term Rewriting systems . Cambridge University Press, 2003.
[24] Rob J. van Glabbeek and Gordon D. Plotkin. On CSP and the a lgebraic theory of eﬀects. In Reﬂections
on the work of C.A.R.Hoare , pages 333–369. Springer, 2010.
Appendix A. Decidability of handler correctness
We prove our results on handler correctness by reducing corr ectness to related questions in
equational logic. An interpretation of an equational theor y T in another T ′ is given by an
assignment of a T ′-term with free variables included in x1, . . . , x n to every function symbol

--- PDF page 34 ---
34 G. D. PLOTKIN AND M. PRETNAR
of arity n of T . This results in an interpretation of every T -term by a T ′-term, and so in
an interpretation of every T -equation by a T ′-equation.
Lemma A.1. Given a ﬁnitary equational theory with ﬁnite signature and ﬁ nitely many
axioms, and an interpretation of this theory in itself, it is a Π2-complete problem to decide
whether the interpretation holds in the initial model of the theory.
Proof. The problem is clearly in Π 2 as an axiom holds in the initial model of such an
equational theory if, and only if, there exists a proof of all its closed instances. Conversely,
take a Π 2 sentence of Peano Arithmetic. Without loss of generality, w e can assume this to
be of the form ∀x. ∃y. ϕ (x, y ), where ϕ (x, y ) deﬁnes a primitive recursive relation R(m, n ).
Changing to ∀x. ∃y. ∃y′ ≤ y. ϕ (x, y ′) if necessary, we can assume that if R(m, n ) holds and
n ≤ n′, then R(m, n ′) holds too. We now deﬁne a ﬁnitary equational theory T with
ﬁnite signature and ﬁnitely many axioms, and an interpretat ion of it in itself, such that
∀x. ∃y. ϕ (x, y ) is true if, and only if, the interpretation of the axioms hol ds in the initial
model of the theory.
Let f1, . . . , f k be a sequence of primitive recursive functions, including a function (cod-
ing) disjunction, each deﬁnable in terms of the previous one s by composition or primitive
recursion, such that fk is the characteristic function of R. Take a constant symbol zero
and a unary function symbol succ. Next, for all the primitive recursive functions fi take: a
function symbol fi of the same arity, and axioms corresponding to the primitive recursive
deﬁnition of fi, written using zero, succ, and the fj with j < i .
Next, take a binary function symbol try and a unary function symbol exists, together
with the following two axioms:
try(x, y ) = fk(x, y ) ∨ try(x, succ(y))
exists(x) = try(x, 0)
where ∨ is the function symbol corresponding to disjunction and n is the n-th numeral,
deﬁned using zero and succ. All this deﬁnes the theory T .
Finally, take the interpretation of the theory T in itself where each function symbol f
other than exists is interpreted by itself — more precisely, the term f(x1, . . . , x n) — and
exists is interpreted by the term 1 . This interpretation of the axioms evidently holds in the
initial model of T if, and only if, try(t, 0) = 1 is provable for all closed terms t.
Next, assume that the sentence ∀x. ∃y. ϕ (x, y ) is true and choose m. Then there exists
an n such that fk(m, n ) = 1 is provable in T . The following sequence of equations is then
provable in T :
try(m, 0) = fk(m, 0) ∨ try(m, 1)
= fk(m, 0) ∨ fk(m, 1) ∨ try(m, 2)
.
.
.
= fk(m
, 0) ∨ · · · ∨fk(m, n ) ∨ try(m, n + 1)
= 1
(with the last holding as fk(m, n ) = 1 is provable). More generally, as fk(m, n ′) = 1 whenever
n′ ≥ n, a similar argument shows that try(m, n ) = 1 is provable for any m and n. It follows
that all closed terms of T are provably equal to numerals. Therefore, as we have also sh own
that try(m, 0) = 1 is provable for all m, the interpretation of the axioms holds in the initial
model.

--- PDF page 35 ---
HANDLING ALGEBRAIC EFFECTS 35
Conversely, assume that try(t, 0) = 1 is provable in T for all closed terms t. Then,
in particular, try(m, 0) = 1 is provable in T for all m. We analyse proofs in T via the
ﬁrst-order term rewriting system obtained by orienting all the axioms of T from left to
right. As the reduction rules are left-linear and there are n o overlaps between them, we
have a Church-Rosser system [23]. Hence, for any two terms t and u, the equation t = u is
provable if, and only if, t and u reduce to a common term.
In particular, by our assumption, for every m, there is some number of steps s such that
try(m, 0) → s 1. We ﬁrst observe that t ∨ u → ∗ 1 if, and only if, t → ∗ 1 or u → ∗ 1. Using
this observation, it follows by induction on s that there exists an n such that fk(m, n ) → ∗ 1.
Therefore the sentence ∀x. ∃y. ϕ (x, y ) is true, concluding the proof.
Proof of Theorem 6.2. Given a simple signature and theory T , following the proof of Propo-
sition 4.3 one recursively constructs a corresponding ﬁnit ary equational theory Tf with ﬁnite
signature and ﬁnitely many axioms whose models are in 1-1 cor respondence with those of
the given theory. Handlers ⊢ H : F 0 handler then correspond to interpretations of Tf in
itself, and are correct if, and only if, the interpretation o f Tf in itself holds in the model of
Tf corresponding to the model F /llbracket0/rrbracket= /llbracketF 0/rrbracketof T . As F /llbracket0/rrbracketis the initial model of T (being
its free model on the empty set), that corresponding model of T is its initial model.
Conversely, given a ﬁnitary equational theory T with ﬁnite signature and ﬁnitely many
axioms, one immediately constructs a simple signature and t heory Ts such that the above
construction yields back the original ﬁnitary equational t heory T (up to a bijection of its
function symbols). Further, given an interpretation of T in itself one immediately constructs
a handler ⊢ H : F 0 handler to which the interpretation of T in itself corresponds.
The result then follows by combining these facts with Lemma A .1.
Lemma A.2. Given a ﬁnitary equational theory with ﬁnite signature and ﬁ nitely many
axioms, and an interpretation of this theory in itself, it is a Σ1-complete problem to decide
whether the interpretation is provable in the theory.
Proof. The proof is very similar to that of Lemma A.1. The problem is c learly in Σ 1.
Conversely, consider a Σ 1 sentence of Peano Arithmetic. This can be taken, without los s of
generality, to be of the form ∃y. ϕ (y), where ϕ (y) deﬁnes a primitive recursive predicate R.
One then deﬁnes a ﬁnitary equational theory T with ﬁnite signature and ﬁnitely many
axioms, and an interpretation of it in itself, such that that ∃y. ϕ (y) is true if, and only
if, the interpretation of the axioms holds in all free models of the theory over ﬁnite sets.
The theory T is much like before, except that try is unary and exists is a constant, the
corresponding axioms are
try(y) = fk(y) ∨ try(succ(y))
exists() = try(0
)
and one considers the interpretation where each function sy mbol other than exists is inter-
preted by itself and exists is interpreted by 1 . This interpretation of the axioms evidently
holds in all free models of T over ﬁnite sets if, and only if, try(0) = 1 is provable.
The rest of the proof then proceeds entirely analogously to, but a little more simply
than, that of Lemma A.1.
Proof of Theorem 6.5. The proof proceeds analogously to that of Theorem 6.2 except that
one considers uniformly simple handlers {⊢ HC : C handler}C .

--- PDF page 36 ---
36 G. D. PLOTKIN AND M. PRETNAR
In particular there is, as before, a ﬁnitary equational theo ry Tf corresponding to any
given theory T over a given simple signature. As before, a given uniformly s imple handler
{⊢ HC : C handler}C then corresponds to an interpretation of Tf in itself. The handler is
correct if that interpretation holds in all models of Tf corresponding to those of T of the
form /llbracketC/rrbracket. Since the /llbracketC/rrbracketinclude all free models of T of the form F n, this last is equivalent
to the interpretation holding in all free models of Tf over ﬁnite sets, and so to its being
provable in Tf .
Proof of Theorem 6.6. Following the proof of Theorem 6.5, we see that, given a theor y
T over a given simple signature, determining the correctness of a given uniformly simple
handler {⊢ HC : C handler}C amounts to determining whether the corresponding interpre -
tation of Tf in itself is provable in Tf . However that is decidable since Tf is.
This work is licensed under the Creative Commons Attributio n-NoDerivs License. To view
a copy of this license, visit http://creativecommons.org/licenses/by-nd/2.0/ or send a
letter to Creative Commons, 171 Second St, Suite 300, San Fra ncisco, CA 94105, USA, or
Eisenacher Strasse 2, 10777 Berlin, Germany