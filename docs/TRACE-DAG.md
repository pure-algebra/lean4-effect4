# Trace agreement proof graph

Status: opened 2026-09-02 with the trace lane; every edge but `semantics` and
`bridges` closed the same day; those two were `required-open` by ruling for
this phase. Amended 2026-09-03: `semantics` is closed for the Lean-to-Lean pair
(the runner is `interpret` of a denotation, packet D1); `bridges` and the host
half of `semantics` stay open by construction — no Lean statement reaches the
host.
Also on 2026-09-03 the frame machine half closed **for the Lean pair only** on 2026-09-03
(packet D4, `Effect4.FrameSimulation.compile_simulates`); read its scope line
before quoting it, and do not read it as closed globally. `bridges` stays open
by construction: no Lean statement reaches the host.

The graph-bearing owner is `TRACE-PG-AGREEMENT`. Its subject is one shared,
service-level trace alphabet (`Effects.Trace.Event`, minted in lean4-effects) and
the claim that two emitters of it agree under a named mask. The emitters are the
traced service over the algebra (`Family.Service.traced`), the Flow runner, and the
host through a Tracer hook and a service proxy. The frame-level stream the host can
also produce (`frames.jsonl`) is recorded and never compared: it is not an emitter
of this alphabet until a projection from `Effect4.FrameEvent` exists
(`docs/FRAMES-DAG.md` separation 7 keeps that projection a bridge obligation).

```text
TRACE-ALPHABET (Effects/Trace.lean)
   |
   +--> TRACED-SERVICE ---- interpret_traced_fst ----> ALGEBRA (Effects/Algebra)
   |
   +--> FLOW-RUNNER (Effect4/Semantics/Runs.lean) ---- internal oracle (m2) ----> TRACED-SERVICE
   |
   +--> HOST-TRACER (harness/trace, effect4-tools)
   |
   v
AGREEMENT under Mask (Effect4/Semantics/Observation.lean)
   |                  \
   v                   --> LOWERING-COVERAGE ledger (docs/LOWERING-COVERAGE.md)
FRAME-BRIDGE (required-open): FrameEvent.toTrace, scheduler Event.toTrace

DENOTATION (Effect4/Semantics/Denotation.lean, D1)
  FLOW-RUNNER = interpret (traceHandler service nameOf) . denote, closed by outcomeRows
  so the Lean pair agrees by theorem, not by comparison under a mask
```

## Edge ledger

| Edge | State | Evidence or remaining work |
| --- | --- | --- |
| identity | `required-closed` | `Effects.Trace.Event`, `Trace.Mask`, `Trace.Val` frozen by `EffectsTest/Trace/TraceContract.lean` (lean4-effects v0.3.0, `test/contracts/trace.contract.md`) |
| construction | `required-closed` | `Family.Service.traced` and `Family.Service.tracedExcept` (v0.3.1); `X.traced`, `X.tracedExcept` emitted by `effect_signature`; receipts in `Effect4Test/Semantics/ObservationContract.lean` |
| laws | `required-closed` | `interpret_traced_fst`, `interpret_tracedExcept_fst`, `project_project`, `project_m2`, `agree_of_agree_m2`, all within `propext`/`Quot.sound` (`EffectsTest/Trace/AxiomReport.lean`) |
| frame-simulation | `required-open` **(value half closed for the Lean pair)** | **Value half.** `Effect4.FrameSimulation.compile_simulates` (`Effect4/Semantics/FrameSimulation.lean`, packet D4, `test/contracts/frame-simulation.contract.md`): for `bound tape <= fuel` and `answersOf H p s0 = tape`, the frame machine started on `compile 0 p` finishes with `exitOf ((interpret H p).run.run s0).1`, with `H` into `ExceptT (Cause Val Unit Unit Unit) (StateT St Id)`; `compile_simulates_fail` is the `Cause.fail` specialisation. It covers the compiled fragment only (`success`, `failure`, `sync`, `onSuccess`; `inFragment` and `compile_inFragment` prove the rest is unreachable). **Region/finalizer half:** the current `compileRegion` in `Effect4/Semantics/RegionSimulation.lean` uses nested `onExit` frames for registrations and converts fuel/unanswered frontiers to empty failures. The unrestricted connection is refuted by `E4-TARGET-CE-019` through `E4-TARGET-CE-021` (`Effect4Test/Counterexamples/Target/RegionSimulationBoundary.lean`, independent amendment to `test/contracts/frame-simulation.contract.md` at `623141b`). Within one scope, the runner and pinned rc.112 Scope give all releases the original closing exit; nested frames instead pass later cleanup failures into earlier finalizers. At a source frontier the runner keeps work live, while this compiler finalizes and reports interruption. The independent local scope component now exists: `Effect4/Runtime/ScopeMachine.lean`, frozen by `test/contracts/scope-machine.contract.md` at `e5a2bdb`, steps stored requests and actual replies with one fixed original exit. `runState_prefix` keeps the exact operation partition and service-state receipt at every pause; `runState_complete` and `runState_restore` prove the complete pair against existing `Scope.closeExitsM` and the exact restoration policy. Its unchanged 27 existing-owner controls, 72 machine guards and ten public axiom receipts pass. This is the sequential close component for either retained strategy label, not parallel scheduling. `Effect4/Runtime/ScopeRestoration.lean` (packet `945f729`) now connects only a completed restored ScopeMachine exit to the actual existing `FrameFiber.step`: nine universal laws cover the complete service-state pair, unfinished pauses, immediate successful restoration, failure-arm skipping through an arbitrary tail, an outer mask and existing cause combination. It adds no carrier or target compiler. The replacement compiler and its general finite-prefix relation matching continuation, environment, tape, open registrations, service state, closing state and endpoint classification remain owed. A second source run hidden in an oracle or a settled-only theorem does not close this general obligation. `unwind_failure` and `close_success` retain their successful-finalizer premise, and the three existing `regions_simulate_*` fixtures remain finite receipts. Generic `onExit`, Scope and the region runner keep their distinct meanings. The finalizer/outcome mask discards other events and full causes; raw failure-list equality is not implied. Every frame simulation remains relative to the named operation-answer agreement, and host agreement remains finite evidence. |
| semantics | **closed for the Lean pair: runner = `interpret ∘ denote`; host stays evidence** (2026-09-03) | `Effect4/Semantics/Denotation.lean` (packet D1, `test/contracts/flow-denotation.contract.md`): a `CheckedFlow` denotes a `Program (Sig a ⊕ₛ DecSig)` — the alphabet through `Alphabet.toFamily` at the constant denotation `Val`, plus a decision summand answering `Unit`. T1 `runTape_eq_interpretRun`: the runner at every fuel is `interpret` under `(guarded traced service).toHandler.sum decisionHandler`, closed by `outcomeRows` (`done` and `frontier` have no former in the algebra and are a function of the result, not operations of any summand). T2 `denoteFuel_eq_denote`: at `fuelFor`'s allotment the fuelled denotation is the fuel-free `denote`, well-founded on `(tape.length, (raw.reachSet block).length)` through `reachSet_length_lt_of_edge` and `CyclesWF`, carried by the `LoopBudget` invariant of `Effect4/Semantics/Fuel.lean`. The `m2` oracle is an instance of T1 on the `incr`, `chooser` and `chosenLoop` flows (`Effect4Test/Semantics/DenotationContract.lean`). Regions are closed the same way (packet D2, `Effect4/Semantics/RegionDenotation.lean`): a `CheckedRegionFlow` denotes a `Program (ScopeSig a ⊕ₛ (RegionOpSig a ⊕ₛ DecSig))` — a scope summand whose four names are `enter`, `acquire`, `leave` and `fail`, an alphabet summand answering `Except Val Val` because a `RegionService` may fail, and D1's decision summand unchanged. `runRegions_eq_interpret`: the region runner at every fuel is `interpret` under `scopeHandler.sum (overStack (regionTraceHandler.sum decisionHandler))` into `StateT (Stack a) (RunM M)`, run from the empty stack and closed by D1's `outcomeRows`. `scopeHandler`'s `leave` arm *is* `closeFrame`, so L1 `closeFrame_log` and L2 `closeFrame_failure`/`_merge`/`_closeResult` are that arm's facts, and its `fail` arm is `unwind`. `runRegions_erase`: on a flow whose every block is `plain` the region runner and the plain runner of `Runs.lean` agree, which is what ties D2 to D1. Receipts on the three region goldens (`Effect4Test/Semantics/RegionDenotationContract.lean`, `Generate.lean region-oracle`). Fuel-free continuation (2026-09-03, frozen packet `test/contracts/region-total-denotation.contract.md` at `8351f88`): `Effect4/Semantics/RegionTotal.lean` defines `denoteRegionsGo` by the same tape/reachability measure, including all region transitions, and `denoteRegionsWF` at the admitted entry. `denoteRegionsFuel_eq_denoteRegionsWF` equates the complete Programs for every admitted flow, input, finite tape and sufficient fuel; `runRegionsCause_eq_interpretWF` carries the complete result, residual tape, ordered failures and log through every lawful service monad. Neither proof assumes successful or reachable scope answers. The frozen battery, four axiom receipts (`propext`, `Quot.sound` only), and independent recursive-body/dependency review close this fuel-free Lean edge. Reachable-stack continuation (2026-09-03, frozen packet `test/contracts/region-safety.contract.md` at `fa53b97`): `Effect4/Semantics/RegionSafety.lean` proves `runRegionsCause_checked_not_stuck`, its wire projection `runRegions_checked_not_stuck`, and `interpretRegionsWF_checked_not_stuck` under the standard handler, for every checked flow, stateful service, input, tape, initial state/log and fuel. Its private `StackAt` derives entry ownership, matching head identity and the full parent chain from current admission and preserves them through the actual runner, including failed acquisition, body failure and failed release. The 31-control frozen battery, exact public ascriptions, three `[propext, Quot.sound]` receipts and independent ownership-proof review pass. This closes the checked-entry reachable-stack obligation. Arbitrary scope handlers may still answer `none`; no success, complete-tape or successful-release premise was added. The plan's `Handler.mapHom (MonadHom.stateT …)` shape is not used: lean4-effects v0.3.1 has no `Hom` module, so the lift is the hand-rolled `Handler.overStack`. Still open on this edge: the frame machine's finalizer half (D4), and everything host-side, which stays evidence The `Script` embedding closed on 2026-09-03 (packet D5): `Script.toFlow_denote` (`Effect4/Target/TypeScript/ScriptDenotation.lean`, receipts in `Effect4Test/Target/TypeScript/ScriptDenotation{Contract,AxiomReport}.lean`) makes the `m2` oracle a corollary for every embedded program rather than an observation — the `Build` invariant is `Appends`/`Ordered`, the induction is `materialize_segment`, `step_segment`, `stepsWalk_denote`, and `interpret_vis_of_pure` is the erasure lemma for the pure literal and atom rows. Not closed by D5: the elaborator's `Program` value and its `Script` are still related only by the operations they perform (`E4-TARGET-CE-016`), not by an equation between them |
| flow-runner | `required-closed` | Flow v2 runner (`Effect4/Semantics/Runs.lean`: `plan`/`step`/`loop`, `FlowService`, `Frontier`, the decision tape `Effect4/Flow/Decision.lean`) with laws `run_checked_not_stuck`, `run_fuel_mono`, `step_choose_consumes_one`, `plan_checked` within the ceiling (`Effect4Test/Flow/RunnerAxiomReport.lean`); the straight-line embedding `Script.toFlow` (`Effect4/Target/TypeScript/ScriptFlow.lean`) is admitted by `Effects.admit` and, since 2026-09-03, denotes the script it was embedded from: `Script.toFlow_denote` (`Effect4/Target/TypeScript/ScriptDenotation.lean`, packet D5) says `denoteGo` of the embedded graph on the empty tape is `liftScript` of `denoteScript`, the program read off the steps; the internal oracle: `generated/traces/flow/*.empty.tsv` (face `lean-flow`) agree with the traced-service goldens under `m2`, and `Generate.lean oracle` now prints three-way agreement per program — traced service, Flow runner, script denotation (part of `scripts/check-trace-goldens.sh`; planted mutant 4/4) |
| counterexamples | `required-closed` | `EF-TRACE-CE-001..003` (lean4-effects), `E4-SEM-CE-008..013`, `E4-TARGET-CE-009..010`; planted mutants in `scripts/test-trace-goldens-gate.sh` (3/3) |
| representation | `required-closed` | TSV wire form rendered only in `Effect4/Target/TypeScript/Trace.lean` (exact-module admission); no `String` in the alphabet. Rendered rows are compared, not events (`E4-TARGET-CE-016`), so the comparison is sound only inside a *declared answer-type profile*: since 2026-09-03 that profile is reified as `Effect4.Target.EffectV4.Spelling` (depth one Stratum V and `Handle "T"`; depth three `Option (Except E A)`, `Except E (Option A)`, `List (A × B)`, `Option (A × B)`, `A × Except E B`; depth four refused with its depth in the message), the host encodes at the spelling a row declares (`parseSpelling`/`wireTyped` in `harness/trace/tracer.ts`) rather than at the shape of the host value, and pure atoms are one declaration with every face derived (`effect_atoms`). Pinned by `test/contracts/answer-profile.contract.md` and `Effect4Test/Target/TypeScript/AnswerProfileContract.lean`; rows `E4-TARGET-CE-024` (a depth-three answer read by shape renders in a different byte order) and `E4-TARGET-CE-025` (an atom declared in one face only); corpus witness `generated/traces/probe.empty.tsv` |
| bridges | `required-open` | `FrameEvent.toTrace`, `FrameEvent.traceOf`, `Event.toTrace` and `Exit.toOutcome` are defined in `Effect4/Target/TypeScript/Simulation.lean` (P-T11): finalizers and outcomes project, everything else to `none`; the patched rc.112 copy (`harness/trace/patched/`, seven observation-only hunks) records frame-level rows in `harness/trace/receipts/patched/` and `scripts/check-trace-patched.sh` pins three scope facts (two releases latest-first through the loop, a single release inline, nested single releases inline inner-first) while every service-level trace stays equal; no theorem relates a host row to a `FrameEvent`, so the primitive stream is still not evidence. Packet D4 does **not** close this row and cannot: `FRAME-L9-HOST-EVIDENCE` and the reification plan's refusal row both say no Lean statement reaches the host. What changed is smaller and real — `FrameEvent.toTrace` / `traceOf` are no longer definitions with nothing on the Lean side to compare, because `compile_simulates` gives the frame machine a proved relation to `interpret`; the frame stream itself is still recorded and never compared |
| targets | `required-closed` | `harness/trace/check.sh`: fixture drift for the straight-line module (`fixture.ts`) and the dispatch-form module (`flow-fixture.ts`, `Effect4/Target/TypeScript/FlowLower.lean`, ruling R7), four host gates, every golden under every mask at a large yield threshold and at the rc.112 floor of 3 (the flow goldens through `flow-tail.ts` with the `Decisions` service answering from the golden's tape), type receipts from `tsc.original` for both modules; region programs (`Effect4/Target/TypeScript/RegionLower.lean`: `Effect.scoped`, `Effect.onExit`, `Effect.acquireRelease`, never `try/finally`) run with a `Regions` service reporting `enter`, `leave` and `finalizer` with the exits the host handed them; receipts under `harness/trace/receipts/` and `receipts/flow/` |
| trust | `required-closed` | axiom receipts for every trace law; gate exemptions for the renderer, the DSL and the numerator only; `scripts/test-trust-gate.sh` green |
| approximation | `required-open` (both runner halves closed in Lean, 2026-09-03) | `Effect4/Semantics/Approximation.lean` (DB-04 laws over the tape runner: fuel exhaustion is a live `frontier`, never a failure or refusal, and more fuel only extends a frontier run). **The region half landed 2026-09-03** over D2's merged failure list: `regionStep_log_extends`, `regionLoop_fuel_stable` (a settled run is bit-identical at every larger fuel -- result, tape, merged failure list, log and service state), `region_obs_mono`/`region_obs_chain` and their `runRegions` forms, `regionLoop_frontier_live` (a fuel frontier is a `fuel` frontier, never `failed`, never `refused`, and the merged failure list is empty) and `regionLoop_failed_head` (a failing run's merged list is headed by the error the wire reports, the carrier `closeFrame_failure_merge` fixes). The region fuel formula is the erased flow's: `regionFuelFor flow tape = fuelFor flow.erase tape = (tape.length + 1) * flow.blocks.length + 1`, and `runRegions_fuelFor_finishes` proves it suffices, so `runRegionsColimitDefault` is total. Receipts on `regionNested`, `regionTwoFail`, `regionBothSucceed` and a merged-failure program in `Effect4Test/Semantics/ApproximationContract.lean`; axioms in `Effect4Test/Semantics/ApproximationAxiomReport.lean` (`propext`, `Quot.sound`). `Generate.lean region-frontier` emits the Lean-face frontier golden of each region program at one fuel below finishing. The row stays open because nothing host-side is claimed: no gate compares a host run at a region fuel frontier |
| logic | `required-closed` for the Lean pair (2026-09-03) | DB-06 over D1's denotation, `Effect4/Semantics/Logic.lean`: `box`/`dia`/`total`/`wp` relative to an answer specification, `wp_iff_wlp_and_total` (every program) and `Flow.wp_iff` (a flow against a tape and input; partiality is the unanswered frontier and the refusal, never fuel), `box_sound`/`dia_complete` against `interpret` for deterministic handlers (`DetRun`: `StateT σ Id` and every `StateT` layer over it, so the runner's `RunM`), `Flow.wlp_runDefault`/`wp_runDefault` through T1/T2, and `box_ofOracle_iff` (against an oracle the box is evaluation, which is what `Effect4Test/Semantics/LogicContract.lean` decides on `incr` and `chooser`). Axioms within the ceiling (`LogicAxiomReport.lean`). Says nothing about the host |
| equivalence | `required-closed` for the Lean pair (2026-09-03) | `Effect4/Semantics/Equivalence.lean`: `Flow.Equiv` (the same denotation against every tape and input; fuel-free by T2), an equivalence relation, with the congruences `Equiv.interpret`, `Equiv.runTape`/`runDefault` (result, unconsumed tape and log agree under every service, from every log, at any sufficient fuel, by T1), `Equiv.log`, `Equiv.wp`/`wlp`/`total`, `equiv_iff_denoteFuel` (the executable face at the allotted fuel) and `equiv_of_erase_eq`. Packet D3's T4 is an instance of this relation on the skeleton denotations. Says nothing about the host |
| coverage | `required-closed` | `generated/lowering-coverage.tsv` (twenty-seven rules: eight straight-line `checked`, eight dispatch-form `covered`, `interrupt-point` `pinned` on the three M2 goldens, three region `checked`, `region-masked` `pinned` on the masked interrupt golden, four structured `covered`, `perform-tuple` `pinned` on the job goldens, `dispatch-fallback` `checked` on the irreducible program by the property loop `generated/lowering-property.tsv`: 200 generated flows, 1277 host runs, 276 frontiers, 318 sites both ways, 3/3 planted lowering mutants caught), `scripts/check-lowering-coverage.sh`, planted mutants (4/4) |
| interrupts | `required-open` **(runner restoration, real mask lowering and host delivery closed for the three goldens, 2026-09-03; Flow result identity and interrupt-point denotation open)** | `Effect4/Flow/Interrupt.lean` owns explicit interrupt decisions at plain performs and region leaves, disjoint from choose sites (`interruptPoint_masked_defers`, `interruptPoint_unmasked_delivers`). The M2 repair (coordinator, after the independent amendment `3fbf47d`, `E4-FLOW-CE-024`/`025`): a `leave` that restores interruptibility delivers a pending interrupt immediately, before the continuation, with no fresh point and no tape read, and a `ret` with a pending interrupt is delivered the same way; `Effect4Test/Counterexamples/Flow/InterruptMaskBoundary.lean` is green and off the red set, and the historical `decide 1000009` expectation is amended. The mask is now lowered: a region in `RegionProgram.masked` emits `Stmt.scopedGenMasked` (typescript v0.4.2, `Effect.uninterruptible` around the scoped generator; rule `region-masked`), `interrupt-tail.ts` requests the interrupt wherever the tape answers it, and rc.112 itself defers under the mask and delivers at restoration: the three interrupt goldens agree under every mask at both yield settings. `Effect4/Runtime/ScopeRestoration.lean` (frozen at `945f729`) is the Scope/Frame side of the same restoration. Open: the Flow result carries no full `Cause`/interrupt identity; nested or failing-exit rules beyond `E4-RUN-CE-025`; an `interruptPoint` case in `Skeleton.denote` |
| fibers | `required-open` — **host protocol, and it cannot become anything else at this model** (2026-09-03) | Packet M3: `Effect4/Concurrency/FiberFamily.lean` declares `Fibers` (`fork`, `forkDetach`, `join`, `awaitValue`, `awaitError`, `interrupt`, `started`, `cleanups`) over a Lean handler that is the **sequential projection** of a two-fiber run — at most one child holds the processor, and *when* is read off the golden's tape rather than derived. The projection's five clauses, each read off the pinned rc.112 install first: a `true` decision at a fork drains the *whole* queue in fork order; `join` and the awaits drain only when their target has not published; `interrupt` never drains, and a queued child interrupted before it runs is never cleaned; the observations never drain. Nine goldens under `generated/traces/fiber/`, one per assertion of `harness/fiber-supervision/runtime-check.ts` that this alphabet can carry, all agreeing with rc.112 under `outcome`, `m1` and `m2` through `harness/trace/fiber-tail.ts`, where the tape drives `TapeScheduler.shouldYield` (the new optional `RunOptions.armed` in `tracer.ts`) and every fork's decision is a `decide` row. Lean receipts: `Effect4Test/Flow/FibersContract.lean` (the exact rows of all nine, plus the five clauses), `Effect4Test/Flow/FibersAxiomReport.lean` (ceiling `propext`; the module is under `Effect4/` and takes no gate exemption). **What this row does not and will not close:** the scheduler's `Machine` carries no program, so nothing in Lean steps two fibers against each other, and two facts stay host-only — `join` of an interrupted child has no Lean outcome to resume into (`E4-SEM-CE-010`, the projection refuses and emits no golden), and a `false` decision is not stable at rc.112's own yield floor, where the run loop starts a deferred child with no decision row to account for it (`E4-SEM-CE-011`, so the `fiber` section runs at the default threshold only). Refused at this alphabet: the tenth runtime-check assertion, `race-reentrant-nonempty-set-includes-late-insertion`, which needs a child that completes another child. Closing this row needs a two-fiber model — a new model, not an extension of `Machine` |
| layers | `required-open` — **host protocol for build identity, memoization and release order; three named facts stay out of reach** (2026-09-03) | Packet M4: `Effect4/Layer/LayerFamily.lean` declares `Layers` (`build`, `provideCount`, `scopeOf`, `close`) over a Lean handler that is a **memo table** — the model of what a program can observe of rc.112's layer build, and nothing more. `build` answers a `Handle "Ref.Ref<number>"`: the *service* object, which rc.112's memo entry replays unchanged on a hit (`layer.memo-build-once`, `layer.memo-get-or-else`); the `Context` around it is not the identity, because `buildWithMemoMap` maps `Context.add(CurrentMemoMap, …)` over it on every build (`layer.build-with-memo-map-service`, probed on the pinned install before the family was written). `scopeOf` answers the `Handle "Scope.Closeable"` `memoMapBuild` allocated for that construction; `provideCount` counts constructions, with a `Layer.fresh` wrapper counted against the layer it wraps (`layer.fresh-drops-memoization`); `close` answers **the services it released, in release order**, which is reverse construction order because the memo entry's finalizer is registered before the construction runs and a scope runs its finalizers in reverse registration order. **A `finalizer` row is not available to this family**, and that is why release is an answer: `Family.Service.traced` is an around-wrapper over a handler in `M`, writing one `op` and one `answer` per operation with no access to `M`'s state, so region and finalizer events cannot be emitted without abandoning the derived tracer. This is the M1 `Scopes` shape, for the M1 reason. Eight goldens under `generated/traces/layer/`, all agreeing with rc.112 under `outcome`, `m1` and `m2` through `harness/trace/layer-tail.ts`, at a large `MaxOpsBeforeYield` and at the rc.112 floor of 3. Lean receipts: `Effect4Test/Flow/LayersContract.lean` (the exact rows of all eight, plus the memo-table clauses), `Effect4Test/Flow/LayersAxiomReport.lean` (eight clauses, all axiom-free; the module is under `Effect4/` and takes no gate exemption). **What this row does not and will not close:** observer counting (a memoized rebuild's finalizer decrements `entry.observers` and releases nothing, so one release per *construction* reaches the wire and never one per build); concurrent memo identity (`memoMapBuild` installs a `Deferred` before the construction, so a second fiber awaits rather than builds — this handler is single-fiber and the corpus is single-fiber); and layer composition (`Layer.provide`, `Layer.merge`, the parent-chain lookup of a forked memo map), which needs a layer *value* on the wire and a layer is a build function, not a handle. `E4-SEM-CE-019` |
| structured-agreement | **closed for ordinary Flow Program equality with interrupts disabled; other profiles and host remain open** (2026-09-03) | **Meaning:** the existing D3 `SkeletonSemantics.lean` owns the target interpreter, T3 dispatch equation and earlier flat T4 theorem. The independent general packet `test/contracts/structure-semantics.contract.md` at `33409c6` is implemented in `Effect4/Target/TypeScript/StructureSemantics.lean` at `1ca39de`. `skeletonStructured_denote_of_fuelFor_le` equates the complete `Skeleton.denote` Program of every actual successfully emitted ordinary checked FlowProgram with its fuel-free `Flow.denote`, for every input, decision tape and target fuel at least `fuelFor`, under `program.interrupts = false`. `skeletonStructured_denote_dispatch_of_emitted` equates actual structured and dispatch Programs at `fuelFor`. Neither theorem assumes flatness, no merges/loops, supplied graph facts, successful answers or a complete tape. Actual emitted merge/loop contexts, simultaneous parameter moves, outward budget ordering and the tape/non-choice termination measure establish the equation. **Lexical dependency:** `StructureOrder` and `StructureDominators` prove actual DFS/RPO, computed parent-chain facts and strict binding of every successfully lowered body. The general proof consumes this actual lexical scope; it does not claim an independent all-path or nearest-dominator specification. `E4-TARGET-CE-018` still refutes the old unrestricted generic break-scope statement. **Evidence:** the frozen 40 controls cover nested merges and loops, entry loops, parameter swaps, arbitrary operation answers, exhausted/mismatched tapes and excess target fuel. Three corrupted-target variants fail the expected checks. Independent saved-source review and all 202 owned-constant trust checks pass; both new public receipts use `[propext, Quot.sound]`. Root module build and unchanged contract/axiom files pass. **Still open:** region nodes and interrupt-point denotation; comparisons at arbitrary smaller target fuel; rendered TypeScript and pinned host agreement. Existing host receipts remain finite evidence and are not promoted by this algebraic Program equality. |
| job-runner | `required-open` — **six goldens host-green; four expressive limits recorded** (2026-09-03) | The first harness program written the way an Effect user would write one (`docs/research/2026-09-03-job-runner.md`): `Jobs` (`connect`/`next`/`run`/`attempt`/`ack`/`requeue`/`disconnect`, the connection an opaque `Handle "JobQueue"`; `next` answers a job ticket and `run`, `ack` and `requeue` are two-parameter operations over it), the region flow `jobRunner` in `harness/trace/Generate.lean` (eighteen blocks, one region, three decision sites, interrupt points declared), its masked variant `jobRunnerMasked` and the aborting-`run` program `jobPoison`. The host service is real: `harness/trace/job-queue.ts` keeps the queue in a JSON file under a temporary directory written and read with `node:fs` on every operation, `attempt` and `run` wait on `Effect.sleep`, and `disconnect` deletes the directory; `harness/trace/job-tail.ts` reports `released`, which is `true` on every golden — the region's release closes the connection on the clean, the failing and the interrupted path alike. Six goldens under `generated/traces/job/` (clean drain, retry, requeue, interrupt mid-queue, interrupt inside the masked critical section delivered at the leave, and the aborting `run`), each agreeing with rc.112 under `outcome`, `m1` and `m2` at the default yield setting and at the rc.112 floor of 3. First goldens to carry **two tapes on one wire**: the choice sites are below `Effect4.Flow.interruptBase` and every interrupt site at or above it (`Point.site_ne_choose`, `sitesSeparated`), so the tail splits `EFFECT4_TAPE` by site. Lean receipts: `Effect4Test/Flow/JobRunnerContract.lean` (the exact rows of all six, admission, site separation, the final queue of each run, and the refusals). **What the program could not express, each with a row:** a flow cannot build a pair — `plan` hands a service one `Val`, `perform` names one request `Var` and an atom is unary, so a two-parameter operation is performable only on a tuple some answer already delivered, which is why `next` hands out a ticket (`E4-FLOW-CE-028`, `Effect4Test/Counterexamples/Target/JobRequest.lean`); an aborting operation ends the run, so retry-after-failure needs the data reading of the error (`E4-FLOW-CE-026`); a flow never branches on a value, so the queue-empty test and the retry bound are tape questions and the carried attempt budget cannot be compared (`E4-FLOW-CE-027`, both in `Effect4Test/Counterexamples/Flow/JobRunner.lean`); and a region hands back exactly one value (`RegionClause.continueTyped`), so a masked critical section *inside* the loop has no spelling and the whole region is masked instead — recorded, no row. The predicted gap "the interrupt tape is per program not per job" is half refuted: `interruptRead` consumes the head only on a site match, so a non-delivering entry at a site moves delivery to the next occurrence, which is how the mid-queue golden interrupts the second job; a tape still names a control point and never the job at it. **Repaired since (2026-09-03):** the packet's `run : Handle × Nat → Nat !! String`, `ack` and `requeue` are performed as written — `familyTable` spells the request as the right-nested product and `Lowering.callOf` destructures the slot at the call (`jobs.ack(b10p3[0], b10p3[1])` over `let b10p3!: readonly [JobQueue, number]`), the new `perform-tuple` rule; `E4-TARGET-CE-022`'s repair column carries it and `Effect4Test/Target/TypeScript/MultiArgContract.lean` pins the bytes. The row stays open because everything it establishes is host evidence: no theorem relates a `Jobs` golden to rc.112, and the two queue models agree only where a golden looks at them |
| deferreds | `required-closed` | the `Deferreds` family (`harness/trace/Generate.lean`): rc.112's one-shot cells behind `Handle "Deferred.Deferred<number, number>"`, `poll` answering `Option (Except Nat Nat)` at Stratum V depth two (the first spelling to nest one namespace inside another, so `Option` and `Result` reach `deferred-fixture.ts` as type-only imports), and `awaitValue`/`awaitError` as the aborting reading; six goldens `generated/traces/deferred/*.tsv` from the sequential projection, agreeing on real `Deferred` objects under every mask at both yield settings (`deferred-tail.ts`, receipts under `harness/trace/receipts/deferred/`); a pending await is a `frontier` on both faces (`RunOptions.stallMs`), rows `E4-SEM-CE-012`/`E4-SEM-CE-013`; contract `Effect4Test/Flow/DeferredsContract.lean` |
| refs | `required-closed` | Packet: the `Refs`/`ERefs` families over the reified rc.112 `Ref`, the second lane over an opaque handle after M1's `Scopes` and the one where the handle is all the family owns. `Handle "Ref.Ref<number>"` prints rc.112's own type, so `harness/trace/ref-tail.ts` keeps no store: every operation is the call of the same name, the ref objects go straight through, and `tracer.ts` `registerHandle` indexes them. Lean face `Effect4/Stateful/RefFamily.lean` (`refsLive`, `erefsLive` over `StateT (List Nat) Id`); receipts `Effect4Test/Flow/RefsContract.lean` (all seven goldens' rows inline, over the imported corpus, so a drifting copy cannot pass) and `Effect4Test/Flow/RefsAxiomReport.lean` (`propext` only, no gate exemption). Seven goldens `generated/traces/ref/*.tsv` agree under every mask at the default yield threshold and at the rc.112 floor of 3. Attacks `E4-SEM-CE-014` (the host handle counter is global while a Lean store is per family; this tail brands one handle type) and `E4-SEM-CE-015` (a same-typed `modify` answer and new state are separated by the golden, never by the type), witnessed in `Effect4Test/Counterexamples/Runtime/Refs.lean`. Separation 7 is reconfirmed on a second family and sharpened: `Ref.set` answers the `MutableRef` and `Ref.update` answers `undefined`, both under a declared `Effect<void>`, so two `void` operations of one rc.112 module disagree about their runtime answer and no runtime-value heuristic could replace the typed rule (`harness/trace/ref-probe.mjs`). Refused: atomicity — every program is single-fiber, `modify` is traced as an ordinary answer, and contention belongs to the `fibers` lane |

## Required semantic separations

1. **Service level, not primitive level.** An event names an operation of a family,
   its request and answer, a decision site, a region, a finalizer, or the outcome.
   The seventeen rc.112 primitives are not events of this alphabet.
2. **Masks are projections.** `project mask` is a filter; `m1` keeps operations,
   answers, failures and the outcome; `m2` adds decisions, regions and finalizers.
   `project m1 = project m1 ∘ project m2` is a theorem, and a fixture pair that agrees
   under `m1` and differs under `m2` is a required self-test.
3. **Outcome is annotation-blind and host-error-identity-blind** under `m1`:
   `(tag, reason tags in order, fail payload)`. This is a decision, recorded here,
   not an accident of the encoder (`FRAME-FB-STACK-ANNOTATION`, `FRAME-FB-HOST-ERROR`).
4. **Two tapes.** The choice tape answers `decide` sites by occurrence with a site
   check; the scheduler tape is the single-fiber preset in this phase. They are
   never reconciled here.
5. **Frontiers compare as frontiers.** Fuel and tape exhaustion end a trace with
   `frontier`; a host op budget ends it the same way. Neither is a failure.
6. **Layer build and teardown are outside the compared window.** Host traces carry
   phase sentinels; agreement covers the `run` phase only. This is about the
   *harness's own* service provision — building the traced service and providing
   it. Packet M4's `Layers` family is not an exception to it: its memo map and
   its enclosing scope are made before the `run` sentinel like any other harness
   state, and what happens inside the window is the family's four operations,
   which is a program performing `build` and `close`, not the harness providing
   a service.
7. **Answers are recorded as typed.** The host proxy encodes an answer by the
   operation's declared answer spelling, so a `void` answer is unit whatever the
   host returns. Found on the first host run: rc.112's `Ref.set` returns the
   mutable ref at runtime under a declared `Effect<void>`; the untyped encoder
   died on it and the trace ended in `done {"failure":[]}`.
8. **Scheduler insensitivity is checked, not assumed.** Every golden runs twice on
   the host, at a large `MaxOpsBeforeYield` (zero yields required) and at the
   smallest value that progresses, which on rc.112 is 3 (the op is counted before
   the yield check and the yield wrapper costs two ops, so 1 and 2 loop forever).
   The service-level rows must be identical; the frame stream shows the yields.

## What agreement does not establish

Semantic preservation beyond the corpus; anything about primitives or frames;
interruption; concurrency or scheduler order; types (a separate receipt); layer
observer counting, concurrent memo identity and layer composition
(`E4-SEM-CE-015`, the `layers` row); host error identity, stack
annotations, defect payloads; termination; byte identity of the generated program;
and any statement about the host from a Lean theorem. These are the refusal rows
of `docs/LOWERING-COVERAGE.md`.

Packet D4 (2026-09-03) moves exactly one item on that list, and only half of it:
"anything about primitives or frames" now has a **Lean** half —
`Effect4.FrameSimulation.compile_simulates` relates the frame machine to
`interpret` on the compiled fragment, relative to an answer tape. The **host**
half does not move: no theorem relates a host row to a `FrameEvent`, and the
frame stream is still recorded and never compared. Interruption, concurrency,
types, host error identity, stack annotations, defect payloads,
termination and byte identity all stay exactly where they were.

Packet M4 (2026-09-03) narrows "layer build, memoization and provided-scope
semantics" rather than closing it. Build identity, the construction count, the
per-construction layer scope and the release order are now observed at the
service level and agree with rc.112 across eight goldens; what stays refused is
named in the `layers` row and in `E4-SEM-CE-015` — observer counting, concurrent
memo identity, and layer composition. None of it is a Lean theorem about the
host, and none of it moves the `semantics` or `bridges` rows.
