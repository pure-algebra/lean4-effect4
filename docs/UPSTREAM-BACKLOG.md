# Upstream backlog: findings against the pinned Effect runtime

Rows here are behaviours of the pinned `effect@4.0.0-rc.112` source (upstream commit
`2600f62f`) that contradict its own types or documentation. The machine records each intentional departure as a signed divergence; a row records the reproduction, the comparison that shows it is a
defect and not a design choice, what we did about it on our side, and whether it has been
reported. Reporting is the owner's decision. Earlier host findings recorded inside
design-issue rows (DI-24's non-nominal service keys, DI-39's `tagIs` type-guard
disagreements, DI-97's `Deferred.poll` answer) and in the research notes stay where they
are until someone touches them; new findings go here.

| ID | Finding | Where in rc.112 | Comparison | Reproduction | Our disposition | Upstream |
| --- | --- | --- | --- | --- | --- | --- |
| `U-01` | A typed failure escapes an interruptible catch. An interrupt recorded while the fiber is masked, followed by a failure leaving the masked region, restores the mask and then discards every failure continuation above it, so the original `Fail` reaches the fiber's exit: `Effect<number, never>` completes with `Fail(42)`. When a later `interruptible` restoration re-masks the walk, a handler typed at one error runs on another's payload, which is a host `TypeError`-class defect from well-typed source. | `internal/core.ts:540-545` (the failure evaluator's discard loop passes the cause on unchanged; no failure stripping exists in the source); `internal/effect.ts:4312-4319` (the mask-restoring frame injects the recorded interrupt on the success path only; on the failure path that continuation is discarded by the same loop). Still present on `Effect-TS/effect-smol` `main` on 2026-09-21 (`core.ts:538-543`, `effect.ts:4217-4225`), by inspection only. | Effect 3.22.2 handles the same interleaving soundly: `dist/esm/internal/fiberRuntime.js:974-1000` skips the handler but replaces the cause by `stripFailures(cause)` (defects and interrupts only), and appends the interrupted cause when a mask is restored with a pending interrupt. Effect 4 kept the abort-without-handlers intent and dropped the stripping. | `docs/research/2026-09-21-foundations-fr08-evidence/VendorEscapeProbe.ts` (bun, the pinned source, hashes in `vendor-sha256.json`; output `vendor-escape.json`); on the Lean machines `E4-SCHED-CE-008`, `Test/Counterexamples/Machine/Semantics/InterruptEscape.lean`. | **Diverged** (owner, 2026-09-21, ruling §4): at a preempted skip of a catch that would have run, both walks pass on `Cause.combine (stripFail cause) ic`, Effect 3's rule; diverged at `5f33fe3c47725bf9ff9c6632d90bea50d7a442b6` (the divergence slice), gated as a signed divergence in the runtime census and as a signed host exception in the truth harness. The runtime agreement theorem keeps its original conclusion and no run premise. The typed-state proof remains slice 5 work. Ruling: `docs/research/2026-09-21-foundations-slices-3-4-review-and-fr08-ruling.md`. Suggested upstream fix: after the discard loop, when the fiber is interruptible with a recorded interrupt, replace the cause by its non-`Fail` reasons combined with the interrupted cause. | not reported (2026-09-21) |
