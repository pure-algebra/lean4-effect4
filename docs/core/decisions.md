# The one list — every open decision of 2026-09-17, deduplicated (2026-09-17; reviewed 2026-09-18)

Folded from: the consolidation note (D-A…D-J), the three seat receipts (daemons D1–D4, author
C1 and its three decisions, run §4–§5), scout C (D1–D12), scout D (D-1…D-8), scout E (E-1…E-10
and its eight slices), scout F (F-1…F-12 and the seven squares), the LCNF survey (§7, the LLVM
steer). Where two or more said the same thing under different names, one row; the sources are
listed so the reasoning can be found. **Owner** = a ruling is needed; **do** = no decision
content, scheduled. Recommendations are the coordinator's, after reading all six inputs.

**Reviewed 2026-09-18 against HEAD** (after the census, `fold_of`, the checker as one
`Except`-valued fold, the `explain` and `effTy` callers slices, and the first Schema cuts): a
status column, verified row by row against the tree with file and line; rows 12, 13, 30 (for
`denote` and `effTy`), 36, 37, 38 closed; rows 39 (the Schema wipe as one ruling) and 40 (the
callers phase, continued) added; the order re-cut at the foot. The owner's steer of 2026-09-17
("coherence of the algebra is the metric, not consumers; convert to folds where the shape
allows, delete at a good place") is applied to the rows argued from consumer counts (4, 9, 12).

## A. Types and schemas at the boundary

| # | decision | recommendation | sources | who | status 2026-09-18 |
| --- | --- | --- | --- | --- | --- |
| 1 | **The canonical schema object.** (a) the Schema AST replaces `Representation`; (b) the AST is a second carrier whose `toRepresentation` fold lands in the pinned 22-tag one; (c) no AST carrier | **(b), after row 6 and row 11 land** — the pin and `fromJson` are about the persisted projection, so (a) discards the estate's only stamped schema claim; the AST is needed only for the three things the projection drops, and only one of them (a transformation) is something Lean owns better than rc.112. This reverses the coordinator's earlier position, on E's evidence | E-1, E §13; D §5; consolidation D-J | owner | open; the evidence moved: `Endpoint` and `Transform` are deleted (`b08f3b58`), so the "transformation" the AST was needed for is gone; the trigger is now the first foreign schema actually read (row 32 step 2), as `ontology.md` §3.4 says |
| 2 | **Names for records and sums.** (a) sugar only; (b) `Ty.record`/`Ty.variant`; (c) annotation-carried names | **(c) now, (b) before the first foreign consumer, as stages** — `select` reads the tagged tuple that `Arrays[Literal t, T]` already is, so annotations keep the eliminator working; a foreign `decodeUnknownSync` never reads an annotation, so the first consumer of a published record dates (b) | D-A, D-1, E-5, C §3.4 | owner | open |
| 3 | **`Ty` and the AST.** Design A (`Ty` embeds, proved) + `Ty.app name args` with `handle t = app t []`; or Design B (the checker over AST nodes) | **A with `Ty.app`** — B's cost is measured: `deriving DecidableEq` and `Repr` fail on `Representation`, and `Ty.key`'s injectivity (hence `normalize`, `sub`, both soundness statements) does not survive `Json` annotations in the key. `Ty.app` is five additions and one S1 wire tag | E-4, E §4, E-R3 | do | not started: `Ty` has no `app` (`src/Effect4/Program/Ty.lean:24-42`). Its need came with row 1, so it waits with row 1. A new constructor now adds a field to the generated `TyAlgebra` and an arm to each of the 16 folded `Ty` traversals — cheaper once the `Ty` callers have moved (row 40) and there is one place to add it |
| 4 | **Checks with meaning.** (a) `Check.holds : Check → Val → Bool` over a closed id list; (b) store-reading; (c) none, agreement per boundary as a theorem | **(a) for the closed list + (c) for the external-handle counter** (`externalValue_iff_fits` beside `Admit.lean`) — the one check that matters at a real boundary is not a predicate on a value; (b) would put the store into every check | E-2, E §3, E §6; D §7.2 | via row 39 | open; no `holds` exists. Row 39 settles it without its own ruling: `Check.lean` (the judgment nobody calls) and `Arch/Accepts.lean` (the third checker) go, `Codec.isValue` and `Shape.fits` remain, the external-handle counter is (c) as a theorem beside `Admit.lean` |
| 5 | **D12's shape.** One theorem `decode ast v = some x ↔ fits ast v`; or the sound/total pair with `isValueAst` **plus** the S-5 harness gate (every recorded corpus exit decodes under its published `Schema.Exit`) | **the pair and the gate, as two claims** — the single theorem is false in the total direction (the S-3 amendment); the Lean theorem and the host gate are about different decoders. This is F's square C6 and D's D-5(b) | E-3, D-5, F C6, D-D | do | not started; `isValueAst` does not exist. Restate at `Ty` (`Codec.decode`, `Codec.isValue`) since the AST carrier waits with row 1 |
| 6 | **`ofSchema` refuses what it cannot represent** (non-empty `checks`, non-`null` payload, except the two checks the bridge mints) | do first; `ofSchema_schema` unaffected; F's exactness (C3) begins here | D-3, F-6, consolidation §2.5 | do | done 2026-09-18 (`4f2fadc8`): `ofSchema` answers exactly the nodes `schema` mints, modulo annotations, and refuses the rest (checks `schema` does not mint, a non-`null` payload, a defect slot that is not `Defect`, an optional or annotated element); `ofSchema_schema` unchanged. Still open: the exactness theorem itself (`ofSchema r = some t → r = schema t` up to annotations) is stated by the refusals, not proved — row 41; and the row stays one of the two `fold_of` refuses (§7.4): its fold needs a wrapper-list shape the converter does not have |
| 7 | **Handles at the boundary.** (a) publish `Number` + the counter check; (b) opaque declaration + a host reviver minting the index; (c) state the exclusion. And: a described handle arriving from another process is refused, admitted in-process | **(b), with the cross-process refusal** — rc.112's own mechanism, the same one the six `effect/schema/*` ids need for row 11; the schema accepts a live handle either way, so the refusal is the MCP face's, not a schema check | D-B, D-2, C-D8, E §6 | owner | open |
| 8 | **`effDocument` keys a requirement on half a `ServiceKey`; `ShapeDoc.document` repeats keys; two docstrings promise `Schema.Struct` text** | fix all three now (key `k{name}_{service}` and the carrier's schema, threading the signature; dedupe by key and refuse a repeated key with different bodies; say "the persisted `SchemaRepresentation` JSON") | D §7.1, §7.4, §1; consolidation §2.6, §2.8 | do | not started, all three verified: `effDocument` keys `service_{k.name.value}` (`Bridge.lean:166`); `ShapeDoc.document` maps `defs` with no dedupe (`src/Effect4/Store/Shape.lean:468`); `src/Effect4/Api.lean:58,575` still say `Schema.Struct({…})`. `ShapeDoc.document` moves with row 39's last step, so fix it there |
| 9 | **`Api.schemaOf`.** A projection with no reader, no law, zero call sites | **give it C6's consumer (row 5) this wave, or delete it** — a published schema nobody decodes under is a claim, not a boundary | F-11(a), D §7.5 | via row 39 | open; still zero call sites (`Api.lean:137`). Under the coherence metric the argument is not the count: it composes two census arrows (`typeOf`, `EffTy.document`) into a publication with no reader, which the vocabulary calls a widening, not an embedding. Deleted under row 39; the publisher of the exit schema is `EffTy.document`, its consumer the S-5 gate (row 5) |
| 10 | **One `Val → Json`.** Four independent images today | **`ShapeDoc.print` survives**, `Schema.encode` proved equal to it on the admitted domain, the harness's two `valJson`s deleted — the shape-directed image survives any change to `Ty` (row 2); the type-directed one does not | F-5, F C4, C §3.2 | owner | open; today `Codec.encode` (`Ty`-directed, `Schema/Codec.lean:224`), `ShapeDoc.print` (`Store/Shape.lean:539`), `Schema.Image.encode` (`Schema/Image.lean:46`, goes with row 39) and one `valJson` left in the truth harness (`harness/truth/session/Keyed.lean:104`) |
| 11 | **Where `Schema.*` text comes from.** (a) a second emitter beside `printAlgebra`; (b) rc.112's `toCodeDocument` + a reviver table; (c) none | **(b)** — one owner for the spelling; the reviver table is row 7's work | D-E, D-6, E slice 5 | owner | open; `Codegen/Schema.lean`'s `printAlgebra` is the emitter today (the `schema-ts` gate) |
| 12 | **`Transform`'s category laws** (identity, associativity at the meaning) or rename the composition API so it does not read as a category | **prove them once `denote` is a fold (row 30)**; if too costly, rename — an unlawful composition API in the schema layer is how a second program language starts | F-8, F §4b | done | moot: `Schema/Transform.lean` and `Laws/Schema/Transform.lean` deleted 2026-09-18 (`b08f3b58`). The laws become the three `denote` equations of `ontology.md` §5.4, stated at `Eff`, where `denote` is now a fold (row 30) |
| 13 | **The schema/program rule.** Schema is a data language; every effectful slot is a hole filled by an `Eff` program with a typing certificate; `Ty`/AST never mention `Eff`; a foreign `Transformation` is a name with an AST-typed signature that any meaning-needing operation refuses | write it into the vocabulary (row 36) | F-7, F §4b; consolidation D-J | done | written: `AGENTS.md` Vocabulary, "Schema and program" (`f8c9b7fe`) |

## B. The run and the MCP surface

| # | decision | recommendation | sources | who | status 2026-09-18 |
| --- | --- | --- | --- | --- | --- |
| 14 | **The table travels by digest.** `Built.digest` over `programBytes ++ tableBytes`; `(digest, Run.id)` the wire identity; `Call`/`Header` carry the digest | **yes, now** — 89% of an answer's bytes and a 231 KB advertised schema are the table by value; `Canonical RowTable` exists. Changes the wire shape of `Header`/`Call` (compat policy) | C-D3, C-D7, D-C | owner | open; no `digest` under `src/Effect4/Api/` |
| 15 | **The MCP server.** Lean `--run` driver in `src/Tools` first, generated bun second; fourteen tools, `run.play` withheld; the server holds the journal, not the `Run`; no reactor over the wire; `ToolSpec` table inside the gate; the run protocol before R12's inspection protocol | **yes, as one choice** — only a Lean host gets `open_total` (a `Module` is a function, a certificate is a `Prop` record); `journal_replays` makes the cached `Run` an optimisation; four of R12's nine commands are buildable and five need a `Doc` that does not exist | C-D1, D2, D4, D9, D10, D11; D-F | owner | open; no `src/Tools`, no `ToolSpec` |
| 16 | **`Await` becomes a structure** `{ key, op, request }`; `Observation.fibers` and `Built.rowNames` named | do — the most-read response field prints as nested arrays today; a projection in the MCP layer would be a second representation | C-D6, consolidation §2.1 | do | half: `Built.rowNames` (`Api/Built.lean:25`) and `Observation.fibers` (`d798feb2`) landed; `Await` is still the tuple `FiberId × Nat × NativeOp × Val` (`src/Effect4/Program/Admit.lean:37`) |
| 17 | **Codecs for the reading types.** `Canonical FiberStatus` and `Canonical Observation` (two manifest lines, `Runner` group); a `Refusals` group for the eight refusal types; `head : T → String` per sum instead of `Repr` | do — no decision content; closes run receipt O-8 and the observation dogfood | D-7, C-D5, C-D12 | do | not started beyond what the `Runner` group already had (`TableRefusal`, `AdmitRefusal`, `HostSession.Refusal`, `tools/Effect4Gen/manifest.json:143-148`); no `FiberStatus`/`Observation` line, no `Refusals` group, no `head` |
| 18 | **`Built.digest` injective and `observe_inspect` / `observe_daemonsQuiet` / `build_check`** — the proofs that tie the faces | do with rows 14 and 16 | consolidation §3 | do | not started; none of the four names exists |

## C. Authoring, daemons, and the older face

| # | decision | recommendation | sources | who | status 2026-09-18 |
| --- | --- | --- | --- | --- | --- |
| 19 | **The three root modules and the daemon words.** `Effect4.Author` / `Run` / `Face` as the imports an agent writes, `Api.*` the deep source; `fork` / `daemon p in s` / `detach p`, no author-written flag at a pin | **yes, as a file-move wave after group A's first slice** | D-G, D-I2, daemons D4 | owner | open; `ontology.md` §3.4 adds: write the three fresh and leave `Api.lean` with what they import, rather than move |
| 20 | **One machine edit.** A path on `RunEvent.forked` (closes `supervision_static` "at those paths"); the race-entrant options named once (a literal at 9 sites) | **yes, together, one rebuild** | D-H, daemons D3, consolidation §2.11 | owner | open |
| 21 | **`nativeSignatureWith`** stays a declaration-site check (`ServiceDef.Agrees`, `BuildRefusal.serviceCarrier`) rather than threading through the checker and the certificate | **keep** until an application needs a seventh carrier — threading changes `Built`, `HostSession.start`, `Run.open` and both soundness statements | D-I3, author receipt | owner | open (`Api/Author.lean:39-50`, `Program/Authoring/Services.lean:49` as described) |
| 22 | **`gen`/`Stmt`** stays a printer spelling with no authoring lift (authored programs never contain `Stmt`), or the statement family is retired from `Eff` | **confirm the first**; the owner ruled `gen` stays on 2026-09-17; the consequence is one constructor family no author can write | D-I, D §8.1 | owner | open; the checker now types the statement sort through `StmtTy` (census §7.5), so the family costs one algebra field per statement constructor and nothing else |
| 23 | **`Api.author` beside `Author.program`** — two verbs, one pipeline, tied by `build_check` | do (row 18); no cut | D-I1 | do | open; both exist (`Api.lean:503`, `Api/Author.lean:73`). `ontology.md` §2 re-cut this as a delete of `Api.author`; `build_check` (row 18) is the keep-both route. One of the two, with row 18 |
| 24 | **The minted-spelling fix** in `Sugar.bindWith`, `Sugar.andThen`, `Loops.iterateWith` and generated `Forms.lean`; one regeneration | do — the red control is pinned | author C1 | do | not started: `Authoring/Forms.lean:48-52` still binds the constant `"_answer1"`; the red control is pinned at `Test/Program/AuthorContract.lean:321` |
| 25 | Settled, recorded: `FiberStatus.root` stays; the six `rfl` proofs stay `rfl`; the error column stays wide; the three named runs return the `Run`; `Observation` derives `DecidableEq` only | — | daemons D1, D2; D-4; run §5 | — | — |

## D. Lowering, TypeScript generation, vendoring

| # | decision | recommendation | sources | who | status 2026-09-18 |
| --- | --- | --- | --- | --- | --- |
| 26 | **The rules for canonical TypeScript**, corrected by E. Every TypeScript artefact is a fold of a *committed table or free object* (`Eff` for programs; the row/`Entry` table for types, folded two ways — a Schema AST for values and a `TypeRef` for types, never one from the other; the LCNF closure for code); no artefact from the Lean environment (`TsGen`'s read retired) or from a hand transcription (`prelude.ts` generated); the emitter targets the TypeScript **syntax** AST; type generation is split — tables own types, LCNF owns semantics, sugar owns authoring | **adopt** — E's count (91 of 7,008 exports are AST-typed entries; `Effect`, `Layer`, `Stream`, `Ref`, `Deferred`, `Queue`, `Scope`, `Fiber`, `Exit` all 0) is why "TypeScript types from the Schema AST" cannot be the rule and "two parallel folds" can | D-J, E-6, E-7, E §10, F-11(b), D §5 | owner | open; `tools/Tools/TsGen.lean` still reads the environment, `harness/truth/prelude.ts` is still hand-written |
| 27 | **The LCNF per-module recipe** (roots → closure + case-site policy → emit → rung 2/3 differential → truth differential → `Entry` table) with *decision agreement on the observation* as the obligation, and the fact that **certificates do not cross** (`Prop` erasure); root order: codecs, the checker, the `Run` transitions, the authoring lifts | **adopt** | F-9, F §6, LCNF survey §5 | owner | open; the checker root is now one fold (`Program/Checker.lean`), the shape the recipe wants |
| 28 | **What "verified lowering" means.** (a) a full CompCert-shaped simulation between `Conform.Lcnf.Semantics` and `SemanticsTarget`; (b) rule coverage for the rungs (every translator rule has a red mutant) plus a simulation on the scalar fragment only | **(b)** — six mutants for a twelve-row walk plus a 50-row table is an under-covered control; the full proof is not the cheapest true claim | F-10, F C7 | owner | open |
| 29 | **The lowering architecture (the LLVM steer).** High tier (OCaml, TypeScript): target descriptions as data (`Profile`, `Lowering`) with decided laws; legalization (promote / expand / custom) as rewrite rules with obligations, seeded from `Conform/Rules.lean`; `SemanticsTarget.Expr` promoted from the rung-3 reader's image to the common currency every printer consumes. Low tier (WebAssembly, native): Lean's reference-counted IR into LLVM, no backend of ours | **adopt after row 31** — the TypeScript printer is the profile's first customer | LCNF survey §7; owner 2026-09-17 | owner | open |
| 30 | **`denote`, `effTy`, `compileEff` onto the fold** (the three exemptions of F's census). `denote` first, `effTy` second; `compileEff` carries `Point` with fuel and risks the ~1,900-line agreement proof | **`denote` and `effTy` this wave; decide whether `compileEff` stays exempt** | F-3, F C1 | owner (for `compileEff`) | `denote` done (`fdd8e460`: `denote`, `denoteB`, `denoteWith`, `denoteBWith` and the four measures as folds with kernel-checked connectors); `effTy` done (census §7.5–§7.8: the checker is one `Except`-valued fold, `effTy` its success by definition, the hand block deleted). `compileEff` (five census rows) is the one remaining ruling; the recommendation is to keep it exempt until the LCNF root order (row 27) reaches it |
| 31 | **The rung-3 TypeScript reader before any emitter breadth** (`TypeScript.Expr → Target.Expr`, refusing by constructor name, the same 20,387 vectors, the six mutants red) | do, in this order — the only artefact that makes "the emitted TypeScript means what the Lean function means" a checked claim | E-10, E slice 7 | do | open |
| 32 | **Vendoring order.** The two package modules (byte-identical regeneration of the 8 existing rows) → `McpSchema`'s 78 harvested Schema ASTs → `Duration` → `Exit`/`Cause`/`Option`/`Result` → the handle modules → `Effect`/`Layer`/`Stream` last, with a published refusal list; scanner = tsgo in `harness/schema-host` (the oxc recognizer has no checker) | **adopt** — the first two steps produce falsifiable evidence with zero emitter work | E-9, E-8, E §11 | owner | open; step 2 is row 1's trigger |
| 33 | Two additions to `lean4-typescript`: a type-parameter binder list on `ConstDecl`/`ProgDecl`; `TypeRef.intersection` if an `Entry` ever needs it (none of the 91 does) | do when row 26's emitter needs them | E-6 | do | open |

## E. Coherence gates and the vocabulary

| # | decision | recommendation | sources | who | status 2026-09-18 |
| --- | --- | --- | --- | --- | --- |
| 34 | **The traversal census as a gate** (C1: every `Eff` traversal is `cataFam alg` for a declared algebra or a named exemption; ten algebras, six exemptions today) and **fusion generated** for `Eff` (C2) | do — one generated table and one generator change; the exemption list's length becomes the honest distance from the principle | F-1, F-2 | do | half: the census is an instrument, not a gate (`#traversal_census`, `Test/Audit/TraversalCensus.lean` prints and asserts nothing); it counts 81 hand traversals over five free objects, 68 with a fold and connector, 13 without (`compileEff` 5 exempt, `Sched` 5, `valCode`, `ofSchema`, one derived instance) — not six. The gate form is scout G's `env_linter` with the exemption list as data (census §8), worth writing once row 40 has shortened the list. Fusion is not generated. **Ruled 2026-09-18 (owner): no gate, no fusion; the census stays an instrument and the exemption list is tracked debt** |
| 35 | **Fragments by exclusion** (`Straight`, `Looped`, C5) and **exactness wherever a read exists** (C3: `ofSchema` first, the JSON codec second) | do — a new constructor silently leaves both fragments today | F-4, F-6 | do | the fragments: done 2026-09-18 (census §7.10) — `Straight` and `Looped` name every constructor, no wildcard and no fallback, so a constructor added to `Eff` is a missing case at each definition until classified. Exactness (`ofSchema`, the JSON codec): open, with row 6 |
| 36 | **The vocabulary in `AGENTS.md`**: free object, algebra, fold, exact embedding (three laws), simulation, located refusal, monoid action; the rule that a new representation is admitted by naming its signature and the kind of each of its arrows; row 13's schema/program rule | do — "the owner asked where to *go*; there is nowhere to go, and the vocabulary is what makes that usable" | F-12, F-7 | done | written (`AGENTS.md` Vocabulary, `f8c9b7fe`) |
| 37 | **CI repaired** (broken since `78684a8`; the other reason the corpus pin went stale) | done 2026-09-18 (`3a394912`: the cache key inline in each cache step); unverified until pushed | ledger | done | unverified until the owner pushes |
| 38 | **`explain` onto the fold**: (a) `fold_of`'s paramorphism over a `Checker.explain*` copy with `explainStmt`, or (b) one `Except TypeRefusal EffTy`-valued fold that is the located-refusal arrow, `effTy` its `toOption` and `explain` its error, `explain_none_iff` by the shape of `Except` | (b) — ruled and landed 2026-09-18 (`traversal-census.md` §7.5) | census §7.5 | done | landed and then finished: the connector `check_eq` named at the ruling has since been deleted; `effTy` and its siblings are the check's success at the root *by definition* (`Program/Typing.lean`), `explain` its refusal (`Program/Checker.lean`), the proof graph at every path (`Laws/Program/Typing/CheckSound.lean`); census §7.7, §7.8 |

## F. Added at the 2026-09-18 review

| # | decision | recommendation | sources | who | status 2026-09-18 |
| --- | --- | --- | --- | --- | --- |
| 39 | **The Schema wipe as one ruling** (`ontology.md` §3.4, less what `b08f3b58` took): `Schema/Check.lean` (1,499 lines: the field-admission judgment, 64 theorems, called by nothing), `Schema/EffectfulField.lean` (960) with `Codegen/EffectfulField.lean` (192), `Schema/Image.lean` with `Laws/Schema/Image.lean` (81), `Arch/Accepts.lean` (117, the third value-fits checker), `Api.schemaOf`, `Annotations.lean` cut to its carrier and two keys (about 150 of 1,193), the `schema-annotations` and `schema-effectful-field` harnesses, the tests §3.4 names; then `Store.render`/`ShapeDoc.document` out of `Store/Shape.lean` into `Schema/OfShape.lean`, so `Store` stops importing `Schema` (`Shape.lean:1` imports `Schema.Authoring` today, the dependency pointing the wrong way) | **rule the list once, land it as a commit series with narrow builds**, in the order: `EffectfulField` first (it imports the standalone `Effects` package — a second effect algebra inside the core root's closure, which the vocabulary forbids whatever its consumers), then `Check`/`Accepts`/`Image`/`schemaOf`, then the `Annotations` trim, then the `render` move. Closes rows 4, 9 and 12's residue; `Schema/Fold.lean` (1,094 generated) stays while `Codegen/Schema.lean`'s printer folds with it | ontology §3; owner steer 2026-09-17 | owner | **ruled 2026-09-18** (owner: "row 39 seems reasonable"); the annotations trim and the `render` move explained to the owner the same day (STATE "What row 39 does"), landing after that note is confirmed; nothing deleted yet |
| 40 | **The callers phase, continued** — the pattern of census §7.8 (the fold is the definition; the hand block, its agreement and its inductions go): the term typer (done, §7.9); `Straight`/`Looped` (row 35, done at the definition, §7.10); the value sort's block (`render`, `encode`, `WF`, `hasTy`, … 16 of 17 folded); the `Ty` block (16 of 17); `ofSchema` and `valCode` with row 6. Also: `Laws/Program/Typing/Check.lean` (58 lines) is kept for one contract test; `tools/Conform/Spec/Reflect.lean` fed the deleted `specs` group and is unaudited | do, in that order; the term typer first (smallest, closes `Term` entirely) | census §7.8; STATE "Next" | do | in progress; the checker's, `explain`'s and the term typer's slices landed (census §7.9: `Term` 11 of 11, 78 hand traversals, 65 with connectors); `Straight`/`Looped` were not a callers move (one definition each) and are done at the definition (§7.10). **Closed 2026-09-18 (owner): the remaining thirteen exemptions (`compileEff`'s five, the reference evaluator's five, `valCode`, `ofSchema`, the derived instance) stay as they are, tracked in census §7.4; nothing is converted for uniformity's sake — a hand definition that is not a fold is not thereby wrong** |

| 41 | **The coherence milestone, restated from the owner (2026-09-18):** not a gate and not uniformity. The question is which proof obligations back the claims the API makes, and which of those are still open. *Have:* a program is admitted with a type or refused with a path and a reason, and the two agree with `HasTy` (`check_sound`/`check_complete`, `explain_none_iff`); a typed program of the straight and loop fragments means what its type says and the compiled machine runs it as the meaning says (`effTy_sound`, loop soundness, `run_eq_meaning`, `loopAgreement`, `run_eq_ref`); a run is its journal (`journal_replays`, `replay_unique`, `drive_eq_play`) and `Run.open` cannot refuse; authored programs are scope-safe by construction; the printer and reader round-trip over the row table (laws 11/12, completeness); a type's schema reads back (`ofSchema_schema`) and the codec agrees with rc.112 (the `schema-codec` gate). *Open, and API-backing:* the concurrent alphabet — fork, race, daemons, the scheduler, timers — has no meaning statement at all; the fragments stop at loops, and `Supervision` exposes all of it. That is the milestone. *Open, boundary-backing, needed when that face is built:* every recorded exit decodes under the schema its program published (row 5); the observation the API returns is what the machine holds (row 18). *Open, target-backing, needed for the TypeScript target claim:* the emitted code means what the Lean function means (row 31) | **the concurrent alphabet's statement is the milestone**; the two boundary rows land with group B, the target row with group D; nothing else on this list is owed | owner 2026-09-18; STATE "Proved" | owner | **ruled 2026-09-18 (owner): the core milestone, no shortcuts.** The design is `docs/research/2026-09-18-typed-state-plan.md`: the typed residual over `RSig = StoreSig ⊕ FiberSig` in the shape of `SoundB`, `denoteR_typed` by induction reusing `sound`/`soundB`'s arms, one preservation lemma per fiber operation on the shared scheduler, the transfer through `BMeans`; implementation from its §3 in order. Scouted 2026-09-18 (`2026-09-18-typed-state-proof-graph-scout.md`, plan §6): eleven corrections folded in; **one ruling owed: `HandlesFit`, a fourth invariant field for fiber handles, since `Val.hasTy` is coarse at `fiberOf` by DI-17 and stays so** (recommended: add the field, leave `Val.hasTy`). Composed 2026-09-18 (`2026-09-18-typed-state-composed-graph.md`, plan §7, pending the owner's read): a generic protocol-typed predicate on the free monad (probed, no axioms), `OpOk` beside `AnswerOk`, a typed stack so delivery is one lemma, the nine delivery sites, hook typing, and the `DeferredNat` column that types an asynchronous completion; the proposal's 17-command frame lemma is false (seven commands), its 55 % cut is 20–30 %. **Two further rulings owed: the promise table `Π` (was `DeferredNat`; per cell, since memoised builds share the deferred store) and layer 0's home (`Laws/Effects/Protocol.lean`, no `Effects` pull-in needed)**; plus a DI owed for the release's exit parameter (typed at the acquire's exit, rc.112 says `Exit<unknown, unknown>`). Step 0 designed and landed 2026-09-18 at the owner's request (`2026-09-18-position-census-design.md` §3a): `#position_census` and its siblings, the source table, the totality gate (74/74), layer 0, and the generated skeleton `Typed/State.lean` parametric in the carrier predicates, built before any predicate as the owner ruled; owed: the obligation ledger and the `make gen` group |
| 42 | **Polymorphic `Ref<A>` and `Deferred<A, E>`.** This cut's rows fix them at `number` (`Native.lean:138-150`: `refTy := .handle "Ref.Ref<number>"`, `deferredTy := .handle "Deferred.Deferred<number, number>"`; `refGet : refTy → nat`, `deferredAwait : deferredTy → nat / nat`). The machine is not monomorphic (`RefHeap := List Val`, `Stores.lean:1166`; a cell's completion is any `Prim`): the restriction is the signature rows, which are fixed `Ty`s, and `Ty`, which has `fiberOf value error` but only `handle (target : String)` for cells and promises. Every real Effect program uses `Ref<A>`; this is the largest visible gap against Effect | **do it before layer 1 and S1**: `Ty.refOf a` and `Ty.deferredOf a e` replace the string handles; rows carry type variables and the checker's `perform` rule instantiates them from the request's type (matching one way, subsumption kept); `Val.hasTy` stays coarse at handles (Effect erases handle types at runtime, so the world's per-cell tables Π/Ρ are the typing, row 44); the printer emits the instantiated arguments through the rows' existing `typeArgs`; `HeapNat` becomes the per-cell heap column. Doing it after S1 restates S1's `perform` arms, `answer_typed`/`progress`, the columns and `CheckSound`'s row lemmas a second time. Correction to the night's notes: the 17th's "template table" is the printer's skeleton table, not schematic rows; nothing exists for this yet | Native.lean:138-150, Ty.lean:24-42, Checker.lean:201-212; composed graph §5; census design §3a; rows 42–43 plan | owner | **ruled 2026-09-18 (owner): do 42 and 43; first order stays, power in the builders.** Step 1 landed the same night (`refOf`, `deferredOf`, `var`, `closed`; plan §2a); step 2 landed (the template calculus `instantiate`/`infer`/`matchTemplate`, `rowTy`, the checker's rule, the laws, row 55's variance; plan §2b, with the findings for 3–5); steps 3–5 (the rows as templates with the store op carrying a binder term, the per-cell table, the faces) next |
| 43 | **Function-taking rows.** `Ref.update f` takes a `FnName` from a five-name alphabet over numbers (`Stores.lean:109-117`); there are no lambdas. But the language already binds by position inside terms: `iterate`'s `test`/`step` are terms at `env ++ [cursor]` (`InterpR.lean:283`), the release at `env ++ [a, exit]`. **Corrected the same night** (`docs/core/language-cut.md` §1): the fix is not closures | **rows take binder-carrying terms**, evaluated at `env ++ [current]` exactly as `iterate` does; `FnName` retires; the term typer types them as it types `iterate`'s. Functions as *values* stay refused by the profile (DI-20/28, the basis) with the content-addressed program as the direction. With row 42 | Stores.lean:109-117; language-cut §1 | owner | **ruled 2026-09-18 (owner) with row 42**; lands as step 3 of the rows 42–43 plan |
| 44 | **Handle typing: coarse values, typed world.** `Val.hasTy` ignores the type arguments of every handle sort (`Program/Typed.lean:55-61`, `Progress.lean:130-141`; DI-17). The alternative is runtime type tags on values | **adopt**: keep `Val.hasTy` coarse (that is Effect's erasure) and type every handle by the world: the fiber table `Γ`, the promise table `Π`, the heap column `Ρ`, with one `HandleFit` family (the `HandlesFit` ruling of plan §6, generalised from fibers to all sorts); no runtime tags, no change to `Val`, the bytes boundary or the OCaml engine | plan §6; scout E.3; composed graph §2 | owner | open, recommended |
| 45 | **The promise table per cell.** Memoised layer builds allocate from the same deferred store and complete with the layer's exit (`Stores.lean:2329-2345`), so a constant column is wrong; `Stores.WF` excludes completions (`STORES-FB-COMPLETION`) | **adopt**: `Π : DeferredKey → Option (Ty × Ty)`, extended at `deferredMake` (the row's types) and `memoBuild` (the layer's), with "every stored completion fits its cell's entry and an `ofRefGet` completion names a live cell" (`E4-STORES-CE-003`) as the column | composed graph §9; StoresLaws.lean:19-22 | owner | open, recommended |
| 46 | **A top type.** rc.112 types the release's exit parameter and `Fiber<unknown>` shapes with `unknown`; `Ty` has `never` and no top, and the checker spells `.handle "unknown"` as a placeholder (`Checker.lean:361-364`) | **add `Ty.unknown`**: `hasTy v unknown = true`, `sub t unknown = true`, printed `unknown`, read back; the placeholder handle retired | Ty.lean:24-42; Checker.lean:361 | owner | **landed 2026-09-18 (L5 of the language push)**: `Ty.unknown`, key 19, top of `sub` (`sub_unknown`), inhabited by every value, absorbing in a union by the antichain; printed, read, on the profile, in the schema bridge (`Schema.unknown`), the OCaml alphabet; the snapshot rules at `Fiber<unknown, unknown>` by subsumption; the placeholder handle retired |
| 47 | **The release, a DI to file.** (a) The checker types a release body's exit parameter at the acquire's exit (`Checker.lean:203`); the reference hands it the scope's closing exit (`scopeAdd` on a closed scope, `Stores.lean:2291-2294`; `acquireInR`, `InterpR.lean:117-124`); rc.112 says `Exit<unknown, unknown>` (`Effect.ts:12930`). (b) The checker leaves the release's error column unconstrained (`:203-205`); rc.112 says `never` | **both, as rc.112**: the parameter at `exitOf unknown unknown` (row 46), the release's error column required `never` (a release cannot fail in Effect; a failing one is a defect). Agreement is clearly better here: the closing exit is the scope's, not the acquire's, and no other typing is sound | composed graph §9; CheckInversion.lean:191-197 | owner | **landed 2026-09-18 (L5)** as rc.112 `Effect.ts:12930`: the release typed at `[a, Exit<unknown, unknown>]`, its error column required `never` (`TypeReason.releaseFails`); DI-94 filed |
| 48 | **The typed stack.** `ScopeFrame.answer`/`.resume` hold continuations with no recorded type (`InterpR.lean:44-45`); the typed-context judgement needs the intermediate types | **existential** intermediate types in `StackOk` (the standard typed evaluation context), no change to `ScopeFrame`, `RSaved` or the pairing `BMeans`; recording types in slots would change the machine's data for no runtime purpose | composed graph §3.2 | me, unless the owner objects | recommended |
| 49 | **Generation in the environment.** The skeleton is emitted today as a file (`Typed/State.lean`) by `Test/Audit/TypedStateEmit.lean`; the book's way is quotations elaborated in place, as `fold_of` does | **adopt R1**: `typed_state_skeleton RState RCmd` elaborates the `Ok` structures where the file sits; a `#print_typed_state` command dumps them for reading; the file and the emit driver are retired; rows become a `typed_position` command with a term antiquote into an environment extension | metaprogramming review R1 | me | recommended |
| 50 | **The obligation ledger as declarations.** A TSV plus a witness-join module was designed; Batteries' `theorem_wanted` elaborates a statement to an axiom-free placeholder (probe Q4) | **adopt R3/R4**: one `theorem_wanted` per (definition, matcher arm, written typed fields) via `matchMatcherApp?`, witnesses matched by `isDefEq` under `forallMetaTelescope`; frame lemmas per (owner, written-field set) generated and proved by a dedicated aesop rule set `TypedState` | metaprogramming review R3, R4 | me | recommended |
| 51 | **Services.** `forkScoped`/`ambientScope`/`service` die with `missingScope` when the context lacks the key (`Fibers.lean:1436`, `:1446`); the checker tracks requirement rows | **`ServiceOk` context-wide**, as Effect's `Context`: every service value in a context fits its key's type; the root program's requirement row is provided at load (`TypedProgram` at an empty row, or a provided context). The requirement discipline then excludes the defect | composed graph §9; CheckInversion.lean:197 | me | recommended |
| 52 | **Progress as a claim.** `Stuck.unknownFiber/unknownScope/unknownRace` halt the machine (`Fibers.lean:389-392`); `badShapeExit` sites number 88 | **adopt as corollaries** of the milestone: a typed run never halts (handles come from forks and makes and are never deleted; scope keys are fresh, `E4-CHECK-CE-016`) and never reaches a wrong-shape exit (the nine decoders get totality lemmas). "No surprises" at runtime is exactly these two | metaprogramming review §3 | owner | open, recommended |
| 53 | **Layer 0's home.** `Laws/Effects/Protocol.lean` imports the pinned `Effects` only | keep it there; move to `Effects/Protocol.lean` (outside the frozen `Algebra/`) at the package's next release; no vendoring of `Effects` into `Effect4`, nothing on the path needs it | composed graph §5 | owner | open, recommended |
| 54 | **External rows.** The reference parks them forever (`InterpR.lean:351`); the store's external half is a refused edge in the census | as is: the table-aware slice states a typed-tape hypothesis against the same `AnswerOk` clause (DI-57) | census design §3a | — | as is |
| 55 | **Variance of the handle sorts.** rc.112 declares `Fiber<out A, out E>` (`Fiber.ts:70`), `Ref<in out A>` (`Ref.ts:59`) and `Deferred<in out A, in out E>` (`Deferred.ts:58`); step 1 of rows 42/43 made `Ty.sub` covariant at all three | `sub` invariant at `refOf`/`deferredOf`, covariant at `fiberOf`: a cell is written through its handle, so a widened handle would admit a write the other alias reads at the narrower type (widen a `Ref<number>` to `Ref<number \| string>` through an `iterate` cursor annotation, write a string, read it at `number`); the per-cell table of step 4 would have refused the soundness proof | plan §2b; `Ty.lean` `sub` | — | landed 2026-09-18 with step 2 |
| 56 | **Wildcards in classifiers.** The standing rule says a proof's catch-all is a wildcard so a new constructor never touches it; `Codec.isSupported` showed the converse (DI-95): a classifier ending in `\| _ => true` put four new constructors into the positive class silently | **a classifier lists its positive arms and closes with an explicit negative**; the case-site policy (`tools/Conform/Effect4/cases-policy.json`) is the ratchet on compiled defaults and `#exhaustive_gate` the printed inventory before a constructor is added; a wildcard is right in a proof and wrong in a classifier | DI-95; `docs/research/2026-09-18-synthesis-tooling-first.md` §3 P2 | owner | ruled 2026-09-18; landed with L5 (`0a2cb898`) |
| 57 | **One TypeScript compiler.** Two lanes ran two compilers (tsgo 7 for the corpus and diagnostics, `typescript@5.9.2` for the target oracle and the truth typecheck), so "assignable" had two meanings | **tsgo 7 (`@typescript/native-preview`, the pinned corpus version) everywhere**: the target oracle, the truth typecheck, `ts/eff`'s typecheck and the assignability differential; `typescript@5.9.2` leaves the pins once no reader needs its parser; row 32's scanner choice stands. "tsgo is faster, it's the future, it's officially recommended, it's what Effect is on" | plan Q8; `tools/target/oracle.ts`; `harness/tsdiag/run-tsdiag.mjs` | owner | ruled 2026-09-18; the migration is the next wave's seat |
| 58 | **Gate removal is aggressive.** The estate adds gates and rarely removes them; three `check-host` members never ran in CI, three conformance drivers were never wired, an informational check gated nothing | **a gate that does nothing goes now, without a replacement first**; a coverage gap while an instrument is rebuilt is acceptable; every slice names what it deletes; the build must get faster, measured by a time ledger read at review (D-L) | plan §5b, 4.8, 4.9; `docs/research/2026-09-13-test-ledger-verdicts.md` | owner | ruled 2026-09-18 |
| 59 | **A generated view whose only consumers are proofs lives under Laws.** The plan put the relational view of `Ty` beside the type (`Program/TyView.lean`); three of its laws close only with aesop, and nothing under the core root imports aesop, because the LCNF/OCaml pipeline is cut from the core's import closure | **emit it under `src/Effect4/Laws/`**: the test is consumers, not subject matter — `Ty.args`/`Ty.sameHead` are data about `Ty`, but no runtime reader, face, emitter or codec reads them. A generated Laws file is audited like hand source (same axiom ceiling, same proof-shape ceiling) and stays out of the case-site policy and the core's exhaustiveness inventory | `tools/Effect4Gen/View.lean`; `docs/research/2026-09-18-seat-T2-receipt.md` §2 D2 | coordinator (seat T2 proposed) | ruled 2026-09-19; landed with the wave-1 merge |
| 60 | **Variance is read off rc.112, never hand-typed.** Decisions row 55 took rc.112's declaration-site variance as the truth for `Ty.sub`'s congruence arms and it lived in a comment | **`tools/Tools/Variances.lean` reads the vendored text** (anchor, span, SHA-256 per declaration) into `tools/Effect4Gen/variances.json`, the hermetic family `variances` before `derived`; the head map is the driver's one hand list and a head without a row is refused; `Exit`/`Option` are aliases and their variance is inferred from the constituents with the evidence recorded; the view's arm lemmas are generated per head (the one-proof route closes but reaches `Classical.choice` in aesop's catch-all case, so the plan's fallback is the branch taken) | `tools/Tools/Variances.lean`; `tools/Effect4Gen/variances.json`; seat T2 receipt §3–§4 | coordinator (seat T2) | ruled 2026-09-19; landed with the wave-1 merge |
| 61 | **The exhaustiveness inventory is an inventory.** Rows 34 and 49 keep the census an instrument and forbid a Test module writing into the tree; the question "which matches break on a new constructor" needed an answer that was not a second gate | **`#exhaustive_gate` prints and never fails**; the gate over compiled default arms stays the case-site policy. Read it before appending a constructor: the `catchAll false` rows are the bill (22 of 51 for `Ty` on 2026-09-19). Cost: `Test.Audit.TraversalCensus` 19 s → 42 s, the first row of the time ledger | `src/Effect4/Laws/Auto/Exhaustive.lean`; `Test/Audit/TraversalCensus.lean`; seat I receipt | coordinator (seat I) | ruled 2026-09-19; landed with the wave-1 merge |
| 62 | **The proof-shape ceiling (D-D realised).** The proof rule bans `first`/`simp_all`/`try` in new proofs; nothing measured the estate's 730 existing ones | **the trust gate's tokenizer counts the three per library source under `src/` and `generated/proof-shape.tsv` pins them as a ceiling that may only fall**; a new source with a counted tactic takes a row before it lands; a row without a source fails; `first` counts only when followed by `|` because it is also a binder name in this tree; `#effect4_print_proof_shape` re-pins. Not a target: a fall passes and is reported | `Test/Audit/AxiomGate.lean`; `generated/proof-shape.tsv` (94 sources, 730 at the pin) | coordinator (seat I) | ruled 2026-09-19; landed with the wave-1 merge |
| 63 | **One citation scanner.** Two programs read every text file of nine trees, one for path existence and one for the line-citation ban on mutable documents; `check-citations` was red at `4b4e6aa3` with no baseline file | **`scripts/check-source-citations.py` asks both questions in one pass**; a cited path that is not a file of the checkout is named in `scripts/source-citations-allowed.txt` with its reason, never baselined silently; `check-internal-citations.sh` is deleted | seat I receipt; `Makefile` `check-citations` | coordinator (seat I) | ruled 2026-09-19; landed with the wave-1 merge |
| 64 | **The atom table has two halves.** An atom's name, arity, const-generic flag, typing and soundness were five matches over the alphabet in two modules, and `all` was a hand list guarded only by a theorem | **runtime half `AtomRow` (`name`, `arity`, `constGeneric`, `prelude`) below the stores in `Machine/Term.lean`, one exhaustive match, projections for the rest; typing half `Spec`/`Scheme` (`mono | variadic | poly | alts | custom`, with the cite) in `Program/NativeAtom.lean`, `typeOf` the scheme applied through the rows' template calculus; `eval` stays a match (LCNF's jump table); `ofName?` a match on the string; `all` generated from the constructor list; soundness once per scheme.** `tagIs` is `mono [string, unknown] bool` now that the top exists (row 46): the ruling seat A asked for is yes — the prelude already spelled the signature and a generated face moving once for metadata is the price of the table being the truth | `src/Effect4/Machine/Term.lean`; `src/Effect4/Program/NativeAtom.lean`; `src/Effect4/Program/AtomInventory.lean`; seat A receipt | coordinator (seat A) | ruled 2026-09-19; landed with the wave-1 merge |
| 65 | **A named bank's registration protocol.** Row 1.1 declared seven banks; how a rule is registered and how a bank is proved to matter were unstated | **`safe destruct` for an inversion that consumes its hypothesis, `safe forward` for a universal conclusion whose hypothesis stays useful, `norm simp` for a recursive definition (aesop refuses it as `unfold`); every bank ships a positive control that closes with `(rule_sets := [X])` and a negative fixture that omits the clause under `#guard_msgs (error)` — never `-X`, which errors at the clause when the set is not active and proves nothing about the goal** | `src/Effect4/Laws/Program/AtomRules.lean`; `Test/Program/AtomRulesRed.lean`; plan 1.2 | coordinator (seat A) | ruled 2026-09-19; landed with the wave-1 merge |
| 66 | **`ofName?` is a match on the string, not a scan of the inventory.** The name lookup sits on the machine's path (`evalTerm` resolves an atom at every term application) and scanned `all` with twenty name computations and compares | **a `match` on the string literal**, which OCaml compiles to a decision tree; the second spelling of the name table is pinned both ways by `ofName?_name` and `ofName?_sound`, one tactic line each, no axioms; the same rule for any later alphabet whose names the machine resolves | `src/Effect4/Machine/Term.lean`; seat A receipt §5 | coordinator (seat A) | ruled 2026-09-19; landed with the wave-1 merge |

## The order (re-cut 2026-09-18 after the owner's steer: the milestone before the MCP server; LCNF handling before the MCP server)

1. **Now, no rulings needed** — the small rows verified open above: 24, 16 (`Await`), 17, 18
   with 23, 5 restated at `Ty`. (Row 40 closed and row 34 ruled out on 2026-09-18: the fold work
   stops here; the thirteen exemptions are tracked, not converted.)
2. **Row 39, one ruling** — the Schema wipe. It closes 4 and 9, takes `Image` out of row 10's
   count, and ends with `render` leaving `Shape.lean` (row 8's `ShapeDoc.document` fix lands in
   the move). Then row 10 (one `Val → Json`) over what remains.
3. **Row 41, the milestone** — the typed-state invariant on the reference machine, in the order
   of `docs/research/2026-09-18-typed-state-plan.md` §3: S0 the statement, S1 `denoteR_typed`, S2
   one preservation lemma per fiber operation on the shared scheduler, S3 the transfer through
   `BMeans` to the compiled machine's exits.
4. **Group D** (rows 26, 27, 28, 31, 32, 29; row 30's `compileEff` ruling when row 27's root order
   reaches it): the TypeScript rules, the recipe, rule coverage, the rung-3 reader, the vendoring
   order, then the profile/legalization/IR refactor with the TypeScript printer as its first
   customer. The visual-plane research (the owner's layout and design-system documents, the
   2026-09-17 plan and its two reviews) is scouted against the printer's substrate before this
   group starts, so the TypeScript printer and the visual plane target one document algebra or
   knowingly two.
5. **Group B** (rows 14, 15, with 18 and 23): the digest, the Lean MCP driver over the `ToolSpec`
   table; then 2, 7, 11 — names at the boundary, handles, the reviver table; then the ledger and
   the pool dogfoods. Moved after group D on the owner's steer of 2026-09-18: the server would
   freeze a wire shape that LCNF handling and the milestone still change.
6. **Group C and the rest** (rows 1 with 3, 19, 20, 21, 22): the AST as second carrier when row 32
   step 2 reads a foreign schema, `Ty.app` with it, the file moves and daemon words, the machine
   edit, the two confirmations.

Owner rows open after the review: 1, 2, 7, 10, 11, 14, 15, 19, 20, 21, 22, 26, 27, 28, 29, 30
(`compileEff` only), 32, 41 — eighteen, with 4 and 9 folded into 39, which is ruled.
