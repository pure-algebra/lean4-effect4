# Effect4 design issues

This register holds the design questions that are **open**. `docs/DESIGN-BASIS.md` holds the
ones that are **settled**: a DB row is an end state with a `Status:` line, and its "what this
basis refuses" clauses are the closed doors. This file is the other half — the questions still
in flight, what each would force to be redone if it were answered late or wrongly, and the
milestone by which answering it is cheapest. It was opened 2026-09-09 from the design-risk
sweep after the error-paths cycle (the companion notes are listed at the end).

A row moves in one direction. **open** — the question is named and nobody has answered it.
**recommended** — a scout or a slice has proposed an answer and stated its cost; work may
proceed on it where the row says so, on the understanding that a different ruling costs a
revert, not a redesign. **ruled** — the owner has decided; the ruling text and its date live in
the row, and the row names the tracked place the ruling was written into: a DB row, a module
header, a contract packet, a counterexample row, a `# reason:` line in
`Test/fixtures/trust-gate/known-red.txt`. **basis** — the ruling has become a `DB-nn` row and
this row is retired to a one-line pointer. A row is never deleted and an id is never reused, as
in `Test/Counterexamples/REGISTER.md`.

Two rules make the register worth keeping.

**A ruling is not made until it is written into a tracked file.** `docs/research/` is
gitignored, and `scripts/check-source-citations.py` accepts citations into it without checking
that they resolve, so a decision recorded only there does not survive a clone — the sweep
counted 504 such citations from 278 tracked files, six of whose targets are already gone. When
a row reaches **ruled**, the same commit puts the ruling in a tracked place and the row cites
it. DI-00 is the worked example: decided in a note, written into DB-15, the truth driver and
the sqlite module header the same day.

**Every row names what it would force to be redone** — wire ordinals, goldens, proofs,
engines, generated families, the printed face, corpus pins — because that, not the size of the
change, is what makes a late answer expensive. Rows are cited by id (`DI-nn`), never by line.

Milestones used in `decide by`: **step 8** (the engine host, waiting on plan v2 Phase 1),
**Phase 1** (the LCNF engine regeneration), **third package** (the next canonical package
table: `HttpClient` over `FetchHttpClient.layer`, chosen because it is the only candidate
executable and deterministic at the pin with `effect` alone, and it forces a package-produced
handle, a receiver property, an informative reason and a layer-shaped requirement — scout E,
2026-09-09), **foreign lift** (admitting wild TypeScript), **release**.

Slice names in the rulings below (S0, S1a, S1b, S2, S3a, S4a/S4b/S4c, S5a/S5b, S6a/S6b) are the
foundation settlement's implementation order; `docs/DESIGN-MAP.md` §"Where the drift is removed
first" is their tracked home. **S0 is this commit**: the decision text of every row below, and
the retained baseline it freezes (`Test/fixtures/baseline/66ee4657/README.md`, DI-47). A ruling
dated 2026-09-09 (S0) is a decision recorded here; the guarantee it asks for is implemented by
the slice the row names, and the two are not the same thing.

Owner-approved Wave 2 amendment (2026-09-09): the recommended decisions in the reviewed
implementation packets are now ruled below and frozen in
`Test/contracts/foundation-wave2.contract.md`. This records authorization and semantics;
delivery receipts, not a **ruled** label, establish implementation completion.

## Six kinds of obligation

The category column names a layer of `docs/DESIGN-MAP.md`; the Codex seat's independent
classification cuts across the layers by the *kind of obligation* a question carries, and a
single change usually carries several (a host row touches K2's type, K3's index, K4's
spelling, K1's transition and K5's evidence). Both cuts are kept: the layer for orientation
and file ownership, the kind for reasoning about change.

| kind | the question it owns | rows whose primary home it is |
| --- | --- | --- |
| K1 meaning and observations | which executions are allowed, under which environment and decisions, and what counts as the same result | DI-04, DI-07, DI-10, DI-11, DI-17, DI-23, DI-31, DI-57, DI-58 |
| K2 static semantics and admission | which programs and values are admitted, and what their typing promises | DI-00, DI-09, DI-12, DI-15, DI-20, DI-26, DI-28, DI-35, DI-38, DI-53, DI-54, DI-61, DI-62, DI-63 |
| K3 representation, identity and evolution | which distinctions are content, which are implementation choices, which old objects keep their identity | DI-01, DI-02, DI-03, DI-05, DI-06, DI-14, DI-22, DI-25, DI-41, DI-43, DI-46, DI-47, DI-56, DI-64 |
| K4 language interfaces and translation | which source forms can be printed, read or lifted, and what survives each translation | DI-19, DI-21, DI-24, DI-27, DI-29, DI-37, DI-39, DI-48, DI-49, DI-55, DI-59 |
| K5 reproducible production and evidence | whether outputs come from the stated inputs, and what claim an actual check established | DI-13, DI-32, DI-33, DI-34, DI-40, DI-42, DI-44, DI-45, DI-52, DI-60 |
| K6 modularity and decision ownership | which interface hides a change, and where the decision lives | DI-08, DI-16, DI-18, DI-30, DI-36, DI-50, DI-51 |

## The register

| id | question | category | status | ruling or recommendation | forces rework of | decide by | pointers |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DI-00 | Which pair a tagged `SqlError` posts. | errors | **basis** | Ruled 2026-09-09: the reason's tag and the driver's message, rc.112's own two-level `catchReason` form, the outer tag implied by the row. | — | — | DB-15; `harness/truth/run-truth.ts` (`taggedPair`); `src/Effect4/Program/Packages/SqliteBun.lean` header |
| DI-01 | Is the unit of publication `bytes` or `(bytes, keys : Cid KeyTable)`, and is the ordinal ledger a stored `table` node kind? | wire/CAS | open | CAS amendment M17's default is yes; the literature backing is Unison's hash/name split and Avro's writer-schema-with-the-data (both say the pair); `Cid` does not exist under `src/` today, so the unit can still be chosen for free. Sequenced 2026-09-09 (S0): the foundation settlement adds no evidence here; the recorded default stands until step 8 | every `Cid`; `keyOf`'s signature; `Kind.byte_le`'s bound; the renaming proofs | step 8, before the first published unit | `src/Effect4/Surface/Provision.lean`; the CAS amendments note (M17) |
| DI-02 | Does an append-only ordinal ledger exist, and which alphabets does it gate? | wire | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): **replaced by DI-47.** The ledger design is not built; the mechanism is the retained baseline captured before any alphabet append (`Test/fixtures/baseline/66ee4657/README.md`) and the `⊑` gate that reads it (S5b), and `Store.Tag`'s missing census (DI-43) is its one missing input. Written into `docs/DESIGN-MAP.md` §L3 and that baseline's README. The original text: build it before anything is published; today no mechanical guard exists. Narrowed (representations scout): not a new artefact but a field group of one `World` datum, with append-only as a proved relation (DI-47) rather than a review rule; `Store.Tag` is the one alphabet with no census at all (DI-43) | `Eff` 0–26, `NativeOp` 0–22, `Store.Tag` 1–12, `HandleKind` 0–7, `Kind` 1–15, service codes 4–9, package row positions — first publication freezes all seven by accident | step 8 | `src/Effect4/Program/Derived.lean`; the CAS amendments note (M16) |
| DI-03 | Where do ordinals live — a ledger file, a pinned Lean value, or M17's `table` node? | wire/generated | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): **replaced by DI-47.** Ordinals live in the one World description the baseline freezes and the gate compares against (`Test/fixtures/baseline/66ee4657/README.md`); there is no ledger file and no second copy. The original text: plan v2, the Lean value plus the M17 node; no text file | every wire golden and hex pin; the `.eff` differential; seventeen ordinal copies, six by hand | step 8 | `src/Effect4/Program/Wire.lean` (the hex pins) |
| DI-04 | Is a published tape, log or exit replay-relative, never host-relative? | wire/CAS | open | M2's default is yes. Sequenced 2026-09-09 (S0): no new evidence; decided with DI-01 at step 8 | the tape, log, exit and checkpoint carriers become bytes plus hand images | step 8 | the CAS amendments note (M2) |
| DI-05 | Is a `ServiceKey`'s position content or machine-relative? | wire | open | machine-relative; M17's key table is what makes a published unit sound. Sequenced 2026-09-09 (S0): it is the DI-22 principle one level up — the published unit carries its link table; decided with DI-01 | ordinals inside every `provideService`, `service` and `provideLayer` subtree; every layer `Cid` | foreign lift | `src/Effect4/Surface/Provision.lean` |
| DI-06 | `Cid` or `Ref` for a program or tape inside a job? | wire/CAS | open | `Cid`; and a `Cid` must carry its kind (IPLD's multicodec argument), or the by-content index answers across kinds. Sequenced 2026-09-09 (S0): no new evidence; decided with DI-01 at step 8 | every published job and receipt; the `byCid` index | step 8 | the CAS design note §12 |
| DI-07 | Does machine and algebra agreement extend past the single-fiber straight-line fragment? | laws | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): DI-09's constructor stays **outside** the plain fragment — covered by `run_eq_ref`, not `run_eq_meaning`, on the same footing as `provideLayer`, `gen` and `whileLoop` — and the gap is named as an exclusion of `Straight` in `Test/contracts/program-denotation.contract.md`, written there by S3a (S0 added only S2's obligations to that packet). The refusal `CORE-FB-SIMULATION` reserves the scope but names no successor. Bringing it inside costs another two to three days of proof and extends the agreement claim to the tree's most-used error head | the code relation, the eight simulation modules, `meaning`, possibly the code shape; every straight-line receipt restated | before any equational or logic layer | `src/Effect4/Laws/Program/Agreement/Machine.lean` (`run_eq_meaning`'s `Straight` premise); `Test/contracts/machine-scheduler-core.contract.md`; `Test/contracts/program-denotation.contract.md` |
| DI-08 | Is Schema in the release, and does `Store.Shape` depend on the checker or only on the document carrier? | architecture | open | none — the acknowledged blocker of the Tier B archive line and the area roots. Sequenced 2026-09-09 (S0): the foundation settlement does not decide it and nothing in S0–S6b depends on it; it stays the oldest open architecture question | the Tier B archive line, the area roots, the `Api` closure, 7,095 lines of checker | now | `src/Effect4/Store/Shape.lean` (imports `Effect4.Schema.Authoring`) |
| DI-09 | Does the error channel get an elimination form? Today it is write-only: `catchCause` binds `Ty.causeOf`, which no value inhabits; no atom projects a cause. | type/faces | **ruled** (2026-09-09, spoken, completed in S0): **both** — one appended `Eff` constructor binding the error value **and** cause-projecting atoms, with derived combinators. The three owed sub-rulings are made below and a fourth is added; sequenced after DI-62, whose image the binder's value is. | The design (the elimination note): (1) **one** constructor, recommended `Eff.catchIf (test) (body handler)` — rc.112's `catchIf`, which is `catch` at `test = true` and spells `catchTag`/`catchTags` exactly, including the whole-cause re-raise on the unmatched path; plain `Eff.catch` is the fallback and would need a registered deviation for `catchTag`. Compiles to the existing `Prim.onFailure`; stays outside the plain fragment (DI-07). (2) **Four atoms**, each an rc.112 export: `causeIsFail`, `causeError : causeOf e → option e` (`Cause.findErrorOption`), `causeIsDie`, `causeIsInterrupt`, accepting `.causeOf e` and `.exitOf a e`; plus `eq` at `.string` and `or`/`and` at `.bool` (DI-35); plus the `Val.hasTy` arm for `.causeOf` that discharges `TYPED-FB-CAUSE`. Not one `squash`: rc.112's `Cause.squash` answers `unknown`, which no `Ty` types. (3) Sugar over (1)+(2): `catchTag`, `catchTags`, `catchIf`, `mapError`, `orElseSucceed`, `orDie`, `match`, `sandbox`, `catchReason`; rc.112 has no `Effect.orElse` — the "or" family is the trailing `orElse?` parameter plus `orElseSucceed`/`firstSuccessOf`. Order: atoms (a day, no golden moves) → the constructor (the one hard proof: `compileEff_means_denoteR` and `denoteEffBody` are exhaustive over `Eff`, two to four days) → sugar (TypeScript plus `Forms` rows). **Ruled 2026-09-09 (S0), the sub-rulings:** (1) the constructor is `Eff.catchIf (test) (body handler)` and its binder is the **error value**, `valOfErr` of the first `Fail` reason (`src/Effect4/Program/ErrorImage.lean` holds `valOfErr`); (2) it stays outside the plain fragment (DI-07); (3) the four atoms are `causeIsFail`, `causeError : causeOf e → option e`, `causeIsDie`, `causeIsInterrupt`, accepting `.causeOf e` and `.exitOf a e`; (4) **the elimination form selects the first `Fail` reason of the cause** (rc.112 `findError`, `vendor/effect-4.0.0-rc.112/src/internal/effect.ts:157`) and converts it exactly once with `valOfErr`. A first failure that is `boom` (no value) retains the whole original cause (`CATCH-FB-BOOM`, a Lean-specific policy rc.112 cannot express); a predicate miss retains the whole cause or runs `orElse`; a hit runs the handler in place of the whole cause, mixed reasons included. The atom `causeError` follows the same rule: `[Fail boom, Fail (tag 7)]` answers `none`, never `some 7`. Fixtures before the compiler proof: a mixed cause (`fail 7` with an interrupt), a `boom`-first cause, a miss on a two-`Fail` cause. Written into: this row now; the constructor docstring in `src/Effect4/Program/Eff.lean` and the atom docstrings in `src/Effect4/Program/Native.lean` land with S3a (docstrings owed). | one appended `Eff` constructor and its arms on both faces and both engines; the `eff`, `derived`, `wire` (the `Eff` ordinal in the manifest) and `ts` families; the 400-program fuzz corpus reseeds on any `Eff` append (the generator's `pick` modulus), moving its measured guards; the ingest census (the foreign benefit is **unmeasured (DI-48)**: the figure once quoted here was the ingest spec's count, not a measurement — the census says two, because the code is masked, and no handler number is quoted in a tracked file until DI-48's rule is reported); on the faces `read.ts` needs no new head table (generated) | before Phase 1, while the append is still byte-free | `src/Effect4/Program/Typing.lean` (`catchCause`); `src/Effect4/Laws/Program/Typed.lean` (`TYPED-FB-CAUSE`); `src/Effect4/Laws/Program/Intro.lean`; `src/Effect4/Laws/Program/DenoteR.lean`; the elimination design note |
| DI-10 | Is there a bind law, and what is a neutral stack (papers review G8)? | laws | **ruled** (2026-09-09, Wave 2 ordering; general law deferred) | The local catchIf compiler/reference clause may land in S3 without a general bind law. Decide any neutral-stack bind law before a logic layer claims it; do not require it before DI-09. The existing fixed-fuel counterexample stands. Written into Test/contracts/foundation-wave2.contract.md §Scope and sequence. | the frame stack, if a general law is wanted; the logic layer inherits whichever shape is chosen | before a general logic-layer bind claim, not before S3 | `src/Effect4/Program/Compile.lean` (`contE`, `contAll`); DB-nn refuses only the fixed-fuel law; `Test/contracts/foundation-wave2.contract.md` |
| DI-11 | Do streams and channels stay fibers plus queues, or does a continuation become a value? | machine/type | open, **amended 2026-09-09 (S0)** | fibers plus queues; no continuation handles in the value language. Amended 2026-09-09 (S0): fibers plus queues is the *implementation route*, not the contract. rc.112's `Done` is a **completion payload** that `Pull.filterDone` removes from the cause, so it is **not** an `Err` constructor and DI-62's admissible image does not have to carry it; the first Stream fragment answers `option (list a)` (a chunk, or the end), and a `Done` payload only where a fragment needs leftovers; queue state is `Open \| Closing \| Done`, capacity zero a rendezvous. Note: the tag table's freeze is today the weakest of the three byte alphabets (no census, no injectivity theorem — DI-43), so DB-11's freeze is asserted rather than enforced | `Val` tags (DB-11 froze them), `Eff` constructors, the frame alphabet, the snapshot format, the engine | before the Queue/Stream slice, after S1a and S2 | DB-11; `src/Effect4/Store/Val.lean` |
| DI-12 | Are scoped and higher-order operations `Eff` constructors, or `RowKind.program` rows carrying a body? | type | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): constructors for the runtime primitives; everything derivable is an `Eff` to `Eff` expansion. Written into `src/Effect4/Program/Eff.lean`'s header by S2 (docstring owed). The named exception (2026-09-09): `catch`/`catchIf` is **not derivable** in this IR — no `Eff` form fails with a cause *value* (`Eff.failCause` takes a `CauseTerm` literal), so an expansion over `catchCause` would need an `Eff.rethrow` constructor, the same append with a spelling rc.112 does not export; the reason is the missing eliminator, not convenience | the central sums and every wire ordinal; both readers; both OCaml faces | before the next family adds a constructor | `src/Effect4/Program/Eff.lean` (`RowKind.program`, `failCause`) |
| DI-13 | One pinned description of the cross-language types, or independent reflections? There are four, not three: `EffWire` hand-lists eighteen families and the twelve tag names beside `World.blocks`, the generator manifest and the LCNF types. | generated | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): one description, the World — and it **closes only when three consumers read it**: `tools/Effect4Gen/manifest.json` checked equal to the World's family list, `src/OCaml5/Tools/EffWire.lean`'s hand lists replaced by a World read, and `ocaml/engine/e4_program.ml`'s mirror generated from or checked against it. The six Lean encoders (DI-41) are a precondition, not the closure. The World describes **structural identities**; wire framing (DI-14) and the LCNF/runtime layout (`src/OCaml5/Lcnf/Types.lean` follows compiler erasure, `src/OCaml5/Tools/LcnfGen.lean` discovers its closure) stay separate columns — never one fixed family list for the LCNF route. Written into `docs/DESIGN-MAP.md` §L3; a DB row when the World lands; consumers migrate in S5b | all four reflections; every OCaml and TypeScript projection; the Derived Lean files; every stamp | before Phase 3 | `tools/Effect4Gen/manifest.json`; `src/OCaml5/Eff/World.lean`; `src/OCaml5/Tools/EffWire.lean` |
| DI-14 | Is a target's in-memory representation free where it has no byte writer? | wire/OCaml | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): yes — a target's in-memory representation is free where no byte writer sees it, and the fact is recorded as the **framing/carrier column of the World**, not as a note, which is what makes it checkable. Wire layout and target runtime layout stay separate columns (DI-13). Written into `docs/DESIGN-MAP.md` §L3 (its tracked home from today); the column itself lands with the World in S5b | reversing it re-frames `ServiceName` on the wire and moves every golden | before a second byte-writing target | the one-generator plan v2 note (table R) — needs a tracked home; `src/OCaml5/Eff/World.lean` |
| DI-15 | Does `Ty` gain a tagged sum, with a subtype relation? `Ty.union` is an untagged union de-duplicated by structural key, so `sqlError` and `kvError` are one type (probed: `Ty.join sqlError kvError == sqlError`). | type | **ruled** (2026-09-09, Wave 2) | Adopt Ty.lit (value : String) at the appended ordinal with Ty.sub in S4b, after deep normalization and before answer unions. Prove hasTy_sub (a b : Ty) (v : Val) (allocated : List String) : Ty.sub a b → Val.hasTy v a allocated → Val.hasTy v b allocated. Admit subtyping at row requests/answers and the selected handler residual sites; do not change answer joining until S4c. Fiber and exit rules claim only columns their membership observes. Literal tests and prod/union represent discriminants; arbitrary payload X in prod (lit tag) X additionally requires a supported error image and recovery laws. S4b does not silently widen S2's error carrier. Add metadata fixtures even where the old coverage skip list omits Ty. Written into Test/contracts/foundation-wave2.contract.md §Types; DB-15. | every generated family (`Ty` is world block 1: `eff_types/wire/json.ml`, the eff manifest, `eff/wire/json.gen.ts`); the two package tables' error columns become `prod (union of lit) string` (a tag set alone would drop the message — scout E's correction — and a union of per-tag pairs where the payload is kept); the contract's type literals; every `.ty` golden (nothing on the printed face, since `Ty` is not printed as syntax — the golden that would fail is a `.ty`, not a `.bin`, and the coverage abort will not catch a missing `Ty` arm); DB-15 | S4b after S4a; before precise tag sugar and S4c | `src/Effect4/Program/Eff.lean` (`Ty`, `union`); `src/OCaml5/Tools/EffGen.lean` (the coverage abort); DB-15; the elimination design note; `Test/contracts/foundation-wave2.contract.md` |
| DI-16 | Does the standards and surface lane live in this repository or its own package? | architecture | open | one decision, once, before Tier B writes Lean. Sequenced 2026-09-09 (S0): no new evidence; DI-36's `Effect4Packages` root is the only root question the settlement moves | namespaces, `lakefile.toml`, the axiom gate's hardcoded roots, every citation, CI | before Tier B | `Test/Audit/AxiomGate.lean` (the root gate) |
| DI-17 | Is the adequacy route C16/C17a/C17b/C18? | laws | **ruled** (2026-09-09, Wave 2 route) | Adopt the local adequacy route: existing FitsWith/FitsIn and allocation weakening, then error/cause/exit membership in S2. Keep fiberOf explicitly coarse. A general preservation invariant requires a defined Reached and remains a later connection, not a prerequisite for these local repairs. Never-stuck, preservation, termination and host correspondence remain distinct statements. The existing laws in 23e5717 are preparation, not the whole invariant. Written into Test/contracts/foundation-wave2.contract.md §Scope and sequence. | a typing invariant beside `Minted`; `TypeBounded` in place of `KeyBounded`; type and requirement environments on the node | local laws in S2; before a whole-machine typing claim | `src/Effect4/Laws/Machine/Handles.lean`; `Test/Program/TypedContract.lean` (the receipt that `causeOf` is uninhabited); the adequacy route note; `Test/contracts/foundation-wave2.contract.md` |
| DI-18 | May `import Lean` enter the audited closure, and does `deriving Canonical` happen? | generated/process | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0), reversing the untracked note: **no** `import Lean` in the audited closure, and no exemption text is owed. Deriving and reflection live in a **tool root** and emit ordinary `.lean` declarations the kernel checks (the nullary `RowKind` pilot's bytes were identical to `tools/Effect4Gen`'s); `scripts/check-library-roots.sh` is unchanged; plan v2 §7 decision 2 is superseded. The six encoders of DI-41 go through `tools/Effect4Gen`, not through a deriving handler, which stays a pilot for later families. Written into `docs/ARCHITECTURE.md` (the roots paragraph) at S0; `scripts/check-library-roots.sh`'s header comment restates it with S5a | roughly 5,100 generated lines and 223 generated theorems, in either direction | before Phase 2 | `scripts/check-library-roots.sh` |
| DI-19 | What is the acceptance test of the first engine regeneration? | OCaml | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): acceptance of the first engine regeneration is the **conformance suite** the reference already defines — the 42 `ocaml/eff` goldens decoded and re-encoded, the 8 wire goldens byte-equal, the 24 truth programs to the same exit (the reference-interpreter model). Regeneration is a precondition, not the test; the differential remains the acceptance of the carrier and extern change only, and it was never shown to clear every engine failure. Native regeneration is gated by this row **alone**, not by the js_of_ocaml framing repair (DI-56). Written into the two `# reason:` lines of `Test/fixtures/trust-gate/known-red.txt` (S0) | both declared-red gates; the engine sources; the differential's corpus width | before Phase 1 | `docs/GENERATED.md` (the LCNF rows); `Test/fixtures/trust-gate/known-red.txt` (its `# reason:` lines should name the suite) |
| DI-20 | Is the requirement channel polymorphic, and is that type-level or term-level? | type | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): **refused as a profile choice, not as an impossibility.** The stored program has no runtime polymorphic syntax and this profile adds none; Lean and OCaml *builders* are polymorphic and instantiate closed `Eff` programs (the reader's `polyArms` atoms are the precedent), so the earlier ground — "polymorphism needs a binder and `Eff` has none" — is corrected: it explains today's restriction, it does not make polymorphism impossible later. The ecosystem audit's "largest DX decision" concerns the effects package's morphism kit, which has no Effect4 consumer. Written into `docs/DESIGN-BASIS.md`'s exclusion list (S0) in that wording. What is owed instead is a **subeffecting** rule (`Row.Subset` as subsumption) in a declarative typing relation, which makes it non-syntax-directed and turns completeness into principality | `Row.diff`, `Provision`, `LayerTy` and every layer signature law; `Typing.lean` | before the second effect family | the ecosystem audit note §11; DB exclusion list (shared with DI-28) |
| DI-21 | Are class-shaped units and `Effect.fn` ever admissible on the foreign face? | faces | deferred | its own grill; not in the ingest packet. Sequenced 2026-09-09 (S0): unchanged; no slice of S0–S6b touches foreign lift | the unit definition, a parameterised `Eff` declaration, both engines, the census denominator, the printer | before foreign lift is a goal | `ts/eff/ingest/ck.ts` (`E-PARAM-SHAPE`) |
| DI-22 | Is a package row's index a position in the concatenated table or in its own package's? Both conventions are live (confirmed: the concatenated rule in the packages module and the foreign readers; per-package in the truth driver and the contract), coinciding today only because the sqlite table is first. | wire/tables | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): a row index is a position in the program's **link table** — the `RowTable` supplied to `Api`. `Packages.table` is one link table (the foreign readers'), each truth fixture's per-package table is another (`harness/truth/Truth.lean`), and both live conventions were already this one; a published unit carries its link table (DI-05). The Lean fixtures are reconciled to it and the header's "the canonical table" is reworded by S1a (docstring owed, `src/Effect4/Program/Packages.lean:36-41`). Row *identity* is a separate question the ruling does not answer: `rowKey = (spelling, trailing)` collides before the third table (DI-64) | six tapes; `packages.gen.ts`; both engines; the table-length pins | third package | `src/Effect4/Program/Packages.lean` vs `harness/truth/Truth.lean` (`hostInputs`); `Test/Api/PackagesContract.lean` |
| DI-23 | Are tapes and the denotation single-fiber, and if not, what orders a multi-fiber tape? | machine | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): the truth claim is **single-fiber**, and the restriction is stated in the claim's own quantifiers (the tape already carries a `fiber` field the comparison never reads; the differential is single-entry). `compareSchedules` is unchanged; the first fork-using host fixture inherits multi-fiber order as a named obligation. Written into the tape docstring of `harness/truth/Truth.lean` (S1a, docstring owed) and the faces packet (DI-50). Caveat (scout E, recommended, not ruled): the FileSystem slice this row anticipates cannot be the next one — rc.112 ships no FileSystem implementation, only `layerNoop`, and `@effect/platform-bun` does not resolve in this tree, so the slice would test the harness's own partial implementation; move it behind an install decision or state the weaker claim explicitly | the tape format; every committed tape; the truth gate's comparison | before the FileSystem slice | `harness/truth/Truth.lean` (the tape docstring); `harness/truth/run-truth.ts` (`compareSchedules`) |
| DI-24 | Does the printed face spell class declarations and requirement rows? | faces | **ruled** (2026-09-09, S0), spelling in S6b | Ruled 2026-09-09 (S0), the policy: a program prints its declared type **always**, with all three parameters; `printDecl`'s present omission when the requirement row is non-empty is the defect. The `R` spelling, measured under `tsc`: the union of the requirement row's service carriers in `printKey` order (`Effect.Effect<A, E, C₁ \| … \| Cₙ>` with `Cᵢ = sig.serviceTy kᵢ`, `never` when the row is empty). It is **not injective in the key** — two keys with one carrier are one component, and providing either satisfies the other at the type level — which the docstring must say, and a key whose `sig.serviceTy` is `none` is the one case still left to inference. The class spelling of keys was deferred to a `Codegen/Layer.lean` that has never existed: that is a citation repair (DI-51), not a missing feature. Written into `src/Effect4/Codegen/Print.lean`'s `printDecl` docstring (S1a, docstring owed) and the faces packet (DI-50); the spelling lands in S6b. Four of the six host programs become two-parameter declarations as soon as DI-63 discharges `Scope` | the printer, the three reader theorems, 408 printed oracles, `ts/eff/read.ts`, both engines, the truth module template | before release | `src/Effect4/Codegen/Print.lean` (the `Layer.lean` forward reference; `printDecl`) |
| DI-25 | Do `Kind`'s constructors grow to the CAS's kinds, and where does the sentinel live? | wire | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): kinds 16–23 by the CAS lane before the ledger; the sentinel moves to 127, reserved forever. Written into `src/Effect4/Store/Kind.lean`'s docstring (CAS lane, docstring owed), beside `ocaml/engine/cas/e4_kind.ml`, which spells 127 already. Confirmed by reading both tables: Lean 1–15 with `byte_le ≤ 15`, OCaml 1–23 with the sentinel already at 127 in its tests; nine Lean sentinel sites | `Kind.byte_le` and its axiom print; nine sentinel sites in six files; the CAS goldens | before the ledger | `src/Effect4/Store/Kind.lean`; `ocaml/engine/cas/e4_kind.ml` |
| DI-26 | Does the failure branch of an external answer carry a theorem? The three admission laws are success-only and `errAdmits` is cited by no theorem. | laws | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): land `external_error_typed` and `external_oracle_error_typed` beside the three success-only laws, and move the shared fold (`errOf`, `valOfErr`, the reason and cause folds) into `src/Effect4/Program/ErrorImage.lean` **below** `Typed` and `Compile`, so the value typing never imports the compiler; `Typed` instantiates the folds with `Val.hasTy` and re-exports `errAdmits` and `hasTyCause` under their existing names, so no client changes. "A definitional unfolding" is corrected to "a short proof from `admitted_row` and the `admitAnswer` failure branch, no induction". Landed already, in `23e5717`: both theorems in `src/Effect4/Laws/Program/Admit.lean` (`external_error_typed` at `[propext]`, `external_oracle_error_typed` at `[propext, Quot.sound]`) and the new module `src/Effect4/Program/ErrorImage.lean`; the instantiation into `Val.hasTy` is S2's cutover. The row's real content: `errAdmits` is not inverse to `errOf` (probed: `Effect.fail("x")` types `.string` and runs as `Err.boom`), which `Ty.tagged` (DI-15) reconciles on the shapes that matter | `errAdmits`'s shape; the adequacy route's typing half; every tape whose row fails | with DI-09 | `src/Effect4/Laws/Program/Admit.lean`; `src/Effect4/Program/Compile.lean` (`errAdmits`, `errOf`) |
| DI-27 | Does the Surface emitter route get a driver and a gate, or is the `Api` codegen export cut and Tier B archived? | architecture/generated | **ruled** (2026-09-09, S0), unexecuted | Ruled 2026-09-09 (S0), as recorded: keep the nine rules and commission them; cut the `Api` codegen export; execute before the MCP and HTTP-API lane. Written into `docs/ARCHITECTURE.md` (the codegen paragraph) at S0. Note: the type-checker ruling's `ts/eff` half landed (the ingest gate runs it) while its truth half did not (DI-49) — that gap is independent of the Surface ruling | 16,260 lines; 26 frozen batteries; three stale contracts; the public `Api` surface | before the MCP and HTTP-API lane | `src/Effect4/Api.lean` (the codegen export); `Test/fixtures/trust-gate/known-red.txt` |
| DI-28 | Is effect polymorphism off the table? | type | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): refused with DI-20 and on DI-20's corrected ground — a profile choice for stored programs, not an impossibility claim about `Eff`. Written into `docs/DESIGN-BASIS.md`'s exclusion list (S0), one line beside DI-20's | `typeOf`, the row discipline, every `Ty` and `Requirement` law | now | DB exclusion list |
| DI-29 | Does the package row type oracle bind the raw package member or the prelude shim? | tables | **ruled** (2026-09-09, Wave 2 amendment) | The oracle binds the actual adapter and its explicit type/import symbols. T0 compares A, E and the full R representation independently in both assignability directions and checks receiver/request parameters separately without assertion casts. Required/optional/rest parameters and supported overloads are explicit. Missing files/types/symbols, diagnostics and accidental any are unresolved/refused, never agreement. Every selected query ID is accounted for. Tool readiness may detect known mismatches; its conformance command remains nonzero until they are resolved. A receiver property binds the shim actually invoked; package-member comparison is separate evidence. Written into Test/contracts/foundation-wave2.contract.md §Target checks. | eight declared row types are unchecked against rc.112 today; `LawfulTable` says nothing about them | T0 before new target conformance claims; S6b integration | `src/Effect4/Codegen/Read.lean` (`LawfulTable`); `tools/Tools/TsGen.lean`; `Test/contracts/foundation-wave2.contract.md` |
| DI-30 | What may enter the `Classical.choice` boundary, and on what grounds? | laws/process | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): the `Classical.choice` boundary is a **named list**, and a declaration joins it only with a `# reason:`-shaped line in `Test/Audit/AxiomGate.lean`'s header naming the theorem and why choice is needed — the gate's own record, in place of the absent file it cites. First case: the 34 WHATWG Promise/Jobs declarations are **refused** unless reproved (the foundation probe reproves `Table.fresh_get` without choice), so no WHATWG dependency enters on their strength. Written into `Test/Audit/AxiomGate.lean`'s header (docstring owed; that file is the coordinator's, never a seat's). Today the gate enforces a seven-module, thirty-six-declaration list by name | the axiom ceiling every receipt quotes | before release | `Test/Audit/AxiomGate.lean` |
| DI-31 | Does `Ann` stay `Unit`, and does `Defect` stay closed with no string payload? | value/errors | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): **one** append, `Defect.error (e : Err)` (`ctor 5 [Err.image.toVal e]`, an `ofDefect` arm, `Defect.image` laws by `cases`), covering the tagged *and* the text error in place of the two constructors first proposed. `orDieCause`: `tag n ↦ user n` unchanged; `text s` and `tagged t m ↦ Cause.die (Defect.error e)`; `boom ↦ badName` unchanged. `defectJson (.error e) = {"error": errJson e}`, `reasonCode (.die (.error _)) = 220`, and the truth comparator compares a `die` carrying `error` **by payload, not by kind**, which closes `E4-HOST-CE-003` with `pSqlOrDie` compared exactly. `Defect.host (text)` stays refused. It lands in **S2, not S3** — a text error must have a defect image the day it exists. Written into `src/Effect4/Machine/Stores.lean`'s `Defect` docstring (S2, docstring owed) and `Test/Counterexamples/REGISTER.md` (E4-HOST-CE-003). `Ann` stays `Unit`. Consequence for DI-09's sugar: an `Effect.orDie` printed over `catch` dies `badName` exactly as `LayerTerm.orDie` does today, so if `Effect.orDie` is to be a printed head, `Defect.tagged` must be ruled with it or the head prints a lie | the alphabet's images, `defectJson`, `reasonCode`, `orDieCause`; for `host`, tape replay and exit comparison | with DI-09 | `src/Effect4/Machine/Stores.lean`; `Test/Counterexamples/REGISTER.md` (E4-HOST-CE-003) |
| DI-32 | Is a stamp match evidence of freshness, and may a second stamp protocol exist? | generated/process | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0), in the build-systems vocabulary (Mokhov, Mitchell, Peyton Jones, *Build Systems à la Carte*): a stamp is a **verifying trace** (fresh inputs), a byte comparison is **constructive** (the committed bytes are what the producer emits). Every family in `docs/GENERATED.md` carries **one or more** words — proved, reproduced, tested, stamped — because a claim can hold several at once: a finite checker run (`tsc`, `#guard`) is *tested*, never *reproduced*; a byte comparison is *reproduced*; provenance alone is *stamped*. That exposes the 119 CAS goldens and the 4 LCNF outputs as **stamped** until DI-45 moves them. Written into `docs/GENERATED.md`'s family table and `docs/DESIGN-MAP.md`'s grades paragraph (S0); each slice carries its own words | what the gate means for the CAS goldens, LCNF and the ingest family | before release | `docs/GENERATED.md`; `scripts/lib/check_generated.py` |
| DI-33 | Do the hand-carried generated families ever join `generate.sh`? | generated | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): `bash scripts/generate.sh --all` (the same thing as no argument) runs one **named order** — derived, eff, wire, cas, ts, readme — with the ingest README renderer last, because it reads three `.gen.ts` files the `ts` family has just written; Truth stays a **host-lane family outside that order** (its Lean half reads the tapes its bun half re-records, so the two producers are a fixed point, not a pipeline stage) and `docs/GENERATED.md` says so. Written into `docs/GENERATED.md` and `scripts/lib/generate.py` by S1b. Before it, "one command" was five of ten families; the ingest README has a renderer and a gate but is outside the generation command, so a `Packages`, `Forms` or `Taxonomy` change reds three gates until it is re-rendered by hand (correction from the Codex seat: it is not producer-less); cheapest is a named order in the map or an `--all` | any table change; the sweep's three gates | before the map is cited as one command | `docs/GENERATED.md`; `scripts/lib/generate.py`; `ts/eff/ingest/render-readme.ts`; `scripts/check-ingest.sh` |
| DI-34 | Are the corpus pins and the wild census inside the repository and inside a gate? | faces/process | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0), the cheapest sufficient act: vendor the two JSON files the census pins (the corpus manifest and the project labels) into `ts/eff/ingest/census/`, leave the 34 checkouts external and let the census **refuse** when they are absent, and make the hard-coded corpus path a required `--root` argument. Written into `ts/eff/ingest/census/pins.ts` and `ts/eff/ingest/cli.ts`; scout D executes it | every published census and fidelity number is unreproducible if the external corpus moves | before release | `ts/eff/ingest/census/pins.ts`; `ts/eff/ingest/cli.ts`; `scripts/check-ingest.sh` |
| DI-35 | Does `eq` become comprehensive and derivable, and where does equality belong? rc.112's `Equal.equals` is structural at this pin; `catchTag` uses `===` on one string. | type/faces | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): widen `eq` to `.string` only, landing with DI-09's atoms; keep the printed `eq` as `===`; write the refusal set into DB-15 (widen one `Ty` at a time, only where `===` compares faithfully; past that the printed `eq` becomes `Equal.equals`, its own slice); file `Val.eqAt : Ty → Val → Val → Bool` as the destination beside `Val.hasTy`; no `Equal` class in the `Effects` package (its claim boundary retires decidable equality). Carry **`or`/`and` at `.bool`** with it: the term language has `not` and no other connective, so `catchTags`' exact test and any predicate for `catchIf` are unspellable without them. Written into `docs/DESIGN-BASIS.md` DB-15 (the refusal set, G9's sentence) at S0; the atom docstrings in `src/Effect4/Program/Native.lean` and the `harness/truth/prelude.ts` header land with S3a (docstrings owed) | the atom families (`polyArms`, not `monoAtoms` — a duplicate `"eq"` breaks the OCaml atom count), the prelude and its self-test, a truth fixture per admitted type | with DI-09 | `src/Effect4/Program/Native.lean` (`eq`, `not`); `src/Effect4/Program/Typed.lean`; `src/OCaml5/Eff/Emit.lean`; the equality note; the elimination design note |
| DI-36 | Where do the canonical package tables live? Under `Effect4.Api` (ruling A2) a data edit invalidates 48 modules across four build roots, including 14 proof modules and the audit, with zero content consumers under `src/`. | architecture/tables | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): an `Effect4Packages` library root imported by tools, tests and the truth driver only; `Api` keeps its table parameter and gains nothing. It is **its own mechanical commit**, not part of S3a. Written into `lakefile.toml` and `Test/Audit/AxiomGate.lean`'s root list by that commit (both coordinator-owned) | `lakefile.toml` and the axiom gate's root list (coordinator-owned); every importer of `Effect4.Program.Packages` | third package | `src/Effect4/Api.lean` (the import); `src/Effect4/Program/Packages.lean` |
| DI-37 | The printed-image versus strict-foreign contracts of the ingest (ordinals per unit from 4 not 10; `true` into a number key refused). | faces | **ruled** (2026-09-09, Wave 2 correction) | Printed and foreign contracts retain their different admission/refusal/key-numbering policies. The current corpus check is conditional fidelity on the shared admitted fragment, not proof that every printed program is foreign-admitted. Require one valid verdict from each foreign reader and compare them before classifying lifts or refusals. Asymmetric, missing, malformed or conflicting verdicts fail. Agreed refusals stay visible; an independent required-positive corpus prevents vacuous acceptance. Two lifts must also equal the printed oracle up to the specified key renumbering. Written into docs/ARCHITECTURE.md and Test/contracts/foundation-wave2.contract.md §Target checks. | both engines' contracts; the corpus gate's oracles; the census | G1-reader before relying on inclusion counts | `ts/eff/ingest/README.md` (generated); `ts/eff/ingest/ck.ts`; `ts/eff/ingest/check-corpus.ts`; `Test/contracts/foundation-wave2.contract.md` |
| DI-38 | Does `EffTy.joinAnswer` become a least upper bound under `Ty.sub`? Today the two arms of `catchCause`/`matchCause`/`branch`/`gen`/`choose` must have equal answer types or one `never`; rc.112 gives `A \| B`. The asymmetry — two of the three columns are semilattices, the answer column is discrete — is the row, not `catchCause` in particular. | type | **ruled** (2026-09-09, Wave 2; S4c separate landing) | Adopt answer unions in S4c, after S4a normalization and S4b subtyping. Prove both upper bounds and leastness for Ty.join under the selected sub relation before changing every answer-merge consumer, including generator answers. Keep successful absent generator answers distinct from refusal. Report changed acceptance and inferred-type results by program identity; old estimates of unchanged .ty files are not a promise. No claim of principal or complete inference. Written into Test/contracts/foundation-wave2.contract.md §Types. | `EffTy.joinAnswer` and every typing rule that joins answers; the fuzz corpus's well-typed count; the OCaml typing mirror | S4c after S4b and its order/join obligations | `src/Effect4/Program/Typing.lean` (`joinAnswer`); `Test/Counterexamples/REGISTER.md` (E4-SURFACE-CE-096); `Test/Program/Gen.lean`; `Test/contracts/foundation-wave2.contract.md` |
| DI-39 | Do the derived error heads become `Codegen.Forms` rows or ingest-spec engine rules? A forms row gives the Lean reader the expansion for free but moves the corpus pin (the precedent that sent the `sql` fold to an engine rule); an engine rule moves nothing but duplicates the expansion in two TypeScript engines. | faces/generated | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): forms rows for the six heads that expand (`catchTag`, `catchTags`, `catchIf`, `mapError`, `orElseSucceed`, `match`) and `Effect.catch` as a head; both engines need the pipe form anyway, and `Forms` already lists these heads as arity metadata. Either choice must fix the engines' pipe segment first: a piped `catchTag` is unknown to the head set (it is in the forms' duals, not its rows) and exits `E-OP-UNKNOWN` before the handler list is consulted. This is also the choice of who owns the classification order (DI-48). A `Forms` change silently reds three gates until the ingest README is re-rendered by hand, which DI-33's named order now fixes. Written into `src/Effect4/Codegen/Forms.lean` and both engines by S3a, with the ingest README re-rendered in the same commit | the corpus pin; `forms.gen.ts`; both engines' `E-HANDLER` lists and `pipeSegment`; the ingest fixtures and ledger | with DI-09's sugar commit | `src/Effect4/Codegen/Forms.lean`; `ts/eff/ingest/check-corpus.ts` (the pin); `ts/eff/ingest/ck.ts`, `oxc.ts` |
| DI-40 | Are the hand-written copies of the atom table gated? The OCaml copy is gated by the generator's abort; both foreign engines hold a literal copy of the atom set and of the handler list with no gate, and the truth prelude is a third hand copy, so an atom added in Lean is silently unknown to them. The types scout counts fourteen copies, **three already stale**: the ingest fidelity round-trip, the fuzz generator's arity table and the OCaml GADT surface all miss `strings`, added 2026-09-08. | generated/faces | **ruled** (2026-09-09, Wave 2 strengthening) | Make native atom identity enumerable in one semantic owner; exhaustive interpretation/typing and generated names derive from it while stored Term.app keeps string spellings. Prove successful nativeAtomTy lookup implies membership in the exported inventory. Both readers and the truth self-test consume the generated projection. Reject a newly accepted atom omitted from export and a missing prelude case. The current hand-enumerated monoAtoms check establishes only listed cases. Written into Test/contracts/foundation-wave2.contract.md §Target checks. | every atom addition; the ingest verdicts on any program using a new atom | G1-atoms before S3 adds atoms | `ts/eff/ingest/ck.ts`, `ts/eff/ingest/oxc.ts` (the atom sets and handler lists); `harness/truth/prelude.ts`; `src/OCaml5/Eff/Emit.lean` (the OCaml abort); `tools/Tools/TsGen.lean`; `Test/contracts/foundation-wave2.contract.md` |
| DI-41 | Do `Ty`, `Row`, `EffTy`, `RowKind`, `RowShape` and `Registration` get Lean `Canonical` instances? Two hosts write bytes for them and Lean has no encoder, no round-trip theorem and no golden; they are exactly the families the OCaml generator's coverage abort skips, checked only by OCaml self-round-trip at one seed. | wire/generated | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): yes — the six families join the generator manifest's Program group (`tools/Effect4Gen/manifest.json` +6, the guards file extended) before Phase 1, and acceptance is that the new Lean bytes agree with the existing OCaml and TypeScript writers (both use declaration order) on a fixture per family. **Correction to the first cut:** `src/OCaml5/Tools/EffGen.lean`'s coverage skip list **stays** until `src/OCaml5/Eff/Goldens.lean` holds a metadata fixture inventory (one value per family) counted by a separate `coverage-metadata.txt` — the generator counts program-tree constructors only, so shrinking the skip list first makes it refuse with zero coverage. The deriving handler is not on this path; it stays a pilot (DI-18). Lands in S5a | the generator manifest; the Derived Lean file; the generated-file count; the coverage skip list; six new golden vectors — an engine regenerated at 20 encoders is regenerated again at 26 | before Phase 1 | `src/OCaml5/Tools/EffGen.lean` (the skip list); `ocaml/eff/eff_wire.ml`; `ts/eff/wire.gen.ts`; `tools/Effect4Gen/manifest.json` |
| DI-42 | Is the hand value tree that cuts the 42 `.bin`/`.json` goldens checked against `Canonical`? A second value tree with its own framing and a complete hand `toVal` cuts the goldens and is the JSON oracle for both faces; nothing in Lean compares it with the generated instances. | generated/wire | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): one cross-check loop in `src/OCaml5/Tools/EffWire.lean` (which already imports both encoders), throwing on disagreement over the goldens corpus; the hand value tree retires when the World lands (DI-13), not before. Lands in S5a with DI-41 | nothing if it passes; if it fails, the 42 `.bin` and every `.json` oracle | with DI-41 | `src/OCaml5/Eff/Goldens.lean`; `tools/Tools/Corpus.lean`; `src/OCaml5/Tools/EffWire.lean` |
| DI-43 | Does `Store.Tag` get a census and an injectivity theorem, as `Kind` and `HandleKind` have? It is twelve bare definitions pinned by literal-byte guards, and it is the alphabet every other one is framed inside; the wire tool's hand tag list should read it. | wire | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): yes — `Tag.all`, `Tag.ofByte?` and `byte_injective` beside the twelve definitions in `src/Effect4/Store/Val.lean`, about thirty lines, no byte moves, and `src/OCaml5/Tools/EffWire.lean`'s hand tag list reads them. Lands in S5a; it is the missing input of DI-02's replacement gate | nothing | before the ledger | `src/Effect4/Store/Val.lean` (the tags); `src/Effect4/Store/Kind.lean` (the model); `src/OCaml5/Tools/EffWire.lean` (the hand list) |
| DI-44 | Is the generated-file map checked in the tree-to-map direction? Every row's stamp is verified and nothing refuses a generated artefact with no row, though the map states that rule. | generated/process | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): about fifteen lines in `scripts/lib/check_generated.py` — walk the tracked files for a `cut-from:` line or a `.cut-from` sidecar and refuse one with no row in the map, the direction the map already states and the gate never checked. Lands in S1b. (The retained baseline carries no stamp on purpose and is not a generated artefact — DI-47.) | nothing; one gate change | before release | `scripts/lib/check_generated.py`; `docs/GENERATED.md` |
| DI-45 | Do the 119 CAS goldens get a constructive check, or is "verifying trace only" recorded as their evidence class? The producer runs under `generate.sh`, but the drift set excludes the family and the comparing gate is declared red. | generated | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): add the CAS goldens to the drift set of `scripts/lib/check_generated.py` (two lines and one count change, 25 → 144), with **both numbers in the commit message**, in S5a. Until that lands their evidence word in `docs/GENERATED.md` is **stamped**, written there at S0 (DI-32), so the map never reads as if they were byte-compared | the drift-count pin and the gate's runtime | with DI-32 | `scripts/lib/check_generated.py`; `docs/GENERATED.md` |
| DI-46 | Do `Store.Root`, `Store.Store`, `Machine.MemoEntry` and `MemoMap` get `Canonical` instances, retiring the six tool-side shapes the CAS goldens invent for them? | wire/CAS | open | yes, with the CAS lane's X2; the tool then cuts Lean bytes for every case. Sequenced 2026-09-09 (S0): no new evidence; CAS commit 7 owns it | the CAS goldens tool's projections; the affected goldens; the engine's readers | with CAS commit 7 | `src/OCaml5/Tools/CasGoldens.lean` |
| DI-47 | Is there a compatibility relation `W ⊑ W'` on the world description, with append-only as a theorem rather than a review rule? | wire/laws | **ruled** (2026-09-09, Wave 2 amendment) | Retain Test/fixtures/baseline/66ee4657 unchanged. Supplement its incomplete payload/tag metadata from immutable revision 66ee465730126048ad90d99d24ce4f12f5bb2982 with extraction provenance. Compare structural/wire compatibility, old-value exact decoding, typing/admission, and execution permission separately. Reject reorder/removal/reframing/tag reuse; declared appends are allowed. Named S2 narrowing and S4 type changes are explicit policy deltas, not wire corruption or blanket permission to refresh old fixtures. Unauthorized permission loss still fails. Current snapshot equality is not a universal compatibility theorem. This replaces DI-02/03; written into docs/DESIGN-MAP.md §L3 and Test/contracts/foundation-wave2.contract.md §Compatibility. | nothing existing; it replaces the ledger design of DI-02/DI-03 | G2 policy/supplement before alphabet appends; G4 consumer gate | the one-generator plan v2 note (step 3.3); `ocaml/engine/e4_program.ml` (the shape, in OCaml); `Test/contracts/foundation-wave2.contract.md` |
| DI-48 | Is a refusal taxonomy's classification a function of the input or of the engines' rule order? A verdict carries the first code met in rule order, so `E-HANDLER` is unreachable behind `E-PARAM-SHAPE`, `E-IMPORT-OPAQUE` and the pipe branch: the census reports two units against thousands of `catchTag` occurrences. | faces/process | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): the classification order is **Lean data** in `src/Effect4/Ingest/Taxonomy.lean`, emitted to `ts/eff/taxonomy.gen.ts`, and the census reports **the rule that fired** beside the code. Until it does, no census number about a refused family means anything and **no handler number appears in a tracked file** — DI-09's benefit reads "unmeasured (DI-48)". Scout D executes the census half; the Lean half lands with S1b | every published census number and every cost estimate that quotes one (DI-09, DI-21, DI-39); the ingest spec's plan to rank handler rows by census | before DI-09's benefit is quoted | `src/Effect4/Ingest/Taxonomy.lean`; `ts/eff/ingest/ck.ts` (the pipe branch, the handler list) |
| DI-49 | Is the printer's own output type-checked anywhere? It is executed on 24 programs and parsed on 400; the TypeScript compiler never sees it, so the basis's type-checker obligation is unmet for the program route. | faces/process | **ruled** (2026-09-09, Wave 2 scope correction) | Typecheck generated truth modules, their prelude and the recorder under the pinned strict options. Repair recorder diagnostics through typed delegation/index handling, not broad casts. The printed typing corpus deliberately includes rejected programs; it is not a uniformly type-correct TypeScript suite. A future typed-subset gate must define its selection/import policy. T0 accounts for unresolved generated annotations, including opaque-handle symbols and the complete requirement row. Evidence is tested, never a Lean semantic theorem. Written into Test/contracts/foundation-wave2.contract.md §Target checks. | whatever the first run finds in the printed image; nothing else | S1c/recorder follow-on; before S6b completion | `docs/DESIGN-BASIS.md` (DB-09); `scripts/check-truth.py`; `scripts/check-ts-eff-corpus.sh`; `Test/contracts/foundation-wave2.contract.md` |
| DI-50 | Does the faces layer get a contract packet? The printer, both readers, the ingest, the truth harness and `ocaml/eff` have no packet among the 41 in `Test/contracts/`; their specification exists only in module headers and untracked notes. | architecture/process | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): one packet naming the nine faces, the evidence word of each (DI-32), the two round-trip laws and their side conditions, the two ingest contracts and the inclusion property (DI-37), the truth claim with its quantifiers (single-fiber, pinned host, 24 programs, 6 tapes — DI-23) and the `ocaml/eff` conformance relation (DI-19); no new batteries — it cites the ones that exist. It is `Test/contracts/faces.contract.md`, in the format of `Test/contracts/README.md`, landing with S1b | nothing; it is the artefact whose absence makes the layer ungraspable | before release | `Test/contracts/README.md` |
| DI-51 | Do bare-filename and prefix-less citations evade the source-citations gate? Two live dangling citations do: the truth harness's `NOTES.md` (four sites) and the printer's `Codegen/Layer.lean` (DI-24). | process | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): write both with repository-relative paths in the next commit that touches those files, so the existing gate refuses until they resolve; extending the token pattern is optional. At S0 the gate still names two dangling targets, both in slices in flight: the truth harness's `NOTES.md` at three sites (S1b) and the new invocation law module's citation of the API module without its `src/` prefix (S1a). Neither is baselined; the gate must go green in the same wave | four comment lines; whichever of the two files is then owed | now | `scripts/check-source-citations.py`; `harness/truth/prelude.ts`; `harness/truth/run-truth.ts`; `src/Effect4/Codegen/Print.lean` |
| DI-52 | Is the OCaml wire differential's `same_program` list maintained, and is it maximal? Four names are held to byte equality against the eight wire goldens; the other four get only "a byte difference is a program difference". | OCaml/generated | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): derive the `same_program` list from the intersection of the two tools' corpora and emit it as **data the test reads** (`ocaml/eff/test/test_lean_wire.ml`, `src/OCaml5/Tools/EffWire.lean`), so the differential's strength is a fact about the corpora rather than about a hand list. Lands in S5b, with DI-19's suite | the differential's strength on four programs | with DI-19 | `ocaml/eff/test/test_lean_wire.ml`; `src/OCaml5/Tools/EffWire.lean`; `src/OCaml5/Tools/EffGen.lean` |
| DI-53 | Is `Ty` equality the target's type equality? `Ty.join` canonicalises only the top level: `.option (union nat string)` and `.option (union string nat)` are distinct values denoting one TypeScript type, and `join x x ≠ x` for a non-canonical `x` (probed: idempotence and the unit fail; commutativity and associativity hold on a sample). | type | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): keep raw `Ty` as data and add `normalize : Ty → Ty` (members sorted by the structural key, deduplicated, right-nested, `never` dropped, recursing through `option`, `list`, `prod`, `except`, `exitOf`, `causeOf`, `fiberOf`), with `join a b = normalize (union a b)`. The laws are stated **through `normalize`**, not raw: `normalize_idem`; `hasTy_normalize : Val.hasTy v (normalize t) a = Val.hasTy v t a`; `join_self : join t t = normalize t`; `join_never : join never t = normalize t`; `Canonical t ↔ normalize t = t`; and idempotence, unit, commutativity and associativity **on canonical inputs**. The earlier "the four contract rows become unconditional" is corrected — raw idempotence and unit are refutable (`x = union nat nat` normalises to `nat`) — and the four rows are restated in this form. A wrapper `CTy := {t // Canonical t}` serves APIs that need ordinary equality; row admission compares by `normalize`. Measured: 0 non-canonical types in 390 occurrences, so no `.ty` golden moves. Written into `src/Effect4/Program/Eff.lean`, `Test/contracts/surface-handler-fits.contract.md` and `ocaml/eff/eff_typing.ml` by **S4a alone**, before `Ty.sub` — a least upper bound over a non-normal order is not least | `Ty.join`; any non-canonical `Ty` literal in a row or fixture; the OCaml hand `insert_member`/`join`; no wire bytes (`Ty` is off the program wire) | with DI-15, before `Ty.sub` | `src/Effect4/Program/Eff.lean` (`members`, `insertMember`, `join`); `Test/contracts/surface-handler-fits.contract.md`; `ocaml/eff/eff_typing.ml` |
| DI-54 | Should `effTy` read `Signature.dom`? It is declared and instantiated but never consulted by the typing, so a `perform` at an external index outside the supplied table types through the placeholder and is well typed in Lean, while the OCaml checker refuses it categorically, against its own stated behaviour. The two typing faces disagree on a program Lean accepts; the 42 `.ty` goldens miss it (probed at the empty table). | type/faces | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): read `dom` in `effTy`'s `perform` and `callback` arms (`guard (sig.dom op)`), as the Lean reader already does — strictly narrowing — and add the probe program `bind (fail 1) (perform (external 0) (var 0))` as a `.ty` golden in the same commit so the corpus covers the class. Measured before the change: **0 of 442** corpus verdicts move (400 sample, 42 golden), so the narrowing is free; the change is re-measured **alone**, not bundled with DI-61's or DI-62's narrowing (DI-60). Written into `src/Effect4/Program/Typing.lean` by S1a | `effTy`'s two arms; no golden expected to move; nothing generated | now — one line | `src/Effect4/Program/Typing.lean` (`Signature.dom`, the `perform`/`callback` arms); `src/Effect4/Program/Native.lean`; `ocaml/eff/eff_typing.ml`; `src/Effect4/Codegen/Read.lean` |
| DI-55 | Does the printed image preserve literal types? A tag-set error column (DI-15) renders `readonly ["A" \| "B", string]`, but the printed expression is `pair("A", m)` and TypeScript widens a string-literal argument to `string` unless the parameter is constrained, so the row's declared type and its printed image would disagree, and DI-29's oracle would red on exactly the rows DI-15 improves. | type/faces | **ruled** (2026-09-09, Wave 2) | Use const-generic A and B parameters for the prelude pair in S4b, retaining its runtime [a,b] body. Preserve literal information rather than weakening the row type or adding leaf assertions. Independent positive residual cases and the wrong-tag negative must be checked by T0/tsc at the pinned compiler. Written into Test/contracts/foundation-wave2.contract.md §Types. | the prelude's `pair`; the type-oracle file when DI-29 lands; every truth fixture printing a tagged error; possibly the printer's `pair` emission | with S4b literal types | `harness/truth/prelude.ts`; `src/Effect4/Codegen/Print.lean`; DI-29; DI-49; `Test/contracts/foundation-wave2.contract.md` |
| DI-56 | What is each target's scalar domain, and must admitted primitive execution remain in it? The generated `Lit` schema accepts negative integers (and `-0`) that the canonical wire writer refuses (probed by the Codex seat); a safe integer need not stay safe under JavaScript addition. | representation/type/host | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): a target's scalar profile is **bounded with explicit refusal, intermediates included** — naturals, framing lengths and every arithmetic *result* stay in the profile or the host refuses; no wrapping, no saturation; the logical `Nat` stays unbounded, and the schema and wire constraints are generated from the profile. A refusal must have an **execution outcome**: the adapter and prelude raise a distinguished `ProfileRefusal` (a tagged host error), the recorder records it as its own row class, and the comparator classifies the run "outside the profile" — neither agreement nor disagreement, with the retained state per the observation policy; the run is counted, not claimed. Until that lands the truth claim names the exercised safe fragment (all 24 programs). Measured: both byte writers already refuse an unsafe integer, but as a *thrown* exception, which inside Effect is a defect, not an admission refusal; what is unrefused is an in-program comparison — `eq(add(M,2), succ(M))` is `true` and `lt(succ M, succ (succ M))` is `false` at `M = MAX_SAFE_INTEGER` — that can steer a branch Lean does not take with a perfectly wirable Boolean answer. That is the case the restricted profile must name. Two instances: the be64 header on js_of_ocaml (`ocaml/eff/eff_frame.ml`, `ocaml/engine/e4_be.ml`; repair by quotient/remainder emission and overflow refusal, S5b) and the TypeScript adapter (S6b). **js_of_ocaml is claimed only under the framing vectors *and* the arithmetic profile *and* the conformance suite**; native regeneration is gated by DI-19 alone. Recommended beside it (scout E, not ruled): state the cell domain as `{string, number in Nat, boolean, null}`, refuse the rest in the **answer** direction too (`Statement.PrimitiveKind` has eight members and the JSON-text codec is lossy or throwing on four), and pin `SqlClient.SafeIntegers = false` as profile data. Written into `docs/DESIGN-BASIS.md` DB-09 (S0) and `Test/Counterexamples/REGISTER.md` (`E4-TARGET-CE-027`, S0) | the generated TypeScript schemas and validators; atom and prelude arithmetic; reader admission; row adapters; cross-language boundary vectors; possibly printed types | before claiming the generated schema describes exactly the executable or serialisable domain | `ts/eff/eff.gen.ts`; `ts/eff/wire.gen.ts`; `harness/truth/prelude.ts`; `ts/eff/test/wire.test.ts`; the Codex categorization note §8 |
| DI-57 | What internal machine-to-reference agreement is claimed for non-empty row tables and external answers? `run_eq_ref` has no table or answer parameter, while the public replay API does. | laws/host | **ruled** (2026-09-09, S0) for the statement; the proof is a later slice | Ruled 2026-09-09 (S0): state the existing theorem **exactly** — `run_eq_ref` holds at the empty table with no oracle answers — in `src/Effect4/Laws/Program/RuntimeR.lean`'s docstring (S6a, docstring owed), and file the target proposition `RunEqRefTableStatement` in `Test/contracts/machine-scheduler-core.contract.md` (S6a). The reference lacks external registration, conversion and allocation, the prepared answer and evaluator selection; the generalised relation needs the table and oracle compatibility premises first, and the proof waits for S6's frozen protocol. Host conformance stays a separate claim | the reference evaluator's parameters; the replay relation and theorem statement; a table-aware proof graph; the host-row acceptance description | before extending the agreement claim to an external package execution | `src/Effect4/Laws/Program/RuntimeR.lean`; `src/Effect4/Api.lean`; `harness/truth/Truth.lean`; DI-07, DI-17, DI-26 |
| DI-58 | Does replay validate the recorded host call, or only consume its answer? The tape envelope names an operation and a request while the Lean decoder reads the answer or failure payload only. | host/replay/process | **ruled** (2026-09-09, Wave 2 amendment) | Use an incremental checked replay session over the existing machine. Validate schema/version, session-local call association, exact RowTable, row/request/fiber/token and completion category against the outstanding call. No preloaded answer-list bypass. Preflight acceptance is not consumption: mark consumed only after application removes the matching guard; zero fuel retains reply and state as a frontier. Late/duplicate/malformed replies refuse without changing execution. A valid incomplete tape remains a frontier. Legacy tapes need explicit migration provenance. Stable published digests remain DI-01/04 work. Written into Test/contracts/foundation-wave2.contract.md §Host protocol. | the tape decoder; the recorder; request encoders; malformed-tape tests; the truth claim and any later published tape format | S6b-0 contract before S6b-1 replay | `harness/truth/prelude.ts`; `harness/truth/Truth.lean` (`tapeAnswer`); `src/Effect4/Program/Admit.lean`; DI-04, DI-23; `Test/contracts/foundation-wave2.contract.md` |
| DI-59 | Where is the DB-15 pair made — inside each program's handlers, or once at the row adapter — and what does the recorder keep beside it? | errors/host | **ruled** (2026-09-09, Wave 2 correction) | Keep program-visible projection at the actual row adapter. Persist a bounded versioned diagnostic projection beside the canonical error; do not claim arbitrary raw host objects/causes are saved losslessly. Selected reason categories/order and retained fields are explicit; unsupported mixed-cause recording must be represented or refused rather than silently keeping the first Fail. Current saved tapes hold only the pair/outer tag and remain historical evidence until migrated. Richer prod (lit tag) X payloads require a real error-image extension or a selected representable X, not literals alone. Written into DB-15 and Test/contracts/foundation-wave2.contract.md §Host protocol. | the eight prelude shims; the four sqlite tapes and `pKv` re-recorded (the byte delta stated in the slice's receipt); `taggedPair`/`outerTag` in `harness/truth/run-truth.ts`; the two package module headers | diagnostic schema in S6b-0/1; payload extension before a dependent third row | `docs/DESIGN-BASIS.md` DB-15; `harness/truth/prelude.ts`; `harness/truth/run-truth.ts`; `src/Effect4/Program/Packages/SqliteBun.lean`, `src/Effect4/Program/Packages/KeyValueStoreMemory.lean`; `Test/contracts/foundation-wave2.contract.md` |
| DI-60 | Do the fuzz corpus's count pins stand for a property, and what must a narrowing commit report? | type/process | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): the corpus keeps its count pins **and** gains the property they stand for. A narrowing `N` of the checker satisfies `acceptsNew p → acceptsOld p` — **new acceptance implies old acceptance**, not "every formerly typed program stays typed", which is the direction the first cut had backwards. Every narrowing commit reports the count delta and **names the programs whose verdict changed** (DI-54: none of 442; DI-62: the four Boolean-carrying sample programs). Negative fixtures stay independent of the corpus, and any `Eff` append reseeds it. Written into `Test/Program/Gen.lean`'s header (S1a/S2, docstring owed) | the corpus header and its guards; every narrowing commit message | with the first narrowing (S1a) | `Test/Program/Gen.lean`; DI-54, DI-62 |
| DI-61 | Which invocation forms does the runtime admit, and what certifies a program and its table before it runs? `perform sleep 1` types and dies `badName`; `perform (external i)` types and parks; a row registered `.deferred` types, passes `LawfulTable` and parks with its answer unused. | type/faces/api | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0). **(a)** A shared dispatcher `asyncRoute (op : NativeOp) (request : Term) (p : Point) : NCode`, extracted from the `callback` arm: `.external _` → the external registration (table-independent at compile), `.sleep` → the timer, otherwise the await registration. The `perform` arm routes `.external _` **first**, then sync rows to the sync route, then async built-ins to `asyncRoute`; the placeholder's `.program` kind is no longer consulted; fuel 0 stays `frontier p`. Two theorems, because `compileEff` has no table: `compile_perform_eq_callback : (∃ i, op = .external i) ∨ op.row.kind = .async → compileEff (.perform op r) p = compileEff (.callback op r) p` on the production compiler, and at the API, for `externalRow table i = some row`, the same equality of `Api.replay` on the admission fixture. **(b)** `Api.checkTable : RowTable → Option TableRefusal` refuses, with its position, a supplied row this runner cannot register (anything but `(async, external)`); `LawfulTable` is unchanged. **(c)** `Api.admitProgram program table : Except AdmitRefusal (AdmittedProgram program table)` (named so because `Program.admit` is decision admission) with fields `typed`, `lawful`, `runnable` — **`readable` is not a field**: `Api.imageCertificate` is a separate optional certificate for the print/read face, because `readable` is exact reconstruction after printing, not executable validity, and the historical wire program `pAwait` is typed, admitted and **not** image-certified. **(d)** `Api.runAdmitted` and `Api.replayAdmitted` consume the certificate; `Api.run`, `replay` and `runSync` stay as raw entry points, documented as unchecked (fixtures, negatives, the truth driver until S6b). **(e)** Fixtures: the 55 × 2 route matrix as `#guard`; `perform sleep 1` through `replay [evaluate, .advance 1, flush]` (not `run`, which takes no tape); `perform (external 0)` consuming the reply; the `.deferred`-registered row refused by `checkTable` at position 0; the six external negatives. Written into `src/Effect4/Program/Compile.lean`, `src/Effect4/Api.lean` and `Test/Program/InvocationContract.lean` by S1a. **Landed at S1a (2026-09-09):** (b), (c), (d), (e) and the behaviour-free half of (a) — `asyncRoute` extracted, `callback` compiling exactly as before. **Not landed:** routing `perform` through it. `denoteR`'s own `perform` arm and `inlineYield` (`src/Effect4/Laws/Program/DenoteR.lean`), `compileEff_perform` (`Handles.lean`) and `compileEff_perform_async`/`_program` (`Intro.lean`) mirror the old arm, and `run_eq_ref` relates machine to denotation with no premise, so the compiler cannot change alone; the gap is pinned as thirteen `THE GAP` guards in the battery and the lockstep change — denotation, compiler, head lemmas, `run_eq_ref`, then the theorem — is the scoped task **S1a-b** | two compile arms; `Api`'s entry points and the admission contract; the invocation battery; no bytes, no goldens | now (S1a) | `src/Effect4/Program/Compile.lean`; `src/Effect4/Api.lean`; `Test/Program/InvocationContract.lean`; DI-54, DI-57 |
| DI-62 | Which error payloads may a program introduce, and what is their machine image? `fail "lost"` types at `.string` and runs as `Err.boom`, which `errAdmits .string` refuses, so the string is lost before any host sees it. | type/errors | **ruled** (2026-09-09, S0) | Ruled 2026-09-09 (S0): **restrict, then extend by one.** `supportedErrTy : Ty → Bool` sits beside `Ty` in `src/Effect4/Program/Eff.lean` (`never`, `nat`, `string`, `prod string string`, `union` of supported members; `lit` when DI-15 lands), and every **introduction** requires it: `effTy` at `fail` and at `yieldError` (which also feeds `errOf` and was missing from the first edit list) and `causeTy` at each `.fail` leaf; `die` and `interrupt` leaves stay `never` and remain admitted, so defect-only and interrupt-only causes are unaffected. `Err.text s` is appended (`ctor 3 [str s]`, an `ofErr` arm, image laws); `errOf` reads `nat ↦ tag`, `[str t, str m] ↦ tagged`, `str s ↦ text`, else `boom`; `valOfErr` inverts off `boom`; `errAdmits` admits `text` at `.string` and its unions. Theorems: `errOf_valOfErr`, `valOfErr_errOf`, and `errAdmits_errOf : supportedErrTy ty → Val.hasTy v ty → errAdmits ty (.fail (errOf v) a) = true` — a declared error type never permits discarded data. `Err.value (v : Val)` stays **refused** (a handle inside a cause would extend the minted-handle invariant into causes). Module layout, to avoid the cycle `errAdmits → Val.hasTy`: the predicate beside `Ty`; `src/Effect4/Program/ErrorImage.lean` (below `Native`) holds `errOf`, `valOfErr` and the folds parameterised by a membership function; `src/Effect4/Program/Typed.lean` instantiates them and re-exports the old names. Host: the comparator's `errJson .boom` becomes `{"boom": null}` so a text `"boom"` cannot collide with it (five `result.json` sites regenerate), `errJson (.text s) = str s`, and the tape decoder accepts a string `failed` only for a row typed at `string` (none today). Tests: **positive** `fail "lost"` typed at `.string`, exiting `fail (text "lost")`, compared on the truth column and recovered by `catchIf` in S3a; **negatives** `fail true`, `yieldError true`, `failCause (fail true)`, `fail (var h)` at a handle type; `failCause (die 7)` and `interrupt` still admitted. Scout E's census of the target: of 269 package `Effect.fail` payloads, 253 are tagged class instances, 14 plain strings, **0** numbers, **0** Booleans, 0 object literals — so `string`'s admission is right, the Boolean negative is well chosen, and `nat` is **the tree's** alphabet, not rc.112's. Written into `src/Effect4/Machine/Stores.lean` (`Err`), `src/Effect4/Program/ErrorImage.lean` and `docs/DESIGN-BASIS.md` DB-15; the appends land in S2 | `Err`'s image (append only, `ctor 3`); `errOf`, `errAdmits`, `orDieCause` (DI-31); three typing arms; the truth wire's `boom` spelling and `reasonCode` (`text ↦ 102`); the fuzz well-typed pin, re-pinned with the four Boolean programs named; print and read contracts unchanged (syntax only) | before DI-09 | `src/Effect4/Machine/Stores.lean`; `src/Effect4/Program/ErrorImage.lean`; `docs/DESIGN-BASIS.md` DB-15; DI-26, DI-31 |
| DI-63 | Does `effTy`'s `.scoped` arm discharge `Scope` from the requirement row, as rc.112's `Effect.scoped` does? | type/faces | **ruled** (2026-09-09, Wave 2) | The scoped typing arm removes sig.scopeKey using Row.diff t.requires (Requirement.single sig.scopeKey), retaining A, E, unrelated requirements and body refusal. This matches rc.112 Effect.scoped at vendor/effect-4.0.0-rc.112/src/Effect.ts:12815-12817 and the existing LayerTy.bodyRequires pattern. Prove the exact typing equation, idempotent discharge and unrelated-key retention; name all changed .ty/results. Execution/finalization is unchanged. Supply resolvable opaque-handle type imports on the target face and compare full R via T0. Written into Test/contracts/foundation-wave2.contract.md §Types. | the `.ty` goldens of every program using `scoped` (`pAcquire.ty`, `pActions.ty` carry non-empty rows today); the truth modules' printed types (the four sqlite programs print typed once the row is empty); `Test/Api/PackagesContract.lean` (two `scoped` programs whose requires are not pinned) | S1c before annotated resource integration | `src/Effect4/Program/Typing.lean`; `src/Effect4/Codegen/Print.lean`; DI-24; `Test/contracts/foundation-wave2.contract.md` |
| DI-64 | Is a row's identity `(spelling, trailing)`, and does it survive a second package that spells a member the same? | wire/tables | **ruled** (2026-09-09, Wave 2; implementation before third table) | Row identity must include the receiver identity for receiver-based members, or use an explicitly qualified binding spelling that retains it. The preferred internal key adds receiver identity; stored package indices remain relative to their supplied table. Update lookup and its round-trip proofs together, and retain same-member/different-receiver positives plus ambiguous/duplicate negatives. T0 compares the receiver actually invoked. A bare spelling/trailing pair is insufficient for a second get/set/remove/clear service. This is a prerequisite for a colliding third table, not permission to claim it already works. Written into Test/contracts/foundation-wave2.contract.md §Scope and sequence. | `rowKey` and `LawfulTable`; every table whose rows are re-keyed; the readers' lookup; possibly the printed call | before third table with a colliding receiver member | `src/Effect4/Codegen/Read.lean`; DI-22, DI-29; `Test/contracts/foundation-wave2.contract.md` |
| DI-65 | Do local host laws accidentally force every host to be deterministic, and does correspondence imply terminal cleanup? | semantics/host | **ruled** (2026-09-09, Wave 2) | General HostSpec transitions admit alternative outcomes and resulting states. Determinism is an additional law after determining decisions are fixed. At-most-once reply consumption is a session property, not uniqueness of all allowed host replies. Intermediate correspondence permits open resources; terminal observation requires completed cleanup and ownership discharge. Match the resource binding's double-close defect; keep its closure diagnostic separate from ordinary use. Refusing a late acquisition reply alone does not release the host resource: its adapter/session owns cleanup. Written into DB-09 and Test/contracts/foundation-wave2.contract.md §Host protocol. | HostSpec/LawfulHostSpec, local models/proofs, resource observer and binding tests | S6b-0 before replay/binding refinement | `src/Effect4/Program/Profile.lean`; `src/Effect4/Program/Admit.lean`; `harness/truth/prelude.ts` |
| DI-66 | Which type information is source data, compiler layout, or a certificate, and how do higher-order checking APIs compose it? | tooling/types | **ruled** (2026-09-09, Wave 2; bounded pilot) | Reuse Image/Canonical, Option/List traversals and lawful functional interfaces in checking code. Keep source reflection, ground Ty, runtime layout and mono-LCNF extraction distinct with explicit checked connections; constructors/destructors consume one layout plan. Pin compiler version/phase and dependency closure; unsupported/opaque data refuses. Preserve nested Option distinctions. Check supplied theorem names against exact reconstructed statements and axiom policy; missing laws, failed automation, counterexamples and finite tests are different results. Std.Do/mvcgen is a bounded pilot with direct-proof fallback. No new stored IR, no import Lean in the audited runtime, and no FFI. Written into Test/contracts/foundation-wave2.contract.md §Tooling. | tool modules, layout/extraction manifests, target diagnostics and optional certificate builders | T0 required for target claims; T1 experiment does not block S1/S2/S4a | `src/Effect4/Store/Image.lean`; `src/Effect4/Store/Canonical.lean`; `src/OCaml5/Lcnf/Dump.lean`; DI-18, DI-29 |

## Tracked records the sweep found already wrong (to correct)

- `AGENTS.md` said the basis runs DB-01…DB-10 (corrected 2026-09-09 with this register's row).
- `src/Effect4/Machine/Fibers.lean` says "the dispatcher table retires it" against DB-13's
  "there is no dispatcher table".
- `Test/contracts/machine-completion.contract.md` asks for a ruling that
  `Test/contracts/machine-handles.contract.md` already made.
- `Test/Counterexamples/REGISTER.md` (the row before E4-HOST) says a repair is open that is
  installed.
- DESIGN-BASIS's "Required proof graph" lists ten of eleven edges `Pending` while the coverage
  document quotes 133 of 135.
- Six tracked citations point at research notes that no longer exist on this machine,
  including the sole citation for DB-01's ruling and the axiom gate's boundary record.
- Three tracked files say `src/Effect4/Program/Eff.lean` "has exactly one theorem,
  `render_ofSpelling`"; it has none, and that name exists nowhere (the handler-fits contract,
  its counterexample file and its battery).
- `Test/Surface/HandlerFitsContract.lean` pins the four `Ty.join` laws by guard, but the file
  is declared red because it imports a `Surface.Handler` module that does not exist; the
  guards are frozen text, not a running check.
- `ocaml/eff/README.md` quotes line counts and a golden count ("39 programs") that are stale;
  `ocaml/engine/e4_program.mli` says the wire has 24 `Eff` arms where it has 27.
- `Test/Program/Gen.lean` says its alphabet is `nativeAtom`; it is ten of the eleven atoms.
- `harness/truth/prelude.ts` cites a `NOTES.md §4` that does not exist (DI-51).
- `docs/DESIGN-MAP.md` §L2 opened "first-order, no binder, no abstraction form" against its own later
  sentence; corrected 2026-09-09 (S0) to "first-order, binding values in `bind`, handlers and
  generators, with no abstraction form".
- `src/Effect4/Program/Packages.lean:36-41` says "the canonical table … the foreign readers read
  under this one table"; it is one link table among several and an index is relative to the table
  supplied (DI-22, reworded by S1a).
- `src/Effect4/Codegen/Print.lean:429-433` (and DI-24 as first written) says a program with a
  requirement has no two-parameter spelling. The four sqlite truth programs are not such programs:
  their rc.112 requirement is empty, and the row is non-empty in Lean only because `effTy`'s
  `.scoped` arm passes it through (DI-63).
- `src/Effect4/Machine/Context.lean:282,291` declares a second `Err` and a second `Defect` in
  namespace `Effect4.Machine.Env` (the header at `:52` calls them old spellings kept as patterns) —
  two alphabets with one name each, which S2's appends must not be written against by mistake.
- `src/Effect4/Program/Packages/KeyValueStoreMemory.lean:24-25` describes `KeyValueStoreError` as
  carrying `message`, `method` and `key`; it also carries `cause?`, and the row's
  `kvError = prod string string` puts a constant in slot one, so the pair discriminates nothing
  there and drops `method` and `key` (DI-59).
- Two untracked records the register now overrides, listed so a reader of either is not misled: the
  elimination note's `catchIf` and `causeError` snippets select the first *convertible* failure,
  where rc.112 selects the first `Fail` and converts once (corrected by DI-09 (4)); and the types
  note gives `Effect.catchReason(errorTag, reasonTag, f)` without its `orElse`, and without the
  point that the **outer tag is an argument** used to narrow the union before the reason tags are
  computed (which is why G1 is sound only for a single-outer-tag row — DB-15, scout E).

## Companion notes

Untracked working notes under `docs/research/` (gitignored; they may not survive a clone,
which is why rulings move out of them): the design-risk sweep, the error-paths scout notes
(friction, types, map, elimination, equality), the host-rows decisions memo (rulings A–G), the
CAS amendments (M1–M17), the one-generator plan v2, the ecosystem audit, the effects papers
review, the adequacy route, the architecture and codegen reviews, all dated 2026-09-02 to
2026-09-09.
