# Effect4 design issues

This register holds the design questions that are **open**. `docs/DESIGN-BASIS.md` holds the
ones that are **settled**: a DB row is an end state with a `Status:` line, and its "what this
basis refuses" clauses are the closed doors. This file is the other half — the questions still
in flight, what each would force to be redone if it were answered late or wrongly, and the
milestone by which answering it is cheapest. It was opened 2026-09-09 from the design-risk
sweep after the error-paths cycle (the companion notes are listed at the end).

A row moves in one direction. **open** — the question is named and nobody has answered it.
**recommended** — a scout or a slice has proposed an answer and stated its cost; work may
proceed on it where the row says so, on the understanding that a different ruling costs a
revert, not a redesign. **ruled** — the owner has decided; the ruling text and its date live in
the row, and the row names the tracked place the ruling was written into: a DB row, a module
header, a contract packet, a counterexample row, a `# reason:` line in
`Test/fixtures/trust-gate/known-red.txt`. **basis** — the ruling has become a `DB-nn` row and
this row is retired to a one-line pointer. A row is never deleted and an id is never reused, as
in `Test/Counterexamples/REGISTER.md`.

Two rules make the register worth keeping.

**A ruling is not made until it is written into a tracked file.** `docs/research/` is
gitignored, and `scripts/check-source-citations.py` accepts citations into it without checking
that they resolve, so a decision recorded only there does not survive a clone — the sweep
counted 504 such citations from 278 tracked files, six of whose targets are already gone. When
a row reaches **ruled**, the same commit puts the ruling in a tracked place and the row cites
it. DI-00 is the worked example: decided in a note, written into DB-15, the truth driver and
the sqlite module header the same day.

**Every row names what it would force to be redone** — wire ordinals, goldens, proofs,
engines, generated families, the printed face, corpus pins — because that, not the size of the
change, is what makes a late answer expensive. Rows are cited by id (`DI-nn`), never by line.

Milestones used in `decide by`: **step 8** (the engine host, waiting on plan v2 Phase 1),
**Phase 1** (the LCNF engine regeneration), **third package** (the next canonical package
table), **foreign lift** (admitting wild TypeScript), **release**.

## The register

| id | question | category | status | ruling or recommendation | forces rework of | decide by | pointers |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DI-00 | Which pair a tagged `SqlError` posts. | errors | **basis** | Ruled 2026-09-09: the reason's tag and the driver's message, rc.112's own two-level `catchReason` form, the outer tag implied by the row. | — | — | DB-15; `harness/truth/run-truth.ts` (`taggedPair`); `src/Effect4/Program/Packages/SqliteBun.lean` header |
| DI-01 | Is the unit of publication `bytes` or `(bytes, keys : Cid KeyTable)`, and is the ordinal ledger a stored `table` node kind? | wire/CAS | open | CAS amendment M17's default is yes; the literature backing is Unison's hash/name split and Avro's writer-schema-with-the-data (both say the pair); `Cid` does not exist under `src/` today, so the unit can still be chosen for free | every `Cid`; `keyOf`'s signature; `Kind.byte_le`'s bound; the renaming proofs | step 8, before the first published unit | `src/Effect4/Surface/Provision.lean`; the CAS amendments note (M17) |
| DI-02 | Does an append-only ordinal ledger exist, and which alphabets does it gate? | wire | open | build it before anything is published; today no mechanical guard exists. Narrowed (representations scout): not a new artefact but a field group of one `World` datum, with append-only as a proved relation (DI-47) rather than a review rule; `Store.Tag` is the one alphabet with no census at all (DI-43) | `Eff` 0–26, `NativeOp` 0–22, `Store.Tag` 1–12, `HandleKind` 0–7, `Kind` 1–15, service codes 4–9, package row positions — first publication freezes all seven by accident | step 8 | `src/Effect4/Program/Derived.lean`; the CAS amendments note (M16) |
| DI-03 | Where do ordinals live — a ledger file, a pinned Lean value, or M17's `table` node? | wire/generated | open | plan v2: the Lean value plus the M17 node; no text file | every wire golden and hex pin; the `.eff` differential; seventeen ordinal copies, six by hand | step 8 | `src/Effect4/Program/Wire.lean` (the hex pins) |
| DI-04 | Is a published tape, log or exit replay-relative, never host-relative? | wire/CAS | open | M2's default is yes | the tape, log, exit and checkpoint carriers become bytes plus hand images | step 8 | the CAS amendments note (M2) |
| DI-05 | Is a `ServiceKey`'s position content or machine-relative? | wire | open | machine-relative; M17's key table is what makes a published unit sound | ordinals inside every `provideService`, `service` and `provideLayer` subtree; every layer `Cid` | foreign lift | `src/Effect4/Surface/Provision.lean` |
| DI-06 | `Cid` or `Ref` for a program or tape inside a job? | wire/CAS | open | `Cid`; and a `Cid` must carry its kind (IPLD's multicodec argument), or the by-content index answers across kinds | every published job and receipt; the `byCid` index | step 8 | the CAS design note §12 |
| DI-07 | Does machine and algebra agreement extend past the single-fiber straight-line fragment? | laws | open | none; the refusal `CORE-FB-SIMULATION` reserves the scope but names no successor. DI-09's constructor is proposed to stay **outside** the plain fragment (covered by `run_eq_ref`, not `run_eq_meaning`), on the same footing as `provideLayer`, `gen` and `whileLoop`; that gap must be named in the denotation contract packet when it lands. Bringing it inside costs another two to three days of proof and extends the agreement claim to the tree's most-used error head | the code relation, the eight simulation modules, `meaning`, possibly the code shape; every straight-line receipt restated | before any equational or logic layer | `src/Effect4/Laws/Program/Agreement/Machine.lean` (`run_eq_meaning`'s `Straight` premise); `Test/contracts/machine-scheduler-core.contract.md`; `Test/contracts/program-denotation.contract.md` |
| DI-08 | Is Schema in the release, and does `Store.Shape` depend on the checker or only on the document carrier? | architecture | open | none — the acknowledged blocker of the Tier B archive line and the area roots | the Tier B archive line, the area roots, the `Api` closure, 7,095 lines of checker | now | `src/Effect4/Store/Shape.lean` (imports `Effect4.Schema.Authoring`) |
| DI-09 | Does the error channel get an elimination form? Today it is write-only: `catchCause` binds `Ty.causeOf`, which no value inhabits; no atom projects a cause. | type/faces | **ruled** (2026-09-09, spoken): **both** — one appended `Eff` constructor binding the error value **and** cause-projecting atoms, with derived combinators. Designed; three sub-rulings owed (below). | The design (the elimination note): (1) **one** constructor, recommended `Eff.catchIf (test) (body handler)` — rc.112's `catchIf`, which is `catch` at `test = true` and spells `catchTag`/`catchTags` exactly, including the whole-cause re-raise on the unmatched path; plain `Eff.catch` is the fallback and would need a registered deviation for `catchTag`. Compiles to the existing `Prim.onFailure`; stays outside the plain fragment (DI-07). (2) **Four atoms**, each an rc.112 export: `causeIsFail`, `causeError : causeOf e → option e` (`Cause.findErrorOption`), `causeIsDie`, `causeIsInterrupt`, accepting `.causeOf e` and `.exitOf a e`; plus `eq` at `.string` and `or`/`and` at `.bool` (DI-35); plus the `Val.hasTy` arm for `.causeOf` that discharges `TYPED-FB-CAUSE`. Not one `squash`: rc.112's `Cause.squash` answers `unknown`, which no `Ty` types. (3) Sugar over (1)+(2): `catchTag`, `catchTags`, `catchIf`, `mapError`, `orElseSucceed`, `orDie`, `match`, `sandbox`, `catchReason`; rc.112 has no `Effect.orElse` — the "or" family is the trailing `orElse?` parameter plus `orElseSucceed`/`firstSuccessOf`. Order: atoms (a day, no golden moves) → the constructor (the one hard proof: `compileEff_means_denoteR` and `denoteEffBody` are exhaustive over `Eff`, two to four days) → sugar (TypeScript plus `Forms` rows). Owed rulings: the constructor shape and that its binder is the error value; whether it enters the plain fragment (default: no, named in the contract packet); the atom names and `causeError`'s option type. | one appended `Eff` constructor and its arms on both faces and both engines; the `eff`, `derived`, `wire` (the `Eff` ordinal in the manifest) and `ts` families; the 400-program fuzz corpus reseeds on any `Eff` append (the generator's `pick` modulus), moving its measured guards; the ingest census (the "8,869 foreign `E-HANDLER` sites" is the ingest spec's count, not a measurement — the census says two, because the code is masked; DI-48 before quoting a benefit); on the faces `read.ts` needs no new head table (generated) | before Phase 1, while the append is still byte-free | `src/Effect4/Program/Typing.lean` (`catchCause`); `src/Effect4/Laws/Program/Typed.lean` (`TYPED-FB-CAUSE`); `src/Effect4/Laws/Program/Intro.lean`; `src/Effect4/Laws/Program/DenoteR.lean`; the elimination design note |
| DI-10 | Is there a bind law, and what is a neutral stack (papers review G8)? | laws | open | the general law is refused by name; a neutrality side condition is the shape on offer | the frame stack, if a general law is wanted; the logic layer inherits whichever shape is chosen | now, before DI-09 lands | `src/Effect4/Program/Compile.lean` (`contE`, `contAll`); DB-nn refuses only the fixed-fuel law |
| DI-11 | Do streams and channels stay fibers plus queues, or does a continuation become a value? | machine/type | open | fibers plus queues; no continuation handles in the value language. Note: the tag table's freeze is today the weakest of the three byte alphabets (no census, no injectivity theorem — DI-43), so DB-11's freeze is asserted rather than enforced | `Val` tags (DB-11 froze them), `Eff` constructors, the frame alphabet, the snapshot format, the engine | before the Queue and PubSub slice | DB-11; `src/Effect4/Store/Val.lean` |
| DI-12 | Are scoped and higher-order operations `Eff` constructors, or `RowKind.program` rows carrying a body? | type | open | constructors for the runtime primitives; everything derivable is an `Eff` to `Eff` expansion. The named exception (2026-09-09): `catch`/`catchIf` is **not derivable** in this IR — no `Eff` form fails with a cause *value* (`Eff.failCause` takes a `CauseTerm` literal), so an expansion over `catchCause` would need an `Eff.rethrow` constructor, the same append with a spelling rc.112 does not export; the reason is the missing eliminator, not convenience | the central sums and every wire ordinal; both readers; both OCaml faces | before the next family adds a constructor | `src/Effect4/Program/Eff.lean` (`RowKind.program`, `failCause`) |
| DI-13 | One pinned description of the cross-language types, or independent reflections? There are four, not three: `EffWire` hand-lists eighteen families and the twelve tag names beside `World.blocks`, the generator manifest and the LCNF types. | generated | deferred → recommended sooner | one, and sooner than Phase 3: six families have byte writers on both hosts and no Lean encoder (DI-41), an asymmetry one datum removes by construction | all four reflections; every OCaml and TypeScript projection; the Derived Lean files; every stamp | before Phase 3 | `tools/Effect4Gen/manifest.json`; `src/OCaml5/Eff/World.lean`; `src/OCaml5/Tools/EffWire.lean` |
| DI-14 | Is a target's in-memory representation free where it has no byte writer? | wire/OCaml | ruled in an untracked note | yes — table R of plan v2; recommended to become a framing/carrier column of the `World` datum rather than a note, which is what makes it checkable | reversing it re-frames `ServiceName` on the wire and moves every golden | before a second byte-writing target | the one-generator plan v2 note (table R) — needs a tracked home; `src/OCaml5/Eff/World.lean` |
| DI-15 | Does `Ty` gain a tagged sum, with a subtype relation? `Ty.union` is an untagged union de-duplicated by structural key, so `sqlError` and `kvError` are one type (probed: `Ty.join sqlError kvError == sqlError`). | type | **ruled to investigate** (2026-09-09, spoken): the sum, incorporating the subtype; designed twice; recommended after DI-09, with the third package | Two forms were designed. The elimination note's `Ty.tagged (tags : List String)` — ordinal 15, a canonical tag set, a `union` merge arm, `hasTy` checking the tag is in the set, `catchTag`'s residual `tags.erase tag`. The types note's **`Ty.lit (value : String)`**, a string-literal type — same ordinal, same rendering (`readonly ["A" \| "B", string]` falls out of the existing `prod`/`union` arms), same blast radius, strictly less code (no merge arm, no `LawfulTable` canonicity clause), and it gives `Ty.sub` its first real fact (`lit s ≤ string`). Recommended: `Ty.lit`. `Ty.sub` lands **with** it, not after (a lub without an order is not a lub), after DI-53's deep canonicalisation, entering at row admission at a supertype and `catchTag`'s residual — and **not** `EffTy.joinAnswer` (DI-38). DB-15's "no record" refusal stands; only "no sum" is corrected. Corrections to this row's first cut: **no golden program is needed** (`EffGen`'s coverage abort skips `Ty`); the OCaml hand edits are three hand-written files (`eff_typing.ml`, `eff_typed.ml`, `eff_typed.mli`) plus `ocaml/engine/e4_program.ml`; and the earlier "`Surface.Handler.fits` entry point" names a module that does not exist (its contract battery is declared red for that reason) | every generated family (`Ty` is world block 1: `eff_types/wire/json.ml`, the eff manifest, `eff/wire/json.gen.ts`); the two package tables' error columns become tag sets; the contract's type literals; every `.ty` golden (nothing on the printed face, since `Ty` is not printed as syntax — the golden that would fail is a `.ty`, not a `.bin`, and the coverage abort will not catch a missing `Ty` arm); DB-15 | third package | `src/Effect4/Program/Eff.lean` (`Ty`, `union`); `src/OCaml5/Tools/EffGen.lean` (the coverage abort); DB-15; the elimination design note |
| DI-16 | Does the standards and surface lane live in this repository or its own package? | architecture | open | one decision, once, before Tier B writes Lean | namespaces, `lakefile.toml`, the axiom gate's hardcoded roots, every citation, CI | before Tier B | `Test/Audit/AxiomGate.lean` (the root gate) |
| DI-17 | Is the adequacy route C16/C17a/C17b/C18? | laws | recommended | half A (`never_stuck`) is independent of everything and can start now; half B's preservation at every point **cannot be stated until DI-09's `hasTy` arm for `causeOf` lands** — today every compiled program binds a handler at a type no value inhabits, so the invariant is false as stated; state it once on points, with the denotation-side and machine-side forms as corollaries | a typing invariant beside `Minted`; `TypeBounded` in place of `KeyBounded`; type and requirement environments on the node | before the logic layer | `src/Effect4/Laws/Machine/Handles.lean`; `Test/Program/TypedContract.lean` (the receipt that `causeOf` is uninhabited); the adequacy route note |
| DI-18 | May `import Lean` enter the audited closure, and does `deriving Canonical` happen? | generated/process | ruled in an untracked note; the mechanism does not exist | yes, under a written exemption checked by the library-roots gate — neither the exemption text nor the check has been written | roughly 5,100 generated lines and 223 generated theorems, in either direction | before Phase 2 | `scripts/check-library-roots.sh` |
| DI-19 | What is the acceptance test of the first engine regeneration? | OCaml | ruled in an untracked note, contradicted by a receipt | the differential is the acceptance test of the carrier and extern change, not of correspondence with Lean; regeneration has *not* been shown to clear every engine failure. Narrowed (faces scout): acceptance is the conformance suite the reference already defines — the 42 `ocaml/eff` goldens decoded and re-encoded, the 8 wire goldens byte-equal, the 24 truth programs to the same exit — the reference-interpreter model; regeneration is a precondition, not the test | both declared-red gates; the engine sources; the differential's corpus width | before Phase 1 | `docs/GENERATED.md` (the LCNF rows); `Test/fixtures/trust-gate/known-red.txt` (its `# reason:` lines should name the suite) |
| DI-20 | Is the requirement channel polymorphic, and is that type-level or term-level? | type | recommended: no | refuse, on the right ground: polymorphism needs a binder and `Eff` has none; the ecosystem audit's "largest DX decision" concerns the effects package's morphism kit, which has no Effect4 consumer. What is owed instead is a **subeffecting** rule (`Row.Subset` as subsumption) in a declarative typing relation, which makes it non-syntax-directed and turns completeness into principality | `Row.diff`, `Provision`, `LayerTy` and every layer signature law; `Typing.lean` | before the second effect family | the ecosystem audit note §11; DB exclusion list (shared with DI-28) |
| DI-21 | Are class-shaped units and `Effect.fn` ever admissible on the foreign face? | faces | deferred | its own grill; not in the ingest packet | the unit definition, a parameterised `Eff` declaration, both engines, the census denominator, the printer | before foreign lift is a goal | `ts/eff/ingest/ck.ts` (`E-PARAM-SHAPE`) |
| DI-22 | Is a package row's index a position in the concatenated table or in its own package's? Both conventions are live (confirmed: the concatenated rule in the packages module and the foreign readers; per-package in the truth driver and the contract), coinciding today only because the sqlite table is first. | wire/tables | open | the concatenated table, as the module header already says; reconcile the Lean fixtures to it | six tapes; `packages.gen.ts`; both engines; the table-length pins | third package | `src/Effect4/Program/Packages.lean` vs `harness/truth/Truth.lean` (`hostInputs`); `Test/Api/PackagesContract.lean` |
| DI-23 | Are tapes and the denotation single-fiber, and if not, what orders a multi-fiber tape? | machine | open | state the restriction now, in the truth claim's quantifiers (the tape already carries a `fiber` field the comparison never reads; the differential is single-entry); the FileSystem slice inherits it as an obligation | the tape format; every committed tape; the truth gate's comparison | before the FileSystem slice | `harness/truth/Truth.lean` (the tape docstring); `harness/truth/run-truth.ts` (`compareSchedules`) |
| DI-24 | Does the printed face spell class declarations and requirement rows? | faces | open | two things are missing, not one: `printDecl` omits the declared type entirely when the requirement row is non-empty, so such a program prints untyped (the ruling); and the class spelling of keys is deferred to a `Codegen/Layer.lean` that has never existed (a citation repair, DI-51) | the printer, the three reader theorems, 408 printed oracles, `ts/eff/read.ts`, both engines, the truth module template | before release | `src/Effect4/Codegen/Print.lean` (the `Layer.lean` forward reference; `printDecl`) |
| DI-25 | Do `Kind`'s constructors grow to the CAS's kinds, and where does the sentinel live? | wire | ruled in an untracked note | kinds 16–23 by the CAS lane before the ledger; the sentinel moves to 127, reserved forever. Confirmed by reading both tables: Lean 1–15 with `byte_le ≤ 15`, OCaml 1–23 with the sentinel already at 127 in its tests; nine Lean sentinel sites | `Kind.byte_le` and its axiom print; nine sentinel sites in six files; the CAS goldens | before the ledger | `src/Effect4/Store/Kind.lean`; `ocaml/engine/cas/e4_kind.ml` |
| DI-26 | Does the failure branch of an external answer carry a theorem? The three admission laws are success-only and `errAdmits` is cited by no theorem. | laws | open | state `external_error_typed` beside the three success-only laws; once `Val.hasTy` has the `.causeOf` arm (DI-09's atoms commit) `errAdmits` and `hasTy` are one fold read two ways and the theorem is a definitional unfolding. The row's real content: `errAdmits` is not inverse to `errOf` (probed: `Effect.fail("x")` types `.string` and runs as `Err.boom`), which `Ty.tagged` (DI-15) reconciles on the shapes that matter | `errAdmits`'s shape; the adequacy route's typing half; every tape whose row fails | with DI-09 | `src/Effect4/Laws/Program/Admit.lean`; `src/Effect4/Program/Compile.lean` (`errAdmits`, `errOf`) |
| DI-27 | Does the Surface emitter route get a driver and a gate, or is the `Api` codegen export cut and Tier B archived? | architecture/generated | ruled in an untracked note, unexecuted | keep nine rules and commission them; cut the `Api` export. Note: the type-checker ruling's `ts/eff` half landed (the ingest gate runs it) while its truth half did not (DI-49) — that gap is independent of the Surface ruling | 16,260 lines; 26 frozen batteries; three stale contracts; the public `Api` surface | before the MCP and HTTP-API lane | `src/Effect4/Api.lean` (the codegen export); `Test/fixtures/trust-gate/known-red.txt` |
| DI-28 | Is effect polymorphism off the table? | type | refused in an untracked note only | yes — put it in DESIGN-BASIS's exclusion list on the same ground as DI-20 (`Eff` has no binder to quantify over); the cost of recording it is one line | `typeOf`, the row discipline, every `Ty` and `Requirement` law | now | DB exclusion list |
| DI-29 | Does the package row type oracle bind the raw package member or the prelude shim? | tables | recommended | a generated `packages.types.gen.ts` of *mutual*-assignability assertions (one-way would not have caught the open row) under the typecheck the ingest gate already runs | eight declared row types are unchecked against rc.112 today; `LawfulTable` says nothing about them | third package | `src/Effect4/Codegen/Read.lean` (`LawfulTable`); `tools/Tools/TsGen.lean` |
| DI-30 | What may enter the `Classical.choice` boundary, and on what grounds? | laws/process | silently assumed | the gate enforces a seven-module, thirty-six-declaration list by name; no rule says what may join it, and the file it cites as the record is absent | the axiom ceiling every receipt quotes | before release | `Test/Audit/AxiomGate.lean` |
| DI-31 | Does `Ann` stay `Unit`, and does `Defect` stay closed with no string payload? | value/errors | silently assumed | `Defect.tagged` is the small byte-stable fix (E4-HOST-CE-003); `Defect.host (text)` is larger and changes what the truth harness proves — rule them separately. Consequence for DI-09's sugar: an `Effect.orDie` printed over `catch` dies `badName` exactly as `LayerTerm.orDie` does today, so if `Effect.orDie` is to be a printed head, `Defect.tagged` must be ruled with it or the head prints a lie | the alphabet's images, `defectJson`, `reasonCode`, `orDieCause`; for `host`, tape replay and exit comparison | with DI-09 | `src/Effect4/Machine/Stores.lean`; `Test/Counterexamples/REGISTER.md` (E4-HOST-CE-003) |
| DI-32 | Is a stamp match evidence of freshness, and may a second stamp protocol exist? | generated/process | recommended | answered in the build-systems vocabulary (Mokhov, Mitchell, Peyton Jones, *Build Systems à la Carte*): a stamp is a **verifying trace** (fresh inputs), a byte comparison is **constructive** (the committed bytes are what the producer emits). Label every family in `docs/GENERATED.md` with one word; that exposes the 119 CAS goldens and the 4 LCNF outputs as verifying-only (DI-45) | what the gate means for the CAS goldens, LCNF and the ingest family | before release | `docs/GENERATED.md`; `scripts/lib/check_generated.py` |
| DI-33 | Do the hand-carried generated families ever join `generate.sh`? | generated | silently assumed | today "one command" is five of ten families, and the ingest README has no producer at all (a `Packages`, `Forms` or `Taxonomy` change silently reds three gates until it is re-rendered by hand); cheapest is a named order in the map or an `--all` | any table change; the sweep's three gates | before the map is cited as one command | `docs/GENERATED.md`; `scripts/lib/generate.py`; `ts/eff/ingest/render-readme.ts` |
| DI-34 | Are the corpus pins and the wild census inside the repository and inside a gate? | faces/process | recommended | the cheapest sufficient act: vendor the two JSON files the census pins (the corpus manifest and the project labels) into the census directory, leave the 34 checkouts external and let the census refuse when absent, and make the hard-coded corpus path a required argument | every published census and fidelity number is unreproducible if the external corpus moves | before release | `ts/eff/ingest/census/pins.ts`; `ts/eff/ingest/cli.ts`; `scripts/check-ingest.sh` |
| DI-35 | Does `eq` become comprehensive and derivable, and where does equality belong? rc.112's `Equal.equals` is structural at this pin; `catchTag` uses `===` on one string. | type/faces | recommended (2026-09-09) | widen `eq` to `.string` only, landing with DI-09; keep the printed `eq` as `===`; write the refusal set into DB-15 (widen one `Ty` at a time, only where `===` compares faithfully; past that the printed `eq` becomes `Equal.equals`, its own slice); file `Val.eqAt : Ty → Val → Val → Bool` as the destination beside `Val.hasTy`; no `Equal` class in the `Effects` package (its claim boundary retires decidable equality). Carry **`or`/`and` at `.bool`** with it: the term language has `not` and no other connective, so `catchTags`' exact test and any predicate for `catchIf` are unspellable without them | the atom families (`polyArms`, not `monoAtoms` — a duplicate `"eq"` breaks the OCaml atom count), the prelude and its self-test, a truth fixture per admitted type | with DI-09 | `src/Effect4/Program/Native.lean` (`eq`, `not`); `src/Effect4/Program/Typed.lean`; `src/OCaml5/Eff/Emit.lean`; the equality note; the elimination design note |
| DI-36 | Where do the canonical package tables live? Under `Effect4.Api` (ruling A2) a data edit invalidates 48 modules across four build roots, including 14 proof modules and the audit, with zero content consumers under `src/`. | architecture/tables | recommended (2026-09-09) | an `Effect4Packages` library root imported by tools, tests and the truth driver only; `Api` already takes a table parameter | `lakefile.toml` and the axiom gate's root list (coordinator-owned); every importer of `Effect4.Program.Packages` | third package | `src/Effect4/Api.lean` (the import); `src/Effect4/Program/Packages.lean` |
| DI-37 | The printed-image versus strict-foreign contracts of the ingest (ordinals per unit from 4 not 10; `true` into a number key refused). | faces | ruled 2026-09-08 in an untracked note only; ready to write down | the two contracts differ on three axes — the admitted language, the refusal discipline, the service-key numbering — of which only the third is written anywhere; state the three in the architecture document's faces section and land a cross-contract test (the printed image as a sub-language of the foreign one, checked over the corpus that exists) in the same commit | both engines' contracts; the corpus gate's oracles; the census | now — one paragraph in a tracked place | `ts/eff/ingest/README.md` (generated); `ts/eff/ingest/ck.ts`; `ts/eff/ingest/check-corpus.ts` |
| DI-38 | Does `EffTy.joinAnswer` become a least upper bound under `Ty.sub`? Today the two arms of `catchCause`/`matchCause`/`branch`/`gen`/`choose` must have equal answer types or one `never`; rc.112 gives `A \| B`. The asymmetry — two of the three columns are semilattices, the answer column is discrete — is the row, not `catchCause` in particular. | type | open, sequenced | incoherent without `Ty.sub`: land DI-53 and `Ty.sub` first, then state the rule as a least-upper-bound premise with `Ty.join` as its algorithmic witness, at which point `joinAnswer` disappears as a concept and the three columns differ only by polarity; six rules change. The measurement is one guard re-run (the fuzz corpus's well-typed count) and moves no `.ty` golden; it closes counterexample `E4-SURFACE-CE-096` | `EffTy.joinAnswer` and every typing rule that joins answers; the fuzz corpus's well-typed count; the OCaml typing mirror | with DI-15, after DI-53 and `Ty.sub` | `src/Effect4/Program/Typing.lean` (`joinAnswer`); `Test/Counterexamples/REGISTER.md` (E4-SURFACE-CE-096); `Test/Program/Gen.lean` |
| DI-39 | Do the derived error heads become `Codegen.Forms` rows or ingest-spec engine rules? A forms row gives the Lean reader the expansion for free but moves the corpus pin (the precedent that sent the `sql` fold to an engine rule); an engine rule moves nothing but duplicates the expansion in two TypeScript engines. | faces/generated | open | forms rows for the six heads that expand (`catchTag`, `catchTags`, `catchIf`, `mapError`, `orElseSucceed`, `match`) and `Effect.catch` as a head; both engines need the pipe form anyway, and `Forms` already lists these heads as arity metadata. Either choice must fix the engines' pipe segment first: a piped `catchTag` is unknown to the head set (it is in the forms' duals, not its rows) and exits `E-OP-UNKNOWN` before the handler list is consulted. This is also the choice of who owns the classification order (DI-48). A `Forms` change silently reds three gates until the ingest README is re-rendered by hand (DI-33) | the corpus pin; `forms.gen.ts`; both engines' `E-HANDLER` lists and `pipeSegment`; the ingest fixtures and ledger | with DI-09's sugar commit | `src/Effect4/Codegen/Forms.lean`; `ts/eff/ingest/check-corpus.ts` (the pin); `ts/eff/ingest/ck.ts`, `oxc.ts` |
| DI-40 | Are the hand-written copies of the atom table gated? The OCaml copy is gated by the generator's abort; both foreign engines hold a literal copy of the atom set and of the handler list with no gate, and the truth prelude is a third hand copy, so an atom added in Lean is silently unknown to them. The types scout counts fourteen copies, **three already stale**: the ingest fidelity round-trip, the fuzz generator's arity table and the OCaml GADT surface all miss `strings`, added 2026-09-08. | generated/faces | recommended, ready to rule — present drift, not potential | one `TsGen` emission of the atom set into `profile.gen.ts` beside `heads`, which the TypeScript gate's stamp already covers; both engines and the prelude's self-test read it; land with DI-09's atoms commit, which adds six names | every atom addition; the ingest verdicts on any program using a new atom | with DI-09 | `ts/eff/ingest/ck.ts`, `ts/eff/ingest/oxc.ts` (the atom sets and handler lists); `harness/truth/prelude.ts`; `src/OCaml5/Eff/Emit.lean` (the OCaml abort); `tools/Tools/TsGen.lean` |
| DI-41 | Do `Ty`, `Row`, `EffTy`, `RowKind`, `RowShape` and `Registration` get Lean `Canonical` instances? Two hosts write bytes for them and Lean has no encoder, no round-trip theorem and no golden; they are exactly the families the OCaml generator's coverage abort skips, checked only by OCaml self-round-trip at one seed. | wire/generated | open | yes — add the six to the generator manifest's Program group before Phase 1; acceptance is that the new Lean bytes agree with the existing OCaml and TypeScript writers (both use declaration order) and the coverage skip list shrinks to the three `Service*` types | the generator manifest; the Derived Lean file; the generated-file count; the coverage skip list; six new golden vectors — an engine regenerated at 20 encoders is regenerated again at 26 | before Phase 1 | `src/OCaml5/Tools/EffGen.lean` (the skip list); `ocaml/eff/eff_wire.ml`; `ts/eff/wire.gen.ts`; `tools/Effect4Gen/manifest.json` |
| DI-42 | Is the hand value tree that cuts the 42 `.bin`/`.json` goldens checked against `Canonical`? A second value tree with its own framing and a complete hand `toVal` cuts the goldens and is the JSON oracle for both faces; nothing in Lean compares it with the generated instances. | generated/wire | open | one cross-check loop in the wire tool (which already imports both encoders), throwing on disagreement over the goldens corpus; or retire the hand tree once `World` exists | nothing if it passes; if it fails, the 42 `.bin` and every `.json` oracle | with DI-41 | `src/OCaml5/Eff/Goldens.lean`; `tools/Tools/Corpus.lean`; `src/OCaml5/Tools/EffWire.lean` |
| DI-43 | Does `Store.Tag` get a census and an injectivity theorem, as `Kind` and `HandleKind` have? It is twelve bare definitions pinned by literal-byte guards, and it is the alphabet every other one is framed inside; the wire tool's hand tag list should read it. | wire | open | yes — `Tag.all`, `ofByte?`, `byte_injective`, about thirty lines; no byte moves | nothing | before the ledger | `src/Effect4/Store/Val.lean` (the tags); `src/Effect4/Store/Kind.lean` (the model); `src/OCaml5/Tools/EffWire.lean` (the hand list) |
| DI-44 | Is the generated-file map checked in the tree-to-map direction? Every row's stamp is verified and nothing refuses a generated artefact with no row, though the map states that rule. | generated/process | open | about fifteen lines: walk tracked files for a `cut-from:` line or a `.cut-from` sidecar and refuse one with no row | nothing; one gate change | before release | `scripts/lib/check_generated.py`; `docs/GENERATED.md` |
| DI-45 | Do the 119 CAS goldens get a constructive check, or is "verifying trace only" recorded as their evidence class? The producer runs under `generate.sh`, but the drift set excludes the family and the comparing gate is declared red. | generated | open | add the family to the drift set (two lines and one count change, 25 → 144) — else record the class in the map (DI-32) | the drift-count pin and the gate's runtime | with DI-32 | `scripts/lib/check_generated.py`; `docs/GENERATED.md` |
| DI-46 | Do `Store.Root`, `Store.Store`, `Machine.MemoEntry` and `MemoMap` get `Canonical` instances, retiring the six tool-side shapes the CAS goldens invent for them? | wire/CAS | open | yes, with the CAS lane's X2; the tool then cuts Lean bytes for every case | the CAS goldens tool's projections; the affected goldens; the engine's readers | with CAS commit 7 | `src/OCaml5/Tools/CasGoldens.lean` |
| DI-47 | Is there a compatibility relation `W ⊑ W'` on the world description, with append-only as a theorem rather than a review rule? | wire/laws | open | define the relation over the first-order description and prove the forward and backward compatibility theorems (two structural inductions); then DI-02's ledger is a guard that the committed world precedes the reflected one, and DI-03's "where ordinals live" is answered by the datum | nothing existing; it replaces the ledger design of DI-02/DI-03 | with Phase 3 | the one-generator plan v2 note (step 3.3); `ocaml/engine/e4_program.ml` (the shape, in OCaml) |
| DI-48 | Is a refusal taxonomy's classification a function of the input or of the engines' rule order? A verdict carries the first code met in rule order, so `E-HANDLER` is unreachable behind `E-PARAM-SHAPE`, `E-IMPORT-OPAQUE` and the pipe branch: the census reports two units against thousands of `catchTag` occurrences. | faces/process | open | publish the classification order as Lean data beside the codes and have the census report the firing rule, not only the code; until then no census number about a refused family means anything, and DI-09's quoted benefit is unmeasured | every published census number and every cost estimate that quotes one (DI-09, DI-21, DI-39); the ingest spec's plan to rank handler rows by census | before DI-09's benefit is quoted | `src/Effect4/Ingest/Taxonomy.lean`; `ts/eff/ingest/ck.ts` (the pipe branch, the handler list) |
| DI-49 | Is the printer's own output type-checked anywhere? It is executed on 24 programs and parsed on 400; the TypeScript compiler never sees it, so the basis's type-checker obligation is unmet for the program route. | faces/process | recommended | a truth-harness `tsconfig.json` and one `tsc --noEmit` in the truth check, the same over the corpus temp directory in the printed-corpus gate; independent of the Surface ruling (DI-27) | whatever the first run finds in the printed image; nothing else | now | `docs/DESIGN-BASIS.md` (DB-09); `scripts/check-truth.py`; `scripts/check-ts-eff-corpus.sh` |
| DI-50 | Does the faces layer get a contract packet? The printer, both readers, the ingest, the truth harness and `ocaml/eff` have no packet among the 41 in `Test/contracts/`; their specification exists only in module headers and untracked notes. | architecture/process | open | one packet naming the nine faces, the evidence class of each, the two round-trip laws and their side conditions, the two ingest contracts and the inclusion property, the truth claim with its quantifiers, and the `ocaml/eff` conformance relation; no new batteries — it cites the ones that exist | nothing; it is the artefact whose absence makes the layer ungraspable | before release | `Test/contracts/README.md` |
| DI-51 | Do bare-filename and prefix-less citations evade the source-citations gate? Two live dangling citations do: the truth harness's `NOTES.md` (four sites) and the printer's `Codegen/Layer.lean` (DI-24). | process | recommended | write both with repository-relative paths in the next commit that touches those files, so the existing gate refuses until they resolve; extending the token pattern is optional | four comment lines; whichever of the two files is then owed | now | `scripts/check-source-citations.py`; `harness/truth/prelude.ts`; `harness/truth/run-truth.ts`; `src/Effect4/Codegen/Print.lean` |
| DI-52 | Is the OCaml wire differential's `same_program` list maintained, and is it maximal? Four names are held to byte equality against the eight wire goldens; the other four get only "a byte difference is a program difference". | OCaml/generated | open | derive the list from the intersection of the two tools' corpora and emit it as data the test reads | the differential's strength on four programs | with DI-19 | `ocaml/eff/test/test_lean_wire.ml`; `src/OCaml5/Tools/EffWire.lean`; `src/OCaml5/Tools/EffGen.lean` |
| DI-53 | Is `Ty` equality the target's type equality? `Ty.join` canonicalises only the top level: `.option (union nat string)` and `.option (union string nat)` are distinct values denoting one TypeScript type, and `join x x ≠ x` for a non-canonical `x` (probed: idempotence and the unit fail; commutativity and associativity hold on a sample). | type | open | normalise deeply — `join` recurses through `option`/`list`/`prod`/`except`/`exitOf`/`causeOf`/`fiberOf`, `Canonical` becomes a structural predicate, and the four owed `Ty.join` contract rows become unconditional — landed before `Ty.sub`, because a least upper bound over a non-normal order is not least; the alternative is comparing by `sub ∧ sub` at every comparison site (Dolan's syntactic/semantic split). Measured: none of the eight package rows is non-canonical today, so no row moves | `Ty.join`; any non-canonical `Ty` literal in a row or fixture; the OCaml hand `insert_member`/`join`; no wire bytes (`Ty` is off the program wire) | with DI-15, before `Ty.sub` | `src/Effect4/Program/Eff.lean` (`members`, `insertMember`, `join`); `Test/contracts/surface-handler-fits.contract.md`; `ocaml/eff/eff_typing.ml` |
| DI-54 | Should `effTy` read `Signature.dom`? It is declared and instantiated but never consulted by the typing, so a `perform` at an external index outside the supplied table types through the placeholder and is well typed in Lean, while the OCaml checker refuses it categorically, against its own stated behaviour. The two typing faces disagree on a program Lean accepts; the 42 `.ty` goldens miss it (probed at the empty table). | type/faces | open — a live disagreement | read `dom` in `effTy`'s `perform` and `callback` arms, as the Lean reader already does; strictly narrowing; add the probe program as a `.ty` golden in the same commit so the corpus covers the class | `effTy`'s two arms; no golden expected to move; nothing generated | now — one line | `src/Effect4/Program/Typing.lean` (`Signature.dom`, the `perform`/`callback` arms); `src/Effect4/Program/Native.lean`; `ocaml/eff/eff_typing.ml`; `src/Effect4/Codegen/Read.lean` |
| DI-55 | Does the printed image preserve literal types? A tag-set error column (DI-15) renders `readonly ["A" \| "B", string]`, but the printed expression is `pair("A", m)` and TypeScript widens a string-literal argument to `string` unless the parameter is constrained, so the row's declared type and its printed image would disagree, and DI-29's oracle would red on exactly the rows DI-15 improves. | type/faces | open (reasoned from TypeScript's widening rules; unverified — no `tsc` run) | constrain the prelude's `pair` (a literal-preserving signature) rather than weaken the declared type, in the same commit as DI-15; the general fix is DI-49 | the prelude's `pair`; the type-oracle file when DI-29 lands; every truth fixture printing a tagged error; possibly the printer's `pair` emission | with DI-15 | `harness/truth/prelude.ts`; `src/Effect4/Codegen/Print.lean`; DI-29; DI-49 |

## Tracked records the sweep found already wrong (to correct)

- `AGENTS.md` said the basis runs DB-01…DB-10 (corrected 2026-09-09 with this register's row).
- `src/Effect4/Machine/Fibers.lean` says "the dispatcher table retires it" against DB-13's
  "there is no dispatcher table".
- `Test/contracts/machine-completion.contract.md` asks for a ruling that
  `Test/contracts/machine-handles.contract.md` already made.
- `Test/Counterexamples/REGISTER.md` (the row before E4-HOST) says a repair is open that is
  installed.
- DESIGN-BASIS's "Required proof graph" lists ten of eleven edges `Pending` while the coverage
  document quotes 133 of 135.
- Six tracked citations point at research notes that no longer exist on this machine,
  including the sole citation for DB-01's ruling and the axiom gate's boundary record.
- Three tracked files say `src/Effect4/Program/Eff.lean` "has exactly one theorem,
  `render_ofSpelling`"; it has none, and that name exists nowhere (the handler-fits contract,
  its counterexample file and its battery).
- `Test/Surface/HandlerFitsContract.lean` pins the four `Ty.join` laws by guard, but the file
  is declared red because it imports a `Surface.Handler` module that does not exist; the
  guards are frozen text, not a running check.
- `ocaml/eff/README.md` quotes line counts and a golden count ("39 programs") that are stale;
  `ocaml/engine/e4_program.mli` says the wire has 24 `Eff` arms where it has 27.
- `Test/Program/Gen.lean` says its alphabet is `nativeAtom`; it is ten of the eleven atoms.
- `harness/truth/prelude.ts` cites a `NOTES.md §4` that does not exist (DI-51).

## Companion notes

Untracked working notes under `docs/research/` (gitignored; they may not survive a clone,
which is why rulings move out of them): the design-risk sweep, the error-paths scout notes
(friction, types, map, elimination, equality), the host-rows decisions memo (rulings A–G), the
CAS amendments (M1–M17), the one-generator plan v2, the ecosystem audit, the effects papers
review, the adequacy route, the architecture and codegen reviews, all dated 2026-09-02 to
2026-09-09.
