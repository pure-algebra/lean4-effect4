# State of the work

One page: what is true at HEAD, the ratified plan, what is next, what the owner must decide. Replaced at every landing. The review log and `COORDINATION.md` are history; this file is the entry point.

## What this is

An agent-first language of algebraic effects whose engine is reified in Lean. One machine, three faces: Lean proves it (the reference, the machine, the certificates), OCaml runs it natively (the same machine compiled through LCNF, the reified Effect runtime as a fast, low-level platform outside any JavaScript host), TypeScript interoperates with the Effect ecosystem (the printer and readers). Programs are data: a canonical `Eff` tree with a digest, a computed typing certificate, folds, a tape-driven run with replay, and a printed image that reads back. Any Effect code the profile covers reads into `Eff`; coverage grows by data (generated rows, proved forms), and an unregistered head refuses by name.

## True at the DI-86 landing (2026-09-16, night)

- Branch `refactor/phase1-phase3`. `make check` green: 320 modules at `[propext, Quot.sound]`; the `Classical.choice` boundary is 3 modules (the third is the authoring scope tactic, meta code only). The reader lane matches Lean's oracle on all 416 programs. The host lanes (`make check-host`) were last run by Codex before `1a97c37`.
- Landed today: the typed surface (one typing certificate `TypedProgram`, certified emission `ModuleEmission`, certified reading `ModuleReading` and `Api.admitModule`, hoisting and reconstruction proofs, typed derived forms, the lexical certificate, one owner each for binding names and the declaration annotation); seat 2's deletion of the profile module (net −246 lines, seven axiom exemptions gone); the authoring surface (`0cb8b6a9`, step 0(a) with it); step 0(b), `Plain` deleted, the agreement reads `Straight` (`6b770453`, 55 declarations fewer); step 0(c), `Eff.suspendDecided` as the one classifier of the suspension boundary with `Straight.suspendDecided_iff` where the fragment meets it (`ff0c042b`).
- The authoring surface is generated code over one table: `tools/Effect4Gen/binders.json` (the binding signature: which argument of which constructor abstracts which binders), the emitter `tools/Effect4Gen/Authoring.lean`, and its two outputs `src/Effect4/Program/Binders.lean` (`Node.binders`, `closedChild`, `childLevel`; the hoisting proofs now read it) and `src/Effect4/Program/Authoring/Lifts.lean` (48 lifts of the constructors an author may write, on the scope-reader carrier of `src/Effect4/Program/Authoring.lean`). `Authoring/Sugar.lean` holds `bindWith`, `flatMap`, `andThen`, `map`, `ifElse` and the Effect-spelled native rows. `Test/Program/AuthoringContract.lean` pins named programs against level trees, typing, runs, the printed image, refusals at paths, and the dogfood's shared-layer modules. `elaborate`, `bind`, `var` use no axioms; `elaborateModule` uses `propext`.
- Scope safety is by construction (`75a6c2b4`): `Eff.scopedAt` is the binder table as an algebra on `Nat → Bool` run by `cata_eff` (`Program/Scoped.lean`, generated), every lift preserves it (48 generated lemmas, `Laws/Program/Authoring/Lifts.lean`), the sugar carries the same lemmas, `authoring_scoped` discharges it by the goal's head, `elaborate_scoped` closes any elaborated program at level 0, and `Node.scopedAt_child` proves the table's two projections agree.
- The universal-algebra refactor (`docs/UNIVERSAL-ALGEBRA-REFACTOR.md`, evaluated and landed with corrections): `EffAlgebra.id` and its siblings with `cata_id_*`, `EffAlgebra.onRef`, `frontierMap` (the frontier family stays: closed layers are constants of it), `foldMapAt_*` (the path fold); `Node` in `Program/Node.lean` with `Node.child`/`setChild` generated from the declarations (`NodeLenses.lean`); `expandRound` is one override of the identity fold; `refSites` and `layerPaths` are two yields of the path fold; `weaken` is generated from the rows with its fold theorem. Rejected from the document: `cata_ctx` and `cata_eff_congr` as written (ill-typed; an inherited attribute is an exponential carrier, which `scopedAt` demonstrates), and retiring the frontier family.
- The native rows are authoring wrappers generated from the row table (`f2d3299a`, `tools/Effect4Gen/Rows.lean`; the hand-written `Ref.*`/`Deferred.*` spellings and their fifteen lemmas are gone). The nineteen derived forms are authoring combinators generated from `Codegen.Forms.all` (`tools/Effect4Gen/Forms.lean`, `Authoring/Forms.lean`): each template read on the scope reader, a binder an argument can see becoming a name parameter, and a guard per form that its elaboration on the table's example arguments is exactly the printer's `Template.expand`, so the reading and the expansion cannot drift; their scope lemmas are `unfold` then `authoring_scoped`. `Sugar.lean` is down to five conveniences. The test clock is the decision tape (`Api/TestClock.lean`): `adjust`, `tape`, `run`, `sleepDeadlines` and `synthesize` by the monoid fold, and the warps `fastForward` and `dilateTime` as one override each of `EffAlgebra.id`; `Test/Api/TestClockContract.lean` pins that a raw run parks, the synthesized tape finishes, one large adjustment stages each sleeper at its deadline, and sleep-free programs are fixed points. The runtime battery is seconds (`50f5ca4f`); `LayerSharingContract` (101s) certifies 516 per-tick snapshots by a metaprogram, and a single 216-tick `reduce` exhausts memory, so it stays as it is until chunked snapshots are tried; `Guard/FrameOwned` (84s) and `Guard/Core` (67s) are proof modules.
- The checker's refusal is located and named (DI-86, `353b7960`): `Program/Typing/Blame.lean` has `TypeReason` (one constructor per rule the checker can refuse by), `TypeRefusal` (path, reason) and `explain`/`blame` as a projection over the one checker, with the law `explain = none ↔ effTy = some` at every sort and environment (`Laws/Program/Typing/Blame.lean`, uniform per-arm proofs at `[propext, Quot.sound]`); `Api.explain`/`Api.blame` resolve layer references as `typeOf` does, with `Api.explain_none_iff`; `Test/Program/BlameContract.lean` pins the paths. The native rows author in the reader's image: the generated wrapper of an `.async` row is a `callback`, so `Effect.sleep` elaborates to what `Api.read` reads and `readable` holds (`callback` left the reader-only profile; `Rows.lean` regenerated). The test clock gained `runSequential` (the synthesized tape) and `runDilated k` (the dilated program under its own tape). The facade's certificate-first half (DI-85): `Api.check` answers the certificate or the located refusal and is total by `Api.explain_none_iff` (the DI-86 laws now live beside their definitions, since the application root never reaches `Effect4.Laws`); `Typed` carries a program with its certificate into `run`, `replay`, `runSync`, `print`, `emit` and `bytes` under a `Budget` with the truth corpus's defaults; `Api.author`/`authorModule` take a named source to a `Typed` in one call, refusing at a name or at a type (`AuthorRefusal`).
- Of the language's four halves, the engine, the interop boundary and program-as-data exist; the agent-facing authoring surface has its first commit and the rest of it is generated next. `Straight` is the sequential sub-language (one fiber, no park, no fork, no tape) and bounds one theorem family, the compositional meaning; every program has the reference as its meaning through `run_eq_ref`.

## The ratified plan (owner, 2026-09-16 evening; rulings DI-79 to DI-90 in `docs/DESIGN-ISSUES.md`)

**M1, the solid core.** Authoring first, then the alphabet:
1. The authoring elaborator (DI-83): a named source tree `Src`, `elaborate` by nearest-binder resolution, layers by name through `restoreAll`, located refusals; the authoring profile of the alphabet as data (DI-84); the checker's refusal projection `blame`/`explain` (DI-86); the additive facade (DI-85: `Api.check`, `Typed.*`, budgets with defaults, `Api.digest`, `Api.author`). Packet: `docs/research/2026-09-16-authoring-ready-packet.md`.
2. The three step-0 refactors of the constructs packet: promote `childLevel` to `Node.binders`, delete `Plain`, `suspendDecided`. **Done** (`0cb8b6a9`, `6b770453`, `ff0c042b`).
3. S1 stable wire tags (constructor tags are declaration positions today, `Program/Wire.lean:16`).
4. The `Eff` series (DI-79): `select`, `iterate`, the `Decision` family; retire `branch`, `whileLoop`, `callback`, `yieldError`; `catchIf` stays (DI-82). Every step regenerates the OCaml engine and runs `check-ocaml`. Packet: `docs/research/2026-09-16-select-and-iterate-ready-packet.md`. One baseline promotion at the end.

**M2, the semantic repairs.** The `Ty` series (`scopeExit`, `fiberId`; DI-81, DI-87), `refSet` answers unit (DI-80), unit spelling and the type-oracle gap (DI-87), full-key service identity, the one keyed route with the facade deletions and the session generated into the OCaml engine (S6), layers (S5), B18's strings off the wire, package rows generated from the pinned package's declarations (DI-89), then the ingest packet: style inverse as data, the canonical reader generated from `Read.lean`, the two engines cut to normalizers, the ingest census back on (DI-88).

**M3, the proofs and the rest.** Straight and loop safety (S8a, S8a-L), observation and clocks (S7), the combinators and compact iteration (S9b, S9c) with the higher-order forms by named list (DI-89), reference typing (S8b), the `Effects` package in-tree (DI-90). `Stream` and `Channel` are the next reification packet after M3.

**Cut from the plan by the rulings:** the §3.6 substitution machinery, the §3.1 at-most-one summary, the §3.8 annotation grammar's parse round trip, per-packet alphabet edits, seat 2's structural-type-argument step (its stash is dropped).

## Next, in order

One list. The redesign's stages are **R0 to R13** (`docs/research/2026-09-16-printer-reader-positions-design.md` §10, §13; renumbered from S so they never collide with the language plan's S0 to S9); the plan's packets keep their S names. The order is scout C's reconciliation (`docs/research/2026-09-16-scout-c-plan-reconciliation.md`): the redesign lives on the surface obligations and touches none of the semantic ones, so it runs beside M2 and M3, and the template table waits for the settled alphabet. R0 landed at `7556ddef`.

0. **The generation medium** (`docs/research/2026-09-16-generation-medium-workshop.md`; the owner's ask of the same night, workshopped first). Five words per signature (`Schema`, `Algebra`, `fold`, `Hom`, `Build`), everything else an instance; the signature persisted as a document; `algebra.gen.ts`, `build.gen.ts` and the corpus guards emitted in `wire.gen.ts`'s conventions; the `run` instance (the AST compiler that can run) joined to the truth harness's tape lane; the census tool classifying every definition D, C, P, M, G, X; the equation-lemma emitter for C and P (`Straight`, `effTy`, `explain` first); then the same emitters for `InspectOp` (the MCP server as generated Effect TypeScript on bun), `Tree`, `Doc`, and the entire rc.112 surface ingested once as entries, each a type with a schema input and a schema output (the owner's standing requirement; R8 pulled forward), rows and forms derived by a fold, `Ty.app` for type-constructor application, no semantics in Lean (rows render as TypeScript and the tape answers), the two hand-written packages regenerated byte-identical as the guard. Its seven questions are answered as recommended (the owner, 2026-09-16 late night: "I'm going with the recommendations for the most part"), with two riders: the Effect APIs live in the generated surface as families, overloaded so that as much of Effect's semantics is representable as possible; and the schema work runs in parallel with the core line (refine the schema APIs; the schema readers: an Effect schema read as a schema, JSON read into a schema). The main line returns to item 5, the `Eff` series, "finish out the core development work: select, iterate, nail down the semantics, then the retirements and the cleanup"; the medium's steps 1 to 3 are the parallel lane.
1. **R1, the `Doc` modules, in-tree.** Drafted at `~/Dev/lean4-doc` (`08a0a52`, local): `Doc` with its algebra, fold, initiality and fusion, the product `Measure`, the house and elastic policies as algebra instances, the stream, `spans`, `locate`, the laws, guards green. Carrier ruled (scout B, the layout note §4.1): the proved `Doc`, not `Std.Format`; no package and no path require. The modules move to `src/Effect4/Doc/*` with guards under `Test/Doc/*`, the two amendments applied (the two widths named apart; `line` and `hardline` lay out alike once chosen), one thin `Doc → Format` export for display, every golden byte-identical.
2. **R2, `Tree` and the positioned render.** The syntax table imported as a document, the render algebra into `Doc Path`, the fusion lemma against `Render.expr`, the span check against `tsgo` on every corpus program. The house fragment keeps running beside it until the goldens match.
3. **R3, blame to code by span containment.** `Api.explain`'s path, R2's spans and `codesOf` joined on R0's lane: agree, contained, disagree per program. The night ask, and the missing third of the S9a agent unlock.
4. **R12's first slice: the inspection protocol over MCP** (the layout note §4.14b, the owner's steer: everything inspectable through one small API in the DevTools Protocol's shape). One `InspectOp` signature with the generic commands (`resolve`, `at`, `children`, `render`, `window`, `spans`, `locate`, `explain`, `blame`), each the generic operation of a structure (lens, fold, monoid search, certificate projection), served over MCP with object-typed success schemas and the protocol's messages as documents pinned to rc.112; `evaluate` (an `Eff` over the rows, typed, replayable) second; the `ToolSpec` table of the nineteen drivers, the typed TypeScript client and the CLI as the second emitter. Additive; touches no alphabet.
5. **The `Eff` series** (DI-79; packet `2026-09-16-select-and-iterate-ready-packet.md`). Steps 0 and 1 are done (step 1: `Decision` with `decide`/`arms`/`binds`, the tag algebra, `decide_typed`; the packet's §3b holds the concrete semantics and the free-versus-hand ledger, §3c the authoring sugar and the per-decision binder rule for both constructors). Step 2, `select` itself, is landed with the owner's split (it prints, `readable` is `false` until R5; packet §1.8 "Landed"), and the two proof modules it touched are split into `Laws/Program/Intro/*.lean` and `Laws/Program/Handles/*.lean` with the compile equations once in `Agreement.lean`. The loop interface is cut to two fields (`LoopNext`, `loopEnter`/`loopResume`; `loopNextAt` and `loopResumeAt` are the one home of a loop's raw-program rule: a test that is not a Boolean and a step that does not evaluate now finish with the wrong-shape code instead of stopping silently; the simulation relates the two decisions by one relation, `LoopNextMeans`). `iterate` is landed the same way (2026-09-17: the constructor with its cursor annotation, typing by subsumption at the annotation with `inv_iterate` and the `HasTy` rule sound and complete, the same loop frame finished by `loopFinishAt`/`loopFinishRAt` with the result's raw-program rule, `intro_iterate`, the printed `reduce` shape under the new head `Effect.map`; `readable` is `false` until R5; packet §2.2 "Landed"). The budgeted meaning is landed too (`Laws/Program/Iter.lean`: `iter`, `iter_succ`, `iter_uniform`, `iter_congr`; `Laws/Program/DenoteB.lean`: `denoteB`, `meaningB`, `denoteB_bind_none`, `denoteB_straight`, `denoteB_mono`, all at `[propext, Quot.sound]`; `Test/Program/DenoteBContract.lean` pins the budgets, the kept stores of an unfinished loop, and agreement with the machine one program at a time). Next the `whileLoop` rewrite, which waits for S1. Host lanes on 2026-09-17 after both landings: `check-truth` (36 programs agree with rc.112), `check-tsdiag`, `check-target`, `check-schema-codec`, `check-ingest-smoke` and `check-ocaml` pass. `check-corpus` reports 18 changed rows against `harness/truth/corpus-results.tsv`, every one an untyped raw program (refused by the checker, never compared with rc.112) whose loop test is not a Boolean: the recorded Lean exit moves from a silent `success` to the wrong-shape defect, which is the loop cut's stated rule; the typed rows are unchanged (121 of 127 runs agree, as before). The baseline is not promoted: it moves once, at the series' end (item 4). `check-citations` fails on an untracked owner doc (`docs/CAS-IFIED-APIS-AND-SCHEMAS.md:335` cites `src/Effect4/Codegen/Casify.lean`, which does not exist). A new constructor costs one row in `binders.json` plus the hand-written owners (`effTy`, `compileEff`, `denote`, `print`, `read`, `Straight`, and on the OCaml face the translation arm in `e4_program.ml` until the program type is declared once; the subterm table and the carrier rows are generated since 2026-09-17, `docs/research/2026-09-16-ocaml-engine-base-abstractions.md`); `print` and `read` leave that list at R4 and R5. Every step regenerates the OCaml face (`make gen-lcnf`, then `make check-ocaml`; the `lcnf` group is not hermetic, make it so).
6. **S1, stable wire tags. Landed 2026-09-17** (packet `docs/research/2026-09-17-s1-stable-wire-tags-ready-packet.md`): one assignment `tools/Effect4Gen/wire-tags.json` (active and retired tags per family; an unlisted family keeps its declaration positions; the program world must be listed), read through `tools/Tools/WireTags.lean` by the Lean codec generator and its guard, the OCaml codec, the golden bytes, the wire manifest and the TypeScript writer; `Shape.sum` carries a tag per case (`caseAt`, `ShapeDoc.wellTagged`); compiled positions stay with the layout checks. `make check-compat` is green again (it had failed since `75d75fdb`): comparison by tag and name under the named policy `Test/fixtures/baseline/66ee4657-supplement-v1.policy.json`, which carries the `choose` history as the one exemption; retained byte vectors held to their digests; promotion from a named committed revision only. No golden byte changed. To add a constructor give it the next unused tag of its family; to retire one move its row to `retired`. **`branch` is retired (2026-09-17)** into `select … .bool` (the select packet §1.9 "Landed"): the rewrite's typing equation over whole programs was checked first (`438b93f2`), then the constructor, its arms and its ten lemma families went (209 declarations fewer); tag 15 is retired and old `branch` bytes refuse; both readers read the conditional as `select` under `.bool`; the corpus images, the seed stream and the corpus index did not move; this is the first wire tag that differs from a compiled position, which found and fixed one conflation in the OCaml byte walker (`e4_subterm.ml`). **`whileLoop` is not retired**: its typing lemma is proved (`effTy_iterate_of_whileLoop`), and its deletion waits for the decision below. Next: that decision, then `callback` and `yieldError`, then B18's deletion list. Known red, older than S1: `make check-tools` stops at `scripts/test-trust-gate.sh` on the fixture `Test/fixtures/trust-gate/expr-equality.lean.txt` (a `String` where the pinned `typescript` package wants a `TypeRef`).
7. **R4 and R5, the template table and the generic reader**, against the settled alphabet. `readable` keeps its name and its domain, guarded by a Boolean equality against today's definition; `Read.lean` comes down to `readable` and the table; `Forms.all` becomes rows of the same table and its nineteen guards become generated `rfl`s; `readable → scopedAt` is proved here and `reconstructible` is never written.
8. **R6, the host reader from the exported table**, replacing `ts/eff/read.ts` and DI-88's LCNF-to-TypeScript backend clause; **R13, the portable face** (the two type-generator rules and the builtin rows so the whole face compiles through LCNF, then the js_of_ocaml bundle).
9. **M2, the semantic repairs**, with the schema and interop half first: DI-89's package rows through **R8** (the Effect module surface read from the pinned package by the `tsgo` checker) and **R7** (the schema printer on the same mechanism), then the `Ty` series, `refSet` answers unit, unit spelling and the type-oracle gap, full-key service identity, the one keyed route with the facade deletions and the session generated into the OCaml engine (S6), layers (S5), B18's strings off the wire, then the ingest packet.
10. **M3, the proofs and the rest**: D5's and D6's statements first, then S8a and S8a-L, S7's second half, S9b and S9c, S8b, and the `Effects` dependency retired as scout C restates DI-90.
11. **R9 and R10** as the remaining agent faces: the tooling protocol's drivers as surface entries, the containment tree over the same `Doc`.

Deferred with a reason: R11 (diff and incremental) until the authoring surface has an edit operation; the two-dimensional layout algebras until R1 has its second consumer; the `eff { ... }` macro until `select` exists.

### Backlog (owner, 2026-09-17)

- **The generated session as the engine's host API.** Put `Effect4.Api.HostSession` (`start`, `submit`, `advance`, `retire`, `inspect`, with refusal as a verdict that leaves the state unchanged) among the LCNF roots, so the OCaml engine's state transitions are generated and its hand driver shrinks to a session table and the I/O that answers parks. Then a thin MCP driver over that session (R12's protocol: start, step, reply, inspect, trace, blame), every response first-order data. This is S6 (M2) meeting R12; it deletes the legacy unkeyed runner.
- **One LCNF codegen API, several targets.** The owner's direction: design the API before patching the OCaml mirrors further, so OCaml, TypeScript and WASM come off one pipeline. The plan is `docs/research/2026-09-17-player-schema-codegen-plan.md`: an instance is a player with a canonical-bytes boundary described by schemas; OCaml's schema AST is Lean's `Val`/`Shape`/`ShapeDoc` compiled through LCNF, not written; a target is a printer plus a profile over the neutral core `Target.Expr`; the store API is deferred with its questions listed; no sync. Its step 1 (load programs with Lean's own `Api.ofBytes` as an engine root; probe run, three primitive rows missing) is what removes `e4_program.ml`. The two engine fixes already landed (generated subterm table, inferred carrier rows) stay; the hand translation `e4_program.ml` is the remaining drift source and goes when the program alphabet is declared once by that API (`docs/research/2026-09-16-ocaml-engine-base-abstractions.md`, item A).

## Owner decisions open

**Retiring `whileLoop` before `iterate` can be read (raised 2026-09-17 by the implementation seat).** `iterate` prints with a cursor annotation and neither reader reads it (`readable` is false until R5). B19 rules out a `TypeRef → Ty` inverse, and the reader has no typing environment from which to recompute the annotation, so the hand reader cannot be taught `iterate` the way it was taught `select … .bool`. Deleting `whileLoop` now makes every loop unreadable until R5: `Api.read`, `admitModule` and both readers refuse them, and 45 of the 410 corpus programs (28 of the 284 readable ones) leave the round trip lane. Three ways: (a) delete now and accept no loop reading until R5; (b) keep `whileLoop` until R5 lands the `iterate` reader, then delete; (c) let the unit loop keep its unannotated image, `let aN = initial` with no `Effect.map`, as the printed form of an `iterate` whose result is `unit` and whose annotation is the initial cursor's own type, which needs the reader to be handed the cursor type and is therefore a typed reader, an R5 design question in itself. Recommended: (b). The typing lemma is already checked, the deletion is mechanical (the `branch` deletion is the template), and nothing in M2 needs `whileLoop` gone; (a) trades a working capability for a smaller alphabet a few weeks early.

The six questions of the night (the order; which move "retire Effects" names; the `readable` guard; the syntax table's source; the layout specification's four corrections; `Ty` against `Document` for the compile rows) are answered from the literature and the scouts in `docs/research/2026-09-16-system-layout-and-abstractions.md` §4: the order as listed above; the dependency retired by copying, the free monad stays the meaning's carrier (DI-90 as scout C restates it), with `lean4-typescript` retired the same way after law 13; a Boolean equality per constructor for any new `readable`; `tsgo`'s own table base-flattened for the image, tree-sitter for foreign code; apply the four corrections; `Ty.handle` for documents. The owner's steer of the same night (no separate package, the proofs stay; everything MCP-inspectable through one small protocol) is folded in (§4.1, §4.14b). The needs round of §5 there (Q1 to Q23) is answered: all as recommended, with the simplicity rule (the most basic thing the algebraic constructions offer, features as discovered). Open now: the generation medium's seven questions (`2026-09-16-generation-medium-workshop.md` §8); the layout note's §6 lists what changes on the ground once it is answered. Older: Standing asks 1 (the `Effects` package) and 2 (DI-39's amendment) are ruled by DI-90 and DI-82. One new ask: retire `gen` with `Stmt` and `Stmts` as the last step of the `Eff` series, after `iterate` exists as the target (generator bodies read into `bind`, `select`, `iterate`; the style inverse of DI-88 keeps the `Effect.gen` spelling for the printer). 364 lines in 24 files mention the statement machine. DI-79 did not list `gen`; recommended yes.

## Process (ratified)

- Speed over ceremony: edit with whatever is fastest, including shell edits; build what you touch; `make check` once per step; `make check-host` per slice; nothing pushed without the owner. `make check` is seconds when nothing relevant changed: the roots lane re-elaborates the test root only when the source inventory changes (the audit runs inside the build), and the citation scans live in `check-host`, keyed on the files they scan.
- One Lean process at a time. A parallel seat runs only in its own worktree on disjoint files, with a brief the owner has seen. Fable seats need the owner's word each time.
- One session per checkout. A forked session that edits the same files is not a seat; it stands down and hands over (2026-09-16 evening: a fork and this session wrote the same authoring files for an hour before the owner ruled that this session leads).
- Generated modules stay self-contained on `effect` alone unless the `select .tag` narrowing probe fails (DI-85).
- The current authorities under `docs/research/` are tracked (the plan, the two open packets, the obligations note, the algebraic-reading ruling, the review log); the rest of the directory stays untracked.

## Where things are

- Plan: `docs/research/2026-09-16-foundational-language-implementation-plan.md` (S0 to S9; its header says the register rows win where they differ). Obligations: `docs/research/2026-09-16-strict-proof-obligations.md`. Register B1 to B21: the top of `docs/research/2026-09-16-implementation-review-log.md`. The algebraic reading and the order ruling: `docs/research/2026-09-16-algebraic-reading-assessment-and-order-ruling.md`.
- Checks: `make check` after every change, `make check-host` per slice, `make check-full` before a release. Lanes are incremental: a lane whose inputs have not changed since its `.lake/check/<lane>` marker is skipped, so the marker's time is the receipt.
