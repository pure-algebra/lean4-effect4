-- GENERATED by src/OCaml5/Tools/Describe.lean from the Lean environment. Do not edit.
import OCaml5.Ml.Reflect

namespace OCaml5.Ml.Derived.Context
open OCaml5.Ml

def serviceKey : StructDesc where
  leanName := "ServiceKey"
  site := "Key.lean:68"
  subst := []
  leanParams := []
  fields :=
    [{ leanName := "name", leanTy := .nm "ServiceName" },
     { leanName := "service", leanTy := .nm "ServiceTypeCode" }]

def service : StructDesc where
  leanName := "Service"
  site := "Context.lean:88"
  subst := []
  leanParams := ["U"]
  fields :=
    [{ leanName := "key", leanTy := .nm "ServiceKey" },
     { leanName := "value", leanTy := .app "ServiceKey.Carrier" [.nm "U", .nm "key"] }]

def context : StructDesc where
  leanName := "Context"
  site := "Context.lean:179"
  subst := []
  leanParams := ["U"]
  fields :=
    [{ leanName := "entries", leanTy := .lst (.app "Service" [.nm "U"]) },
     { leanName := "keysNodup", leanTy := .app "List.Nodup" [.nm "ServiceKey", .app "List.map" [.app "Service" [.nm "U"], .nm "ServiceKey", .app "Service.key" [.nm "U"], .nm "entries"]] }]

def reference : StructDesc where
  leanName := "Reference"
  site := "Context.lean:405"
  subst := []
  leanParams := ["U"]
  fields :=
    [{ leanName := "key", leanTy := .nm "ServiceKey" },
     { leanName := "default", leanTy := .app "ServiceKey.Carrier" [.nm "U", .nm "key"] }]

def err : InductiveDesc where
  leanName := "Err"
  site := "Context.lean:771"
  subst := []
  leanParams := []
  ctors :=
    [{ leanName := "boom", args := [] },
     { leanName := "tag", args := [{ leanName := "code", leanTy := .nm "Nat" }] }]

def defect : InductiveDesc where
  leanName := "Defect"
  site := "Context.lean:777"
  subst := []
  leanParams := []
  ctors :=
    [{ leanName := "notImplemented", args := [] },
     { leanName := "asyncFiber", args := [] },
     { leanName := "badName", args := [] },
     { leanName := "serviceNotFound", args := [{ leanName := "key", leanTy := .nm "ServiceKey" }] },
     { leanName := "unknownLayer", args := [{ leanName := "index", leanTy := .nm "Nat" }] }]

def val : InductiveDesc where
  leanName := "Val"
  site := "Context.lean:792"
  subst := []
  leanParams := []
  ctors :=
    [{ leanName := "unit", args := [] },
     { leanName := "nat", args := [{ leanName := "n", leanTy := .nm "Nat" }] },
     { leanName := "bool", args := [{ leanName := "b", leanTy := .nm "Bool" }] },
     { leanName := "fiber", args := [{ leanName := "id", leanTy := .nm "FiberId" }] },
     { leanName := "fibers", args := [{ leanName := "ids", leanTy := .lst (.nm "FiberId") }] },
     { leanName := "scopeHandle", args := [{ leanName := "scope", leanTy := .nm "Nat" }] },
     { leanName := "memoMap", args := [{ leanName := "id", leanTy := .nm "Nat" }] },
     { leanName := "promise", args := [{ leanName := "cell", leanTy := .nm "Nat" }] },
     { leanName := "pair", args := [{ leanName := "first", leanTy := .nm "Val" }, { leanName := "second", leanTy := .nm "Val" }] },
     { leanName := "exitOk", args := [{ leanName := "value", leanTy := .nm "Val" }] },
     { leanName := "exitErr", args := [{ leanName := "cause", leanTy := .app "Cause" [.nm "Err", .nm "Defect", .nm "FiberId", .nm "Ann"] }] },
     { leanName := "exitNil", args := [] },
     { leanName := "exitCons", args := [{ leanName := "head", leanTy := .nm "Val" }, { leanName := "tail", leanTy := .nm "Val" }] },
     { leanName := "ctxNil", args := [] },
     { leanName := "ctxCons", args := [{ leanName := "key", leanTy := .nm "ServiceKey" }, { leanName := "value", leanTy := .nm "Val" }, { leanName := "rest", leanTy := .nm "Val" }] }]

def contextUpdate : InductiveDesc where
  leanName := "ContextUpdate"
  site := "Context.lean:1002"
  subst := []
  leanParams := []
  ctors :=
    [{ leanName := "setTo", args := [{ leanName := "context", leanTy := .nm "Ctx" }] },
     { leanName := "provide", args := [{ leanName := "that", leanTy := .nm "Ctx" }] },
     { leanName := "provideService", args := [{ leanName := "key", leanTy := .nm "ServiceKey" }, { leanName := "value", leanTy := .nm "Val" }] }]

end OCaml5.Ml.Derived.Context
