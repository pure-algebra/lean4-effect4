-- GENERATED by tools/Effect4Gen/Main.lean from the Lean environment. Do not edit.
-- Regenerate (scripts\generate-derived.ps1 runs this for every group; -Verify refuses a diff):
--   lake env lean -M 4096 --run tools\Effect4Gen\Main.lean --group Pin --imports Effect4.Store.Pin,Effect4.Store.Node --out src\Effect4\Store\PinDerived.lean --append tools\Effect4Gen\guards\pin.lean --kind Effect4.Store.Pin=source --kind Effect4.Store.Tree=tree \
--     Effect4.Store.PinRole Effect4.Store.Pin Effect4.Store.Tree
-- Carriers read from: Effect4.Store.Pin, Effect4.Store.Node
-- Acceptance guards appended verbatim from: tools\Effect4Gen\guards\pin.lean
import Effect4.Store.Pin
import Effect4.Store.Node

/-!
# Pin — generated `Canonical` instances

One emitted declaration per rule of the facts note's Q2 and Q5: `shapeDoc`, `toVal`,
`ofVal`, `ofVal_toVal`, `ofVal_exact`, `fits` and the instance, per type, in
declaration order, fields through their own types' instances; a `Content` instance
follows where the group names the carrier's kind. The proofs are lane S1's template
scripts; nothing here was written or repaired by hand.
-/

set_option autoImplicit false

namespace Effect4.Store

namespace PinGen

namespace PinRoleC

def shapeDoc : ShapeDoc :=
  ⟨.sum "PinRole"
     [("public", []),
      ("internal", [])],
   []⟩

def toVal : _root_.Effect4.Store.PinRole → Val
  | .public => .ctor 0 []
  | .internal => .ctor 1 []

def ofVal : Val → Option (_root_.Effect4.Store.PinRole)
  | .ctor 0 [] => some .public
  | .ctor 1 [] => some .internal
  | _ => none

set_option linter.unusedSimpArgs false in
theorem ofVal_toVal (a : _root_.Effect4.Store.PinRole) : ofVal (toVal a) = some a := by
  cases a <;> simp [toVal, ofVal, Canonical.ofVal_toVal]

theorem ofVal_exact {v : Val} {a : _root_.Effect4.Store.PinRole} (h : ofVal v = some a) :
    v = toVal a := by
  unfold ofVal at h
  split at h
  all_goals first
    | (injection h with h; subst h; rfl)
    | (rename_i w
       obtain ⟨x, hx, hj⟩ := Option.map_eq_some_iff.mp h
       subst hj
       simp only [toVal]
       rw [Canonical.ofVal_exact hx])
    | exact nomatch h

theorem fits (a : _root_.Effect4.Store.PinRole) : shapeDoc.accepts (toVal a) = true := by
  cases a with
  | «public» =>
    exact accepts_sum _ _ _ 0 "public" [] [] rfl (acceptsFields_nil _)
  | «internal» =>
    exact accepts_sum _ _ _ 1 "internal" [] [] rfl (acceptsFields_nil _)

instance instCanonical : Canonical (_root_.Effect4.Store.PinRole) :=
  ⟨shapeDoc, toVal, ofVal, ofVal_toVal, ofVal_exact, fits⟩

end PinRoleC

namespace PinC

def shapeDoc : ShapeDoc :=
  ⟨.struct "Pin" [("package", (shape _root_.String).root),
     ("version", (shape _root_.String).root), ("file", (shape _root_.String).root),
     ("declaration", (shape _root_.String).root),
     ("role", (shape _root_.Effect4.Store.PinRole).root), ("anchor", (shape _root_.String).root),
     ("offsetStart", (shape _root_.Nat).root), ("offsetEnd", (shape _root_.Nat).root),
     ("spanDigest", (shape _root_.Effect4.Store.Digest).root),
     ("text", (shape (_root_.List (_root_.String))).root)],
   (shape _root_.String).defs ++ (shape _root_.Effect4.Store.PinRole).defs ++
     (shape _root_.Nat).defs ++ (shape _root_.Effect4.Store.Digest).defs ++
     (shape (_root_.List (_root_.String))).defs⟩

def toVal : _root_.Effect4.Store.Pin → Val
  | .mk a0 a1 a2 a3 a4 a5 a6 a7 a8 a9 => .ctor 0 [Canonical.toVal a0, Canonical.toVal a1,
      Canonical.toVal a2, Canonical.toVal a3, Canonical.toVal a4, Canonical.toVal a5,
      Canonical.toVal a6, Canonical.toVal a7, Canonical.toVal a8, Canonical.toVal a9]

def ofVal : Val → Option (_root_.Effect4.Store.Pin)
  | .ctor 0 [v0, v1, v2, v3, v4, v5, v6, v7, v8, v9] =>
    match Canonical.ofVal (α := _root_.String) v0, Canonical.ofVal (α := _root_.String) v1,
        Canonical.ofVal (α := _root_.String) v2, Canonical.ofVal (α := _root_.String) v3,
        Canonical.ofVal (α := _root_.Effect4.Store.PinRole) v4,
        Canonical.ofVal (α := _root_.String) v5, Canonical.ofVal (α := _root_.Nat) v6,
        Canonical.ofVal (α := _root_.Nat) v7,
        Canonical.ofVal (α := _root_.Effect4.Store.Digest) v8,
        Canonical.ofVal (α := (_root_.List (_root_.String))) v9 with
    | some a0, some a1, some a2, some a3, some a4, some a5, some a6, some a7, some a8,
      some a9 => some ⟨a0, a1, a2, a3, a4, a5, a6, a7, a8, a9⟩
    | _, _, _, _, _, _, _, _, _, _ => none
  | _ => none

theorem ofVal_toVal (a : _root_.Effect4.Store.Pin) : ofVal (toVal a) = some a := by
  obtain ⟨a0, a1, a2, a3, a4, a5, a6, a7, a8, a9⟩ := a
  simp [toVal, ofVal, Canonical.ofVal_toVal]

theorem ofVal_exact {v : Val} {a : _root_.Effect4.Store.Pin} (h : ofVal v = some a) :
    v = toVal a := by
  unfold ofVal at h
  split at h
  · next v0 v1 v2 v3 v4 v5 v6 v7 v8 v9 =>
    split at h
    · next b0 b1 b2 b3 b4 b5 b6 b7 b8 b9 h0 h1 h2 h3 h4 h5 h6 h7 h8 h9 =>
      injection h with h
      subst h
      simp only [toVal]
      rw [Canonical.ofVal_exact h0, Canonical.ofVal_exact h1, Canonical.ofVal_exact h2,
        Canonical.ofVal_exact h3, Canonical.ofVal_exact h4, Canonical.ofVal_exact h5,
        Canonical.ofVal_exact h6, Canonical.ofVal_exact h7, Canonical.ofVal_exact h8,
        Canonical.ofVal_exact h9]
    · exact nomatch h
  · exact nomatch h

theorem lift_String (x : _root_.String) :
    acceptsIn shapeDoc.defs (shape _root_.String).root (Canonical.toVal x) = true :=
  acceptsIn_mono_of_subset (fun _ hp => mem_append_of_left (mem_append_of_left (mem_append_of_left (mem_append_of_left (hp)))))
    _ _ (Canonical.fits x)
theorem lift_PinRole (x : _root_.Effect4.Store.PinRole) :
    acceptsIn shapeDoc.defs (shape _root_.Effect4.Store.PinRole).root
      (Canonical.toVal x) = true :=
  acceptsIn_mono_of_subset (fun _ hp => mem_append_of_left (mem_append_of_left (mem_append_of_left (mem_append_of_right (hp)))))
    _ _ (Canonical.fits x)
theorem lift_Nat (x : _root_.Nat) :
    acceptsIn shapeDoc.defs (shape _root_.Nat).root (Canonical.toVal x) = true :=
  acceptsIn_mono_of_subset (fun _ hp => mem_append_of_left (mem_append_of_left (mem_append_of_right (hp))))
    _ _ (Canonical.fits x)
theorem lift_Digest (x : _root_.Effect4.Store.Digest) :
    acceptsIn shapeDoc.defs (shape _root_.Effect4.Store.Digest).root (Canonical.toVal x) = true :=
  acceptsIn_mono_of_subset (fun _ hp => mem_append_of_left (mem_append_of_right (hp)))
    _ _ (Canonical.fits x)
theorem lift_ListString (x : (_root_.List (_root_.String))) :
    acceptsIn shapeDoc.defs (shape (_root_.List (_root_.String))).root
      (Canonical.toVal x) = true :=
  acceptsIn_mono_of_subset (fun _ hp => mem_append_of_right (hp))
    _ _ (Canonical.fits x)

theorem fits (a : _root_.Effect4.Store.Pin) : shapeDoc.accepts (toVal a) = true := by
  obtain ⟨a0, a1, a2, a3, a4, a5, a6, a7, a8, a9⟩ := a
  apply accepts_struct
  exact
    (acceptsFields_cons _ _ _ _ _ _ (lift_String a0)
      (acceptsFields_cons _ _ _ _ _ _ (lift_String a1)
        (acceptsFields_cons _ _ _ _ _ _ (lift_String a2)
          (acceptsFields_cons _ _ _ _ _ _ (lift_String a3)
            (acceptsFields_cons _ _ _ _ _ _ (lift_PinRole a4)
              (acceptsFields_cons _ _ _ _ _ _ (lift_String a5)
                (acceptsFields_cons _ _ _ _ _ _ (lift_Nat a6)
                  (acceptsFields_cons _ _ _ _ _ _ (lift_Nat a7)
                    (acceptsFields_cons _ _ _ _ _ _ (lift_Digest a8)
                      (acceptsFields_cons _ _ _ _ _ _ (lift_ListString a9) (acceptsFields_nil _)))))))))))

instance instCanonical : Canonical (_root_.Effect4.Store.Pin) :=
  ⟨shapeDoc, toVal, ofVal, ofVal_toVal, ofVal_exact, fits⟩

/-- Every node carrying a `Pin` files under kind `source`. -/
instance instContent : Content (_root_.Effect4.Store.Pin) := ⟨.source⟩

end PinC

namespace TreeC

def shapeDoc : ShapeDoc :=
  ⟨.struct "Tree" [
     ("bindings", (shape (_root_.List (_root_.Prod (_root_.String) (_root_.Effect4.Store.AnyRef)))).root)],
   (shape (_root_.List (_root_.Prod (_root_.String) (_root_.Effect4.Store.AnyRef)))).defs⟩

def toVal : _root_.Effect4.Store.Tree → Val
  | .mk a0 => .ctor 0 [Canonical.toVal a0]

def ofVal : Val → Option (_root_.Effect4.Store.Tree)
  | .ctor 0 [v0] =>
    match
        Canonical.ofVal (α := (_root_.List (_root_.Prod (_root_.String) (_root_.Effect4.Store.AnyRef)))) v0 with
    | some a0 => some ⟨a0⟩
    | _ => none
  | _ => none

theorem ofVal_toVal (a : _root_.Effect4.Store.Tree) : ofVal (toVal a) = some a := by
  obtain ⟨a0⟩ := a
  simp [toVal, ofVal, Canonical.ofVal_toVal]

theorem ofVal_exact {v : Val} {a : _root_.Effect4.Store.Tree} (h : ofVal v = some a) :
    v = toVal a := by
  unfold ofVal at h
  split at h
  · next v0 =>
    split at h
    · next b0 h0 =>
      injection h with h
      subst h
      simp only [toVal]
      rw [Canonical.ofVal_exact h0]
    · exact nomatch h
  · exact nomatch h

theorem lift_ListProdStringAnyRef (x : (_root_.List (_root_.Prod (_root_.String) (_root_.Effect4.Store.AnyRef)))) :
    acceptsIn shapeDoc.defs (shape (_root_.List (_root_.Prod (_root_.String) (_root_.Effect4.Store.AnyRef)))).root
      (Canonical.toVal x) = true :=
  acceptsIn_mono_of_subset (fun _ hp => hp)
    _ _ (Canonical.fits x)

theorem fits (a : _root_.Effect4.Store.Tree) : shapeDoc.accepts (toVal a) = true := by
  obtain ⟨a0⟩ := a
  apply accepts_struct
  exact
    (acceptsFields_cons _ _ _ _ _ _ (lift_ListProdStringAnyRef a0) (acceptsFields_nil _))

instance instCanonical : Canonical (_root_.Effect4.Store.Tree) :=
  ⟨shapeDoc, toVal, ofVal, ofVal_toVal, ofVal_exact, fits⟩

/-- Every node carrying a `Tree` files under kind `tree`. -/
instance instContent : Content (_root_.Effect4.Store.Tree) := ⟨.tree⟩

end TreeC

end PinGen

/-! ## Acceptance: a pin as `source` content

Appended verbatim by `--append`; not generated. The laws of the class made executable on one
well-formed pin (its span digest recomputed from its own lines, as `Pin.isWellFormed` does),
the reader's refusals, the kind the `--kind` option filed the carrier under, and the frame of
a typed reference to a pin: forty-two bytes, `Tag.ref`, the `source` kind byte, the address. -/

namespace PinAcceptance

def lines : List String := ["const releaseCapacity = (self) => {", "}"]

/-- One pin, well-formed by construction: two lines, offsets `0..1`, the digest of the lines
joined by newlines with a trailing newline. -/
def sample : Pin :=
  { package := "effect", version := "4.0.0-rc.112", file := "src/Queue.ts",
    declaration := "releaseCapacity", role := .internal, anchor := "const releaseCapacity",
    offsetStart := 0, offsetEnd := 1,
    spanDigest := sha256 (lines.foldr (fun line acc => line.toUTF8.data.toList ++ (10 :: acc)) []),
    text := lines }

#guard sample.isWellFormed

-- The laws, run: the image reads back, through the class and through the bytes; a byte
-- appended or dropped is refused; the image fits the shape.
#guard PinGen.PinC.ofVal (PinGen.PinC.toVal sample) = some sample
#guard Canonical.decode (α := Pin) (Canonical.encode sample) = some sample
#guard Canonical.decode (α := Pin) (Canonical.encode sample ++ [0]) = none
#guard Canonical.decode (α := Pin) (Canonical.encode sample).dropLast = none
#guard (Canonical.shape Pin).accepts (Canonical.toVal sample)
#guard [PinRole.public, .internal].all fun r => PinGen.PinRoleC.ofVal (PinGen.PinRoleC.toVal r) = some r

-- The kind, and the spec's root: a struct.
#guard Content.kind Pin = .source
#guard (Canonical.document Pin).representation.tag = .objects

-- The printer follows the shape: keys in declaration order, the role as its constructor's
-- name, the digest as lowercase hex, the lines as an array.
#guard Canonical.print sample =
  .obj [ ("package", .str "effect"), ("version", .str "4.0.0-rc.112"), ("file", .str "src/Queue.ts")
       , ("declaration", .str "releaseCapacity"), ("role", .str "internal")
       , ("anchor", .str "const releaseCapacity"), ("offsetStart", Arch.Json.ofNat 0)
       , ("offsetEnd", Arch.Json.ofNat 1), ("spanDigest", .str sample.spanDigest.hex)
       , ("text", .arr (lines.map .str)) ]

-- A typed reference to a pin: `Tag.ref`, the `source` kind byte, thirty-two address bytes;
-- another kind byte or a short digest is refused before any store is consulted.
#guard (Canonical.encode (⟨sha256 []⟩ : Ref Pin)).length = 42
#guard (Canonical.encode (⟨sha256 []⟩ : Ref Pin)).take 10 = [11, 0, 0, 0, 0, 0, 0, 0, 33, 1]
#guard Canonical.decode (α := Ref Pin) (Canonical.encode (⟨sha256 []⟩ : Ref Pin)) =
  some (⟨sha256 []⟩ : Ref Pin)
#guard Canonical.decode (α := Ref Pin) (Val.encode (.ref 2 (sha256 []).bytes)) = none
#guard Canonical.decode (α := Ref Pin) (Val.encode (.ref 1 (List.replicate 31 0))) = none

end PinAcceptance

/-! ## Receipts -/

#print axioms PinGen.PinRoleC.toVal
#print axioms PinGen.PinRoleC.ofVal_toVal
#print axioms PinGen.PinRoleC.ofVal_exact
#print axioms PinGen.PinRoleC.fits
#print axioms PinGen.PinRoleC.instCanonical
#print axioms PinGen.PinC.toVal
#print axioms PinGen.PinC.ofVal_toVal
#print axioms PinGen.PinC.ofVal_exact
#print axioms PinGen.PinC.fits
#print axioms PinGen.PinC.instCanonical
#print axioms PinGen.PinC.instContent
#print axioms PinGen.TreeC.toVal
#print axioms PinGen.TreeC.ofVal_toVal
#print axioms PinGen.TreeC.ofVal_exact
#print axioms PinGen.TreeC.fits
#print axioms PinGen.TreeC.instCanonical
#print axioms PinGen.TreeC.instContent

end Effect4.Store
