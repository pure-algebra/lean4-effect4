-- cut-from: rev=f921bc7-dirty toolchain=4.33.1 inputs=c0b468dcf2057d661770746a71d0afccb3b31452f134c292cd405c0e31a509cb
-- GENERATED by tools/Effect4Gen/Main.lean from the Lean environment. Do not edit.
-- Regenerate (tools/Effect4Gen/Driver.lean runs this for every group; --verify refuses a diff):
--   lake env lean -M 4096 --run tools/Effect4Gen/Main.lean --group Api --imports Effect4.Api.Frontier,Effect4.Store.Canonical --out src/Effect4/Api/Derived.lean --append tools/Effect4Gen/guards/api.lean \
--     Effect4.FiberId Effect4.Api.HostProtocol.Key Effect4.Machine.Exhaustion \
--    Effect4.Api.FrontierReason
-- Carriers read from: Effect4.Machine.Fiber, Effect4.Api.Frontier, Effect4.Machine.Fibers
-- Acceptance guards appended verbatim from: tools/Effect4Gen/guards/api.lean
import Effect4.Api.Frontier
import Effect4.Store.Canonical

/-!
# Api — generated `Canonical` instances

One emitted declaration per rule of the facts note's Q2 and Q5: `shapeDoc`, `toVal`,
`ofVal`, `ofVal_toVal`, `ofVal_exact`, `fits` and the instance, per type, in
declaration order, fields through their own types' instances; a `Content` instance
follows where the group names the carrier's kind. The proofs are lane S1's template
scripts; nothing here was written or repaired by hand.
-/

set_option autoImplicit false

namespace Effect4.Store

namespace ApiGen

namespace FiberIdC

def shapeDoc : ShapeDoc :=
  ⟨.struct "FiberId" [("value", (shape _root_.Nat).root)],
   (shape _root_.Nat).defs⟩

def toVal : _root_.Effect4.FiberId → Val
  | .mk a0 => .ctor 0 [Canonical.toVal a0]

def ofVal : Val → Option (_root_.Effect4.FiberId)
  | .ctor 0 [v0] =>
    match Canonical.ofVal (α := _root_.Nat) v0 with
    | some a0 => some ⟨a0⟩
    | _ => none
  | _ => none

theorem ofVal_toVal (a : _root_.Effect4.FiberId) : ofVal (toVal a) = some a := by
  obtain ⟨a0⟩ := a
  simp [toVal, ofVal, Canonical.ofVal_toVal]

theorem ofVal_exact {v : Val} {a : _root_.Effect4.FiberId} (h : ofVal v = some a) :
    v = toVal a := by
  unfold ofVal at h
  split at h
  · next v0 =>
    split at h
    · next b0 h0 =>
      injection h with h
      subst h
      simp only [toVal]
      rw [Canonical.ofVal_exact h0]
    · exact nomatch h
  · exact nomatch h

theorem lift_Nat (x : _root_.Nat) :
    acceptsIn shapeDoc.defs (shape _root_.Nat).root (Canonical.toVal x) = true :=
  acceptsIn_mono_of_subset (fun _ hp => hp)
    _ _ (Canonical.fits x)

theorem fits (a : _root_.Effect4.FiberId) : shapeDoc.accepts (toVal a) = true := by
  obtain ⟨a0⟩ := a
  apply accepts_struct
  exact
    (acceptsFields_cons _ _ _ _ _ _ (lift_Nat a0) (acceptsFields_nil _))

instance instCanonical : Canonical (_root_.Effect4.FiberId) :=
  ⟨shapeDoc, toVal, ofVal, ofVal_toVal, ofVal_exact, fits⟩

end FiberIdC

namespace KeyC

def shapeDoc : ShapeDoc :=
  ⟨.struct "Key" [("fiber", (shape _root_.Effect4.FiberId).root),
     ("token", (shape _root_.Nat).root)],
   (shape _root_.Effect4.FiberId).defs ++ (shape _root_.Nat).defs⟩

def toVal : _root_.Effect4.Api.HostProtocol.Key → Val
  | .mk a0 a1 => .ctor 0 [Canonical.toVal a0, Canonical.toVal a1]

def ofVal : Val → Option (_root_.Effect4.Api.HostProtocol.Key)
  | .ctor 0 [v0, v1] =>
    match Canonical.ofVal (α := _root_.Effect4.FiberId) v0,
        Canonical.ofVal (α := _root_.Nat) v1 with
    | some a0, some a1 => some ⟨a0, a1⟩
    | _, _ => none
  | _ => none

theorem ofVal_toVal (a : _root_.Effect4.Api.HostProtocol.Key) : ofVal (toVal a) = some a := by
  obtain ⟨a0, a1⟩ := a
  simp [toVal, ofVal, Canonical.ofVal_toVal]

theorem ofVal_exact {v : Val} {a : _root_.Effect4.Api.HostProtocol.Key} (h : ofVal v = some a) :
    v = toVal a := by
  unfold ofVal at h
  split at h
  · next v0 v1 =>
    split at h
    · next b0 b1 h0 h1 =>
      injection h with h
      subst h
      simp only [toVal]
      rw [Canonical.ofVal_exact h0, Canonical.ofVal_exact h1]
    · exact nomatch h
  · exact nomatch h

theorem lift_FiberId (x : _root_.Effect4.FiberId) :
    acceptsIn shapeDoc.defs (shape _root_.Effect4.FiberId).root (Canonical.toVal x) = true :=
  acceptsIn_mono_of_subset (fun _ hp => mem_append_of_left (hp))
    _ _ (Canonical.fits x)
theorem lift_Nat (x : _root_.Nat) :
    acceptsIn shapeDoc.defs (shape _root_.Nat).root (Canonical.toVal x) = true :=
  acceptsIn_mono_of_subset (fun _ hp => mem_append_of_right (hp))
    _ _ (Canonical.fits x)

theorem fits (a : _root_.Effect4.Api.HostProtocol.Key) : shapeDoc.accepts (toVal a) = true := by
  obtain ⟨a0, a1⟩ := a
  apply accepts_struct
  exact
    (acceptsFields_cons _ _ _ _ _ _ (lift_FiberId a0)
      (acceptsFields_cons _ _ _ _ _ _ (lift_Nat a1) (acceptsFields_nil _)))

instance instCanonical : Canonical (_root_.Effect4.Api.HostProtocol.Key) :=
  ⟨shapeDoc, toVal, ofVal, ofVal_toVal, ofVal_exact, fits⟩

end KeyC

namespace ExhaustionC

def shapeDoc : ShapeDoc :=
  ⟨.sum "Exhaustion"
     [("fuel", []),
      ("tape", [])],
   []⟩

def toVal : _root_.Effect4.Machine.Exhaustion → Val
  | .fuel => .ctor 0 []
  | .tape => .ctor 1 []

def ofVal : Val → Option (_root_.Effect4.Machine.Exhaustion)
  | .ctor 0 [] => some .fuel
  | .ctor 1 [] => some .tape
  | _ => none

set_option linter.unusedSimpArgs false in
theorem ofVal_toVal (a : _root_.Effect4.Machine.Exhaustion) : ofVal (toVal a) = some a := by
  cases a <;> simp [toVal, ofVal, Canonical.ofVal_toVal]

theorem ofVal_exact {v : Val} {a : _root_.Effect4.Machine.Exhaustion} (h : ofVal v = some a) :
    v = toVal a := by
  unfold ofVal at h
  split at h
  all_goals first
    | (injection h with h; subst h; rfl)
    | (rename_i w
       obtain ⟨x, hx, hj⟩ := Option.map_eq_some_iff.mp h
       subst hj
       simp only [toVal]
       rw [Canonical.ofVal_exact hx])
    | exact nomatch h

theorem fits (a : _root_.Effect4.Machine.Exhaustion) : shapeDoc.accepts (toVal a) = true := by
  cases a with
  | «fuel» =>
    exact accepts_sum _ _ _ 0 "fuel" [] [] rfl (acceptsFields_nil _)
  | «tape» =>
    exact accepts_sum _ _ _ 1 "tape" [] [] rfl (acceptsFields_nil _)

instance instCanonical : Canonical (_root_.Effect4.Machine.Exhaustion) :=
  ⟨shapeDoc, toVal, ofVal, ofVal_toVal, ofVal_exact, fits⟩

end ExhaustionC

namespace FrontierReasonC

def shapeDoc : ShapeDoc :=
  ⟨.sum "FrontierReason"
     [("commandFuel", []),
      ("compileFuel", [("fiber", (shape _root_.Effect4.FiberId).root)]),
      ("awaitHost", [("key", (shape _root_.Effect4.Api.HostProtocol.Key).root)]),
      ("awaitTimer", [("fiber", (shape _root_.Effect4.FiberId).root),
        ("wakeAt", (shape _root_.Nat).root)]),
      ("awaitDecision", [])],
   (shape _root_.Effect4.FiberId).defs ++ (shape _root_.Effect4.Api.HostProtocol.Key).defs ++
     (shape _root_.Nat).defs⟩

def toVal : _root_.Effect4.Api.FrontierReason → Val
  | .commandFuel => .ctor 0 []
  | .compileFuel a0 => .ctor 1 [Canonical.toVal a0]
  | .awaitHost a0 => .ctor 2 [Canonical.toVal a0]
  | .awaitTimer a0 a1 => .ctor 3 [Canonical.toVal a0, Canonical.toVal a1]
  | .awaitDecision => .ctor 4 []

def ofVal : Val → Option (_root_.Effect4.Api.FrontierReason)
  | .ctor 0 [] => some .commandFuel
  | .ctor 1 [v0] => (Canonical.ofVal (α := _root_.Effect4.FiberId) v0).map .compileFuel
  | .ctor 2 [v0] => (Canonical.ofVal (α := _root_.Effect4.Api.HostProtocol.Key) v0).map .awaitHost
  | .ctor 3 [v0, v1] =>
    match Canonical.ofVal (α := _root_.Effect4.FiberId) v0,
        Canonical.ofVal (α := _root_.Nat) v1 with
    | some a0, some a1 => some (.awaitTimer a0 a1)
    | _, _ => none
  | .ctor 4 [] => some .awaitDecision
  | _ => none

set_option linter.unusedSimpArgs false in
theorem ofVal_toVal (a : _root_.Effect4.Api.FrontierReason) : ofVal (toVal a) = some a := by
  cases a <;> simp [toVal, ofVal, Canonical.ofVal_toVal]

theorem ofVal_exact {v : Val} {a : _root_.Effect4.Api.FrontierReason} (h : ofVal v = some a) :
    v = toVal a := by
  unfold ofVal at h
  split at h
  all_goals first
    | (injection h with h; subst h; rfl)
    | (rename_i w
       obtain ⟨x, hx, hj⟩ := Option.map_eq_some_iff.mp h
       subst hj
       simp only [toVal]
       rw [Canonical.ofVal_exact hx])
    | (split at h
       · rename_i b0 b1 h0 h1
         injection h with h
         subst h
         simp only [toVal]
         rw [Canonical.ofVal_exact h0, Canonical.ofVal_exact h1]
         done
       all_goals exact nomatch h)
    | exact nomatch h

theorem lift_FiberId (x : _root_.Effect4.FiberId) :
    acceptsIn shapeDoc.defs (shape _root_.Effect4.FiberId).root (Canonical.toVal x) = true :=
  acceptsIn_mono_of_subset (fun _ hp => mem_append_of_left (mem_append_of_left (hp)))
    _ _ (Canonical.fits x)
theorem lift_Key (x : _root_.Effect4.Api.HostProtocol.Key) :
    acceptsIn shapeDoc.defs (shape _root_.Effect4.Api.HostProtocol.Key).root
      (Canonical.toVal x) = true :=
  acceptsIn_mono_of_subset (fun _ hp => mem_append_of_left (mem_append_of_right (hp)))
    _ _ (Canonical.fits x)
theorem lift_Nat (x : _root_.Nat) :
    acceptsIn shapeDoc.defs (shape _root_.Nat).root (Canonical.toVal x) = true :=
  acceptsIn_mono_of_subset (fun _ hp => mem_append_of_right (hp))
    _ _ (Canonical.fits x)

theorem fits (a : _root_.Effect4.Api.FrontierReason) : shapeDoc.accepts (toVal a) = true := by
  cases a with
  | «commandFuel» =>
    exact accepts_sum _ _ _ 0 "commandFuel" [] [] rfl (acceptsFields_nil _)
  | «compileFuel» a0 =>
    exact accepts_sum _ _ _ 1 "compileFuel" _ _ rfl
      (acceptsFields_cons _ _ _ _ _ _ (lift_FiberId a0) (acceptsFields_nil _))
  | «awaitHost» a0 =>
    exact accepts_sum _ _ _ 2 "awaitHost" _ _ rfl
      (acceptsFields_cons _ _ _ _ _ _ (lift_Key a0) (acceptsFields_nil _))
  | «awaitTimer» a0 a1 =>
    exact accepts_sum _ _ _ 3 "awaitTimer" _ _ rfl
      (acceptsFields_cons _ _ _ _ _ _ (lift_FiberId a0)
        (acceptsFields_cons _ _ _ _ _ _ (lift_Nat a1) (acceptsFields_nil _)))
  | «awaitDecision» =>
    exact accepts_sum _ _ _ 4 "awaitDecision" [] [] rfl (acceptsFields_nil _)

instance instCanonical : Canonical (_root_.Effect4.Api.FrontierReason) :=
  ⟨shapeDoc, toVal, ofVal, ofVal_toVal, ofVal_exact, fits⟩

end FrontierReasonC

end ApiGen

/-! P2b carrier acceptance: round trips, exact decoding and generated shapes. -/
namespace ApiAcceptance
open Effect4 Effect4.Machine Effect4.Api

def fibers : List FiberId := [⟨0⟩, ⟨1⟩, ⟨37⟩]
def keys : List HostProtocol.Key := [⟨⟨0⟩, 0⟩, ⟨⟨1⟩, 7⟩]
def exhaustion : List Exhaustion := [.fuel, .tape]
def reasons : List FrontierReason :=
  [.commandFuel, .compileFuel ⟨0⟩, .awaitHost ⟨⟨1⟩, 7⟩, .awaitTimer ⟨2⟩ 19, .awaitDecision]

#guard fibers.all fun x => Canonical.decode (α := FiberId) (Canonical.encode x) = some x
#guard keys.all fun x => Canonical.decode (α := HostProtocol.Key) (Canonical.encode x) = some x
#guard exhaustion.all fun x => Canonical.decode (α := Exhaustion) (Canonical.encode x) = some x
#guard reasons.all fun x => Canonical.decode (α := FrontierReason) (Canonical.encode x) = some x
#guard reasons.all fun x => Canonical.decode (α := FrontierReason) (Canonical.encode x ++ [0]) = none
#guard reasons.all fun x => (Canonical.shape FrontierReason).accepts (Canonical.toVal x)
#guard Canonical.decode (α := Exhaustion) [] = none
#guard Canonical.decode (α := HostProtocol.Key) [] = none

end ApiAcceptance

/-! ## Receipts -/

#print axioms ApiGen.FiberIdC.toVal
#print axioms ApiGen.FiberIdC.ofVal_toVal
#print axioms ApiGen.FiberIdC.ofVal_exact
#print axioms ApiGen.FiberIdC.fits
#print axioms ApiGen.FiberIdC.instCanonical
#print axioms ApiGen.KeyC.toVal
#print axioms ApiGen.KeyC.ofVal_toVal
#print axioms ApiGen.KeyC.ofVal_exact
#print axioms ApiGen.KeyC.fits
#print axioms ApiGen.KeyC.instCanonical
#print axioms ApiGen.ExhaustionC.toVal
#print axioms ApiGen.ExhaustionC.ofVal_toVal
#print axioms ApiGen.ExhaustionC.ofVal_exact
#print axioms ApiGen.ExhaustionC.fits
#print axioms ApiGen.ExhaustionC.instCanonical
#print axioms ApiGen.FrontierReasonC.toVal
#print axioms ApiGen.FrontierReasonC.ofVal_toVal
#print axioms ApiGen.FrontierReasonC.ofVal_exact
#print axioms ApiGen.FrontierReasonC.fits
#print axioms ApiGen.FrontierReasonC.instCanonical

end Effect4.Store
