# Effect Core v1 contracts

Status: scaffold only.

Frozen per-slice signatures and contract batteries will be placed here only
after the breaker and coordinator agree on the exact quantified statements.
The semantic implementation lives under `EffectCore/`; this directory never
contains a substitute implementation.
