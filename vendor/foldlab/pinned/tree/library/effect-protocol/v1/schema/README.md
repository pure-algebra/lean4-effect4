# Protocol schema

Status: scaffold only.

The accepted schema will define canonical ordering, byte encoding, identifier
uniqueness, imported-identity references, and profile closure. No placeholder
schema is supplied before its decoder, canonicalizer, and mutation controls
exist.
