# One line ending in the tree: the model's surfaces regenerate
# byte-identically, so checkout must not invent CRLF differences.
*.lean text eol=lf
*.json text eol=lf
*.toml text eol=lf
*.md   text eol=lf
