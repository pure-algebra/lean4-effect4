# The OCaml estate

OCaml is the primary host of the Effect model (ruling of 2026-09-04, `docs/research/
2026-09-04-ocaml-estate-and-parity.md`). This directory is one dune workspace holding every
OCaml program of the estate; its Lean half is the lake library `OCaml5` under `src/OCaml5/`
(root module `src/OCaml5.lean`). Nothing here is a spike any more: each project is a library
with thin drivers, one component per file with a property list at its head, generated files
marked and regenerable, and a report behind every number (`STANDARDS.md`).

One runtime: **the visible machine**, the Lean `RunMachine` itself, running in OCaml as
Lean's own compiler IR translated into typed OCaml (`gen/`), with `engine/` the host engine
under that generated machine (`docs/research/2026-09-08-engine-brief.md`). Every fiber,
frame, park token, pending resume and event is a field of one value, so it can be inspected,
serialised, moved between domains and messaged through the model's own decision alphabet.
Fidelity is the compiler's. `eff/` is the Lean program IR `Eff` as an OCaml language, so
OCaml can author programs the Lean machine decodes exactly.

The routes that once stood beside it are archived on the local branch `archive/ocaml5-avatar`
at `14e6835` (engine brief ruling: one OCaml engine, `gen/`): the avatar (the same machine as
OCaml 5 effect handlers, `avatar/`), the daemon `effect4d` that served it (`server/`), its
wasm host (`wasm/`), route 1's OCaml half (`link/`, whose host core `engine/` re-homed), and
the spike probes (`probes/`). Route 1's Lean half, `src/OCaml5/Bridge.lean`, is still in the
tree only because `harness/truth/Truth.lean` cites it and is a stamp input of the truth
family; it leaves with the next edit that re-stamps the truth artefacts. The host over the
engine that replaces the daemon is owed by the host-rows slice.

## Map

| directory | what it is | build | check | read |
| --- | --- | --- | --- | --- |
| `gen/` | route 2: `fibers_gen.ml` (every top-level function of `Fibers.lean`), `machine_gen.ml` (the dispatcher and `RunMachine` projections) and `api_gen.ml` (`Effect4.Api.run`/`replay` and their closure), GENERATED by `src/OCaml5/Tools/LcnfGen.lean` from Lean's mono-phase LCNF; `gen_check` (against `avatar_reference.ml`, a marked copy of the retired avatar's dispatcher) | `dune build gen` | `dune test gen` | `gen/NOTES.md` |
| `engine/` | the host engine under the generated machine: `api_engine.ml` (the same LCNF projection over the engine's carriers), the carriers (fibers view, trace, memo, ppath, table), the scheduler over domains (`e4_sched`), the mailbox, triggers, admission, the CAS (`cas/`), the query, the differential against `gen/` | `dune build engine` | `dune test engine` (declared red until Phase 1 regenerates the engines); `engine/tools/gen-check.sh` | `docs/research/2026-09-08-engine-brief.md`, the `.mli` headers |
| `eff/` | the `Eff` IR as an OCaml library: generated `eff_types`/`eff_wire`/`eff_json`/`eff_native` (`src/OCaml5/Tools/EffGen.lean`), the typing checker, the GADT surface `eff_typed`, goldens from the Lean side | `dune build eff` | `dune test`: goldens byte-identical, checker = `typeOf`, the wire property test | `eff/README.md`, `eff/REPORT.md` |
| `goldens/eff/` | the canonical bytes of the Lean program corpus, written by `src/OCaml5/Tools/EffWire.lean` through `Effect4.Program.Wire` | — | compared with `eff/goldens` | `eff/REPORT.md` |
| `tools/` | `ml-check.sh` (the language model's battery), `lib/toolchain.sh` | — | — | the scripts' headers |

The Lean half, `src/OCaml5/`:

| module | what it is |
| --- | --- |
| `Runtime/Effect` | the OCaml 5 `Stdlib.Effect` handler machine (65 arms): the model `Lib/Eio` and `Lib/Picos` build on and the site `Ml/Profile` cites for the effect primitives |
| `Runtime/Value` | backend-relative values (`Sys.int_size` per host), the carrier `Ml/Profile` cites |
| `Ml/*` | the OCaml language model: typed syntax, the canonical printer, the profile checker (an allowlist of constructs and library signatures), Lean structure/inductive → OCaml type reflection, the `{ f with }` → mutation pass; `MlTest` is its battery |
| `Lib/*` | Lean carriers with laws for the libraries the engine uses (`Map`, `Set`, `Deque`, `Order`, `Sexp`, `Stream`, `Eio`, `Picos`, `Derived`); `Lib/Deque` projects onto `Effect4.Machine.Dispatcher` by `rfl` |
| `Eff/*` | the `Eff` program IR as OCaml: the closed world read off the environment (`World`), the emitters of the `eff/` library (`Emit`), the goldens and corpus (`Goldens`) |
| `Lcnf/*` | the LCNF → OCaml backend: dump, naming, types, the translation rules and the builtin table |
| `Bridge` | route 1's session API exported to C; its OCaml half is archived and the module stays only until the truth family is next re-stamped (see above) |
| `Tools/*` | the `--run` drivers, each a thin `main` over a library module: `LcnfDump`/`LcnfGen` (route 2 and the engine), `EffGen` (over `Eff`), `EffWire` (the wire goldens), `CasGoldens` (the CAS goldens), `Describe` (derives a description of a Machine inductive from the environment; its avatar outputs are archived, and `tools/Effect4Gen` borrows its environment walk) |

The rest of the OCaml 5 reification (the handler machine's invariants and witnesses, the
js_of_ocaml machine and the native ≈ jsoo relation, jsoo's block IR, the Term → Code
compiler, the CPS pass and its proofs, the Promise host, the `Term` renderer and the term
fuzz) is archived with the avatar it targeted; its findings are in the research notes those
modules cite.

`harness/truth/` (tracked beside this estate) is the Lean-vs-rc.112 exit differential over
the program corpus (`run-truth.ts`, bun, `Truth.lean`); the OCaml hosts are its owed third
column.

## Building

The switch is `opam switch effect4`: OCaml 5.1.1, dune 3.24, js_of_ocaml 5.7.1 pinned (the
modelled backend), the Jane Street v0.16 set. On the PC the switch lives in WSL Ubuntu and
every command below runs there:

```bash
eval $(opam env --switch=effect4 --set-switch)
cd /mnt/c/Users/kokok/Dev/lean4-effect4/ocaml
dune build            # gen, eff, engine
dune test eff gen     # engine's tests are the declared-red sweep row until Phase 1
```

The Lean half is `lake build OCaml5` from the repository root.

## Generated, and how to regenerate

The inventory, the producers and the gates are `docs/GENERATED.md`; `bash scripts/generate.sh`
runs the producers in dependency order and `bash scripts/check-generated.sh` checks them.

| generated | generator | command |
| --- | --- | --- |
| `gen/machine_gen.ml`, `gen/fibers_gen.ml`, `gen/api_gen.ml`, `engine/api_engine.ml` | `src/OCaml5/Tools/LcnfGen.lean` | the command in each file's header (`--only lcnf`, Phase 1) |
| `eff/eff_{types,wire,json,native}.ml`, `eff/eff_manifest.txt`, `eff/goldens/*` | `src/OCaml5/Tools/EffGen.lean` | `lake env lean -M4096 --run src/OCaml5/Tools/EffGen.lean ocaml/eff` |
| `goldens/eff/*` | `src/OCaml5/Tools/EffWire.lean` | `lake env lean -M4096 --run src/OCaml5/Tools/EffWire.lean ocaml/goldens/eff` |
| `engine/cas/goldens/*` | `src/OCaml5/Tools/CasGoldens.lean` | `lake env lean -M4096 --run src/OCaml5/Tools/CasGoldens.lean ocaml/engine/cas/goldens` |

## Owed

* `gen/` and `engine/`: the Phase 1 regeneration from the current machine (the timer, `advance`,
  the wake protocol) and the prelude repair (`docs/research/2026-09-08-one-generator-plan-v2.md`);
  then the carrier vocabulary that retires the prelude's hand bodies;
* `engine/`: the host over the engine that answers parks (the host-rows slice), which is what
  replaces the archived daemon;
* `eff/`: the two theorems its report states (erase ⇒ `wellTyped`; encode/decode exactness);
* the OCaml hosts as the third column of `harness/truth`.
