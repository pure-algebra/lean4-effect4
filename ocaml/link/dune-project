(lang dune 3.0)
; link/: the Lean machine held by OCaml as an opaque value (route 1), and the host core
; around it (mailbox, worker, router, ring, host). Standalone: build from this directory
; with `dune build --root .` (tools/build.sh does the Lean half first). The `effect4` opam
; switch: OCaml 5.1.1, dune 3.24.
(name effect4-bridge)
(generate_opam_files false)
