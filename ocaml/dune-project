(lang dune 3.0)
; The OCaml estate as one dune workspace: `gen/` (the LCNF route), `eff/` (the Eff IR as
; an OCaml library) and `engine/` (the host engine); see `dune` and README.md. The avatar
; and the daemon that were once its two projects are archived on `archive/ocaml5-avatar`.
