/**
 * Generated by Effect4 (Effect v4 profile).
 *
 * Host: typescript 7.0.2
 * Runtime: node 22 --experimental-strip-types
 * Language service: @effect/tsgo@0.38.0
 * Library: effect@4.0.0-rc.112
 *
 * The pure atoms of the harness, declared by `effect_atoms`.
 *
 * Do not edit.
 */
import { Option, Result } from "effect"

/** Host body of the pure atom `succ`; its Lean model is `succ`. */
export const succ = (n: number): number => n + 1

/** Host body of the pure atom `orZero`; its Lean model is `orZero`. */
export const orZero = (e: Result.Result<number, string>): number => Result.isSuccess(e) ? e.success : 0

/** Host body of the pure atom `firstOr`; its Lean model is `firstOr`. */
export const firstOr = (r: Option.Option<Result.Result<number, string>>): number => Option.isSome(r) ? (Result.isSuccess(r.value) ? r.value.success : 0) : 0

/** Host body of the pure atom `dec`; its Lean model is `dec`. */
export const dec = (n: number): number => n - 1

/** Host body of the pure atom `flagToNat`; its Lean model is `flagToNat`. */
export const flagToNat = (flag: boolean): number => flag ? 1 : 0

/** Host body of the pure atom `pollValue`; its Lean model is `pollValue`. */
export const pollValue = (cell: Option.Option<Result.Result<number, number>>): number => Option.isSome(cell) && Result.isSuccess(cell.value) ? cell.value.success : 0

/** Host body of the pure atom `addNat`; its Lean model is `addNat`. */
export const addNat = (left: number, right: number): number => left + right
